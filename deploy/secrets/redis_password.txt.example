# Redis 存取密碼(URL-safe hex,內嵌於 redis_url)。
# 跑 bash deploy/generate-secrets.sh 自動生成(處理 dual-write)。
# 手動 fallback: docker run --rm alpine/openssl rand -hex 24 > deploy/secrets/redis_password.txt
# dual-write: 修改此檔後須同步重新生成 redis_url.txt
change_me_redis_password
