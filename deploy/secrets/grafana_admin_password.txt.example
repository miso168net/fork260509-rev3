# Grafana 管理員密碼(base64,僅經 grafana GF_SECURITY_ADMIN_PASSWORD__FILE 檔注入、非 URL)。
# 跑 bash deploy/generate-secrets.sh 自動生成。
# 手動 fallback: docker run --rm alpine/openssl rand -base64 24 > deploy/secrets/grafana_admin_password.txt
# 018 obs U1:grafana service 以 _FILE 讀此檔當 admin 密碼;observability 為 profiles:[obs,metrics] opt-in。
change_me_grafana_admin_password
