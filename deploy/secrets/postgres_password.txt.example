# PostgreSQL 資料庫密碼(URL-safe hex,內嵌於 database_url)。
# 跑 bash deploy/generate-secrets.sh 自動生成(處理 dual-write)。
# 手動 fallback: docker run --rm alpine/openssl rand -hex 24 > deploy/secrets/postgres_password.txt
# dual-write: 修改此檔後須同步重新生成 database_url.txt
change_me_postgres_password
