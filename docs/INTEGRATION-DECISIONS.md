# rev3 整合決策與實施帳（INTEGRATION-DECISIONS）

> 本檔 = [`INTEGRATION-DESIGN.md`](INTEGRATION-DESIGN.md)（凍結藍圖）的**伴生活帳**：藍圖只被引用，所有「會動的」都記這裡。
> - **§1 決策紀錄表** — 全書待決／拍板唯一清單（原 DESIGN 附錄 G 整表遷入 @ 2026-06-12）
> - **§2 實施階段** — 波次執行 as-built 帳（計畫的設計在 DESIGN §8.4、執行的帳在此；rev2 DESIGN §10 的外移對應物）
>
> **優先序**：本檔 §1 > DESIGN 本文 — 衝突時以本檔為準；DESIGN 本文的「待決N／⚠️x」字樣以本檔現況為準。
> **分工**：CHECKLIST（hook 注入）持「當前波快照＋拍板項一行索引」；MILESTONES 收 commit 里程碑流水＋CHECKLIST §2/§3 完成內容的鏡像歸檔（其 §2 只存一行式快照、波次詳帳仍在本檔 §2）；完整職責分工見 CLAUDE.md §7。
> **本檔不被 hook 注入**——會長無妨（表格帳本性質，與 MILESTONES 同類）。

---

## §1 決策紀錄表（全書唯一清單；user 親決前不入凍結集）

> **編輯紀律**：新增 ⚠️／待決必登本表；拍板後該列改「✅ 已決（日期）＋結論全文」，**不回填 DESIGN 本文**（DESIGN 重鑄時批次摺合、版本 +0.1、該列補標「已併入 vX.Y」）；DESIGN 勘誤不經本表、直接最小 patch。「最晚決策點」= 再不決就 block 哪一步；「所在章」指 DESIGN 章節。文中「待決⑥」泛指 ⑥a~⑥d 四子項。

| # | 決策點 | 工程預設 ⚠️ | 所在章 | 最晚決策點 |
|---|---|---|---|---|
| 待決① | router 結構：維持 flat-in-main 還是重整 `router/` 樹 | ✅ 已決(2026-06-13)：**flat-in-main 沿用**（rev2 as-built 已驗證模式：全部 route 逐條 `.route()` 寫 `main.rs`、`endpoint_coverage_lint` 鎖「main.rs==ENDPOINT_REGISTRY==seed」三源一致；lint 直接沿用、零 scaffold 改寫）。重整 `router/` 樹被否（lint 第一源從單檔變多檔、丟已驗證模式） | §1.5・§8.1 | ✅ 已決 |
| 待決② | wire contract 機器化（OpenAPI／contract test／維持 grep） | ✅ 已決(2026-06-12)：**C+ typings-as-oracle** — typings 抽 JSON Schema 當裁判（唯讀、不動官方檔）＋ coverage gate（router 每條 route 必有 contract case、缺＝CI 紅）＋ 碼表 table-driven（§7.3）＋ CDP capture 降為補充回歸 fixture ＋ lie ledger（顯式覆寫帳本、初始空）；B 案留「endpoint 增速再評」 | §7.2 | ✅ 已決 |
| 待決③ | 縱切第一刀：User 直刀 vs `system_settings` 打樣 | ✅ 已決(2026-06-16)：**B — `system_settings` 打樣為第一刀**（user 親決、推翻前次 A=User 直刀傾向）。以最輕、低風險、欄少的 `system_settings` 便宜排練 §8.1 全管線（migration→facade→handler→enforce→wire→frontend）、骨架先打通再上最重的 User 刀。⇒ 波 1 第一刀＝system_settings 打樣（此 rationale 為 B 原始價值 prop、user 實際理由待補） | §8.3 | ✅ 已決 |
| 待決④ | 選擇性 FK | ✅ 已決(2026-06-13)：**採工程預設 — 僅 join 表 `sys_user_role` 加 FK、其餘 11 表維持零**（join 表純關聯＋硬刪＋無 soft-delete 互動 → FK 零代價高收益、DB 層擋懸空列；log/token/casbin 維零——soft-delete 與 system-actor null 語意與 FK 相棘；零 FK 處 application-RI 義務照 §3.3 集中清單） | §3.1 | ✅ 已決 |
| 待決⑤ | §3/§5 凍結邊界：哪些進 constitution、哪些留設計書 | ✅ 已決(2026-06-12)：**採工程預設** — archetype（§3.2 四變體 A/B/C/D 整組）+ 行為島 invariants（§4 三台狀態機）+ wire 碼表（§5.4/§7.3，含 PageRes 形與 envelope 例外）入凍結（constitution §I.6/§I.7/§I.3）；欄級字典與常數值（grace 秒數等）留設計書。配套：constitution §V.3 分級 — §I.7 方向性不變式反轉=MAJOR、其餘 invariant 細項調整=MINOR | §3・§9 章首注 | ✅ 已決 |
| 待決⑥a | user-facing 儀表板 | rev3 v1 = 固定儀表板、0 新表（§2 表 #1） | §2 | 入波排程時（不 block 波 0-3） |
| 待決⑥b | 報表匯出 PDF/CSV | rev3 v1 = 同步匯出、0 新表（§2 表 #2） | §2 | 同 ⑥a |
| 待決⑥c | AES-256 靜態加密 | rev3 v1 = 磁碟/tablespace 層（§2 表 #3） | §2 | prod 部署定稿前（加密卷屬部署期決策） |
| 待決⑥d | 合規姿態升級 | 維持現姿態（§2 表 #4） | §2 | 對外／多租戶觸發時 |
| ⚠️a | 效能／可用性數字 | ✅ 已決(2026-06-16)：**批准保守預設**——list 讀 p95<300ms／寫(含同 txn 審計) p95<500ms／login p95<1s（argon2id 主成本）／可用性 99.5%/月（容許計畫性維護、恢復＝重啟容器）。對齊 §1.3「≤50 並發 admin 後台、不設吞吐 SLA」；波 1 起為驗收目標、`/speckit-plan` C-V 納入 | §1.3 | ✅ 已決 |
| ⚠️b | 審計查詢讀端 + UI 補做 | ✅ 已決(2026-06-16)：**做、排波 2 殿後刀**——補三 log 表（`sys_operation_log`/`sys_access_log`/`sys_login_attempt`）查詢讀端 + Super-only manage UI（MODAL-WIRING use (e)）；排序在波 2 核心 data-island（Menu→Role；`system_settings` 已移波 1 第一刀〔待決③〕）**之後**（read-only reporting、可殿後讓核心 CRUD 先清波 2）。落地需：3 讀 route + R_SUPER policy seed + 新 sys_menu seed（審計頁）+ §5.8 讀端 filter 索引（operation/access 需補、login 已就緒）+ wire 從零設計（rev2 零讀端、無 mock 可鏡像） | §1.2・§5.0 | ✅ 已決 |
| ⚠️c | `/auth/error` | ✅ 已決(2026-06-12)：**翻案 — 做**（echo 端點）。配套完整包：`alova/request`＋`alova/scenes`＋`function/request` 三 demo 頁進 sys_menu seed（初始僅勾 R_SUPER、下放交 ROLE 勾選層）；端點補 `/auth/error`＋`sendCaptcha`/`verifyCaptcha`（stub 雙模、⚠️m captcha 依賴就此解決）＋`/mock/getLastTime`（回 `{time}`）；§1.4 兩條「不做」同步翻案 | §1.4・§6.1 | ✅ 已決 |
| ⚠️d | redis-stack image tag | ✅ 已決(2026-06-13)：**建 stack 當下即 pin 數字版**（infra/deploy 刀寫 compose 時查當下 stable 直接 pin;升版走顯式 bump commit;對齊 §1.6 版本鎖點哲學） | §1.6 | ✅ 已決 |
| ⚠️e | `5000` 的 HTTP status 配對 | ✅ 已決(2026-06-12)：**一律 HTTP 200 信封**（對齊前端 msg 顯示通道僅 200 生效＋「business error 走 200」總則）；`AppError::Internal`→HTTP 500 mapping 標 test-only 或刪除；contract test 鎖 `5000`→200 | §5.4・§7.3 | ✅ 已決 |
| ⚠️f | 13 碼矩陣整組凍結（含 4 保留碼） | ✅ 已決(2026-06-12)：**整組凍結**（保留碼是前端 `.env` 分組實值、刪碼違 §I.1）；contract test 斷言「後端從不發出 7778/8889/9998/9999」 | §7.3 | ✅ 已決 |
| ⚠️g | constitution 重鑄措辭（§I.5 `axum-casbin`＋§9.6 Q5 rev1 指涉） | ✅ 已決(2026-06-12)：`axum-casbin` 重鑄為「enforce 層全新寫（in-tree、無獨立 crate）」；Q5/§I.5 對 rev2 source 立場＝**受控參照** — 讀允許（grep/閱讀對照驗證）、拷貝禁止（重新打字消化）、**防回歸條款**（rev3 拍板已推翻的行為不得帶回）；工具 crate `sea-orm-adapter`/`xdb` 例外自 rev2 整檔拷貝 | §9.2・§9.6 | ✅ 已決 |
| ⚠️h | 排程性拍板重議（§9.3 表之拍板 §11.2/§11.8/§11.13） | 重議走 amendment、不默改 | §9.3 | 重議觸發時 |
| ⚠️i | L4 授權模式 | ✅ 已決(2026-06-12)：**窄邊界精神的 rev3 起點映射** — i-1：MODAL-WIRING ★ v1.0.0 即授五用途 (a)~(e)（rev2 五次擴邊已驗證過的邊界、⚠️q 整批移植立即需要；**新用途 (f) 起仍走 amendment**）；i-2：BUILD-CONFIG ★ **不收錄**（⚠️p 後議題消解、軌道清單 5→4〔1★〕，日後需 build 改動走 amendment 新授） | §9.5 | ✅ 已決 |
| ⚠️j | rust-api 源倉 | ✅ 已決(2026-06-12)：**沿用倉、換分支**——`fork260509-rev2-anew-rust-api` 倉名（含 rev2）為永久名保留、分支改 `rev3-admin-rust-api`（已落地） | 附錄 A | ✅ 已決 |
| ⚠️k | migration 檔名 | ✅ 已決(2026-06-13)：**短編號 `mNNN_<name>`**（如 `m001_create_sys_user`;NNN 遞增、sea-orm 依檔名序執行;語意在檔名、無長零串——rev2 live 稽核實證 8 條引用多打一個 0 的抄錄必錯,附錄 C） | 附錄 C | ✅ 已決 |
| ⚠️l | settings 多 key 熱讀 | 需要時把單鍵 swap 推廣為 keyed map（設計變更、非預設） | §5.6 | 不阻塞（需要時） |
| ⚠️m | §1.2 尾巴（alt-login stub 4 + captcha 2）入波或重議 | ✅ **已決（2026-06-22、user 親決 C 案）：重議→延後出波3**——alt-login 4 流程 stub〔reset-pwd/code-login/register/bind-wechat〕＋其 captcha 移 **post-波3 v1-completeness slot**（接地 mapping：前端 3 表單已完整但後端 4 全缺、bind-wechat 空殼+真 OAuth、低 v1 功能價值、與波3 實質 authz/policy 不同主題;詳 CHECKLIST §3.D）。**#13/§11.13「v1 啟 stub mode」拍板不變**、僅 re-schedule within v1（§11.13 排程性拍板、走 §9.5 amendment、非默改）。**註(2026-06-12)**：captcha 2 端點隨 ⚠️c 完整包拍定（stub 雙模）但 **2026-06-22 mapping 實測未落地**（rust-api 無 sendCaptcha/verifyCaptcha route、前端 useCaptcha 為 mock）→ 與 alt-login 同延、見 CHECKLIST §3.D ⚠️c-完整包 finding | §1.2・§8.2 | ✅ 已決 |
| ⚠️n | 三 log 表 DB retention 政策 | rev3 v1 僅容量監控、retention defer | §1.3・§10.5 | 不阻塞（容量警示觸發時） |
| ⚠️o | application-RI 驗證下沉 facade 層 | ✅ 已決(2026-06-16)：**hybrid 分層**——intra-entity 純 ref 檢查（如 `sys_menu` reparent 3+1 guard）下沉 facade 自驗（slim model-層 error enum、不依賴 `AppError`→無層級倒置；handler 映 biz 2222）；跨 facade／需 context／restore-skip（grant role-code 解析、archive restore 不驗）留 handler orchestrate；DB 能擋者（unique）續走 `DbErr`→handler `sql_err()` 映碼。取代「全 handler 層驗」舊取向。接受代價：少數自驗 method 多一個 error enum＋handler match（受控）；換得 intra-entity RI 集中/繞不過、且避開 full facade-down 的層級倒置／restore 衝突／跨 facade 耦合。機制與分層詳見 DESIGN §3.3。user 親決 | §3.3 | ✅ 已決 |
| ⚠️p | demo menu 隱藏機制 | ✅ 已決(2026-06-12)：**翻案 — 全部 demo 頁進 sys_menu seed、初始僅勾給 R_SUPER**；「隱藏機制」議題消解（hideInMenu／pageExcludePatterns 皆不啟用），可見性全交 ROLE 勾選層（casbin menu 維度）治理下放。推翻 rev2 §11.5 的隱藏取向（constitution-rev3 重鑄時同步改寫、⚠️g 同梱）。配套盤點 **✅ 完成（2026-06-12）**：API 依賴者共 4 頁＝⚠️c 完整包三頁＋`plugin/excel`（用既有官方端點 `getUserList`、零新端點）；其餘 demo 頁純前端；`demoRequest` 線路 vanilla 閒置。詳 §6.1 表 | §6.1 | ✅ 已決 |
| ⚠️q | worktree 內容起點（base-web／rust-api 帶不帶 rev2 程式碼） | ✅ 已決(2026-06-12)：base-web=**clean-slate 血緣**（自 `example` 衍生、已落地）＋**整批移植** `rev2-admin-base-web` 完成接線＋rev2→rev3 改名（附錄 A.2）；rust-api 依 §8 波次**從零重寫**（已落地、`main`@Initial commit） | §8.4 波 -1・附錄 A | ✅ 已決 |
| ⚠️r | id 序列化策略（rev2「id 全字串」凍結存廢） | ✅ 已決(2026-06-12)：**廢除字串凍結 — 逐欄位忠實 typings**。DB 一律 i64 自增（BIGSERIAL）；僅在 rust-api **序列化邊界**對 typings 宣告 string 的欄位轉換（`MenuRoute.id`・`userId`）；其餘（`CommonRecord.id`/`parentId`/`MenuTree.id/pId`/`Role.id`、write payload `ids`）回 JSON number；serializer 加 2^53 fail-loud 守衛。**推翻 rev2 constitution §I.3／§11.10 的 string 拍板**（刻意偏離、constitution-rev3 重鑄時與 ⚠️g 同梱處理）；rev2 025-I1 類 type-lie 自此根除、lie ledger 初始為空 | §7.2・§9.2・§9.3 | ✅ 已決 |
| ⚠️s | fork-delta 執行紀律（授權後 inline／改值的變動方式） | ✅ 已決(2026-06-12)：**雙模式＋統一標記**（起因：upstream example 常態更新、rebase 須快速定位 fork 差異）— 修改型（MODAL-WIRING (a)(b)、ADAPT `.env` 改值等）**原行註解保留**緊鄰新行＋標記；新增型（(c)(d)(e) 插入/新檔）標記圈界、新檔僅檔頭一行；標記統一含 `rev3-inline` token（grep＝完整 fork patch set）；**rebase 同步紀律**（解衝突時註解原行更新為 upstream 現行版）；⚠️q 整批移植時修改型逐處補 example 原行註解（B 案）。落 constitution §III fork-delta 紀律＋§I.1＋Check Q2；inline_coverage_lint 候選登 CHECKLIST §3.H | constitution §III（§9.4 重鑄時摺合） | ✅ 已決 |
| ⚠️t | schema 交付模型（逐刀建表 vs 波 0 一次全建） | ✅ 已決(2026-06-13)：**rev2 終態 squash 基線＋rev3 delta 顯式分離，波 0 一次全建**——002 刀以 `m001_rev2_schema.rs`（12 表終態忠實濃縮〔口徑：m001 實建 11——10 手寫＋casbin 委派 adapter；＋seaql 框架自建＝12，002 analyze L2 勘誤〕、零 FK 原樣）＋`m002_rev2_seeds.rs`（seed 淨效果——**勘誤 2026-06-13/002 brainstorm**：原文「17 條」對不上任何實數口徑〔seed 檔名 16 支／含 INSERT 20 支〕，重錨定＝**92 列／6 表**：sys_user 3・sys_role 3・sys_user_role 3・sys_menu 10・casbin_rule 72・system_settings 1，UPDATE 回填淨值併入終態列；源碼＋活庫雙驗）落地 rev2 基線（`mNNN_<name>` 短編號〔⚠️k〕、35→2 squash＝重寫非照拷〔§I.5 受控參照〕）；rev3 拍板 delta 顯式 m003+（④ FK、⚠️p/⚠️c seed 擴充）；**pg_dump 雙庫互 diff 驗證閉環**（參考庫＝pristine 重放、normalize 規則與 diff 範圍見 002 brainstorm §3.5——argon2id／`\restrict`／seaql_migrations／timestamps・setval 正規化）。後刀 migration 工序改「驗表已在＋補刀特有 seed」。屬 §8 交付序調整（§8.6 不構成設計變更）；§3.4「同刀建表」字面讓位其防 retrofit 精神（一次全建更強滿足）。誕生於 001 brainstorm（docs/superpowers/001-infra-deploy.md §2） | §3.4・§8.1・§8.2 | ✅ 已決 |
| ⚠️u | constitution §IV 缺 §I.4 工作流對應題（**amendment 提案**） | ✅ 已決(2026-06-16)：**不採納、日後不再議**——維持 constitution §IV 現 9 題、不增第 10 題（「plan/tasks 是否把 push/merge 排入實作期」）、不 bump。原提案(2026-06-13)背景：001 `/speckit-analyze` 發現 T021 曾排 worktree push、9 題結構上抓不到。**push/merge 不入實作期的紀律已固化於 CLAUDE.md §3**（★核心紀律＋階段 2「絕不 push/merge」）＋§I.4 凍結令＋人工 review 已足，constitution §IV 增題冗餘；本項 close | constitution §IV | ✅ 已決 |
| ⚠️v | casbin_rule 建表方式（委派 adapter vs 直接 CREATE；牽動 sub-crate 刀時序） | ✅ 已決(2026-06-13)：**委派式——m001 內 `sea_orm_adapter::up()` 建 8 欄基底＋同檔 ALTER 補 3 治理欄成 11 欄終態；sea-orm-adapter sub-crate 併入 002**（§I.5 拷貝例外整檔拷貝、`casbin = 2.20` pin 隨刀；**xdb 留 audit 刀**首個消費者帶入；**sub-crate 獨立刀消解**）。保「單一 schema 來源」紀律（rev2 m005 注記原則）＋與 Auth 刀 runtime `SeaOrmAdapter::new` 的 if_not_exists 自建一致。直接 CREATE 案被否：雙 schema 來源＋`unique_key_sea_orm_adapter` CONSTRAINT 細節手抄對齊成本＋adapter 升版 drift 隱患。誕生於 002 brainstorm（docs/superpowers/002-rev2-schema-baseline.md §2） | §3.1・§8.2 | ✅ 已決 |
| ⚠️w | login lockout（登入失敗限流）刀位 + 設計 | ✅ 已決(2026-06-25)：**落地＝019-login-lockout**（merge `dbc5902f`、波4 後獨立刀、feature branch 保留；rust-api `5cec936`／base-web `5e667f9d`）。對既有 `sys_login_attempt` **唯讀消費**、login 前置滑動 15 分窗 count `success=false` 數、per-user 5次/15分／per-ip 20次/15分 任一達門檻即短路擋（既有 **2222** + i18n key `auth.login.locked`、不進 login_inner）；gated 列匯流既有單一寫點〔success=false/operator=None/ctx 四欄〕＝sticky+審計留痕、滑動窗自動解、**fail-OPEN**（count DbErr→`.unwrap_or(0)`→放行）。三待決定案：①**刀位**＝波4 後獨立刀（只讀資料、不依波3 狀態機）②**政策值**＝per-user 5／per-ip 20、窗 15 分（v1 固定、runtime 可調＋per-IP 白名單 defer；per-ip 門檻刻意高免誤鎖 NAT）③**best-effort 寫**＝足（縱深防禦、非強一致）。★ **D3 措辭 supersede**：原議「前端顯示鎖定 N 分鐘」改為**靜態一般化訊息**（FR-012：不洩剩餘時間/觸發維度/帳號存在性＝防枚舉一致）。Constitution 9/9、13 碼矩陣不變（2222 reuse 不破 ⚠️f）。詳 [MILESTONES `dbc5902f`](INTEGRATION-MILESTONES.md)。 ／ 原案：**做**——消費 audit-overlay 刀備齊的 `sys_login_attempt` 的 `(client_ip,created_at)`／`(attempted_user_name,created_at)` 索引（DESIGN §5.8／附錄 F「服務 login lockout 內部查詢」、rev2 FR-008），login 前置 count 時窗內 fail 數→超閾值回 lockout 碼擋提交＋前端顯示「鎖定 N 分鐘」。**資料源由 audit-overlay 刀備齊、本 ⚠️w 為消費 gate**；per-ip lockout 因 audit-overlay 備齊真實 client IP 而可行（per-ip／per-user〔兩索引都備〕／both＝排程時定）。設計待決：①刀位/wave（有狀態 auth 行為→波 3 候選；只讀資料、可提前獨立排）②政策值（X 次/N 分鐘）③login-attempt 失敗寫可靠度（best-effort 夠 defense-in-depth）。誕生於 audit-overlay brainstorm（user 親提 2026-06-14、v2 2026-06-15） | §5.0・§8.2 | ✅ 已決 |
| ⚠️x | endpoint_coverage_lint 是否為波 0 出口硬條件（三守恆之一） | ✅ 已決(2026-06-14)：**波 0 換波豁免**——`endpoint_coverage_lint`（鎖 main.rs router==ENDPOINT_REGISTRY==seed、target `EXPECTED_ROUTE_COUNT=35` gated route，DESIGN §7.1／§8.4）**結構上需首個受保護業務端點存在才能立**；波 0（地基刀群）**零 gated 業務端點**、無法在波 0 達成。故 user 親決：波 0 出口三守恆**豁免** endpoint_coverage_lint、**移交波 1 第一刀**（首個 gated endpoint wiring 時一併立、enforce_mw route_layer 首掛，§3.9／§3.6）；波 0 餘二守恆（entity_access_lint／migration up→down→up）照常。本碼 ⚠️x 正式登記此豁免（與 ⚠️w login lockout 分開） | §8.4・§3.9 | ✅ 已決 |
| ⚠️y | biz-error `msg` 多語系策略 | ✅ 已決(2026-06-16)：**A — 前端翻譯（`msg` 改載穩定 i18n key）**。wire `Res<T>{data,code,msg}` 的 `msg` 由人話改載**穩定 key**（如 `biz.role.notFound`）；base-web（soybean v2.2.0＋vue-i18n 11、現代 typed i18n）將 `onBackendFail`/`showErrorMsg` 改以 `$t(msg)` 翻譯、locale 補 error 命名空間；後端**語言無關**（產 key、不在地化）。base-web 為 wire＋i18n 權威（§I.1）→ 屬其契約演進；base-web 改動走 §III fork-delta（⚠️s `rev3-inline` 標記）。**graceful fallback**：vue-i18n `$t` 未命中 key 回傳原字串 → 過渡期未鍵化的 msg 仍可顯示。**接受代價**：後端 log/curl/audit 的 `msg` 變 key（人話可讀性降）——以此換「單一 i18n 家（base-web）＋後端語言無關」。否決 B（後端在地化）：免後端另養訊息 catalog＋雙邊翻譯漂移。**key 命名規約／error 命名空間提前於 003-envelope brainstorm 落定**（2026-06-16 細化、scope A：4 根 `common/auth/biz/system`＋文法 `<root>.<entity>.<condition>`＋13 固定碼 key＋兩端接線＋base-web locale 外包一層 top-level `backend`＋wire 去前綴〔canonical 形＝`backend.<root>.<entity>.<condition>`、wire `msg` 去前綴〕；code-keyed〔`error.2222`〕否決；見 `docs/superpowers/003-envelope.md`）；刀 1 system_settings **僅依規約加首批 per-entity biz key 值**。誕生於 ⚠️o 範例 msg 討論。user 親決 | §7.3・§5.4 | ✅ 已決 |
| ⚠️z | `⚠️a` ~ `⚠️z` 用盡 | 下一個 `決策點 編號` 從 `⚠️aa` ~ `⚠️az` 再往後為 `⚠️ba` ... | 此處 | ✅ 已決 |
| ⚠️aa | base-web i18n inline 接線軌道（service/request 攔截器＋locales＋app.d.ts Schema 不在 §III 既有軌道、§I.1 需軌道才合規） | ✅ 已決(2026-06-16)：**A — constitution §III 新增 `BASE-WEB-I18N-WIRING ★` 軌道**（MINOR amendment、constitution v1.0.0→**v1.1.0**）。授權三範圍：**(i)** `service/request/{index,shared}.ts` 的 msg 翻譯接線〔限 modal `content` :71／`onError` backend-msg extraction :109／dedup companions :64,:51；不改控制流〕、**(ii)** `locales/langs/{zh-cn,en-us}.ts` 新增 top-level `backend` 命名空間、**(iii)** `typings/app.d.ts` `App.I18n.Schema` 新增 `backend` 型別＋`translateBackendMsg` helper；走 fork-delta `rev3-inline`。背景：⚠️y 已授權 i18n 改動本身、但 §I.1「不動 inline、例外見 §III」要求有對應軌道才合規（003-envelope `/speckit-plan` Constitution Check Q2/Q7 觸發）；重新設計避不開（翻譯必發生在攔截器顯示點）。否決 B（擴既有軌道：i18n inline 與 ADAPT/WRAPPER 純新檔語意混淆、audit 不清）／C（視 ⚠️y 足不 amend：§I.1→§III 治理鏈留洞）。誕生於 003-envelope `/speckit-plan` Phase 0。user 親決 | §III・§I.1 | ✅ 已決 |
| ⚠️ab | constitution §I.3「碼/msg 字串為 wire 凍結事實」措辭早於 ⚠️y（msg 改載 i18n key）、易誤讀為凍結 msg 人話值 | ✅ 已決(2026-06-16)：**constitution §I.3 措辭 PATCH（v1.1.0→v1.1.1、§V.3 PATCH＝釐清）**——改為「碼為 wire 凍結事實；`msg` 自 ⚠️y 起載穩定 i18n key〔非人話字串、後端語言無關、前端 `$t` 譯〕；完整矩陣與 key 規約見 DESIGN §7.3」。**非新決策**＝對齊既決 ⚠️y、消除內文 staleness；spec/plan/tasks 本就合規（003-envelope `/speckit-analyze` C1）。誕生於 003-envelope `/speckit-analyze` C1。user 親決（與 U2/I1 同批 commit、§V.2「獨立 commit」由 user 顯式 override） | §I.3 | ✅ 已決 |
| P-011-1 | 角色軟刪不清 casbin 授權 + code 可重用 → 重建同 code 靜默繼承舊授權（HIGH 提權、2026-06-26 CDP 第2輪全測發現）；修向 + grant 並發窗處置 | ✅ 已決(2026-06-27、user 親決)：**修向＝刪時 archive 授權（root-cause、資料不丟）**——`deleteRole`/`batchDeleteRole` 軟刪同交易 archive-move 該 code 全維 casbin p-policy（reuse `insert_archived`、reason=`role_soft_delete`）+ 自 casbin_rule 移除、archived>0→`reload_and_publish`；回收桶含這些列但**不可手動復原**（讀時 `created_at` 衍生 restorable＝denylist `!=role_soft_delete` AND active 角色 created_at<archived_at、**零 archive mutation／零 migration**、守 §I.6 archetype D；單一規則同滿足 FR-005〔role_soft_delete 不可復原〕+ FR-012〔既有撤銷列於刪/重用後不可復原〕）、restorePolicy 拒→**2222 `biz.policy.notRestorable`**（縱深防禦、非僅前端隱藏）。否決硬刪（丟 forensic）／重建時才清（治標留 dormant）／禁 code 重用（侵入需 migration）。**★ grant-during-delete 並發窗＝拍板 A（收緊徹底堵）**：spec.md:79/123 + C-V-12 明文 grant 也須與 delete 序列化、但 U1 只鎖 delete+restore（concurrency review 接住、escalate 主線）；user 拍板 A→`set_role_dimension`/`set_role_endpoints` txn 內 FOR-UPDATE 鎖同一 `sys_role` 列、角色已刪則拒 grant（→`biz.role.notFound`、復用既有 i18n 鍵），四類 casbin 寫端〔delete/batch/restore/grant〕全先鎖 sys_role 再寫、同序無死鎖（否決 B＝接受窄窗+改文件）。落地＝**020-role-delete-policy-archive**（merge `ebbd6c2c`、feature branch 保留；U1 rust `46b2941`..`3c65d17`／U1b `67ff2bd`／U2 base-web `fec0a6d`）；3 執行單元 Workflow 驅動、rust 226 測+curl C-V-1/2/5/6/7/8+CDP C-V-9+holistic 冷讀全綠、Constitution 9/9、0 migration/crate/route。**推翻 `specs/011-role-management/data-model.md` 的「軟刪殘留 menu policy 無害（可刪角色必無人用）」前提**（code 重用路徑下不成立、已加 as-built 註記）。詳 [MILESTONES `ebbd6c2c`](INTEGRATION-MILESTONES.md)。 | §I.7・§3.K | ✅ 已決 |
| ⚠️ac | constitution §I.6「無 retrofit」措辭易誤讀為禁一切 schema 後改（vs 標的應為 archetype 審計欄、防 rev2「建表漏審計欄事後補」之債） | ✅ 已決(2026-06-21)：**constitution §I.6 釐清 PATCH（v1.1.1→v1.1.2、§V.3 PATCH＝釐清）**——「無 retrofit」標的＝**archetype 審計欄**；**既有表加 domain 業務／forensic 欄之【刻意、規劃、可逆】演進不在此限**（例 D11：審計 log 表加 IP forensic 欄、寫端依設計排後續刀〔013、`m006` 可逆〕為合規演進；前提＝archetype B 欄規則不變〔不加 `updated_*`/`deleted_*`、不可竄改〕、非「忘帶事後補」意外債）。**非新決策＝釐清原意**；013 plan Constitution Check item 8 由此正式背書（原 Complexity Tracking 破例升為合規演進）。誕生於 013 `/speckit-analyze` C1。user 親決 | §I.6 | ✅ 已決 |
| ⚠️ad | login lockout 在分散式打單帳號下的 DB 讀取/寫入放大（硬化 019/⚠️w）＋「已生效鎖定」的服務方式＋007/019 審計 FR 反轉 | ✅ 已決(2026-06-28、user 親決 brainstorm＋clarify)：**落地＝021-login-lockout-redis-cache**（merge `5e4f3228`、硬化 ⚠️w/019 後獨立刀、feature branch 保留；rust-api `047a232`／outer pin `ec42bd4e`）。對「已生效鎖定」在 login gate 最前端加一層 **Redis 負快取（L1 deny 層）**、DB 維持鎖定真相 → 堵分散式打單帳號（per-user 維度）下的 DB 放大（per-user 無-LIMIT COUNT＋鎖中逐筆 INSERT 在 botnet 聚合下→PG 飽和拖垮全系統；nginx per-IP limit_req 擋不住聚合 per-user 速率）。**D1** 雙維度（`lockout:ip:{ip}`＋`lockout:user:{name}`、L2 達門檻依觸發維度 `set_ex`；只記 ip 擋不住分散式 per-user）／**D2** 固定 TTL 900s 命中不 refresh（攻擊停 ~900s 自癒、不變永久鎖、否決 refresh-on-hit＝攻擊者每<900s 戳一下即永久鎖死 Super）／**D3** ②b 鎖後 L1 命中不逐筆寫稽核＋②c 節流麵包屑（Redis `INCR`→`GETDEL`→`tracing::warn!(target=security.lockout, suppressed=N)`→loki、≤1/60s/key、非稽核表列、零 migration）。**fail-OPEN**（Redis-down/op 失敗→L1 跳過退 L2 既有 DB gate、§I.7 §4.3 persist-then-cache 非權威快取、鎖定正確性由 DB 滑動窗 count 維持）；reuse 019 政策值（per-user 5／per-ip 20／窗 900s＝TTL）；**0 migration/crate/route/wire**、redis facade +`incr`/`take_suppressed`/`mark_locked`/`is_locked`。**★ 有意識反轉 007-audit-overlay FR-004/SC-002（每終端結果 exactly-one 列）＋ 019 FR-008（gated 列 sticky 審計痕跡那半）**——L1 命中早 return 不寫列、量級訊號移觀測層 ②c；非違憲（§I.6 archetype B 只規範 append-only/不可竄改、未規範每嘗試必寫；上鎖前歷程＋觸發那發走 L2 仍照寫＝FR-006/sticky 計數那半由 DB 真相維持）。**★ 歸屬勘誤（收尾 3 獨立 reviewer grep 實證）**：021 設計鏈（research D7/plan/tasks/checklists/brainstorm）原誤標反轉「**019 FR-004**」；實證 019 FR-004＝**真實 client IP 防偽**（`specs/019-login-lockout/spec.md:80`、021 完全未碰），「exactly-one per terminal result」之擁有者實為 **007-audit-overlay FR-004（:108）/SC-002（:137）**。已比照 020 對 011 的 as-built 處理：於 007 FR-004/SC-002 與 019 FR-008 加 forward-pointing as-built 註記（保留原文）、並修正 021 chain 全部誤標（brainstorm §9 附史料勘誤指標）。**clarify 2 Q firm**：distinct-source 來源廣度可選/遞延（不做 HLL PFADD/PFCOUNT）、告警規則配置遞延 ops/obs（本刀只發可告警結構化訊號＝滿足 SC-004）。帳號-DoS/IP 信任白名單/CAPTCHA 切 019 §4.2 獨立 future（本刀僅留 L0 trusted-ip seam、FR-012）；本刀把鎖做硬（Redis 不像 DB 會 fail-open-under-load）→ 帳號-DoS 更穩定生效 → 緩解需求升高、宜緊接排程。否決：refresh-on-hit TTL（永久鎖）／bounded-count LIMIT（快取已使其多餘）／②c 寫稽核表列（污染鎖定 COUNT＋需 migration）。**單一 rust 執行單元（Workflow 驅動 implementer TDD→spec∥quality review→fix loop）**、rust 195 unit+8 live 測零回歸+lint×2+prod build、活體 acceptance 22 PASS/0 FAIL（鎖後 0 DB 寫、雙維度 G1 end-to-end〔L2-write key==L1-read key、含 IPv6〕、TTL 不 refresh、fail-OPEN 退 L2、②c 節流恰 1 筆、零回歸）、3 獨立 reviewer holistic 冷讀全 pass、Constitution 9/9。詳 [MILESTONES `5e4f3228`](INTEGRATION-MILESTONES.md)。 | §5.0・§I.7 | ✅ 已決 |

---

## §2 實施階段（rev3 波次 as-built 帳）

> 骨架衍生自 DESIGN §8.4 交付波次（波次定義／出口條件在彼、執行紀錄在此）。
> **回填紀律**：波／Phase 完成後，as-built（feature 清單＋merge SHA＋日期）回填本節對應波；CHECKLIST「Roadmap & Phase 狀態」該波收縮為一行指本節。本節**永久留此、DESIGN 重鑄不摺**（執行帳不屬於藍圖 — rev2 §10 的 65 次補丁教訓）。

### 波 -1 — repo 建構 ✅ 全完成（2026-06-12）

- ✅ outer repo＋worktree/submodule 註冊（`2ec9cda`；⚠️j／⚠️q 拍板已落地）
- ✅ CLAUDE.md-rev3／.gitignore 家族／.specify spec-kit 殼
- ✅ 設計書入 docs/（`7fd1ac6`）＋拍板回填（`88f9011`）＋歸位改名 INTEGRATION-DESIGN.md（`4aa7c89`）＋C 方案文件體系（DECISIONS/CHECKLIST/MILESTONES，`4724549`/`4300b54`）
- ✅ graphify 首次建圖（`8f66fe0`，2060 nodes/311 communities）
- ✅ base-web standalone 容器化＋mock wire ground truth 捕獲與對映（000：`46591c4`/`309099d`/`aea0e18`/`e898421`）
- ✅ SessionStart hook（`.claude/settings.json`＋`hook-git-submodule-SOP.sh`；自 rev2 原樣承接、workspace-agnostic 零改動、實測健檢＋CHECKLIST 注入跑通，`ed2a789`）
- ✅ constitution-rev3 v1.0.0 重鑄凍結（`167db96`，獨立 commit；§9 快照 carry＋13 項拍板融入＋§V.2 提案位置=DECISIONS §1＋⚠️s fork-delta 紀律；雙輪驗證〔忠實度 8 項＋操作性 4 項〕後凍結）
- ✅ 出口條件四項全綠（2026-06-12 驗）：session 健檢綠（hook 實測）／constitution v1.0.0 獨立 commit／設定・部署層 grep rev2 歸零（豁免：`fork260509-rev2-anew-rust-api` 倉永久名〔⚠️j〕與史料引用；`.gitignore`/`.graphifyignore` 2 處註解殘影同輪修正）／`/speckit-*` 指令可用（scripts＋skills 在位）

### 波 0 — 地基 ✅ 全完成（2026-06-18）

> 七刀全收（001~007）；每刀 commit 史見 [MILESTONES §1](INTEGRATION-MILESTONES.md) 對應行、設計/驗收全帳見 `specs/<刀>/`。CHECKLIST §2 該波已收縮為一行指本節。

- ✅ **001-infra-deploy**（`c9ffad5`，2026-06-13）— master compose 5 service＋migrate gate＋acme 殼＋dev/prod override＋deploy/ 全套＋rust-api scaffold（/health＋空 migrator＋lock pin）；C-V-0~8 實機全綠（SC-001~007）、捕獲並修 redis `--dir` 持久化真 bug
- ✅ **002-rev2-schema-baseline**（`9233ae0`，2026-06-13）— 前代 35 支 migration squash 為 4 支基線（m001 schema 11 表終態／m002 seed 92 列 6 表 argon2id／m003 user_role FK ×2 RESTRICT／m004 demo 選單 66＋policy 全 R_SUPER）＋`sea-orm-adapter` 整檔拷入（§I.5⚠️v）；C-V-0~9 全綠（SC-001~008）、修兩層 seed drift（id 序＋normalize 第六規則 row-order，user 拍板方案 A、契約留痕 migration-chain.md §3）
- ✅ **（sub-crate 刀消解）**（⚠️v，2026-06-13）— `sea-orm-adapter` 併入 002（委派式建表直接消費者）、`xdb` 併入 007（首個消費者）；§I.5 唯二拷貝例外不變
- ✅ **003-envelope**（`13a01b1`，2026-06-16）— 統一 `Res<T>`/`PageRes<T>`＋`AppError` 9 變體凍結 13 碼矩陣（reserved 4 碼型別層無變體＝編譯期 guard）＋`.fallback(handler_404)`；base-web Schema backend＋雙語 langs＋`translateBackendMsg`＋service/request 4 翻譯點（⚠️aa rev3-inline）；觸發 constitution v1.1.0 amend（§III `BASE-WEB-I18N-WIRING ★`）＋v1.1.1（⚠️ab §I.3 措辭 PATCH）；key 規約 ⚠️y 落定；C-V-0~3 全綠（SC-001~008）
- ✅ **004-soft-delete-infra**（`a1105f0`，2026-06-17）— L2 `entity` workspace crate（11 模組 DeriveEntityModel／130 欄；tstz→DateTimeWithTimeZone／jsonb→Json／INET→IpNetwork）＋L4 `SoftDeletable` trait＋3 facade impl＋`find_active`＋facade 唯一管道守恆 `entity_access_lint`（兩階段 whiten＋path-root scan、自測雙證非 vacuous）；C-V-0~3 全綠、holistic PASS 零 findings（SC-005 零業務 endpoint/migration）
- ✅ **005-audit-op-log**（`98f1f7e`，2026-06-17；audit ×2 之首＝op-log 刀）— L4 `model/audit.rs`（`mutate_in_txn` 泛型 `C:TransactionTrait` 同 txn 原子審計＋`AuditOperation`/`Operator`/`Event`/`Serialize`）＋op-log append-only sink（`facade/sys_operation_log::write_in_txn`、archetype B）＋`sys_user` redact（password→`<redacted>`）＋單一 `soft_delete` proof（§I.6 deleted_at/deleted_by 成對、no-op 回 Ok(None)）；C-V-0~3 全綠（atomic live smoke 三路徑非 vacuous）、3-lens holistic mergeReady 零 blocker
- ✅ **006-auth-island-min**（`e279f23`，2026-06-17；Auth 島最小段＋runtime 骨幹第一刀）— config `_FILE` 優先 secret＋長度/change-me 守門／AppState{db,jwt,enforcer}／Casbin enforcer boot（embedded 3-tuple RBAC `from_str`）＋JWT HS256 雙鑰 sign/verify／argon2id／`enforce_mw` bearer〔3333 fail-CLOSED〕→is_current〔7777 fail-OPEN〕＋login（簽 access/refresh＋token_hash=sha256(refresh)＋set_pointer+insert_token 同 txn 原子＋1000 collapse 不洩存在）／getUserInfo（DB-fresh roles＋casbin v2=button buttons＋User→User01 alias＋userId 字串）；time-pin landmine 排除（jwt9→simple_asn1→time 真圖，pin time 0.3.37+simple_asn1 0.6.3 配 1.86）；C-V-0~6 全綠、17 FR+9 SC PASS
- ✅ **007-audit-overlay**（`96280d8`，2026-06-18；波 0 末刀＝audit ×2 之二＝audit overlay）— L3 `xdb` crate 整檔零改拷入（§I.5⚠️v）＋L7 `audit_ctx`（`resolve_client_ip` XFF trusted-proxy〔peer-gate→rightmost-untrusted→fail-safe、anti-spoof、8 純測〕／`extract_trace_id`／`audit_mw` 全域最外層 operator-gate／`to_audit_operator` seam）＋2 append-only facade sink（`sys_access_log`/`sys_login_attempt`、`IpAddr→IpNetwork::from`）＋state/config（trusted_proxy_cidrs fail-safe／xdb_ready／XDB_FILEPATH）＋login inner/outer split exactly-one（not-found/wrong-pwd 同 1000、operator pre/post-identity）＋op-log threading live smoke（T018）；boot `Path::exists` 守門→`searcher_init`（缺檔降級不 panic、R1）＋`connect_info`；T009 compose env＋T019 Dockerfile xdb COPY（Manifest＋Source〔benches〕＋runtime .xdb＋builder `--locked`）；C-V-0~9 全綠、holistic PASS（16 FR+9 SC 全 MET）；推進 DESIGN §5.9（直連→XFF trusted-proxy、DESIGN-detail 非 Amendment、下次重鑄摺合）；零 migration/entity/型遷移/base-web/i18n/nginx、enforce_mw 未動、SC-007 零回歸
- ✅ **波 0 出口條件四項達標**（DESIGN §8.4）：dev stack `up --wait` healthy✅（001）／三守恆〔entity_access_lint✅ 004・migration up→down→up✅ 002・endpoint_coverage_lint ⚠️x 豁免移波1〕／envelope 13 碼 contract✅（003）／login→getUserInfo→enforce 鏈 curl✅（006）→ 換波 1
- 波 0 期間 constitution amend：**v1.1.0**（⚠️aa §III `BASE-WEB-I18N-WIRING ★` 軌道）＋**v1.1.1**（⚠️ab §I.3 措辭 PATCH，msg 載 i18n key 對齊 ⚠️y）

### 波 1 — 第一刀＝`system_settings` 打樋 ✅ 全完成（2026-06-18）

> 1 刀打樋（merge `b52dafe`、feature branch `008-system-settings` 保留）＝以最輕 KV entity `system_settings` 便宜跑完 §8.1 縱切全管線（facade→handler→enforce→main→wire→frontend）、達 3 個全專案首次。零 migration（m005 MOOT）、無新 crate。

- ✅ **008-system-settings**（merge `b52dafe`，2026-06-18；波 1 唯一刀＝system_settings KV 打樋）— **3 全專案首次**：(1) **首個 policy-governed 端點**——新 `require_policy(path,method)` per-route layer（`from_fn_with_state`、DB-fresh `roles_of_user(db,claims.uid)` 不信 `claims.roles`〔FR-005〕→`enforce_role_path_method`→`PermissionDenied` 5003/403）、`enforce_mw` 本體一行不動（§3.4／contract §1）＝兩層 auth→authz，5003→403 live 首証（Admin/User GET/POST→403 不洩值）；(2) **首個 007 op-log threading live consumer**——update handler 經 `ctx.to_audit_operator(claims.uid)` 取真 operator/IP/trace 餵 `mutate_in_txn`，op-log `operator_ip` 由恆 None→真 INET round-trip（C-V-5 顯式 IpNetwork 203.0.113.7、by-trace 查證、savepoint rollback）；(3) **立 `endpoint_coverage_lint`**（⚠️x 波0 豁免移波1）——鏡像 entity_access_lint 只讀掃描，斷言 registered==as-built registry（5 route）＋policy-governed⊆m002 p-policy seed（**非** §7.1 @35 字面、registered⊆seeded 方向），含 scan_self_test 五面自證。實作：L4 facade（find_all／find_by_key／update_by_key〔mutate_in_txn 同 txn op-log UPDATE、查無 Ok(None) no-op、entity_id None+key 入 payload、before/after audit_json〕＋build_update_active_model 純測 seam〔成對 Set〕＋AuditSerialize）＋L5 handler（get_system_settings flat／update_setting 先驗後改：notFound/invalidValue→2222、to_audit_operator 餵）＋L4 require_policy＋L1 main（GET/POST 各掛 route_layer require_policy＋外層 enforce_mw、既有 health/login/getUserInfo 不動）＋L8 lint；base-web static 頁（rev3-system-settings.ts WRAPPER **首個 rev3-* 檔**／rev3-system-settings.d.ts ADAPT merge Api.SystemManage／views/manage/system-settings/index.vue MODAL-WIRING(e) 簡 KV 頁 enum→NSwitch／app.d.ts Schema＋zh-cn/en-us locale backend.biz.systemSettings.*＋route.manage_system-settings＋page.manage.systemSettings.title／elegant-router 重生路由檔一併 commit）。4 執行單元 Workflow 驅動（U1 `8e5a024`→U2 `4f4952d`→U3 `3874182`／U4 `5fdd6f0`＋§2 trim 未用 page-label key `223bc83e`）；C-V-0~11 全綠、holistic 雙 lens（spec-完整性＋simplicity/wire/regression）ready-to-merge 0 blocking。**零 migration/entity/schema**（m005 MOOT、m002:162-165/251 已 seed policy×2/menu/sys_menu）、無新 crate、enforce_mw 未動、base-web 既有檔未動、SC-006/007 零回歸；FR-010 hot-apply 確認未引入（零 session_mode consumption/watcher）。前端 static Option A：menu-Casbin-visibility＋dynamic-menu〔拍板#7〕延波2 Menu 刀（D1 follow-up：選單可見性已由 010 兌現／R_ADMIN policy 校正見 CHECKLIST §3.F）；§5.6 watcher/`session_mode` consumption→波3。
- ✅ **波 1 出口條件達標**（DESIGN §8.4）：§8.1 工序 9 列全過（C-V-0~11 全綠）／CDP 經 front-nginx 真 `/api` 路徑驗收✅（C-V-7 super toggle→toast「更新成功」→DB off↔on、零 vite/console error 證 vite 熱載 rev3 wrapper、Admin API 403）／`system_settings` §5.0 列逐面勾消（讀全列／改單鍵／super-only 403／value_type 驗 2222／同 txn 審計原子／無增刪鍵路徑）→ **波 1 全完成**

### 波 2 — data islands ✅ 全完成（2026-06-19；第一刀＝User、第二刀＝Menu、第三刀＝Role、殿後刀＝Audit）

> 業務 entity 縱切群（建議序 User→Menu→Role；rev2 一 feature 兩 entity → rev3 拆刀紀律）。**第一刀＝009 User 已收**（merge `07b67d2`、feature branch 保留）／**第二刀＝010 Menu 已收**（merge `3810103`、feature branch `010-menu-management` 保留）／**第三刀＝011 Role 已收**（merge `b80c9e3`、feature branch `011-role-management` 保留）／**殿後刀＝012 Audit 已收**（merge `55d34ff`、feature branch `012-audit-log-query` 保留）。User 刀首度 exercise §5.8 分頁/filter／`PageRes`／M:N join 寫／23505→2222 寫端 map／INSERT op-log；Menu 刀達 §I.2 動態路由首兌現（getUserRoutes Casbin v2='menu' 過濾）＋reparent guard facade slim enum＋flat→tree 序列化（祖先包含）＋`AuditOperation::Restore` 首 consumer＋`.env` dynamic flip（#7）；Role 刀達**全專案首個 runtime casbin policy WRITE**（DB-first `set_role_dimension`、constitution §I.7 §4.2 ①②④⑤、與 010 getUserRoutes 讀寫閉環）＋delete guards（種子+使用中+自身）。

- ✅ **009-user-management**（merge `07b67d2`，2026-06-18；波 2 第一刀＝User 資料島）— 使用者管理頁 mock→真後端 6 端點 CRUD（getUserList／getAllRoles／addUser／updateUser／deleteUser／batchDeleteUser）＋角色 M:N（`replace_roles_in_txn` 與 user 寫同一 `mutate_in_txn`、同時 commit/rollback）＋同交易審計（INSERT/UPDATE/SOFT_DELETE op-log、`operator_ip` 真 INET、`to_audit_operator` 餵）＋越權防護（6 端點各 `require_policy`：read getUserList=R_SUPER+R_ADMIN／getAllRoles=+R_USER_COMMON、write addUser·updateUser·deleteUser·batchDeleteUser=R_SUPER；DB-fresh roles 不信 claims.roles）＋停用登入 gate（login `status==2`→LoginFailed 1000 不洩存在/防枚舉）。**多個全專案首次**：(1) **§5.8 分頁/filter 首 exercise**（空字串守門〔axios 未設 filter 序列成 `?status=&name=`→serde `Some("")`、handler `.filter(|v|!v.is_empty())` 當未設、enum/i16 `Some("")→Ok(None)`〕＋模糊 ILIKE〔userName/nickName/userEmail〕、userPhone/enum 精確）；(2) **`PageRes` 首消費者**（getUserList 大列表）；(3) **M:N join 寫**（user_role 同 txn replace）；(4) **prod `argon2 hash_password`**（addUser/updateUser 新密碼）；(5) **23505→2222 `sql_err()` 寫端 map 首落地**（⚠️o、`DbErr::sql_err()`→`SqlErr::UniqueConstraintViolation`→biz 2222、blanket `From<DbErr>` 不改、續走 5000）；(6) **INSERT op-log 首 consumer**（entity_id=`Some(after.id)`）。**2 clarify 拍板**（記 spec.md `## Clarifications`）：① 防自鎖對稱守門 self-lock（禁超管自我移除超管角色／自我停用→`biz.user.selfLockForbidden` 2222、整筆拒）；② 批次刪除缺漏 idempotent skip（已不存在/已刪 id 靜默略過；cannot-delete-self 仍為獨立整批 pre-check 拒）。**★ as-built 偏離（ILIKE 處方校正）**：plan/research-R6 原處方 `PgExpr::ilike(...).escape()` 編譯過但 **runtime 失效**——sea-query 0.32.7 `drop_right_escape_hack` 只認 `BinOper::Like`、不含 pg `PgBinOper::ILike`→渲染成 `ILIKE … ESCAPE` 非法 SQL→5000；U1 活體（§5.8 curl≠modal、CLAUDE.md §3）抓到、改用 `LOWER(col) LIKE ESCAPE`，校正 4 處文件（research R6／data-model §1／`contracts/user-management-contract.md` §3.2〔**跨 feature 權威、供 Role/Menu 刀繼承**〕／verification-commands C-V-0）；另 `find_active_by_id` polish（update 改呼之、DRY、消 dead-code warning）。實作分層：facade（getUserList §5.8 filter＋`PageRes`／getAllRoles／create+update `mutate_in_txn`＋`replace_roles_in_txn`／soft_delete／batch idempotent／`find_active_by_id`／23505 sql_err 寫端 map）＋handler（6 端點＋空字串守門＋self-lock 守門＋停用 gate＋`to_audit_operator` 餵）＋require_policy（6 端點 seed-backed）＋login status gate；base-web＝`rev3-system-manage.ts` WRAPPER＋`UserUpsertModel` typings＋`views/manage/user/modules/drawer/index.vue` MODAL-WIRING (a)＋移除 getRoleOptions mock＋locale `backend.biz.user.*`（4 鍵、先 Schema 後 locale）。4 執行單元 Workflow 驅動（U1 reads `7daa622`→U2 create·update `79f4983`→U3 delete·gate `977203f`／U4 base-web `c1806680`＋polish `8ccea9d`＋doc `5c0e3f7`）；C-V 全綠（build／62 bin＋7 live `--test-threads=1`／`endpoint_coverage_lint` `[&str;11]` 6 端點全治理＋`entity_access_lint`／C-V-9~11 CDP 真發 request〔空 param+模糊・add/edit/delete/batch 真打後端〕／C-V-13 零回歸〔blanket From 未改・enforce_mw/require_policy 未改・base-web 既有檔未改〕／C-V-14 prod target image build）、holistic review PASS（12 FR+11 SC 全 covered、cross-unit seams sound、無 overbuild）。**零 migration/entity/schema、無新 crate**；rust-api `3874182`→`8ccea9d`、base-web `223bc83e`→`c1806680`。D1 follow-up：前端 hasAuth button gating＋dynamic menu（getUserRoutes）＋選單可見性（menu policy 收 user/system-settings 對非 super）→ **波2 Menu 刀**（009 延此、授權靠後端 403 已擋、非破口；與 008 D1 同家族、選單可見性 010 兌現／policy 校正見 CHECKLIST §3.F）。

- ✅ **010-menu-management**（merge `3810103`，2026-06-19；波 2 第二刀＝Menu 資料島）— `/manage/menu` mock→真後端 **11 端點**：/route（`getConstantRoutes` public／`getUserRoutes`·`isRouteExist` auth-only）＋/systemManage 8 *Menu*（`getMenuList/v2`·`getMenuTree`·`getAllPages`·`addMenu`·`updateMenu`·`deleteMenu`·`batchDeleteMenu`·`restoreMenu`、皆 R_SUPER require_policy）。**動態角色選單（§I.2 首兌現）**：`getUserRoutes` auth-only→DB-fresh `roles_of_user`→`menu_routes_for_roles`（Casbin `v2='menu'` 過濾、鏡像 `buttons_for_roles`）→過濾 `list_active`→flat→tree（前端零過濾）；`.env` `VITE_AUTH_ROUTE_MODE` static→dynamic（拍板#7 兌現）。**3 全專案首立**：(1) `menu_routes_for_roles`；(2) **reparent 3+1 guard facade slim enum**（⚠️o：`ReparentError{Db,TargetMissing,NotDirectory,WouldCycle,ProtectedFixed}` 不依賴 AppError、`would_cycle` 純函式、handler 映 biz 2222）＋`DeleteError{Db,Protected,HasActiveChildren}`（批次整批拒 no-partial：同 txn 先全 `delete_check`、任一失敗整批 rollback）；(3) **menu flat→tree 序列化**——★ **祖先包含規則**（節點出現 iff route_name∈visible 或有可見後代；`manage` 目錄不在任何 menu policy 但其子在、必須祖先納入、否則管理選單孤兒崩潰、CDP 實證）。**`AuditOperation::Restore` 首 consumer**（restore 孤兒→`parent_id=None` 頂層、entity_id+before/after）。沿 008/009：`require_policy`／op-log threading／envelope／`sql_err()`→2222／`endpoint_coverage_lint`（`[&str;11]`→`[&str;22]`、8 menu 端點全治理）。**D1 兌現**（008+009 D1 follow-up）：選單可見性（非 super 不見 manage_menu/role/system-settings）＋前端 hasAuth gating（menu 頁 `menu:*`＋retroactive user 頁 `user:*`；system-settings skip＝無 code+super-only moot）。wire id 兩域（`MenuRoute.id` string／`Menu.id` number、⚠️r）。**2 clarify 拍板**（spec.md `## Clarifications`）：① 批次刪父子整批拒（逐項獨立驗證、批內任一 protected/有 active 子〔即使子同批〕→整批拒、no-partial、不做批內 cascade/排序）；② retroactive hasAuth gating 含 user/settings。**★ 3 as-built 例外/校正**：(1) **route store rev3-inline 例外**（`store/modules/route/index.ts` `initConstantRoute` dynamic 分支 `addConstantRoutes(data)`→`addConstantRoutes([...staticRoute.constantRoutes, ...data])`）——rust `getConstantRoutes` 回 `[]`（login/403/404/500 為前端 builtin、非 sys_menu）、dynamic 原碼丟棄前端 static 常數致 login 未掛載「No match for login」（FR-002 導回登入破口、SC-011 回歸）；R-cr 已預示此 builtin 假設誤之 contingency（「需最小 store rev3-inline 保留 local 常數、屆時 surface」）；**user 拍板 option 1**（最小合併、保前端常數所有權 §I.2）；CDP 驗 login dynamic 可達+fail-fallback 導回登入。原訂「route store 不改」之 C-V-11 註此 1 處授權例外；(2) `getMenuList/v2` 回**裸陣列樹**（非 typings `MenuList=PaginatingQueryRecord` 分頁包；base-web 新 `fetchGetMenuListV2` honest type＋custom transform、既有 `system-manage.ts/.d.ts` 未動）；(3) `getAllPages`/`distinct_pages` 回**裸 page 名**（component '$' 後段去 'view.' 前綴、排除 layout 容器；原回完整 component 字串致前端 page 下拉雙重拼接 garbage、主線 act-on-code 抓到、折進 U3）。**4 執行單元 Workflow 驅動**（U1 動態路由讀 `1369ca1`→U2 寫 CRUD `00436d1`→U3 刪除+回收桶+統一清單 `c377444`／U4 base-web `9fab3c4f`→`2385b212`→`5debb0a3`→login 修 `a59c2738`）；每單元 implementer→spec review→quality review（皆 pass 無 fix loop）＋主線獨立 ground-truth 自驗（git show --stat＋容器內 cargo build/test/live＋psql 無污染）＋逐單元兩段式 commit（worktree→outer pin、S9）。C-V-0~12 全綠（build `--locked`／純測〔menu_tree 祖先包含·reparent·delete_guard·active_model·wire id·bare_page〕／88 bin＋5 live `--test-threads=1`〔menu_crud·menu_guard·menu_recycle〕／`endpoint_coverage_lint` `[&str;22]`＋`entity_access_lint`／C-V-8 policy-gate Admin/User→5003／typecheck／CDP 三角色側欄差異〔D1〕+CRUD 真發+restore+hasAuth〔R_ADMIN user:edit-only〕+2222 在地化+login dynamic+fail-fallback／零回歸／prod image build）、final holistic review **READY-TO-FINISH**（11/11 SC MET、零 blocking、wire 三端對齊無型謊、cross-unit PASS）。**零 migration/entity/schema、無新 crate**（表＋seed＋policy＋button code m001/m002/m004 已備、Constitution 9/9 PASS）；rust-api `8ccea9d`→`c377444`、base-web `c1806680`→`a59c2738`。**Role×Menu 授權（getRoleMenu/updateRoleMenu/getRoleHome/updateRoleHome+menu-auth-modal）留 Role 刀**（消費本刀 getMenuTree/getAllPages）。010 follow-up（CHECKLIST §3.B/§3.F/§3.H）：login fallback transient「No match」〔upstream soybean resetStore 排序、cosmetic〕／manage_policy-archive view 缺〔波3 future feature seed gap、Super console、dropped from sidebar〕／R_ADMIN user:edit button vs super-only endpoint seed 不對齊〔忠實 seed〕。

- ✅ **011-role-management**（merge `b80c9e3`，2026-06-19；波 2 第三刀＝Role 資料島）— `/manage/role` mock→真後端 **9 端點**：getRoleList〔R_SUPER+R_ADMIN〕／addRole·updateRole·deleteRole·batchDeleteRole·getRoleHome·updateRoleHome〔R_SUPER〕／getRoleMenu·updateRoleMenu〔**protected** R_SUPER〕。**★ 全專案首個 runtime casbin policy WRITE＝DB-first（B1 校正後、constitution §I.7 §4.2 合規）**：net-new `model/facade/sys_casbin_rule::set_role_dimension<C:TransactionTrait>(conn,role_code,dimension,desired_objs,operator,trace,role_id)→Result<SetDimensionOutcome,SetDimensionError{Db,Rejected(Vec<String>)}>`——於【單一】`mutate_in_txn`（AppState.db 同 txn、read+reject-check+write+op-log 原子無 TOCTOU）：read current `entity::casbin_rule`〔11-col、filter ptype='p'∧v0=role_code∧v2=dimension〕→diff(current.v1 vs desired、dedup desired 防重複 menu_id 撞 UNIQUE)→**②protected-reject**〔to_revoke 任一 `protected=true`→經 R 值回 `Rejected`〔非 DbErr 通道〕、提交 no-op 空 txn＝零變更、handler 映 `biz.role.menuProtected` 2222〕→`delete_many` revoke＋逐 obj `ActiveModel{ptype,v0,v1,v2,v3-5:'',protected:false,created_at:now,created_by:Some(op.id)}.insert` grant→op-log `AuditEvent{Update,entity_table:"casbin_rule",entity_id:role_id,before/after=route_name 集}`〔同 txn、★原子〕；`changed=!to_revoke.is_empty()||!to_grant.is_empty()`。handler `update_role_menu` 在 txn commit **後**且 `outcome.changed`→`state.enforcer.write().await.load_policy()`〔⑤ 全量 reload、本地單實例〕。**絕無 enforcer MgmtApi 寫**（remove_filtered_policy/add_policies——觸 in-memory＋adapter auto_save 旁路＋非原子稽核＝rev2-034 anti-pattern、§4.2 ①④ 反轉＝B1，本刀根除；grep 全 diff 唯一命中＝禁令 doc-comment）。讀端 getRoleMenu 復用 010 `menu_routes_for_roles(enforcer,&[code])`〔in-memory 讀、合規〕→route_names→menu id。**讀寫閉環**：updateRoleMenu 寫 v2='menu'+reload＝010 getUserRoutes 讀端寫端對手（改某角色選單→該角色 getUserRoutes 側欄反映、CDP 換角色登入實證）。**delete guards**（handler 前置、任一觸→2222 整筆/整批拒 DB 無變）：① seeded hardcode {R_SUPER,R_ADMIN,R_USER_COMMON}→`seededProtected`；② net-new `sys_user_role::count_users_by_role_id(role_id)>0`→`inUse`；③ self `roles_of_user(claims.uid)` codes contains role.code→`cannotDeleteSelfRole`；批次整批拒 no-partial。**id↔route_name 映射**（net-new `sys_menu::ids_for_route_names`/`route_names_for_ids`、orphan/missing skip、D1 tolerant）。角色首頁 getRoleHome/updateRoleHome＝`sys_role.home` entity 讀/寫（net-new `set_home`、`mutate_in_txn` 原子 UPDATE op-log、與 010 `home_of_roles` 讀端對齊）。roleCode 23505→`biz.role.duplicateRoleCode` 2222（沿 009 `map_write_err` `sql_err()`）。沿 008/009/010：`require_policy`／op-log threading／envelope／`PageRes` §5.8 filter（空字串守門）／`endpoint_coverage_lint`（`[&str;22]`→`[&str;31]`、9 role 端點全治理、9 皆 m002 seed）／`entity_access_lint`（entity::casbin_rule **僅** sys_casbin_rule facade 豁免、handler/main 零 path-root）。**3 拍板**（spec.md `## Clarifications`、brainstorm D1-D3）：① D1 scope＝menu-auth only（button-auth/endpoint-auth＋policy 治理機留波3）；② D2 治理姿態＝**DB-first 合規**（★ B1 校正後——原 MgmtApi 非原子旁路違 §4.2 ①④、改直寫 casbin_rule＋同交易原子審計＋②protected-reject＋寫後 reload；archive/restore/un-protect/PolicyMutated-優化/publish-watcher 治理機＝波3 功能交付排序、非 invariant 反轉）；③ D3 delete guards＝種子+使用中+自身（批次整批拒）。wire id 域＝number（`Role.id`／`roleId`／`menuIds`、⚠️r、獨立參數不轉 String）。**3 執行單元 Workflow 驅動**（U1 Role CRUD `2332002`〔含 op-log 跨表 entity_id 守門 fix〕→U2 Role×Menu DB-first+home `e3d828e`〔含 2 review-minor：set_role_dimension dedup desired／`SetDimensionError` #[allow(dead_code)] payload〕→U3 base-web `caa1e4bc`），每單元 implementer→spec review→code review（皆 pass 無 fix loop）＋主線獨立 ground-truth 自驗（git show --stat＋容器內 build/lint/live＋psql 無殘留＋★ B1 grep 零 MgmtApi）＋逐單元兩段式 commit（worktree→outer pin、S9）。final holistic review（fresh-agent）**SHIP**（10/10 SC MET、B1 grep-confirmed 零 MgmtApi、讀寫閉環閉合、零 overbuild〔button-auth-modal 仍 mock、無 wave-3 leak〕）＋2 nit 修：G1 `role_home` live op-log 斷言原絕對計數 `==1`〔假設無前序〕、CDP F2 對 R_ADMIN〔id=2〕改首頁留 committed UPDATE 列被 READ COMMITTED 看見→非冪等偽紅、加本測試專屬 `trace_id='role-home-smoke'` 守門隔離自身寫（`30de791`）；N1 C-V-7 doc method 對齊（deleteRole/batchDeleteRole=DELETE、原 POST→405 在 policy 層之前非真授權測）。C-V-0~11 全綠（build `--locked`／純測 role_delete_guard·role_active_model／`endpoint_coverage_lint` `[&str;31]`＋`entity_access_lint`／live role_crud·role_guard·**role_menu_loop**〔DB-first 讀寫閉環＋②protected-reject＋★F1 原子審計〔op-log 恰一筆 entity_table='casbin_rule'∧entity_id=role_id∧UPDATE、filter 雙欄〕、非 txn 隔離→snapshot 全 11-col+byte-restore+psql 驗 casbin_rule 回原狀無殘留〕·role_home／C-V-7 policy-gate Admin/User 9 端點→5003／typecheck／CDP 經 :31080/api 真發 request＋讀寫閉環換角色側欄＋★F2 換角色登入落地新首頁＋hasAuth gating〔Admin→403、無 manage_role menu〕＋cleanup+psql baseline／prod image build）。**零 migration/entity/schema、無新 crate**（表＋3 角色＋9 端點 policy＋`role:*` button code m001/m002 已備、Constitution **9/9 PASS**〔Q9 DB-first 合規〕）；rust-api `c377444`→`30de791`、base-web `a59c2738`→`caa1e4bc`。011 follow-up（CHECKLIST §3.F/§3.H）：menuProtected 訊息泛化〔Rejected payload 攜 protected objs 但 handler 泛化、波3 surface〕／revoke=hard DELETE〔archive/restore/治理機＝波3、復用 set_role_dimension pattern〕／C-V-9 CDP throwaway 角色 1 筆 casbin_rule op-log 審計列〔cosmetic、功能表 pristine〕。

- ✅ **012-audit-log-query**（merge `55d34ff`，2026-06-19；波 2 殿後刀＝Audit；★ 波 2 data islands 全完成）— 把三張既有 append-only 日誌表接出**唯讀查詢端＋Super-only 審計中心單頁三分頁**（/manage/audit、操作異動/API存取/登入嘗試）：3 唯讀端點 getOperationLog/getAccessLog/getLoginAttempt〔R_SUPER GET、§5.8 分頁/filter〕。**★ 首個波 2 帶 migration 的刀＝m005 delta**（`m005_audit_log_query.rs`、Migrator m004 後）：`up` execute_unprepared raw SQL〔鏡像 m002 seed/m001 index〕＝① sys_menu `manage_audit`〔parent=manage subquery、menu_type=2、/manage/audit、component=`view.manage_audit`、i18n_key=`route.manage_audit`、protected=false、`ON CONFLICT (route_name) WHERE deleted_at IS NULL DO NOTHING`＝**F3 partial unique**〔bare conflict-target 會 runtime error〕〕②casbin_rule 4 列〔3 GET R_SUPER＋manage_audit menu policy v2='menu'、8-col、`ON CONFLICT (ptype,v0..v5)`〕③CREATE INDEX IF NOT EXISTS ×4〔operation/access × created_at、(operator_id,created_at)〕；`down` 對稱〔DELETE casbin 限 (v0,v1,v2) tuple+v3-5='' 防過刪／DELETE sys_menu by route_name／DROP INDEX ×4〕；**無新業務表、無 ALTER 既有日誌表（§I.6 archetype A 六審計欄未觸）、up→down→up 可逆**（主線親跑 menu/policy/idx 1/4/4→0→1/4/4、日誌表 41/744/214 不變）。**讀端 3 sink facade +list**〔鏡像 sys_role::list 的 apply_if/paginate.fetch_page.num_items、★ order_by_desc(created_at,id)〔時序新到舊 FR-002、F4 校正 sys_role 的 asc〕、page 0-based〕＋OperationLog/Access/LoginAttemptFilter；**★ IP 模糊＝`host(col)::text LIKE $1`**〔net-new 無 codebase 先例、`Expr::cust_with_values`、host() 去 mask、主線 psql+facade live+HTTP 三證命中 dev 172.x／排除 999/203.0〕；文字模糊 `LOWER(col) LIKE ESCAPE`〔**非 PgExpr::ilike**、沿 009 ⚠️o 校正、user-management-contract §3.2 繼承〕；精確 eq〔operation/method/success/http_status/entity_id〕／範圍 gte-lte〔created_at〕／operator_ids is_in。**net-new `sys_user::names_for_ids<C>(conn,&[i64])→HashMap<i64,String>`**〔`Id.is_in`、**不濾 deleted_at＝含已刪、審計保留歷史操作者名**、沿 find_by_id；live 證已軟刪 id=12 cdpUser009 仍 resolve〕＋`ids_for_name_ilike`〔operator by 名→ids〕；handler enrich：records operator_id 集→names_for_ids→operatorName〔NULL=系統 op〕。**handler 3 端點**〔system_manage.rs〕§5.8 normalize〔current/size／空字串守門 normalize_str_filter〕＋operator_name→ids＋**日期 parse**〔`parse_audit_date` RFC3339∨YYYY-MM-DD、僅起/僅訖各自寬鬆 gte-only/lte-only、畸形→`AppError::Biz("biz.audit.invalidDateRange")` 2222、F2〕＋parse_http_status/parse_success/**parse_entity_id**〔皆空字串守門〕＋Model→**honest wire item**〔id:i64 經 wire_id 2^53 守門、entityId/operatorId:`Option<i64>` number 域 ⚠️r〔非 wire_opt_i64 字串〕、operatorIp:`IpNetwork.ip().to_string()` 去 mask、nullable→Option **無 skip_serializing_if**〔顯示 null、FR-011〕、payloadBefore/After:`Option<serde_json::Value>` 任意 JSON〔casbin 列實為字串陣列〕、createTime:to_rfc3339〕→PageRes。main.rs audit Router group 3 GET `route_layer(require_policy(path,"GET"))`+`.layer(enforce_mw)`+`.merge`（path/method 與 m005 casbin seed 逐字對齊）。**★ endpoint_coverage_lint [31→34]＋seed 來源修正**〔斷言 A 原僅讀 `m002_rev2_seeds.rs`→3 audit policy 在 m005 找不到會 FAIL；改 `all_migration_seeds()` glob 讀 `migration/src/m0*_*.rs` concat 涵蓋 m005+未來刀、不動 frozen m002〕；entity_access_lint 綠〔handler/main 零 path-root entity::、3 facade list 走 model/facade/ 豁免〕。**base-web 100% 淨新、wire 從零**：`rev3-system-manage.ts` 3 fetch〔fetchGetOperationLog/AccessLog/LoginAttempt〕＋`pruneNullParams`〔剔未設 filter〕／`rev3-system-manage.d.ts` 3 honest item〔逐欄對齊 rust wire、**payload→`unknown\|null` 非 Record**〔§9 過度具現校正〕、nullable→`string\|null`/`number\|null`〕＋SearchParams＋List；MODAL-WIRING **(e) 新管理頁** `views/manage/audit/index.vue`〔NTabs 3 tab〕＋modules 3 子表〔NDataTable+filter 列〔NInput 模糊/NSelect 下拉/NDatePicker daterange→ISO/operator 按名〕+NPagination、op-log `renderExpand` payload before/after JSON.stringify〕；i18n `page.manage.audit.*`＋`backend.biz.audit.invalidDateRange`〔先 Schema app.d.ts 後 locale zh-cn/en-us〕；Super-only 靠 m005 menu policy R_SUPER＋dynamic getUserRoutes〔頁面層無顯式 hasAuth、非 super 側欄不見〕；frozen 不改〔route store/transform getViewName 邏輯/system-manage.ts/auth.ts/request/routes.ts；imports.ts/transform.ts routeMap/components.d.ts 為 elegant-router/unplugin 自動生成 1 行 data、連同 commit〕。**★ biz key 分層校正（主線 grep+攔截器源碼定案）**：rust 端所有 biz key 無 backend 前綴〔`biz.audit.invalidDateRange`、鏡像 biz.user.*〕、base-web `translateBackendMsg(msg)=$t('backend.'+msg)`〔locales/index.ts:26〕補 backend.→locale key=`backend.biz.audit.invalidDateRange`〔data-model §6 寫 rust 端帶 backend. 為文件錯誤、會雙補→key not found〕。**★ CDP 接住真 bug（curl≠modal 經典案例）**：U3 CDP 抓到 `getOperationLog?entityId=` 空字串→**HTTP 400「cannot parse integer from empty string」整頁掛**〔U2 為 http_status/success 做 Option<String>+parse 守門卻漏 entity_id〔Option<i64> 直接 serde〕〕；補後端權威守門 `parse_entity_id`〔Option<String>+空字串→None/valid→Some/畸形→2222、鏡像 parse_http_status、`fca64a0`〕＋前端 pruneNullParams 雙防線（FR-008）。**唯讀（FR-010/SC-006）**：純 SELECT、無 mutate_in_txn、**不寫 op-log**〔查詢前後 op-log 41→41〕；live/CDP **無 cleanup**〔access-log per-request +N=F1 既有基建軌跡、非本功能自審〕。**4 commits Workflow 驅動+主線編排**（U1 m005 migration `fce18c4`→U2 rust 讀路徑 `be94fcd`→entityId 守門修正 `fca64a0`→U3 base-web 審計頁 `1764154e`；每執行單元一支 Workflow〔implementer TDD→spec-compliance review→quality review、皆 pass〕＋主線單元邊界獨立 ground-truth 自驗〔`git show --stat` 抓偷加檔＋容器內 build/lint/live＋C-V-4 curl policy-gate＋親跑 m005 up/down/up＋親看 CDP 截圖〕＋逐單元兩段式 commit〔worktree→outer pin、S9〕）。C-V-0~9 全綠（build／純測 19〔含 4 parse_* 守門〕／`endpoint_coverage_lint` `[&str;34]`+全 migration glob seed＋`entity_access_lint`／live audit_query〔分頁/守門/精確/文字模糊/★IP 模糊首驗/operator 按名含已刪/範圍/enrich〕／policy-gate Admin·User 9 端點→5003 Super→200／m005 up→down→up／typecheck／CDP 經 :31080 真發 request 3 tab+payload 展開+super-only／零回歸〔frozen infra+base-web frozen 未動、/health ok〕／prod target image build〔m005+facade/handler 編入、無新 crate〕）、final holistic review（fresh-agent 冷讀）**無 blocker**〔12 FR+9 SC+6 Edge Cases 全覆蓋、cross-unit wire/filter 參數/日期/biz key/授權/lint 接縫一致〕。**零 ALTER/新業務表、無新 crate**（Constitution v1.1.1 **9/9 PASS**；m005 delta 為唯一 schema 變更〔seed+索引、可逆〕）；rust-api `30de791`→`fca64a0`、base-web `caa1e4bc`→`1764154e`。**遞延後續刀**：D11 client_ip(peer)/xff_ip/real_ip 三欄統一 IP forensic 模型＋寫端 ALTER〔同 007 XFF 完整化+Cloudflare CF-Connecting-IP〕／D4 009 user 角色變更 payload delta／log retention／pg_trgm fuzzy GIN〔scale〕／CSV 匯出／2xx-5xx 類別 quick-filter。004 follow-up（access/login client_ip decode round-trip 經本刀 entity Model 讀回已證）／005 follow-up（op-log operation 字串契約本刀已 exercise）隨 012 觸發解決〔均已歸檔 MILESTONES §3〕。

### D11 遞延刀 — 013 XFF→real_ip 鑑識 ✅ 全完成（2026-06-21；波 2 後、波 3 前獨立刀）

> 收 012 D11（IP forensic 模型＋寫端 capture）＋007 `resolve_client_ip` XFF 完整化為【同一刀】。把 007 單值解析升級為兩層信任模型，屬 D11 排程刀、不屬任何交付波次。

- ✅ **013-xff-real-ip-forensics**（merge `8980d00`，2026-06-21）— 把 007 audit overlay 的**單一信任集 rightmost-untrusted** 單值解析重寫為**兩層信任模型＋四欄鑑識×三審計表＋審計中心顯示/篩選＋nginx CF 驗證閘/Tunnel**。**① 解析核心**（`server/src/audit_ctx.rs` 純函式 TDD）：`normalize_xff_tokens` IIS-aware〔split `[\s,+]`、剝 v4`:port`/v6`%zone`/`[v6]:port`、garbage drop、空→空〕／`resolve_client_ip(peer,xff,&TrustModel)→(IpAddr,Confidence)` 四分支〔A peer∉trusted→`Direct`／B 鏈含 CDN 段＝**Tier-1 位置錨不硬 gate**〔最右 CDN 左一非 CDN、CDN 右側盲剝、**錨免疫漏設我方 public**〕→`CdnAnchored`／C Tier-2 由右而左逐點 skip `internal_default∪my_public∪binding.internal`〔連續多跳/my_public 連續逐點再比〕第一個皆非→`ProxyClean`、走過 `dual_role` my_public 或 binding 相鄰內網不符→`ProxySoft`／整鏈受信→`Fallback`(peer)〕／`apply_cf_overlay(base,cip,verified,real)→Confidence` top-level〔verified==1∧cip 有值∧base∈{anchored,clean,soft}→cip==real?`CdnVerified`:`CdnMismatch`；否則原樣、**MUST NOT 覆蓋 real_ip**（FR-005）〕／`Confidence` 七態+`as_str` 字串契約。**信任 config**：net-new `toml` 0.8.23 parse-only workspace dep（1.86 `--locked` 編過）＋`TrustModel{cdn(+connecting_ip_header)/my_public(+dual_role)/internal_default/bindings}`、`parse_trust_model` 純可測＋`load_trust_model`〔讀 `TRUST_MODEL_FILE`、缺檔 fallback flat env→all-direct〕、CIDR String 收+post-parse、**per-set fail-safe**；`AppState.trusted_proxy_cidrs`→`trust_model:Arc<TrustModel>`。**② 四欄鑑識×三表**：`m006_audit_ip_forensics` 可逆 delta〔execute_unprepared 鏡像 m005、rename `client_ip→real_ip`/`operator_ip→operator_real_ip`+add `peer_ip`/`ip_confidence`/`operator_*`、down 嚴格反序、m001 凍結 PG RENAME 自動跟 index、**up→down→up+fresh temp-DB m001..m006 全鏈可逆**〕+entity 三檔同步；`audit_mw` 讀 cf header+overlay+四欄+xdb 續餵 real_ip。**★ op-log 4/4 補完（user 親決「補滿 4/4」、反轉 D7「不動 16 facade」）**：`AuditOperator` 維持 `Copy`〔`{id,peer,real}`〕+net-new `AuditMeta` bundle thread operator xff/confidence 過 ~15 facade mutate fn 的 AuditEvent 構造〔原硬寫 `None`→填 meta〕→op-log 三表全 4/4 live（psql 證）；三 sink+login sink+honest wire〔rename+add、nullable 顯 null 無 skip、IP `.ip()` 去 mask、ip_confidence 7-literal〕。**③ 012 審計中心擴充**（base-web）：四欄顯示〔C2 序 `ip_confidence`(NTag 七態著色、共用 `ip-confidence-tag.tsx`)→`peer_ip`→`real_ip`→`x_forwarded_for`、operation `operator_` 前綴〕+三模糊一下拉篩〔C1：facade peer_ip ip_host_like+ip_confidence eq+op-log 補 operator_x_forwarded_for ilike+handler `normalize_str_filter` 空字串守門〔全新欄 Option<String>、無 axum 400 風險〕；三模糊 NInput+ipConfidence NSelect〕+i18n 雙語〔Schema 先擴+confidenceTag 七鍵〕；**confidence 七態字串 rust as_str↔d.ts literal↔options↔i18n byte-identical**。**④ nginx CF 閘/Tunnel**（`89e0fdf` deploy）：http `geo $remote_addr $x_cf_verified`〔CF 官方 v4×15+v6×7 ∪ cloudflared loopback、**$remote_addr 不可偽造、不含 docker bridge 以利測偽造**〕+`map` strip 偽造 CF-Connecting-IP；location /api/ `proxy_set_header X-CF-Verified/CF-Connecting-IP`〔**覆寫 client 偽造同名 header＝FR-010 核心**〕、既有 4 header 零回歸、**不啟 realip**；`trust-model.toml` 啟用 cdn CF 段（geo 一致）+loopback。**FR-010 偽造 negative 端到端證**：非 CF 源偽造 X-CF-Verified:1+CF-Connecting-IP:6.6.6.6 經 nginx→覆寫 0+strip→rust overlay 不 fire→psql `cdn_anchored`（≠`cdn_mismatch`）。**6 執行單元 Workflow 驅動**（U1 解析核心 `74f5c4d`→U2 rename 原子 `493f015`+U2b op-log 4/4+測試隔離 `c1fe429`→U3 顯示 `25cab888`→U4 篩選 rust`627ebc6`/base-web`e79e7aa8`→U5 nginx CF `89e0fdf`→U6 polish `3f2ebc6`〔final holistic+清 IpForensics orphan〕）；每執行單元一支 Workflow〔implementer TDD→spec-compliance review→fix loop→quality review→fix loop、皆 pass〕＋主線單元邊界獨立 ground-truth 自驗〔git show --stat 抓改名完整性/偷加檔＋容器內 build/lint/live `--ignored --test-threads=1`＋psql 三表四欄＋curl 空 param 守門＋FR-010 偽造 negative〕＋逐單元兩段式 commit〔worktree→pin、S9〕。C-V-0~9 全綠（純測 36+12 案例 resolve_cases/lint 2+2/m006 up→down→up+fresh temp-DB 全鏈/live 四欄 psql/curl honest null/CDP 顯示+篩選/CF 正向 cdn_verified·mismatch/Tunnel/FR-010 negative/**prod target image build**〔新 toml external dep 非新 crate、multi-stage `--locked` 無 COPY 缺口〕/零回歸 18 ignored live+012 既有行為+既有 nginx header），final holistic review（fresh-agent 冷讀、不信前面 review）**PASS**〔5 US+10 FR+7 SC 全覆蓋、跨單元接縫 byte-identical 無型謊〕。**1 user 拍板**（op-log 4/4、AuditMeta thread、反轉 D7）＋⚠️ac（constitution §I.6 釐清 v1.1.2 背書 m006 ALTER 合規演進）。**schema＝m006 可逆 delta（rename+add IP forensic 欄、§I.6 archetype B 六審計欄未觸）、新 toml 外部 dep、無新 crate**；Constitution 9/9 PASS。pins：rust-api `fca64a0`→`3f2ebc6`、base-web `1764154e`→`e79e7aa8`。**收 012 遞延**：D11 IP forensic 模型（013 採**四欄** peer/real/xff/confidence、較 D11 原「三欄」更完整）＋007 XFF 完整化全兌現；**遞延後續刀**：D4（角色變更 payload delta）／log retention（⚠️n）／pg_trgm fuzzy GIN／CSV 匯出／2xx-5xx 類別 quick-filter／`operator_id` never-read warning（007 起 pre-existing、非 013 範圍）。merge --no-ff、feature branch `013-xff-real-ip-forensics` 保留。

### 波 3 — 行為島＋policy ✅ 全完成（2026-06-22）

> 行為島 2 刀＋policy 縱切，三刀全收：**014 Auth/Token/Session 合刀**（merge `3066bc2`）＋**015 Policy 治理島 §4.2 完成刀**（merge `65f1838`）＋**016 Button-Endpoint policy 縱切〔三維 RBAC runtime 編輯〕**（merge `fa17def`、feature branch 皆保留）。波 3 出口三項達標〔§4 三台機器 invariants 逐條自動化驗證／7777·8888 兩通道 CDP〔014〕／protected 拒撤 live〔015 menu＋016 endpoint〕〕→換波 4 observability。**alt-login 4 流程 stub 刀＝⚠️m 2026-06-22 重議→延後出波3**（user 親決 C、移 post-波3 v1-completeness slot、CHECKLIST §3.D）。

- ✅ **014-auth-token-session**（merge `3066bc2`，2026-06-22;波 3 第一刀＝Auth/Token/Session 合刀）— 把 006 鋪的 Auth 島最小段（login＋enforce_mw＋pointer＋is_current stub）補成 DESIGN §4.1（token rotation chain）＋§4.3（single-session lifecycle）兩台行為島狀態機，並補硬即時撤銷＋cleanup-job＋2-instance 多副本驗證。**★ 接地揭露 rust-api 完全沒有 Redis client（憲法 §I.7「Redis 僅快取」假設它存在、實際零）→ 本刀含建 Redis 基建**：`server/src/redis.rs` RedisHandle〔Client＋MultiplexedConnection 多工 get/set_ex/del/publish＋subscribe_pubsub 獨立 pub-sub 連線、全 fail-OPEN 不可達回 None/() 不 panic〕＋`AppState.redis:Arc<Option<RedisHandle>>`＋`single_session_default:Arc<AtomicBool>`〔§I.7 runtime store〕＋`config.rs` load_redis_url〔APP_REDIS_URL_FILE 檔優先〕＋main boot best-effort 連線降級＋`spawn_settings_watcher`〔SUBSCRIBE settings:invalidate→重載 AtomicBool、斷線指數 backoff 1→30s 重連重訂閱〕＋008 update_setting commit 後 PUBLISH;新 `redis` 0.27.6 crate（tokio-comp、1.86 `--locked` 編過、無 transitive MSRV bump）。**§4.1 token rotation**：`/auth/refreshToken`〔public、verify refresh→`Logout`8888〔絕不 3333/9999/9998〕→pointer-first `is_current`→7777→sha256→find_by_hash→decide_rotation〕＋`auth/session.rs::decide_rotation` 純函式四分支〔active→Rotate／used&(now−used_at)<GRACE_SECS(30)→Benign／≥grace·revoked·used_at NULL→Reuse fail-closed／None→NotFound〕＋`sys_token` facade〔find_by_hash／find_by_hash_for_update FOR UPDATE／mark_used〔WHERE status=active 冪等〕／revoke_chain〕＋新 jti＝Uuid 使 JWT body byte-distinct→token_hash 不撞 UNIQUE。**★ TOCTOU lock-then-redecide 加固（SC-002）**：原 pre-read 判決後才鎖列、併發 revoke_chain 落地→mark_used rows=0 卻仍重鑄;改 Rotate/Benign 進 txn FOR UPDATE 鎖後對【鎖住列】重跑 decide_rotation，locked Reuse→revoke+8888 絕不 issue_rotated_pair（`toctou_revoked_chain_locks_to_reuse_no_rebake` #[ignore] live 證 active 列數=0）。**§4.3 single-session**：`resolve_policy` 純函式〔session_policy∈{on,off,inherit}×全域 single_session_default→On/Off〕＋`is_current` 改 policy-aware〔簽名 db→&AppState、`sys_user::session_gate_of` 一趟 PK 取(policy,pointer) 取代 current_session_id_of、effective Off→Ok 放行【修 006「永遠踢」bug】、On pointer≠sid→7777、Err/None fail-OPEN〕＋login `revoke_other_chains`〔effective On 撤該 user 既有 active 鏈〕＋set_pointer 永遠執行＋session_policy wire〔getUserList item＋sessionPolicy honest literal／updateUser UserWrite＋session_policy／build_update_active_model None→不改、user_write_from_req 驗∈{inherit,on,off}→2222〕。**硬即時撤銷（閉 006 Auth 島 follow-up）**：Redis denylist `revoked:user:{uid}=revoked_at EX access_ttl`＋`denylist_gate`〔enforce_mw is_current 後 ＋ refresh verify 後【雙查 defense-in-depth】、`iat<=revoked_at`→Logout 8888〔★ `<=` 非 `<`：同秒登入/撤銷 iat==revoked_at fail-secure、implementer 自驗實測 batchDelete 同秒漏網抓到、FR-007 安全要求〕、fail-OPEN〕＋`revoke_user_sessions`〔Redis set_revoked 先 primary＋DB txn `revoke_all_user_chains`〔WHERE user_id AND status<>revoked＝撤 **active+used 全鏈**、防被竊 used token grace 內 Benign 重鑄繞撤 SC-004〕＋`clear_pointer`〕→接 009 deleteUser〔Some 才撤〕／batchDeleteUser〔迴圈內逐 id D-02〕／updateUser〔status==2 停用〕，warn-not-fatal。**cleanup-job 新 workspace crate**〔`cleanup-job/src/main.rs`、dry-run 預設只 count／--execute 物理刪／冪等／單旗標〔std::env::args、無 clap〕、sea-orm＋tokio raw Postgres SQL `expires_at<now()−make_interval(60s)`〔與 status 無關、不依賴 entity 避 DateTimeWithTimeZone feature-gate〕、SKEW_MARGIN_SECS=60≠GRACE_SECS=30〕＋**prod Dockerfile 四處 COPY（D-01 CRITICAL）**〔Manifest cleanup-job/Cargo.toml＋Source cleanup-job/src＋builder cp target/release/cleanup-job→/out＋runtime COPY /out/cleanup-job→/usr/local/bin/、entrypoint cleanup-job) case 001 已預接;prod build 實機驗三 binary 落地〔cleanup-job 5.7MB exec 非 not-found〕〕。**base-web session_policy UI**〔`rev3-system-manage.d.ts` SessionPolicy literal＋UserUpsertModel intersection〔不動 frozen system-manage.d.ts User〕＋UserListItemRev3 honest 型;`constants/business.ts` sessionPolicyRecord/Options 鏡像 userGenderRecord;i18n 先 Schema 後 locale〔page.manage.user.sessionPolicy 三態＋backend.biz.user.invalidSessionPolicy〕;drawer NSelect MODAL-WIRING ★ (a) rev3-inline〔edit 讀 rowData.sessionPolicy fallback inherit〕;CDP 7777 modal／8888 logout 兩通道乾淨登出實機驗〕。**2-instance 多副本驗證（C1 拍板 B-驗證版）**〔docker-compose.dev.yml `rust-api-2` profiles:[multi]、:31082、共用 jwt/DB/Redis secret〔_FILE 同檔→token 跨實例可驗〕＋獨立 rust_api2_target 隔離 build lock、entrypoint cargo run --bin server 避 cargo-watch rebuild-loop;C-V-6 S0 跨實例 token 驗章／S1 watcher 收斂／S2 shared DB pointer 踢 7777／S3 denylist 跨副本 8888／S4 CLIENT KILL TYPE pubsub→兩實例 1s backoff 重訂閱→post-kill flip 仍收斂;dev 預設仍 1 instance、watcher U1 已夠韌無 rust 改〕。**8 執行單元 Workflow 驅動**（U1 Redis 基建 `ddec5c2`→U2 rotation＋TOCTOU 加固 `a2ea33b`→U3 single-session `44885d9`→U4 denylist `b37fac3`→U5 cleanup-job＋Dockerfile worktree`a9d3986`/outer`7c08ed2`→U6 base-web `f3b2bf07`→U7 compose `fc41667`→U8 polish＋holistic），每執行單元一支 Workflow〔implementer TDD→spec-compliance review→fix loop→quality review→fix loop、皆 pass〕＋主線單元邊界獨立 ground-truth 自驗〔git show --stat 抓範圍＋容器內 force-touch build/test/live＋psql/redis-cli＋frozen 未動驗〕＋逐單元兩段式 commit〔worktree→outer pin、S9〕。**★ 3 個設計時/自驗攔截的 spec 未明示真漏洞**（皆零 schema、附自動化驗證、reviewer 一度標 design-conforming 遞延、被主線/implementer 接住）：① U2 TOCTOU revoke-during-rotate 重鑄〔SC-002〕→lock-then-redecide;② U4 撤鏈只撤 active 漏 used→被竊 used token grace 內 Benign 重鑄〔SC-004〕→撤 active+used;③ U4 `iat<revoked_at` 同秒漏網〔FR-007 100%〕→`<=` fail-secure。**C-V-0~10 全綠**（decide_rotation/resolve_policy 純測／lint `endpoint_coverage_lint[&str;35]`〔/auth/refreshToken public 納入〕＋entity_access_lint／live rotation·single-session·denylist·cleanup·2-instance〔容器內 --test-threads=1〕／CDP 7777·8888 兩通道／prod target image build 三 binary 落地〔redis crate --locked 編入、cleanup-job COPY 無缺口〕／零回歸 cargo test 146 passed＋lint 2+2、migration `git diff 3f2ebc6..HEAD migration/` 空〔6 m-檔不變、up idempotent〕、base-web pnpm typecheck、/health 200、006/008/009/013 spot-check 0000），final holistic review（fresh-agent 冷讀、自跑 build＋146 test＋live US1/US3 curl 接地）**PASS**〔5 US＋14 FR＋10 SC＋§I.7 §4.1/§4.3 invariants 逐條有自動化驗證、cross-unit 接縫一致無型謊〕。**零 migration**〔schema 全在 m001/m002 ⚠️t 波0全建、`sys_token` 9 欄無 jti〔jti 在 JWT〕、`sys_user.session_policy`/`current_session_id` 既有、Redis 無表〕、新 `redis`＋`cleanup-job` crate、Constitution **9/9 PASS**。**4 拍板**：C1 多實例 B-驗證／C2 撤銷硬即時 fail-OPEN／C3 per-user session_policy UI 做／C4 alt-login 排除本刀〔⚠️m〕。**Redis sess:{uid} pointer 快取按 data-model X-01 sanctioned defer**〔is_current 直讀 DB pointer、§I.7-compliant;policy 同 row 免費取得、快取僅帶 stale-hit bug class〕。pins：rust-api `3f2ebc6`→`a9d3986`、base-web `e79e7aa8`→`f3b2bf07`、deploy/compose 直 outer。**遞延後續**：Auth 島 follow-up（token 即時撤銷/JWT 參數硬編）已閉口;research.md D4 `<`→`<=` doc-drift 已校正;casbin enforcer 跨實例 pub-sub＝波3 Policy-governance 刀（接本刀 Redis 基建/watcher 範式）;1 pre-existing flaky `op_log_atomic_three_paths`〔全 --ignored 套件 shared-seed op-log count 污染、非 014 回歸〕。merge --no-ff、feature branch `014-auth-token-session` 保留。

- ✅ **015-policy-governance**（merge `65f1838`，2026-06-22;波 3 第二刀＝Policy 治理島 §4.2 完成刀）— 把 011 `set_role_dimension` 鋪的 casbin policy 寫側（DB-first＋②protected-reject＋reload-on-`changed`、revoke=HARD DELETE、reload local-only）補成 **DESIGN §4.2 完整治理島**。**① revoke→archive-move**（`model/facade/sys_casbin_rule::set_role_dimension` revoke 段改寫）：DELETE 前自已讀的 `current` 過濾被撤完整 11-col Model→`sys_casbin_policy_archive::insert_archived(&txn, rows, Some(operator.id), "role_dimension_revoke")`〔created_at 自 casbin_rule NN 升 archive nullable Some、archived_at=now、≤32 char reason〕→DELETE casbin_rule，**同一 `mutate_in_txn` txn＝原子 archive-move**（非硬刪丟失）;②protected-reject 維持在任何寫之前（FR-005 零回歸）。**② restore 三態**（net-new `restore<C:TransactionTrait>(conn,archive_id,meta)→Result<RestoreOutcome{Applied/NoOp/NotFound},DbErr>`，單一 `mutate_in_txn`）：讀 archive by id 查無→**NotFound**〔不審計〕／live 7-col〔ptype/v0-v5 全等〕已存在→**NoOp**〔delete archive 消費、不 INSERT、不審計〕／else→**Applied**〔INSERT casbin_rule〔protected=false、★ `created_at=archive.created_at.unwrap_or(now)` 跨表 nullable→NN coerce〕+delete archive+`AuditEvent{Restore, entity_table:"casbin_rule", entity_id:None, payload_after:{role:v0,target:v1,dimension:v2}}` 同 txn〕;handler 映 Applied/NoOp→0000、NotFound/parse-fail→2222 `biz.policy.notFound`。**③ PolicyMutated gate**（net-new `pub(crate) reload_and_publish(&AppState)→Result<(),AppError>`＝`enforcer.write().load_policy()`〔reload 失敗→Internal〕+`redis.publish(crate::CASBIN_INVALIDATE_CHANNEL,"")`〔best-effort fail-OPEN〕）：**updateRoleMenu 由 `if outcome.changed{load_policy}` 改【無條件】`reload_and_publish` on Ok(Applied)〔含空-diff、§4.2 ③「不優化」凍結、調整 011 reload-on-`changed`、user 2026-06-22 確認＝§4.2-faithful 非 amend〕**;restorePolicy match Applied→reload+publish／NoOp·NotFound→skip;Rejected 早退不 reload。`SetDimensionOutcome.changed` 生產不再讀（測試仍斷言）→`#[allow(dead_code)]` 保契約。**④ 跨副本 pub-sub**（net-new `spawn_policy_watcher(redis,enforcer)` in main.rs，**嚴格鏡像 014 `spawn_settings_watcher`**）：SUBSCRIBE `casbin:policy:invalidate`→`enforcer.write().load_policy()`〔Ok→info 收斂、Err→warn fail-OPEN 不 panic〕、串流斷→backoff 1→*2→min30 重訂閱（FR-008 不脫鉤）、Redis None→單副本 fallback;復用 014 Redis 基建+net-new `pub(crate) const CASBIN_INVALIDATE_CHANNEL`;boot 用 `state.redis.clone()/state.enforcer.clone()`。**⑤ 回收桶 UI**（base-web `views/manage/policy-archive/{index.vue,modules/policy-archive-table.vue}` MODAL-WIRING (e)）：rev3 typings〔`ArchivedPolicy` 8 欄 honest 逐欄對齊後端 wire/`ArchivedPolicySearchParams` RecordNullable/`ArchivedPolicyList`、不動 frozen〕＋service〔`fetchGetArchivedPolicies` pruneNullParams／`fetchRestorePolicy` String(id) request<null>〕＋list 鏡像 012 audit〔useNaivePaginatedTable+searchParams null 初始+reset/search〕＋restore 鏡像 010 menu〔NPopconfirm→fetchRestorePolicy→reload+restoreSuccess toast〕＋i18n 先 Schema 後 locale〔page.manage.policyArchive.* + route.manage_policy-archive〕＋**elegant-router 自動生成 route 註冊 4 檔**〔imports/routes/transform/elegant-router.d.ts、建 view 觸發 dev server 重生、fresh checkout 必需〕;**restore 鈕無 button-policy 種子、不以假碼 hasAuth 隱藏（後端 require_policy 為唯一安全邊界、保 SC-006）、頁級 R_SUPER 靠 menu policy+dynamic route**。net-new `model/facade/sys_casbin_policy_archive`（insert_archived/list 分頁鏡像 sys_operation_log::list/restore/RestoreOutcome）＋handler getArchivedPolicies〔honest `PageRes<ArchivedPolicy>`〕/restorePolicy＋main 2 route R_SUPER require_policy＋`AS_BUILT_ROUTES` 35→37。**4 執行單元 Workflow 驅動、rust 全程 serial**（EU1 archive facade+revoke→archive-move+role_menu_loop teardown 清 archive+facade live `5163d5a`→EU2 reload_and_publish+2 handler+2 route+AS_BUILT 37+gate 改011+gate 測 `383515b`→EU3 spawn_policy_watcher `2ad2029`→EU4 回收桶 UI `0adfd12d`），每執行單元一支 Workflow〔implementer TDD→spec-compliance review→fix loop→code-quality review〕＋主線單元邊界獨立 ground-truth 自驗〔git show --stat 抓範圍＋容器內 force-touch build/lint/live `--test-threads=1`＋psql 無殘留〕＋逐單元兩段式 commit〔worktree→outer pin、S9〕。**3 主線 checkpoint 接住的 review/實作缺口**：① EU1 cq 2 medium〔`list/restore/RestoreOutcome` dead_code `#[allow]`／D5 ZZZ 合成 casbin_rule 列只 inline 清→移無條件 teardown 防中途失敗污染 dev seed〕;② EU3 cq nit〔docstring 插入 health() doc 中間→移回〕;③ **EU4 elegant-router route 註冊 4 檔+route.manage_policy-archive i18n 遺漏**〔implementer 誤報無 route 變動、base-web restart 後 `git status` 見 `m base-web`、主線最終 checkpoint 接住補入並 typecheck 綠〕。**C-V-0~7 全綠**（build／兩 lint〔entity_access＋endpoint_coverage `[&str;37]` registered==as-built、Assertion A m002 seed〕／facade live `archive_restore`〔D1 archive-move 原子/D2 Applied/D3 NoOp/D4 NotFound/D5 created_at-NULL coerce/D6 protected-reject archive 零變更〕＋gate live〔空-diff Applied 仍 reload changed=false／`reload_publish_roundtrip` redis subscribe 證 publish 綁對 channel〕／**C-V-4 curl Super 200·Admin 5003·NotFound 2222**／**C-V-5 2-instance 跨副本收斂**〔rust-api-2 :31082、A revoke→B getRoleMenu 反映、A restore→B 反映、**CLIENT KILL TYPE pubsub→~1s 重訂閱→post-kill 仍收斂** FR-008〕、id-snapshot op-log 清理 dev DB 零殘留／**C-V-6 CDP 回收桶 UI**〔已登入 Super tab→/manage/policy-archive、list 真打 getArchivedPolicies 真資料、**點復原鈕→NPopconfirm→restorePolicy 200→列消失+list reload+psql 證 archive 消費/menu 復原**、**010「View component not found」console error 消解**〕／C-V-7 零回歸〔22 ignored+146 non-ignored 全綠〕＋migration/entity `git diff 7ec8fc3..HEAD` **零-diff**＋**prod target image build**〔多階段不破、無新 crate〕），final holistic review（fresh-agent 冷讀、grep 驗）**READY_TO_MERGE**〔5 §4.2 invariants〔①DB-first grep 零 MgmtApi remove_filtered/add_policies〔僅 doc-comment+#[cfg(test)] MemoryAdapter〕／②protected-reject 在任何寫前／③gate 含空-diff／④原子+restore 審計記 {role,target,dimension} 非懸空 archive_id／⑤reload+publish+watcher〕+SC-001~008+US1~US3 全覆蓋、wire byte 對齊無 type-lie、零 overbuild〔un-protect/purge 正確缺席〕〕。**零 migration、零新 crate**（`sys_casbin_policy_archive` 表 m001:665／2 endpoint policy seed m002:166-167／menu policy m002:168/253／role_policy m002:356-358 **全波0已備**、`git diff 7ec8fc3..HEAD migration/` 空、Constitution **9/9 PASS**）。**1 拍板：A un-protect/re-protect 不做＝受保護核心維持不可經 UI 撤銷**（防誤鎖核心存取、§4.2-faithful、做它須 §V.2 amend ② protected-reject 可繞）。**遞延後續**：011 menuProtected 訊息泛化〔Rejected payload 攜 protected objs 但 handler 泛化、Button-Endpoint 刀若 surface 具體 route_names 再帶 detail〕;Redis sess pointer 快取按 X-01 sanctioned defer。pins：rust-api `7ec8fc3`→`2ad2029`、base-web `f3b2bf07`→`0adfd12d`。merge --no-ff、feature branch `015-policy-governance` 保留。

- ✅ **016-button-endpoint-policy**（merge `fa17def`，2026-06-22;波 3 殿後刀＝Button-Endpoint 三維 RBAC runtime 編輯;★ 波 3 全完成）— 把 011 `set_role_dimension`（menu 寫側）＋015 治理島（archive/restore/protected-reject/PolicyMutated gate/spawn_policy_watcher）**延伸到 button＋endpoint 兩授權維度**，補完三維 RBAC（選單可見性／操作按鈕／API 端點）的 runtime 編輯。**① button 維度＝撿現成（reuse 011+015 全免費繼承）**：`updateRoleButton` handler 直呼 `set_role_dimension(&db, &role.code, "button", &codes, meta, role_id)`（dimension-agnostic、v2='button' 固定、套用 011 寫側＋015 archive/protected-reject/gate）；net-new `model/facade/sys_menu::all_buttons`〔讀全 active sys_menu 的 `buttons` JSON〔Option<Json>=serde_json::Value〕→純函式 `buttons_from_menu_jsons` 解析 [{code,desc}]→Vec<Button{code,label:=desc}> 字典序去重〕為 registry 權威源（非 casbin union、避漏「定義了但無人授」按鈕）＋`auth/enforce::button_codes_for_role(enforcer,role_code)`〔鏡像 buttons_for_roles 單角色 v2='button'〕讀端;Button DTO camelCase honest。**② endpoint 維度＝net-new `set_role_endpoints<C:TransactionTrait>(conn,role_code,desired:&[(String,String)]/*（path,method）*/,meta,role_id)→Result<SetEndpointsOutcome{changed},SetEndpointsError{Db,Rejected(Vec<(String,String)>)}>`**——**★★ 正確性核心**：endpoint policy＝真實 (v0=role, v1=path, v2=HTTP method) 列〔`require_policy`/`enforce_role_path_method` enforce 查的就是這些〕、v2=method 逐列變動、`set_role_dimension`（固定-v2 模型）套不上 → 新 facade 做 (path,method) 雙鍵 diff，但治理 helper〔insert_archived／mutate_in_txn／reload_and_publish／spawn_policy_watcher〕**全復用 015**。單一 `mutate_in_txn`：(a) dedup desired；(b) read-current〔ptype='p'∧v0=role∧`v2 ∈ HTTP_METHODS`〔net-new const `HTTP_METHODS=&["GET","POST","PUT","DELETE","PATCH"]`+`is_endpoint_method` 辨識〕〕；(c) (path,method) 雙鍵 diff（HashSet<(String,String)>）；(d) **②protected-reject 任何寫之前**〔to_revoke 含 `protected=true`→`Rejected(被擋 (path,method))` 零變更〕；(e) revoke〔`insert_archived(reason="role_endpoint_revoke")`+**DELETE 按 id**〔`Column::Id.is_in(to_revoke_ids)`、**非** set_role_dimension 的 `V2.eq(dimension)+V1.is_in`〔對變動 v2 會誤刪/漏刪〕〕同 txn 原子〕；(f) grant〔逐 (path,method) insert `v1=path,v2=method,protected=false,created_at=now`〕；(g) op-log〔changed→AuditEvent Update、payload before/after=(path,method) 集〕。**★★ 全程動真實 (role,path,method) 列、絕無 v2='endpoint'／v1='path:method' 平行編碼**〔平行編碼=建出不驅動 enforce 的假列=功能假性;grep 全刀 diff 證寫端 v2 只 `Set(method.clone())`〔grant〕/`Set(m.v2.clone())`〔archive/restore 照搬〕、字面 "endpoint" 僅在 `dimension_display` 推導+list filter；**U2 Workflow 加 encoding-correctness 對抗證偽、9 項證偽全擊破**〕。讀端 net-new `auth/enforce::endpoint_pairs_for_role`〔讀該 role v2∈HTTP_METHODS 的 (v1,v2)〕＋prod const `ALL_ENDPOINT_POLICIES`〔registry、36 對=已註冊 policy-governed route、含 6 編輯端點〕;Endpoint DTO{path,method,label?} camelCase honest。handler `updateRoleEndpoints`：set_role_endpoints→Ok(Applied,含空-diff)→`reload_and_publish`／Rejected→2222 `biz.role.endpointProtected`〔帶被擋〕／Db→5000。**③ §4.2 五 invariants 維度無關延伸（invariant-faithful、非 amendment;invariant 文字本就 casbin_rule/archive 維度無關）**：① DB-first〔set_role_dimension/set_role_endpoints 直寫 entity::casbin_rule、零 MgmtApi〕／② protected-reject〔endpoint 15 protected seed=鎖出守門〔恢復路徑：getRole/updateRoleMenu·getRole/updateRoleEndpoints·getAllButtons/getRoleButton/updateRoleButton·getArchivedPolicies/restorePolicy·getSystemSettings/updateSystemSetting·getDeletedMenus/restoreMenu·updateUserSessionPolicy〕、button 0 protected moot〕／③ PolicyMutated gate〔Applied〔含空-diff〕→reload+publish·Rejected→skip〕／④ revoke/restore+審計同 txn 原子／⑤ reload=全量 load_policy+PUBLISH casbin:policy:invalidate（spawn_policy_watcher 跨副本收斂、015 既有零改）。**④ 回收桶三維 v2-推導（★ 同改兩處、不改 archive_reason）**：(a) handler `getArchivedPolicies` 的 `dimension` 由 net-new `dimension_display(v2)=match{"menu"|"button"=>原值,_=>"endpoint"}` display 層推導〔menu/button no-op、僅 endpoint 列〔v2=method〕改顯 "endpoint" 群組〕；(b) `sys_casbin_policy_archive::list` facade dimension filter 加 endpoint 特例〔dimension=="endpoint"→`V2 IN HTTP_METHODS`、menu/button→`V2.eq`〕〔原 `apply_if(dimension,V2.eq)` 對 endpoint 配不到任何列=壞〕;**restore 審計 `dimension` 欄記 raw v2**〔endpoint=method、保 (path,method) 完整 forensic 精度、與回收桶 display 推導刻意不對齊=by-design M3〕;archive_reason 並存〔menu/button="role_dimension_revoke"、endpoint="role_endpoint_revoke"〕、回收桶靠 v2-推導非 archive_reason 分維度。**⑤ base-web 三維 role drawer UI（MODAL-WIRING (c)）**：`button-auth-modal.vue` **un-mock**〔移除全 mock〔硬塞 button1..10/console.log〕、嚴格鏡像 menu-auth〔defineModel('visible')+watch(visible) lazy init、`!error` 才 success〕、tree=Button[]/key-field="code"/checks=string[] codes〕；`endpoint-auth-modal.vue` **新建**〔鏡像 button-auth、NTree key=(path,method) 合成〔method+空格+path、submit 對稱 parseKey 首空格拆解〕、hard-replace fetchUpdateRoleEndpoints〕；`role-operate-drawer.vue` 第三 useBoolean+NButton+`<EndpointAuthModal>`〔v-if="isEdit" 區、:role-id〕；6 wrapper〔fetchGetAllButtons/fetchGetRoleButton/fetchUpdateRoleButton/fetchGetAllEndpoints/fetchGetRoleEndpoints/fetchUpdateRoleEndpoints、roleId 維 number ⚠️r、直接路徑 import〕＋Button/Endpoint typings〔declaration-merge 不動 frozen〕＋i18n〔app.d.ts Schema page.manage.role.endpointAuth+backend.biz.role.endpointProtected、zh-cn/en-us locale、先 Schema 後 locale〕。**⑥ typings 收斂 fold-in（D5/U5、最低優先）**：`RoleListItemRev3=Omit<Role,'roleDesc'>&{roleDesc:string|null}`／`UserListItemRev3` 擴 honest 三欄〔nickName/userPhone/userEmail:string|null+既有 sessionPolicy〕／**新增 `fetchGetRoleListRev3`·`fetchGetUserListRev3` honest wrapper**〔接地校正：getRoleList/getUserList 在 frozen `system-manage.ts`、故新 rev3 wrapper〔鏡像 fetchGetMenuListV2 範式〕非改 frozen、view+drawer rowData prop widen、消 User/Role wire type-lie〕。net-new main.rs 6 route R_SUPER require_policy〔加 roles group〕＋`endpoint_coverage_lint` `AS_BUILT_ROUTES` 37→43+**M2 雙向 registry assertion**〔`ALL_ENDPOINT_POLICIES`==registered policy-governed:⊇防漏端點〔UI 授不到〕+⊆防 stale phantom〔孤兒 casbin 列〕〕+Assertion A〔6 編輯端點 m002:148-153 已 seed〕。**5 執行單元 Workflow 驅動（rust serial→base-web 跨棧並行〔U3 rust ∥ U4 base-web、U5 ∥ U3〕）**（U1 button 後端 `44ffd4f`→U2 endpoint 後端〔含 encoding 對抗證偽 phase〕`4929406`→U3 治理統一 回收桶 v2-推導+gate+cv6 2-instance `066f91b`→U4 base-web UI `d2a1990d`→U5 typings 收斂 `c2c61893`），每執行單元一支 Workflow〔implementer TDD→spec-compliance review→fix loop→code-quality review、皆首輪 pass〕＋主線單元邊界獨立 ground-truth 自驗〔git show --stat 抓範圍〔U1 6 檔/U2 5 檔/U3 3 檔+cv6 腳本/U4 8 檔/U5 6 檔〕＋容器內 force-touch build/lint/live `--test-threads=1`＋psql H1 baseline＋**主線親跑 cv6 2-instance 全綠**〕＋逐單元兩段式 commit〔worktree→outer pin、S9〕。**C-V-0~8 全綠**（build／兩 lint〔entity_access＋endpoint_coverage `[&str;43]` registered==as-built、Assertion A 6 端點 m002 seed、M2 雙向 registry〕／live button `button_loop`〔revoke→archive-move〔reason=role_dimension_revoke〕+grant+restore+button_codes 反映+teardown 清自身 casbin op-log〔trace 016-button-smoke、保 012 t_cas==2 baseline H1〕〕／live endpoint `endpoint_loop`〔雙鍵+**鎖出**〔撤 protected getRoleMenu→Rejected 帶被擋、現役+archive 零變更〕+**enforce 自 DB 建 Enforcer 驗 revoke→拒/grant→通**+**UI-gateway** 撤非保護 getRoleList→回收桶 restore 回〔H2〕+teardown trace 016-endpoint-smoke〕／gate 精準 `gate_precision`〔空-diff Applied→reload+publish、Rejected→不 reload 不 publish〕／curl Super 6 端點 200·Admin 403／**2-instance cv6**〔副本 A updateRoleButton 撤→B getUserInfo buttons 收斂、A updateRoleEndpoints 撤→B 對 (role,path,method) enforce 收斂 403、A restore→B 恢復、CLIENT KILL TYPE pubsub→watcher 重訂閱仍收斂、收尾 --profile multi down+op-log 清〕／**CDP 三維 UI 6/6**〔Super→/manage/role→edit drawer 三鈕在地化〔菜单/按钮/接口权限、endpointAuth resolve 非 raw key〕+button modal 真打 12 真實按鈕〔非 mock〕+endpoint modal 真打 36 真實 (method,path)+寫端真驅動 (role,path,method) enforce 列+撤 protected updateRoleEndpoints→在地化「受保护的核心接口存取不可移除」+原子拒〔R_SUPER 仍 38 端點/archive·oplog 零增長〕+0 console error+manage 頁無回歸〕／零回歸〔25 ignored+155 non-ignored 全綠、011 role_menu_loop/015 archive_restore/012 audit_query〔t_cas==2〕並存〕+zero-migration〔`git diff 2ad2029..066f91b migration/` 空〕+**prod target image build**〔binary `/usr/local/bin/server` 18.6MB 落地、零新 crate、多階段不破〕+base-web pnpm typecheck），final holistic review（fresh-agent 冷讀+2 fresh 接地 agent 交叉）**READY_TO_FINISH**〔endpoint 編碼鐵則成立=真實 (v0,v1=path,v2=method) 列無平行編碼〔grep 全刀+enforce-level live 證〕、§4.2 五 invariants 維度無關延伸、SC-001~008+US1~US3 全覆蓋、6 端點完整 wiring、constitution 9/9、**0 critical/high/medium**、3 LOW〔L1/L2/L3 見下〕〕。**dev DB 寫端原子無自身殘留**（CDP 後複驗 archive==0/R_USER_COMMON 還原/R_SUPER endpoint==38/protected==15、H1 baseline 守住）。**零 migration、零新 crate**（6 編輯端點 policy `m002:148-153`〔全 protected R_SUPER〕／button policy 16／endpoint policy〔15 protected〕／button JSON `sys_menu.buttons` `m002:209-240` **全波0 m002 已備**、`AS_BUILT_ROUTES`/`ALL_ENDPOINT_POLICIES`=測試常數/prod 程式常數非 schema、Constitution **9/9 PASS**）。**4 拍板**〔brainstorm user 親決 2026-06-22〕：scope C（button+endpoint 完整 runtime 編輯）／un-protect 不做（延續 015 A、endpoint 15 protected 不可 UI 撤）／回收桶 v2-推導／endpoint 鎖出靠既有 15 protected seed。**3 LOW 非阻擋**：L1〔rust update_role_button 把 Rejected 映 `biz.role.buttonProtected` 但無對應 i18n 鍵;button 0 protected 故**不可達**、不補 i18n〔為不可達情境加碼違 §2〕、登 CHECKLIST backlog〕／L2〔TS 型 RoleButtonUpdate/RoleEndpointsUpdate 宣告未引用、data-model 有記載=API 形狀文件、留〕／L3〔回收桶 display(v2-推導 endpoint) vs restore 審計(raw-v2 method) 刻意不對齊=by-design M3〕。**接地校正**：T001 文件假設「endpoint policy 39/menu 17/總 72」為 stale grounding——實際 DB=完整 m001~m006 seed〔menu 84/endpoint 42/button 16/protected 15〕+1 列零散 runtime grant〔R_SUPER manage menu〕=143 p-rows;button=16/protected=15 吻合、registry 從實際註冊 route 導出+M2 lint 把關故 count drift 不影響正確性。pins：rust-api `2ad2029`→`066f91b`、base-web `0adfd12d`→`c2c61893`。merge --no-ff、feature branch `016-button-endpoint-policy` 保留。

### pre-波4 017-audit-center-enhancement ✅ 全完成（2026-06-23；波 3 後、波 4 前獨立 enhancement 刀）

> 把 CHECKLIST §3.C 審計中心三個 doable 子項（C-1 http_status class filter／C-3 CSV 匯出／C-4 op-log 角色 delta）組一刀;非交付波次、屬波 4 前獨立 enhancement（鏡像 D11/013 獨立刀處理）。

- ✅ **017-audit-center-enhancement**（merge `c7f5936`，2026-06-23）— 審計中心（012 建、R_SUPER-only 三分頁）補三項 forensic/operational 可視性缺口。**C-4 op-log 角色 delta**（rust `8b58c7c`）：純函式 `model/audit.rs::with_roles(Value,&[String])`〔Object→insert 排序後 "roles" 陣列、空→[] 非 null、非 Object passthrough、既有 key〔含 current_session_id〕保留〕＋三寫端 `sys_user.rs` payload enrich〔create:before=None/after=with_roles(role_codes)；update:replace_roles_in_txn【之前】讀 `roles_of_user(&txn,id)`→before/after 各快照；soft_delete〔del+batch 共用〕:刪前讀→before=刪前集/after=[]〕＋擴充既有 4 支 #[ignore] live smoke〔trace_id+entity_table 隔離、非新平行測〕。**C-3 CSV 匯出**（rust `57de37c`+base-web `368964bf`）：三審計讀端點加 `export:Option<String>` query 變體〔`parse_export` "true"/"1"→true〕、export 分支 `facade::list(db,0,CSV_EXPORT_CAP=1萬,filter)` ★繞過 normalize_size→手寫 `csv_escape_field`/`records_to_csv`〔UTF-8 BOM 僅檔首、F2 逗號/引號/換行整列不錯位〕→`Res::ok(csv String)` CSV-in-envelope；op-log CSV 專屬 rolesBefore/After 欄〔自 payload["roles"] 抽、F5 空值語意：空陣列→"[]"／None 或無 key→""〕+payloadBefore/After 完整 JSON 獨立欄;base-web `downloadCsv` util〔Blob+a[download]、檔名 `<table>_<ts>.csv`〕+3 export wrapper〔flatRequest {data,error}、pruneNullParams+export:'true'〕+三表匯出鈕+截斷 toast〔itemCount>CAP〕。**C-1 http_status class filter**（rust `e988a29`+base-web `3713f94a`+`a555326d`）：純函式 `parse_http_status_class`〔2xx/4xx/5xx→半開區間 [lo,hi)、空/None/畸形→Ok(None) 不 2222〔FR-012、異於 parse_http_status〕〕+AccessLogFilter `http_status_class:Option<(i32,i32)>`+list 範圍 apply_if〔gte(lo).and(lt(hi))、與既有單值 eq 並存=AND〕+handler 組 filter〔export 與分頁共用同一 filter〕;base-web class NSelect〔全部=null→pruneNullParams 剔除→wire 缺席→後端 None〕+typing httpStatusClass+i18n。**5 執行單元 Workflow 驅動**（EU1 C-4→EU2 C-3 後端→EU3 C-3 前端→EU4 C-1 全棧→EU5 polish），每執行單元一支 Workflow〔implementer TDD→spec-compliance review→fix loop→code-quality review〕＋主線單元邊界獨立 ground-truth 自驗〔git show --stat 抓範圍〔曾抓 EU2/EU4 implementer「先前 run」confabulation、實 git 全對〕＋容器內 force-touch cargo build/test/live `--test-threads=1`〔DATABASE_URL 走 /run/secrets〕＋**容器網路 node http 打 rust-api:31081**〔host port-forward 退化期繞道、wire 驗證可靠通道〕＋bump pin S9〕。**C-V-0~5 全綠**（純測 with_roles/csv ×5/parse_http_status_class/parse_export／三表 export wire〔BOM+rolesBefore/After 欄+Admin 403+0 列僅表頭+F2〕／class filter wire〔4xx 收窄/空+畸形守門/AND/2xx&200 交集〕／psql op-log roles delta〔trace_id 隔離〕／**CDP UI**〔三 tab 匯出鈕→實際 `.csv` 下載+blob+截斷 toast、class NSelect 選 4xx→列表收窄〕／零 migration diff＋prod target image build＋全量 cargo test 166 passed＋lint 2+2＋pnpm typecheck）。**★ CDP 抓修一個 curl≠modal bug**：class filter label 誤用 `filter.statusClass`〔placeholder 措辭〕當 NFormItemGi label，應為 `col.statusClass` 名詞〔對齊既有 col.* label 慣例〕→主線改 4 處+restart base-web+re-CDP 確認（`a555326d`）。final holistic review（fresh-agent 冷讀）**ALL PASS**〔SC-001~008 全覆蓋、跨子功能接縫〔C-4 enrich→C-3 CSV roles F5 兩端一致／C-1 filter 與 C-3 export 共用同一 AccessLogFilter〕、wire 3 端對齊無 type-lie、Constitution 9/9〔item 2/7：audit 頁 rev3 自建〔012 `1764154e`〕增強+page.manage UI i18n、非 frozen-upstream inline、非 BASE-WEB-I18N-WIRING backend.* 命名空間〕〕。**5 拍板**〔brainstorm D1-D5 user 親決〕+1 clarify〔spec.md Q1 roles_before/after 專屬欄〕、見 [CHECKLIST §5](INTEGRATION-CHECKLIST.md)。**零 migration/端點/crate**〔C-3 export 復用既有 3 GET 端點 query 變體=零 policy seed、C-4 jsonb payload 內欄、C-1 query filter;`git diff b63928b..e988a29 migration/` 空、`endpoint_coverage_lint[&str;43]` 不變、Constitution 9/9 PASS〕。pins：rust-api `57de37c`→`e988a29`、base-web `368964bf`→`a555326d`。**遞延 follow-up**：CSV 截斷信號嚴格化〔F4、前端 itemCount 可能 stale、嚴格保證=後端 export 帶截斷旗標、CHECKLIST §3.C〕／C-2 pg_trgm fuzzy GIN〔out-of-scope、scale-gated〕／C-5 archive retention/purge〔out-of-scope〕。merge --no-ff、feature branch `017-audit-center-enhancement` 保留。

### 波 4 — observability ✅ 全完成（2026-06-25；包覆全體之刀、一刀三執行單元）

> 包覆全體之 ops 觀測層（profiles opt-in、一般 `up` 不啟）：一刀 018-observability、三執行單元 obs-min→obs-full→dashboard（merge `c1a3224`、feature branch 保留）。出口三項達標〔obs/metrics profile 起停乾淨〔C-V-0〕／provisioning 重建無 crash-loop〔C-V-5〕／rust-api log+metrics 兩軌可查〔C-V-2 trace_id join＋C-V-3 promql〕〕。

- ✅ **018-observability**（merge `c1a3224`，2026-06-25）— 完全 opt-in 維運觀測層，三執行單元漸進（U0 setup＋U1 obs-min log＋U2 obs-full metrics＋U3 dashboard＋Polish）。**U0 setup**：obs dep〔axum-prometheus 0.7.0／metrics 0.23／metrics-exporter-prometheus 0.15(df=false)／ureq 2(df=false)＋workspace tracing-subscriber +json feature〕＋★MSRV 1.86 gate〔容器內 build 綠、`cargo tree -i time` 僅 jsonwebtoken→simple_asn1〔**obs 鏈零 time edge**〕、敏感 transitive 全對齊 rev2 known-good〔quanta 0.12.6／metrics-util 0.17.0／portable-atomic 1.13.1／zerofrom 0.1.8／ureq 2.12.1〕、無需新 pin〕＋grafana_admin_password secret〔generate-secrets leaf 4→5、補 DESIGN:706 落差〕＋compose 頂層宣告〔4 obs 卷＋secret〕（rust pin `8f84d3c`）。**U1 obs-min**：rust〔main.rs subscriber `.json()`＋audit_ctx.rs audit_mw `.instrument(info_span!("request",trace_id,method,path))` ＋ **★FR-006 explicit completion event**〔`.instrument(span)` 單獨對無 in-span event 請求〔/health〕不輸出 log 行〔實證 loki 0 行〕、且 span 欄位落 JSON `span`/`spans` 非 `fields`→補 `tracing::info!(trace_id=…,http_status=…,"request completed")` event→trace_id 落 event fields→loki `|json` 攤平為 **`fields_trace_id`**〔經驗確證之 join key〕〕〕＋loki 3.7.2／alloy v1.16.1〔docker-SD、★KEEP relabel compose_project=rev3-admin〕／grafana 13.0.2〔**★`$__file{}` provider** 非無效 `__FILE` 後綴、實測 file-pw 登入 200·admin/admin 401〕＋loki datasource〔uid loki+deleteDatasources guard〕（rust pin `56f8d01`）。**U2 obs-full**：rust〔main.rs public `/metrics`〔axum-prometheus PrometheusMetricLayer::pair 裝全域 recorder、matched-path endpoint label〕＋enforce.rs `casbin_enforce_total{decision}` 2-site〔allow/deny、observe-only 不改判決控制流、fail-closed DB-error 不計 R3(a)〕＋cleanup-job `push_metrics` best-effort pushgateway〔std SystemTime 不引 chrono、ureq PUT、推失敗只 warn 不改 exit〕＋`endpoint_coverage_lint` AS_BUILT +`/metrics` 43→44〕＋prometheus v3.12.0〔4 job scrape、retention 15d〕／postgres_exporter v0.19.1〔DATA_SOURCE DB=soybean_admin_rust〕／redis_exporter **v1.85.0-alpine**〔★REDIS_ADDR=redis://redis-stack:6379〕／pushgateway v1.11.3＋3 grafana-managed alert〔**★5xx rule noDataState=OK+expr `or vector(0)` 修 idle false-firing**〔data-model §1.3 對 3 rule 一律 Alerting、但 ratio query 在零 5xx 回空向量→noData 誤升 Firing；修為 healthy 恆 0/inactive、down 類 rule 維持 Alerting 語意正確〕〕（rust pin `6871907`）。**U3 dashboard**：provider＋6 dashboard〔4 greenfield master-overview/rust-api/cleanup-job/audit-log〔★compose_project=rev3-admin＋fields_trace_id＋`|~ ^{` JSON guard〕＋2 community pin postgres〔grafana 9628 rev8〕/redis〔763 rev6〕、datasource hardcode prometheus uid、刪 __inputs/__requires〕（無 rust）。**Polish**：prod obs internal-only override〔config bind-mount 對齊 dev、無 host port〕＋holistic review 3 LOW 校正〔preflight +grafana_admin_password／prometheus datasource +deleteDatasources guard／verification-commands C-V-1/3/4 容器-工具校正〕。**4 Workflow 驅動（U1/U2/U3 各 implementer→spec-review∥quality-review→fix loop＋整體 holistic 3 lens＋綜合）**，每執行單元主線邊界獨立 ground-truth 自驗〔git show --stat 抓範圍＋容器內 build/test＋C-V live＋bump pin S9〕。**★ 3 設計時/review 攔截的真缺陷**〔皆經驗判定、附驗證〕：① FR-006 span-only 不足→explicit event〔主線 brainstorm 預警、U1 implementer 經驗判定補入〕；② grafana `__FILE` 後綴無效→`$__file{}`〔U1 quality-review HIGH、主線實測登入確證〕；③ 5xx alert idle false-firing→noDataState=OK〔主線把 holistic LOW 升修、實證 idle 3 rule inactive〕。**C-V-0~8＋prod build＋MSRV 全綠**〔C-V-0 隔離〔無 profile 0 obs service/卷〕／C-V-1 log 採集〔loki 收 8 service〕／**C-V-2 log↔audit trace_id 關聯**〔psql sys_access_log.trace_id == loki fields_trace_id、多 marker 重現〕／C-V-3 metrics〔4 prometheus target up==1〔含 redis REDIS_ADDR 驗〕＋casbin_enforce_total{allow}＋cleanup pushgateway gauge〕／C-V-4 3 grafana alert〔grafana alerting API、idle inactive〕／C-V-5 重啟×2 冪等／**C-V-6 6 dashboard CDP UI 實渲染**〔截圖實證、未 defer〕／C-V-7 `/api/metrics`→404＋內網 scrape／C-V-8 零回歸〔lint 2+2＋168 unit＋既有端點 200 code=0000＋base-web HTML〕／**C-V-prod release multi-stage build green**〕，final holistic review（fresh-agent 冷讀 3 lens＋綜合）**CONCERNS 無 BLOCK**〔FR-001~022/SC-001~009 全功能滿足、5 LOW 皆遞延/已修〕。**零 base-web 改動〔對 rev3-admin-root base-web gitlink 零變動〕、零 migration、零新 workspace crate**〔obs dep 加既有 server/cleanup-job crate、§3 四處 COPY 不觸發〕、Constitution **9/9 PASS**〔casbin_enforce_total observe-only 不改 §I.7 §4.2 enforce 判決／cleanup push best-effort 不改 §4.1 單旗標·idempotent invariant〕。**2 user 拍板**〔D1 一刀三單元／D2 rev2 對等+hindsight〕。詳 [REVIEW-018-observability.md](REVIEW-018-observability.md)。**遞延 backlog**：postgres 9628 板 docker k8s-template-var 空態〔spec 容許〕／request-completion /health log 噪音〔FR-006 設計、可選 path 過濾〕／data-model §1.3 noDataState＋C-V contract 全面 `/speckit-analyze` 校正。pins：rust-api `483eeba`→`6871907`、base-web 零變動、deploy/compose 直 outer。merge --no-ff、feature branch `018-observability` 保留。
