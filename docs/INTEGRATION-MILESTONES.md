# rev3 commit 里程碑永久紀錄（INTEGRATION-MILESTONES）

> **append-only、不在 SOP hook 注入**（避免 CHECKLIST 膨脹）。三區、§2/§3 與 CHECKLIST 同號區鏡像對應：
> 「§1 commit 里程碑表」收每筆 docs／feature commit 一行（表尾 append）；
> 「§2」收 CHECKLIST §2 完成波的收縮節（✅ 標題＋blockquote 摘要;批次搬入後 CHECKLIST 永遠聚焦當前波）；
> 「§3」收 CHECKLIST §3 已完成 follow-up 節的細節（批次搬入）。
> 歸檔流程見 [CLAUDE.md §7.5](../CLAUDE.md)。**與 [DECISIONS §2](INTEGRATION-DECISIONS.md) 的分工**：波次 as-built **詳帳**在 DECISIONS §2（權威）；本檔 §2 只收 CHECKLIST 的一行式快照歸檔，查波次細節一律去 DECISIONS。

---

## §1 commit 里程碑表

| commit | 日期 | 主題 |
|---|---|---|
| `f8e0728` | 2026-06-11 | Spec-Kit presets / extensions / skills 落地 |
| `f6c4939` | 2026-06-11 | .gitignore 補強 — 排除 Claude 憑證與 fork 子專案 |
| `e006b7c` | 2026-06-11 | CLAUDE.md 從 rev2 改寫為 rev3（身分／⏳ 目標態／port 3XXXX） |
| `8ebce10` | 2026-06-11 | CLAUDE.md 微調 — pre-hook 檔名、§7 跨檔引用改語意錨 |
| `2ec9cda` | 2026-06-11 | 註冊 base-web/rust-api 為 submodule（worktree+submodule 雙重身分落地） |
| `0713db4` | 2026-06-12 | CLAUDE.md 拔除 worktree/submodule 已落地 ⏳ |
| `03a1b01` | 2026-06-12 | bump base-web 到 dd771540 — x_fork.branch-origin.md 分支來源紀錄 |
| `fb87a67` | 2026-06-12 | bump rust-api 到 f73ef10 — x_fork.branch-origin.md 分支來源紀錄 |
| `7fd1ac6` | 2026-06-12 | 設計權威 INTEGRATION-DESIGN-rev3.md 入 docs/ |
| `88f9011` | 2026-06-12 | DESIGN 拍板回填 — 官方權威序＋六項拍板＋對賬修補 |
| `8f66fe0` | 2026-06-12 | graphify rev3 首次建圖（2060 nodes／311 communities／83.7x） |
| `46591c4` | 2026-06-12 | 000：保存 base-web/apifox mock API 捕獲原始資料 |
| `309099d` | 2026-06-12 | base-web standalone dev compose |
| `aea0e18` | 2026-06-12 | 000：補抓 mock 缺口端點＋CDP scripts git-track |
| `e898421` | 2026-06-12 | 000：13 端點對映表＋設計書比對文件落地 |
| `4aa7c89` | 2026-06-12 | INTEGRATION-DESIGN-rev3.md 歸位改名 INTEGRATION-DESIGN.md |
| `4724549` | 2026-06-12 | C 方案落地 — DESIGN 凍結藍圖＋INTEGRATION-DECISIONS.md 伴生活帳 |
| `4300b54` | 2026-06-12 | CHECKLIST/MILESTONES 落地 — 注入活檔成形、波 -1 文件層收齊 |
| `9d8303f` | 2026-06-12 | 外檔引用全面查驗修正 — ⏳ 同步現實＋DESIGN amendment 動線勘誤 |
| `e8e1a99` | 2026-06-12 | §7.1 改定 — rev2 研究三檔不移植不重作 |
| `ed2a789` | 2026-06-12 | SessionStart hook 落地 — 自 rev2 原樣承接（零改動） |
| `71cdd61` | 2026-06-12 | §7.5 歸檔 — MILESTONES append＋CHECKLIST 進展滾動 |
| `4ec9cc6` | 2026-06-12 | MILESTONES 三區化收整 — user 鏡像設計補一致性 |
| `612ddcd` | 2026-06-12 | 波 -1 constitution 重鑄項加 §V.2 提案位置搬家註記 |
| `167db96` | 2026-06-12 | **constitution-rev3 v1.0.0 凍結** — 13 項拍板融入（含 ⚠️s fork-delta 紀律）；波 -1 出口四項全綠、**波 -1 全完成** |
| `c9ffad5` | 2026-06-13 | **001-infra-deploy 全綠收刀（波 0 第一刀）** — master compose 5 service＋migrate gate＋dev/prod override＋deploy/ 全套＋rust-api scaffold（worktree `5d69c06`）；C-V-0~8 實機全綠（SC-001~007）、捕獲 redis `--dir` 持久化真 bug 並修；merge --no-ff、feature branch 保留 |
| `9233ae0` | 2026-06-13 | **002-rev2-schema-baseline 全綠收刀（波 0 第二刀）** — 前代 35 支 migration squash 為 4 支基線（m001 schema 11 表終態〔10 手寫＋casbin 委派 vendored adapter＋ALTER 治理欄〕／m002 seed 92 列 6 表〔argon2id 單一 hash〕／m003 user_role FK ×2 RESTRICT／m004 demo 選單 66＋policy 全 R_SUPER）＋sea-orm-adapter 整檔拷入（§I.5、worktree `91cfc80`）；tests/002 四支驗證 scripts（normalize 六規則）＋2 基準檔；C-V-0~9 實機全綠（SC-001~008）、修 m002 兩層 seed drift（id 順序 bug＋normalize 第六規則 row-order 正規化〔user 拍板方案 A、契約留痕 migration-chain.md §3〕）；merge --no-ff、feature branch 保留 |
| `082e366` | 2026-06-16 | **constitution v1.0.0→v1.1.0 amend**（⚠️aa、§V.3 MINOR） — §III 新增 `BASE-WEB-I18N-WIRING ★` 軌道；觸發＝003-envelope `/speckit-plan` Constitution Check Q2/Q7（base-web i18n 接線必改 inline：`service/request` 攔截器／`locales` backend 命名空間／`app.d.ts` Schema；⚠️y 已授權改動本身、但 §I.1 需 §III 軌道才合規）；授權三範圍 (i) 攔截器 msg 翻譯接線〔不改控制流〕(ii) locales backend 命名空間 (iii) app.d.ts Schema backend 型＋helper；走 fork-delta `rev3-inline`；user 親決 A |
| `13a01b1` | 2026-06-16 | **003-envelope 全綠收刀（波 0 第三刀）** — 統一回應信封＋msg-i18n key 規約 scope A 縱切兩端一刀：rust-api(`2d55a38`) `envelope.rs`(Res/PageRes＋IntoResponse)＋`error.rs`(AppError 9 變體凍結碼矩陣、reserved 4 碼型別層無變體＝編譯期 guard、`Biz(Cow)`、無 From<DbErr>/sea-orm〔R7〕)＋`main.rs` .fallback(handler_404)；in-crate 契約測 8 綠（碼/serde 欄序/http 映射/非人話無 CJK/文法 conformance〔US3〕）。base-web(`c2ad92f`、⚠️aa `BASE-WEB-I18N-WIRING ★`、rev3-inline) `app.d.ts` Schema backend 型＋雙語 langs（9 鍵）＋`translateBackendMsg` helper＋`service/request` 4 翻譯點（單一 translatedMsg 共用、:63 includes 守門納入、onError truthy-guard 保 fallback）；零新 npm dep。key 規約 ⚠️y 落定（4 根 common/auth/biz/system＋`<root>.<entity>.<condition>`＋去前綴 wire/前端 backend.）。C-V-0~3 全綠（rust test 8／curl 4040／typecheck＋tsx 10／prod build）、wire 3 端對齊零型謊；merge --no-ff、feature branch 保留 |
| `a1105f0` | 2026-06-17 | **004-soft-delete-infra 全綠收刀（波 0 第四刀）** — L2 entity 層（新 `entity` workspace crate、11 模組 DeriveEntityModel 機械反射 m001／130 欄；tstz→DateTimeWithTimeZone／jsonb→Json／INET→IpNetwork〔with-chrono/json/ipnetwork〕、worktree `698f48a`）＋L4 soft-delete 地基（`SoftDeletable` trait〔lint-clean 純 sea_orm 泛型〕＋3 facade impl〔sys_user/role/menu、entity:: 走 lint 豁免〕＋`find_active`、`a34aa1a`）＋facade 唯一管道守恆 `entity_access_lint`（兩階段 whiten 抹白+path-root `entity::` scan〔前界非 ident 非 `:`〕、scan fn 自測雙證非 vacuous、`e9a6d5c`）＋prod Dockerfile entity Manifest＋Source COPY（outer `cc78264`）；U1 with-ipnetwork 1.86 build 早驗綠〔ipnetwork 0.20.0 入圖、無退 String〕＋inert time 0.3.47 lock 註解校正（`3f87a25`）；C-V-0~3 全綠（build／lint 2 passed／live smoke 1 passed txn-rollback／prod image 158MB）、SC-007 零回歸、holistic review PASS 零 findings；零業務 endpoint/facade 方法/migration（SC-005）；merge --no-ff、feature branch 保留 |
| `98f1f7e` | 2026-06-17 | **005-audit-op-log 全綠收刀（波 0 第五刀；audit ×2 之首＝op-log 刀）** — L4 audit 機制地基：`model/audit.rs`（`AuditOperation`/`AuditOperator`/`AuditEvent`/`AuditSerialize`＋`mutate_in_txn` 泛型 `C:TransactionTrait`〔業務寫＋op-log 寫綁同一 DatabaseTransaction、同時 commit 或同時 rollback〕、純資料層零 `entity::`、worktree `8b5bdde`）＋op-log append-only sink（`facade/sys_operation_log::write_in_txn`、唯一構造 op-log ActiveModel、無 update/delete、archetype B）＋`sys_user` redact（`impl AuditSerialize` 手構 json、`password`→`<redacted>`、Model 無 Serialize derive〔R-C〕）＋單一 `sys_user::soft_delete` proof（`into_active_model`＋§I.6 `deleted_at`/`deleted_by` 成對、no-op 回 `Ok(None)`、worktree `ae9a721`）＋mod.rs 檔頭校正（T001、`3d9578f`）；C-V-0~3 全綠（build／redact 純測 1 passed／atomic live smoke 三路徑〔commit 寫 op-log／no-op 不寫／rollback write-then-Err 業務+審計一起不留、非 vacuous〕1 passed／entity_access_lint 守恆 2 passed）、holistic review 3-lens（FR/SC 覆蓋・原子性〔sea-orm savepoint 源碼實證〕・scope/append-only）mergeReady 零 blocker；零端點/migration/Cargo.toml/新 crate/entity 變動（SC-005）、機制表中立（SC-006）、SC-007 /health 零回歸；merge --no-ff、feature branch 保留 |
| `e279f23` | 2026-06-17 | **006-auth-island-min 全綠收刀（波 0 第六刀＝Auth 島最小段＋runtime 骨幹第一刀）** — runtime 骨幹（`config.rs` secret `_FILE` 優先＋長度/change-me 守門／`state.rs` AppState{db,jwt,enforcer}／`main.rs` boot：connect→`init_enforcer`〔SeaOrmAdapter＋`DefaultModel::from_str` embedded 3-tuple RBAC〕→mount /auth/*＋enforce layer、worktree 經 `87e10ab`）＋auth 機制（`auth/jwt.rs` HS256 雙鑰 sign/verify〔exp/iss/aud〕／`auth/password.rs` argon2id verify／`auth/enforce.rs` `enforce_mw` bearer〔3333 fail-CLOSED〕→is_current〔7777 fail-OPEN、pointer-truth-in-DB〕gate＋`enforce_role_path_method`/`buttons_for_roles` seam）＋facade（`sys_token::insert_token`／`sys_user_role::roles_of_user`／`sys_user::find_by_user_name·set_pointer·current_session_id_of·find_by_id`）＋`error.rs` `From<DbErr>`→Internal；login（`42ce024`：argon2 verify＋簽 access/refresh JWT＋token_hash=sha256(refresh)＋set_pointer+insert_token 同 plain txn 原子〔SC-008〕＋1000 collapse 不洩帳號存在）／getUserInfo（`b9de316`：DB-fresh roles＋casbin v2=button buttons＋User→User01 alias＋userId 字串）。5 執行單元 Workflow 驅動（deps+time-pin `04fc6f8`〔jsonwebtoken9→simple_asn1→time 進真圖、pin time 0.3.37+simple_asn1 0.6.3 配 1.86 MSRV〕→foundation `87e10ab`→login `42ce024`→getUserInfo+enforce/session `b9de316`→final-verify）；C-V-0~6 全綠（build／11 純測〔JWT5·argon2·3·enforce-seam/buttons/union 3〕／entity_access_lint 2／live login·getUserInfo·1000·3333·7777＋psql 原子證／CDP i18n toast「用户名或密码错误」／prod image build `--locked` 加 dep）、holistic spec+quality review 17 FR+9 SC 全 PASS 零 blocker（FR-008 stale-token DB-mutation 活證 claims hint-only）；零 migration/entity/base-web/i18n/compose（SC-009）、SC-007 /health 零回歸；merge --no-ff、feature branch 保留 |
| `96280d8` | 2026-06-18 | **007-audit-overlay 全綠收刀（波 0 第七刀〔末〕＝audit overlay；本刀收齊波 0）** — L3 `xdb` crate 整檔零改拷入（§I.5⚠️v、once_cell workspace dep、criterion/rand dev-tree 入 lock、time 0.3.37/simple_asn1 0.6.3 pin 不動、worktree `2fc0696`）＋L7 `audit_ctx`（`RequestContext`／`resolve_client_ip` XFF trusted-proxy〔peer-gate→rightmost-untrusted→fail-safe、anti-spoof、8 純測〕／`extract_trace_id`／`audit_mw` 全域最外層 operator-gate／`to_audit_operator` op-log threading seam）＋2 append-only facade sink（`sys_access_log`／`sys_login_attempt`、`IpAddr→IpNetwork::from` seam）＋state/config（`trusted_proxy_cidrs` fail-safe 空／`xdb_ready`／`XDB_FILEPATH`）＋main boot `Path::exists` 守門→`searcher_init`（缺檔 warn 降級不 panic、R1）＋`into_make_service_with_connect_info`＋login inner/outer split exactly-one（`15e6491`：not-found/wrong-pwd 同 1000、operator pre/post-identity）＋op-log threading live smoke（`fc4b50e`、T018）；T009 compose env（dev `XDB_FILEPATH=xdb/resources/...`＋`TRUSTED_PROXY_CIDRS` fail-safe）＋T019 Dockerfile xdb COPY（Manifest＋Source〔含 benches〕＋runtime `/app/resources/ip2region.xdb`＋builder `--locked`）。C-V-0~9 全綠（build `--locked`／3 純測 8+5+4／entity_access_lint 2／live login-attempt 成敗各列・access-log operator-gate・真 INET 無 42804・region 内网・behind-proxy 真 client IP・op-log INET round-trip／prod target image build 含 .xdb 11070083B）、holistic review PASS 零缺陷（16 FR＋9 SC 全 MET）；零 migration/entity/型遷移/base-web/i18n/nginx（SC-009）、enforce_mw 未動、SC-007 /health 零回歸；推進 DESIGN §5.9（直連→XFF trusted-proxy、DESIGN-detail 非 Amendment）；**波 0 出口四項達標**（entity_access_lint✅／migration up→down→up✅〔endpoint_coverage_lint ⚠️x 豁免移波1〕／envelope 13 碼✅／login→getUserInfo→enforce✅）→ 波 0 全完成；merge --no-ff、feature branch 保留 |
| `b52dafe` | 2026-06-18 | **008-system-settings 全綠收刀（波 1 第一刀＝system_settings KV 打樋；波 1 全完成）** — 3 全專案首次：(1) 首個 policy-governed 端點——新 `require_policy(path,method)` per-route layer（`from_fn_with_state`、DB-fresh `roles_of_user(db,claims.uid)` 不信 `claims.roles`〔FR-005〕→`enforce_role_path_method`→`PermissionDenied` 5003/403）、`enforce_mw` 本體一行不動（§3.4）＝兩層 auth→authz、5003→403 live 首証（Admin/User GET/POST→403 不洩值）；(2) 首個 007 op-log threading live consumer——update handler 經 `ctx.to_audit_operator(claims.uid)` 取真 operator/IP/trace 餵 `mutate_in_txn`、op-log `operator_ip` 由恆 None→真 INET round-trip（C-V-5 顯式 IpNetwork 203.0.113.7 by-trace 查證＋外層 txn savepoint rollback 不污染 seed）；(3) 立 `endpoint_coverage_lint`（⚠️x 波0 豁免移波1）——鏡像 entity_access_lint 只讀掃描、斷言 registered==as-built registry（5 route）＋policy-governed⊆m002 p-policy seed（非 §7.1 @35 字面）、含 scan_self_test。L4 facade（find_all/find_by_key/update_by_key〔mutate_in_txn 同 txn op-log UPDATE、查無 Ok(None) no-op、entity_id None+key 入 payload〕＋build_update_active_model 純測 seam＋AuditSerialize）＋L5 handler（get_system_settings flat／update_setting 先驗後改：notFound/invalidValue→2222、to_audit_operator 餵）＋L4 require_policy＋L1 main（GET/POST 各 route_layer require_policy＋外層 enforce_mw）＋L8 lint；base-web static 頁（rev3-system-settings.ts WRAPPER 首個 rev3-* 檔／rev3-system-settings.d.ts ADAPT／views/manage/system-settings/index.vue MODAL-WIRING(e) 簡 KV 頁 enum→NSwitch／app.d.ts Schema＋zh-cn/en-us locale backend.biz.systemSettings.*＋route/page key）。4 執行單元 Workflow 驅動（U1 read+require_policy `8e5a024`→U2 update+op-log `4f4952d`→U3 lint `3874182`／U4 base-web `5fdd6f0`＋§2 trim `223bc83e`）；C-V-0~11 全綠（build／純測 value_type+active_model／lint endpoint_coverage+entity_access／live op-log INET round-trip＋policy-gate 5003＋2222×2／CDP toast「更新成功」off↔on／typecheck／prod build／零回歸 perf 讀5.8ms改7.9ms）、holistic 雙 lens ready-to-merge 0 blocking；**零 migration/entity/schema**（m005 MOOT、m002:162-165/251 已 seed）、無新 crate、enforce_mw 未動、base-web 既有檔未動、SC-006/007 零回歸；rust-api `fc4b50e`→`3874182`、base-web `c2ad92f`→`223bc83e`；merge --no-ff、feature branch 保留 |
| `07b67d2` | 2026-06-18 | **009-user-management 全綠收刀（波 2 資料島首刀）** — 使用者管理頁 mock→真後端 6 端點 CRUD（getUserList/getAllRoles/addUser/updateUser/deleteUser/batchDeleteUser）＋角色 M:N（`replace_roles_in_txn` 與 user 寫同一 `mutate_in_txn`）＋同交易審計（INSERT/UPDATE/SOFT_DELETE op-log、operator_ip 真 INET）＋越權防護（6 端點 require_policy：read getUserList=R_SUPER+R_ADMIN／getAllRoles=+R_USER_COMMON／write addUser·updateUser·deleteUser·batchDeleteUser=R_SUPER、DB-fresh roles）＋停用登入 gate（login status==2→LoginFailed 1000、防枚舉）。多個全專案首次：§5.8 分頁/filter 首 exercise（空字串守門＋模糊 ILIKE userName/nickName/userEmail、userPhone/enum 精確）／`PageRes` 首消費者／M:N join 寫／prod argon2 hash_password／23505→2222 `sql_err()` 寫端 map 首落地（⚠️o、blanket From<DbErr> 不改）／INSERT op-log 首 consumer（entity_id=Some(after.id)）。2 clarify 拍板（記 spec.md ## Clarifications）：防自鎖對稱守門 self-lock（禁超管自我移除超管角色/自我停用→`biz.user.selfLockForbidden` 2222 整筆拒）＋批次刪除缺漏 idempotent skip（已不存在/已刪 id 靜默略過、cannot-delete-self 仍獨立整批 pre-check 拒）。★ as-built 偏離：plan/research-R6 原處方 `PgExpr::ilike().escape()` 編譯過但 runtime 失效（sea-query 0.32.7 `drop_right_escape_hack` 只認 `BinOper::Like`、不含 `PgBinOper::ILike`→ILIKE+ESCAPE 渲染成非法 SQL→5000）；U1 活體（§5.8 curl≠modal）抓到、改用 `LOWER(col) LIKE ESCAPE`，校正 4 處文件（research R6／data-model §1／contract §3.2〔跨 feature 權威供 Role/Menu 刀繼承〕／verification-commands C-V-0）＋`find_active_by_id` polish（update 改呼之、DRY、消 dead-code warning）。4 執行單元 Workflow 驅動（U1 reads `7daa622`→U2 create·update `79f4983`→U3 delete·gate `977203f`／U4 base-web `c1806680`＋polish `8ccea9d`＋doc `5c0e3f7`）；base-web＝rev3-system-manage.ts wrapper＋UserUpsertModel typings＋drawer/index.vue MODAL-WIRING (a)＋移除 getRoleOptions mock＋backend.biz.user.* i18n（4 鍵、先 Schema 後 locale）。C-V 全綠：62 bin＋7 live（`--test-threads=1`）＋endpoint_coverage_lint `[&str;11]`（6 端點全治理）＋entity_access_lint＋C-V-9~11 CDP 真發 request（空 param+模糊／add/edit/delete/batch 真打後端）＋C-V-13 零回歸（零 migration/entity/schema、blanket From 未改、enforce_mw/require_policy 未改、base-web 既有檔未改）＋C-V-14 prod target image build；holistic review PASS（12 FR+11 SC 全 covered、cross-unit seams sound、無 overbuild）。**零 migration/entity/schema、無新 crate**；rust-api `3874182`→`8ccea9d`、base-web `223bc83e`→`c1806680`；D1 follow-up（前端 hasAuth gating＋dynamic menu getUserRoutes＋選單可見性）延波2 Menu 刀（CHECKLIST §3.F／選單可見性 010 已兌現）；merge --no-ff、feature branch 保留 |
| `3810103` | 2026-06-19 | **010-menu-management 全綠收刀（波 2 第二刀＝Menu）** — 動態角色選單（getUserRoutes Casbin `v2='menu'` 過濾、§I.2 首兌現、前端零過濾、tree 祖先包含〔dir 不在 visible 但子可見仍納入〕）＋選單 CRUD（reparent 3+1 guard）＋統一回收桶（已刪除欄、restore 孤兒→頂層）＋越權防護＋D1（選單可見性自動達成＋前端 hasAuth gating menu＋retroactive user）＋`.env` static→dynamic（#7 兌現）。**11 端點**（/route getConstantRoutes〔public〕·getUserRoutes·isRouteExist〔auth-only〕／8 /systemManage *Menu* R_SUPER）。3 全專案首立：`menu_routes_for_roles`（鏡像 buttons_for_roles、v2='menu'）／reparent guard facade slim `ReparentError`（⚠️o、`would_cycle` 純函式）／menu flat→tree 序列化；`AuditOperation::Restore` 首 consumer；`DeleteError` 批次整批拒 no-partial；getAllPages 裸 page 名（修 component 字串 wire 缺口）。4 單元 Workflow 驅動（U1 動態路由讀 `1369ca1`→U2 寫 CRUD `00436d1`→U3 刪除+回收桶 `c377444`／U4 base-web+.env dynamic `a59c2738`）、每單元 implementer→spec review→quality review＋主線獨立 ground-truth 自驗＋逐單元兩段式 commit（worktree→pin）。C-V-0~12 全綠（88 bin＋5 live `--test-threads=1`／`endpoint_coverage_lint` `[&str;22]` 8 menu 端點全治理＋`entity_access_lint`／C-V-8 policy-gate Admin/User→5003／CDP 三角色側欄差異〔D1〕+CRUD 真發+restore+hasAuth gating〔R_ADMIN user:edit-only nuance〕+2222 在地化＋login dynamic 可達+fail-fallback 導回登入／C-V-11 零回歸／C-V-12 prod image build）、holistic review READY-TO-FINISH（11/11 SC、零 blocking、wire 三端對齊無型謊）。**零 migration/entity/schema、無新 crate**；rust-api `8ccea9d`→`c377444`、base-web `c1806680`→`a59c2738`。2 clarify 拍板（批次刪父子整批拒＋retroactive gating 含 user/settings）。★ 3 as-built 例外（route store 1 處授權 rev3-inline 保留前端 builtin 常數路由〔修 FR-002 R-cr 預示缺口、user 拍板〕／getMenuList/v2 裸陣列 honest type〔base-web fetchGetMenuListV2+transform、既有檔未動〕／getAllPages 裸 page 名）；D1 follow-up（008+009 D1 follow-up）已兌現；merge --no-ff、feature branch 保留 |
| `b80c9e3` | 2026-06-19 | **011-role-management 全綠收刀（波 2 第三刀＝Role）** — `/manage/role` mock→真後端 **9 端點**（getRoleList〔R_SUPER+R_ADMIN〕／addRole·updateRole·deleteRole·batchDeleteRole·getRoleHome·updateRoleHome〔R_SUPER〕／getRoleMenu·updateRoleMenu〔protected R_SUPER〕）：角色 CRUD＋**角色×選單授權**（與 010 getUserRoutes `v2='menu'` 讀端形成讀/寫閉環）＋角色首頁。**★ 全專案首個 runtime casbin policy WRITE＝DB-first**（B1 校正後）：net-new `model/facade/sys_casbin_rule::set_role_dimension`——於【單一】`mutate_in_txn`（AppState.db 同 txn）read current `entity::casbin_rule`〔11-col〕→diff→**②protected-reject**〔to_revoke 含 `protected=true`→整批拒、零變更〕→`delete_many` revoke＋`ActiveModel.insert` grant＋op-log〔entity_table="casbin_rule"、entity_id=role_id、同 txn 原子〕；handler 在 commit 後且 `outcome.changed`→`enforcer.write().await.load_policy()`〔⑤ reload〕；**絕無 enforcer MgmtApi 寫**（remove_filtered_policy/add_policies）——滿足 constitution §I.7 §4.2 ①DB-first/②protected/④原子/⑤reload（archive/restore/un-protect/PolicyMutated-優化/跨實例 publish-watcher 治理機留波3）。delete guards（seeded hardcode {R_SUPER,R_ADMIN,R_USER_COMMON}／in-use `count_users_by_role_id`>0／self `roles_of_user`、批次整批拒 no-partial）；id↔route_name 映射（net-new `sys_menu::ids_for_route_names`/`route_names_for_ids`、orphan/missing skip）；`set_home` entity 寫原子；roleCode 23505→`biz.role.duplicateRoleCode` 2222。base-web＝`rev3-system-manage.ts` 8 wrapper〔roleId/menuIds 維 number ⚠️r、body id→String〕＋`RoleUpsertModel` ADAPT＋MODAL-WIRING (a) role/index/drawer/menu-auth-modal〔menuProtected 2222 interceptor 在地化〕(b) hasAuth `role:*` gating＋`backend.biz.role.*` i18n 6 鍵〔含 menuProtected、先 Schema 後 locale〕；button-auth-modal 留 mock（波3）。**3 執行單元 Workflow 驅動**（U1 Role CRUD `2332002`→U2 Role×Menu DB-first+home `e3d828e`→U3 base-web `caa1e4bc`），每單元 implementer→spec review→code review（皆 pass）＋主線獨立 ground-truth 自驗（git show --stat＋容器內 build/lint/live＋psql 無殘留）＋逐單元兩段式 commit（worktree→pin、S9）；final holistic review **SHIP**（10/10 SC、B1 零 MgmtApi grep-confirmed、讀寫閉環閉合、零 overbuild）＋2 nit 修（G1 role_home live op-log 斷言加 trace_id 守門〔非冪等、`30de791`〕／N1 C-V-7 method 對齊 deleteRole/batchDeleteRole=DELETE〔doc〕）。C-V-0~11 全綠（build `--locked`／純測 role_delete_guard·role_active_model／`endpoint_coverage_lint` `[&str;31]` 9 端點全治理＋`entity_access_lint`〔entity::casbin_rule 僅 sys_casbin_rule facade〕／live role_crud·role_guard·**role_menu_loop**〔DB-first 讀寫閉環＋②protected-reject＋F1 原子審計、snapshot+byte-restore+psql 驗無殘留〕·role_home／C-V-7 policy-gate Admin/User→5003／typecheck／CDP 真發 request＋讀寫閉環換角色側欄＋F2 首頁 redirect＋hasAuth gating＋cleanup／prod image build）。**零 migration/entity/schema、無新 crate**（表＋3 角色＋9 端點 policy＋`role:*` button code m001/m002 已備、Constitution 9/9 PASS）；rust-api `c377444`→`30de791`、base-web `a59c2738`→`caa1e4bc`。merge --no-ff、feature branch 保留 |
| `55d34ff` | 2026-06-19 | **012-audit-log-query 全綠收刀（波 2 殿後刀＝Audit；★ 波 2 data islands 全完成）** — 審計查詢讀端＋Super-only 審計中心單頁三分頁（/manage/audit、操作異動/API存取/登入嘗試）：3 唯讀端點 getOperationLog/getAccessLog/getLoginAttempt〔R_SUPER GET〕。**★ 首個波 2 帶 migration 的刀＝m005 delta**（manage_audit menu seed＋3 讀端 R_SUPER policy＋menu policy＋operation/access btree filter 索引×4；execute_unprepared 鏡像 m002 seed/m001 index；sys_menu `ON CONFLICT (route_name) WHERE deleted_at IS NULL` partial unique〔F3〕；**無新業務表、無 ALTER 既有日誌表、up→down→up 可逆**）。3 sink facade +list〔鏡像 sys_role::list、order_by_desc created_at,id、apply_if 逐欄、page 0-based〕＋filter struct；**★ IP 模糊 `host(col)::text LIKE`**〔net-new 無 codebase 先例、`Expr::cust_with_values` $1、主線 psql+live+HTTP 三證命中 172.x／排除 999〕；文字模糊 `LOWER(col) LIKE ESCAPE`〔非 PgExpr::ilike、沿 009〕；精確 eq／範圍 gte-lte／operator_ids is_in。net-new `sys_user::names_for_ids`〔**unfiltered 含已刪**、審計保留歷史名、live 證 id=12 cdpUser009〕＋`ids_for_name_ilike`；handler 3 端點 §5.8 空字串守門＋operator by 名→ids＋日期 parse〔parse_audit_date RFC3339/date-only、僅起/僅訖寬鬆、畸形→`biz.audit.invalidDateRange` 2222、F2〕＋honest wire〔id number、`IpNetwork.ip()` 去 mask、nullable→Option 無 skip_serializing_if、payload `Option<Value>` 任意 JSON〕。main audit group 3 GET require_policy R_SUPER＋enforce_mw＋.merge。**★ endpoint_coverage_lint [31→34]＋seed 來源改全 migration glob**〔`all_migration_seeds()` 讀 m0*_*.rs 涵蓋 m005、原僅讀 m002 會 FAIL〕。base-web 100% 淨新：rev3 3 fetch＋pruneNullParams／3 honest typing〔payload→`unknown\|null` 非 Record〕／views/manage/audit NTabs 3 tab＋3 子表〔NDataTable+逐欄 filter+分頁、op-log payload renderExpand〕／i18n page.manage.audit＋backend.biz.audit.invalidDateRange〔先 Schema 後 locale〕／Super-only 靠 m005 menu policy＋dynamic getUserRoutes〔頁面層無顯式 hasAuth〕。**★ biz key 分層校正**：rust `biz.audit.invalidDateRange`〔無 backend 前綴〕vs base-web `backend.biz.audit.invalidDateRange`〔攔截器 translateBackendMsg 補 backend.〕。**★ CDP 接住真 bug（curl≠modal）**：getOperationLog?entityId= 空字串→400 整頁掛〔U2 守 http_status 卻漏 entity_id〕→補後端 `parse_entity_id` 權威守門〔`fca64a0`〕＋前端 pruneNullParams 防線。**唯讀無 op-log 寫**〔查詢前後 op-log 41→41；access-log per-request +N=F1 基建、非自審〕、live/CDP 無 cleanup。**4 commits Workflow 驅動+主線編排**（U1 m005 `fce18c4`→U2 rust 讀端 `be94fcd`→entityId 修正 `fca64a0`→U3 base-web `1764154e`；每單元 implementer→spec review→quality review 皆 pass＋主線單元邊界獨立 ground-truth 自驗〔git show --stat 抓偷加檔＋容器內 build/lint/live＋C-V-4 curl policy-gate＋親跑 m005 up/down/up＋CDP 截圖〕＋逐單元兩段式 commit S9）。C-V-0~9 全綠（build／純測 19〔含 parse_audit_date/http_status/success/entity_id〕／lint[34]+entity_access／live audit_query〔IP-fuzzy 首驗〕／policy-gate Admin·User→5003 Super→200／m005 up→down→up／typecheck／CDP 3 tab 真發+payload 展開+super-only／零回歸／prod build），final holistic（fresh-agent）**無 blocker**（12 FR+9 SC+6 Edge 全覆蓋、cross-unit wire/filter/授權/i18n/lint 接縫一致）。**D11 三欄 IP forensic 模型＋D4 角色 payload delta＝後續刀遞延**。rust-api `30de791`→`fca64a0`、base-web `caa1e4bc`→`1764154e`；merge --no-ff、feature branch 保留 |
| `82f49d4` | 2026-06-21 | **constitution amend ⚠️ac（PATCH v1.1.1→v1.1.2）** — §I.6 釐清「無 retrofit」標的＝archetype 審計欄；既有表加 domain 業務/forensic 欄之【刻意/規劃/可逆】演進不在此限（例 D11：013 審計 log 表加 IP forensic 欄、`m006` 可逆）；前提 archetype B 欄規則不變（不加 `updated_*`/`deleted_*`、不可竄改）、非意外債。**非新決策＝釐清原意**，013 plan Constitution Check item 8 由此正式背書（原 Complexity Tracking 破例升合規演進）。誕生於 013 `/speckit-analyze` C1、user 親決。憲法§I.6+version＋DECISIONS §1 ⚠️ac＋CHECKLIST §5 已決 28 |
| `8980d00` | 2026-06-21 | **013-xff-real-ip-forensics 全綠收刀（D11 遞延刀＝XFF→real_ip 鑑識；波 2 後獨立刀、收 012 D11+007 XFF 完整化）** — 把 007 單值 XFF 解析重寫為**兩層信任模型**＋四欄鑑識×三審計表＋審計中心顯示/篩選＋nginx CF 閘/Tunnel。**解析核心**（`audit_ctx.rs`）：`normalize_xff_tokens` IIS-aware〔split `[\s,+]`、剝 v4`:port`/v6`%zone`/`[v6]:port`、garbage drop〕／`resolve_client_ip→(IpAddr,Confidence)` 四分支〔peer-gate `Direct`／Tier-1 CDN 位置錨**不硬 gate** `CdnAnchored`〔最右 CDN 左一非 CDN、盲剝免疫漏設〕／Tier-2 `internal_default∪my_public∪binding.internal` 逐點 skip `ProxyClean`〔dual_role/binding 相鄰不符→`ProxySoft`〕／整鏈受信 `Fallback`〕／`apply_cf_overlay` top-level〔`X-CF-Verified=1`∧`CF-Connecting-IP`∧base∈{anchored,clean,soft}→cip==real?`CdnVerified`:`CdnMismatch`、**real_ip 不變**〕／`Confidence` 七態 as_str DB/wire 契約／`TrustModel`〔cdn(+connecting_ip_header)/my_public(+dual_role)/internal_default/bindings、CIDR String 收+post-parse、per-set fail-safe 退保守、缺檔 fallback flat env〕＋新 `toml` 0.8.23 parse-only workspace dep（1.86 `--locked` 編過）。**四欄鑑識×三表**：`m006` 可逆 delta〔rename `client_ip→real_ip`/`operator_ip→operator_real_ip`+add `peer_ip`/`ip_confidence`/`operator_*`、entity 同步、m001 凍結 PG RENAME 自動跟 index、up→down→up+fresh chain m001..m006 可逆〕；`audit_mw` 讀 cf header+overlay+四欄；**★ op-log 4/4 補完**＝`AuditOperator` 維持 Copy（peer/real）+net-new `AuditMeta` bundle thread operator xff/confidence 過 ~15 facade mutate fn（**user 親決「補滿 4/4」、反轉 D7「不動 16 facade」**）；三 sink write+login sink+honest wire。**012 審計中心擴充**：四欄顯示〔C2 序 `ip_confidence`(NTag 七態著色)→`peer_ip`→`real_ip`→`x_forwarded_for`、operation `operator_` 前綴、共用 `ip-confidence-tag.tsx`〕+三模糊一下拉篩〔C1：`peer_ip` ip_host_like/`ip_confidence` eq/op-log 補 `operator_x_forwarded_for` ilike、空字串守門 curl 證〕+i18n 雙語〔confidence 七態 byte-identical rust↔d.ts↔options↔i18n〕。**nginx**（`89e0fdf` deploy）：`geo $remote_addr $x_cf_verified`〔CF 官方 v4×15+v6×7 ∪ cloudflared loopback、$remote_addr 不可偽造〕+`map` strip 偽造 CF-Connecting-IP+`proxy_set_header` 覆寫〔**FR-010 偽造中和：非 CF 源偽造→`cdn_anchored`≠`cdn_mismatch`、psql 證**〕+不啟 realip+既有 4 header 零回歸；Tunnel〔trust-model internal_default loopback、案例 11 `cdn_verified`〕。**6 執行單元 Workflow 驅動**（U1 解析核心 `74f5c4d`→U2+U2b 四欄持久化×三表 `c1fe429`→U3 顯示 `25cab888`→U4 篩選 rust`627ebc6`+base-web`e79e7aa8`→U5 nginx CF `89e0fdf`→U6 polish `3f2ebc6`〔清 IpForensics orphan〕），每單元 implementer→spec review→quality review＋主線單元邊界獨立 ground-truth 自驗〔git show --stat＋容器內 build/lint/live＋psql 四欄＋curl 守門/偽造 negative〕＋逐單元兩段式 commit（worktree→pin、S9）。C-V-0~9 全綠（純測 36+12 案例/lint 2+2/m006 可逆+fresh temp-DB 全鏈/live 四欄 psql/curl 空 param 守門/FR-010 偽造 negative/CF 正向 cdn_verified·mismatch/Tunnel/**prod target image build**〔新 toml dep multi-stage 無 COPY 缺口〕/零回歸 18 ignored live+012 既有行為），final holistic review（fresh-agent 冷讀）**PASS**〔5 US+10 FR+7 SC 全覆蓋、跨單元接縫一致無型謊〕。pins：rust-api `fca64a0`→`3f2ebc6`、base-web `1764154e`→`e79e7aa8`、deploy 直 outer。Constitution 9/9 PASS（item 8 ⚠️ac 背書）；merge --no-ff、feature branch 保留 |
| `3066bc2` | 2026-06-22 | **014-auth-token-session 全綠收刀（波 3 第一刀＝Auth/Token/Session 合刀）** — 把 006 Auth 島最小段補成完整行為島：DESIGN §4.1 token rotation chain＋§4.3 single-session lifecycle＋硬即時撤銷＋cleanup-job＋2-instance 多副本驗證。**★ 接地揭露 rust-api 零 Redis client→本刀含建 Redis 基建**（`server/src/redis.rs` fail-OPEN facade〔get/set_ex/del/publish/subscribe_pubsub、不可達回 None/() 不 panic〕＋AppState `redis:Arc<Option<RedisHandle>>`＋`single_session_default:Arc<AtomicBool>`＋main boot best-effort 連線+`spawn_settings_watcher`〔SUBSCRIBE settings:invalidate→重載、斷線指數 backoff 重訂閱〕＋008 update 後 PUBLISH；新 `redis` 0.27.6 crate 1.86 `--locked` 編過）。**§4.1 rotation**：`/auth/refreshToken`〔verify→8888/pointer-first is_current→7777/decide_rotation 四分支〕＋`decide_rotation` 純函式〔active→Rotate/used&<GRACE(30)→Benign/≥grace·revoked·used_at NULL→Reuse fail-closed/NotFound〕＋sys_token facade〔find_by_hash/find_by_hash_for_update/mark_used 冪等/revoke_chain〕＋新 jti byte-distinct 防同秒撞鍵；**★ TOCTOU lock-then-redecide 加固**〔Rotate/Benign 進 txn FOR UPDATE 鎖後對鎖住列重跑 decide_rotation、併發 revoke 落地→鎖讀見 revoked→Reuse→8888 絕不重鑄、閉 SC-002；`toctou_revoked_chain_locks_to_reuse_no_rebake` live 證 active 列數=0〕。**§4.3 single-session**：`resolve_policy` 純函式〔三態×全域〕＋`is_current` policy-aware〔簽名改 &AppState、`session_gate_of` 一趟 PK 取(policy,pointer)、effective Off→放行修「永遠踢」bug、On pointer≠sid→7777、fail-OPEN〕＋login `revoke_other_chains`〔effective On 撤舊 active 鏈〕＋session_policy wire〔getUserList/updateUser 三端 honest literal〕。**硬即時撤銷（閉 006 Auth 島 follow-up）**：Redis denylist `revoked:user:{uid}`＋`denylist_gate`〔enforce_mw＋refresh 雙查、`iat<=revoked_at`→8888 同秒 fail-secure、fail-OPEN〕＋`revoke_user_sessions`〔Redis 先+DB 撤 **active+used 全鏈**〔防 used grace 內 Benign 重鑄繞撤、SC-004〕+清 pointer〕→接 009 deleteUser/batchDelete〔逐 id D-02〕/updateUser(status==2)。**cleanup-job 新 workspace crate**〔dry-run 預設只 count/--execute 物理刪/冪等/單旗標、raw Postgres `now()-make_interval(60s)`、不依賴 entity 避 chrono feature-gate〕＋**prod Dockerfile 四處 COPY**〔Manifest/Source/builder cp/runtime COPY、D-01 binary 落地實機驗 5.7MB exec 非 not-found〕。**base-web session_policy UI**〔rev3 typing intersection 不動 frozen User＋constants sessionPolicyOptions＋i18n Schema-先-locale-後〔sessionPolicy 三態+backend.biz.user.invalidSessionPolicy〕＋drawer NSelect MODAL-WIRING ★ (a) rev3-inline＋CDP 7777 modal/8888 logout 兩通道乾淨登出〕。**2-instance 多副本驗證**〔docker-compose.dev.yml `rust-api-2` profiles:[multi] 共用 jwt/DB/Redis secret 獨立 target、C-V-6 跨進程收斂+shared DB pointer 踢+denylist 跨副本+**CLIENT KILL pubsub→watcher 重訂閱韌性**、watcher U1 已夠韌無 rust 改〕。**8 執行單元 Workflow 驅動**（U1 Redis 基建 `ddec5c2`→U2 rotation+TOCTOU `a2ea33b`→U3 single-session `44885d9`→U4 denylist `b37fac3`→U5 cleanup-job+Dockerfile `a9d3986`/outer `7c08ed2`→U6 base-web `f3b2bf07`→U7 compose `fc41667`→U8 polish+holistic），每單元 implementer→spec review→quality review＋主線單元邊界獨立 ground-truth 自驗〔git show --stat 抓範圍＋容器內 build/test/live＋psql/redis-cli〕＋逐單元兩段式 commit S9。**3 設計時攔截的 spec 未明示真漏洞**（皆零 schema、附自動化驗證）：U2 TOCTOU〔SC-002〕／U4 used-token grace 重鑄〔SC-004、撤鏈須含 used〕／U4 同秒撤銷漏網〔implementer 自驗抓、`<`→`<=` fail-secure FR-007〕。C-V-0~10 全綠（decide_rotation/resolve_policy 純測／lint `[&str;35]`〔/auth/refreshToken 納入〕+entity_access／live rotation·single-session·denylist·cleanup·2-instance／CDP 兩通道／prod build 三 binary 落地／零回歸 146 passed+lint 2+2），final holistic（fresh-agent 冷讀）**PASS**〔5 US+14 FR+10 SC+§I.7 §4.1/§4.3 invariants 全覆蓋+逐條自動化驗證、cross-unit 接縫一致〕。**零 migration**〔schema 全在 m001/m002、`git diff 3f2ebc6..HEAD migration/` 空、6 m-檔不變、Redis 無表〕、新 `redis`+`cleanup-job` crate、Constitution 9/9 PASS。4 拍板（C1 多實例 B-驗證/C2 撤銷硬即時 fail-OPEN/C3 per-user UI/C4 alt-login 排除）。Redis sess pointer 快取按 data-model X-01 sanctioned defer〔is_current 直讀 DB〕。pins：rust-api `3f2ebc6`→`a9d3986`、base-web `e79e7aa8`→`f3b2bf07`、deploy/compose 直 outer。merge --no-ff、feature branch `014-auth-token-session` 保留。 |
| `65f1838` | 2026-06-22 | **015-policy-governance 全綠收刀（波 3 第二刀＝Policy 治理島 §4.2 完成刀）** — 把 011 `set_role_dimension` 鋪的 casbin policy 寫側（DB-first＋②protected-reject＋reload-on-`changed`、revoke=HARD DELETE、reload local-only）補成 **DESIGN §4.2 完整治理島**：revoke→**archive-move**（snapshot 被撤 11-col→INSERT `sys_casbin_policy_archive`→DELETE casbin_rule **同 txn 原子**、可復原非硬刪）＋**restore 三態**（net-new `restore<C:TransactionTrait>(conn,archive_id,meta)→RestoreOutcome{Applied/NoOp/NotFound}`：查無→NotFound 2222／live 7-col〔ptype/v0-v5〕已存在→NoOp〔消費 archive 不重插不審計、0000〕／else→Applied〔INSERT casbin_rule〔★ created_at 跨表 nullable→NN `unwrap_or(now)` coerce〕+DELETE archive+Restore op-log `{role,target,dimension}` 同 txn〕）＋**PolicyMutated gate**（net-new `reload_and_publish(&state)`＝`enforcer.write().load_policy()`+`PUBLISH casbin:policy:invalidate`〔best-effort fail-OPEN〕；**updateRoleMenu 由 reload-on-`changed` 改 reload-on-Applied〔含空-diff、§4.2 ③「不優化」、調整 011、user 2026-06-22 確認〕**、restorePolicy Applied→reload+publish／NoOp·NotFound→skip）＋**跨副本 pub-sub**（net-new `spawn_policy_watcher`〔SUBSCRIBE casbin:policy:invalidate→`load_policy()`、嚴格鏡像 014 `spawn_settings_watcher`、fail-OPEN backoff 重訂閱、復用 014 Redis 基建+`CASBIN_INVALIDATE_CHANNEL` const〕）＋**回收桶 UI**（base-web `views/manage/policy-archive` MODAL-WIRING (e)、list 鏡像 012 audit／restore 鏡像 010 menu、NPopconfirm→fetchRestorePolicy→reload、R_SUPER-only 靠 menu policy+後端 require_policy〔無假碼 hasAuth 隱藏保 SC-006〕）。net-new `model/facade/sys_casbin_policy_archive`（insert_archived/list 分頁/restore）＋handler getArchivedPolicies〔honest `PageRes<ArchivedPolicy>` 8 欄〕/restorePolicy〔三態 0000/2222〕＋main 2 route＋`AS_BUILT_ROUTES` 35→37；base-web rev3 typings/service（fetchGetArchivedPolicies pruneNullParams／fetchRestorePolicy String(id)）＋i18n（先 Schema 後 locale＋route.manage_policy-archive）＋**elegant-router 自動生成 route 註冊 4 檔**（建 view 觸發、fresh checkout 必需、主線最終接住）。**4 執行單元 Workflow 驅動、rust 全程 serial**（EU1 archive facade+revoke→archive-move+teardown+facade live `5163d5a`→EU2 reload_and_publish+2 handler+2 route+AS_BUILT 37+gate 改011+gate 測 `383515b`→EU3 spawn_policy_watcher `2ad2029`→EU4 回收桶 UI `0adfd12d`），每單元 implementer(TDD)→spec-compliance review→fix loop→code-quality review＋主線單元邊界獨立 ground-truth 自驗（git show --stat＋容器內 build/lint/live `--test-threads=1`＋psql 無殘留）＋逐單元兩段式 commit（worktree→pin、S9）；EU1 cq 2 medium 主線修（dead_code #[allow]／D5 ZZZ 無條件 teardown 防 seed 污染）、EU3 cq nit 修（docstring）、**EU4 elegant-router route 檔+route i18n 遺漏主線最終 checkpoint 接住**。C-V-0~7 全綠（build／兩 lint〔entity_access＋endpoint_coverage `[&str;37]` registered==as-built〕／facade live `archive_restore`〔D1 archive-move 原子/D2 Applied/D3 NoOp/D4 NotFound/D5 created_at-NULL coerce/D6 protected-reject archive 零變更〕＋gate live〔空-diff Applied 仍 reload／reload_publish redis subscribe 往返證綁對 channel〕／**C-V-4 curl Super 200·Admin 5003·NotFound 2222**／**C-V-5 2-instance 跨副本收斂**〔A revoke→B getRoleMenu 反映、A restore→B 反映、**CLIENT KILL pubsub→~1s 重訂閱→仍收斂**FR-008〕dev DB 零殘留／**C-V-6 CDP 回收桶 UI**〔list 真打真資料、**點復原→restorePolicy 200→列消失**、**console error 消解（010 manage_policy-archive）**〕／C-V-7 零回歸〔22 ignored+146 non-ignored 全綠〕＋migration/entity **零-diff**＋**prod target image build**），final holistic review（fresh-agent 冷讀 grep 驗）**READY_TO_MERGE**〔5 §4.2 invariants〔①DB-first 零 MgmtApi/②protected-reject/③gate/④原子+審計/⑤reload+publish〕+SC-001~008+US1~US3 全覆蓋、wire byte 對齊無 type-lie〕。**零 migration、零新 crate**（archive 表 m001:665／2 endpoint+menu policy seed m002 全波0已備、`git diff 7ec8fc3..HEAD migration/` 空、Constitution 9/9 PASS）。**1 拍板：A un-protect 不做＝protected 硬守門**（§4.2-faithful、做它須 §V.2 amend ②）。pins：rust-api `7ec8fc3`→`2ad2029`、base-web `f3b2bf07`→`0adfd12d`。merge --no-ff、feature branch `015-policy-governance` 保留。 |
| `fa17def` | 2026-06-22 | **016-button-endpoint-policy 全綠收刀（波 3 殿後刀＝Button-Endpoint 三維 RBAC runtime 編輯;★ 波 3 全完成）** — 把 011 `set_role_dimension`（menu 寫側）＋015 治理島（archive/restore/protected-reject/gate/watcher）延伸到 **button＋endpoint 兩授權維度**，補完三維 RBAC runtime 編輯。**button＝撿現成**：`updateRoleButton` 直呼 `set_role_dimension(role,"button",codes)`（dimension-agnostic、v2='button' 固定）、011 寫側＋015 治理島全免費繼承;`all_buttons` 自 `sys_menu.buttons` JSON registry 萃取〔code,label〕字典序去重、`button_codes_for_role` 鏡像 buttons_for_roles 單角色。**★ endpoint＝新 `set_role_endpoints<C>(conn,role_code,desired:&[(path,method)],meta,role_id)`**（net-new、(path,method) 雙鍵 diff）——endpoint policy=真實 (v0=role,v1=path,v2=HTTP method) enforce 列（require_policy 查的列）、v2=method 逐列變動套不上 set_role_dimension 固定-v2 模型 → 單一 `mutate_in_txn`：read-current〔v2∈HTTP_METHODS〕→雙鍵 diff→**②protected-reject 任何寫前**〔to_revoke 含 protected→`Rejected(Vec<(path,method)>)` 零變更〕→revoke〔`insert_archived` reason="role_endpoint_revoke"+**DELETE 按 id**〔非 V2.eq、防變動 v2 誤刪〕同 txn 原子〕→grant〔insert v1=path,v2=method,protected=false〕→op-log;★★ 全程動真實 (role,path,method) 列、**無 v2='endpoint' 平行編碼**（grep 全刀證、字面 "endpoint" 僅在 `dimension_display` 推導+list filter）。治理 helper（insert_archived/mutate_in_txn/reload_and_publish/spawn_policy_watcher）全復用 015。**§4.2 五 invariants 維度無關延伸**（①DB-first 零 MgmtApi／②protected-reject〔endpoint 15 protected=鎖出守門·button 0 protected moot〕／③gate〔updateRoleButton/Endpoints Applied〔含空-diff〕→reload+publish·Rejected→skip〕／④revoke/restore+審計同 txn 原子／⑤reload=全量 load_policy+PUBLISH casbin:policy:invalidate）。**回收桶三維 v2-推導（同改兩處）**：`dimension_display(v2)=match{menu|button=>原值,_=>endpoint}` display 層 + archive list facade dimension="endpoint"→`v2 IN HTTP_METHODS` filter〔menu/button 維持 V2.eq〕、**不改 archive_reason**、restore 審計記 raw-v2〔method、forensic 精度、與 display 推導刻意不對齊 M3〕。**registry**：`ALL_ENDPOINT_POLICIES`（prod const 36 對=已註冊 policy-governed route）+`endpoint_pairs_for_role`;**M2 雙向 registry assertion**（registry==registered:⊇防漏端點+⊆防 phantom 孤兒）。**base-web 三維 role drawer UI（MODAL-WIRING (c)）**：button-auth-modal **un-mock**〔嚴格鏡像 menu-auth、key-field=code、checks string[]、watch(visible) lazy〕＋endpoint-auth-modal **新建**〔(path,method) 合成 key〔method+空格+path〕、submit 對稱拆解、hard-replace fetchUpdateRoleEndpoints〕＋role-operate-drawer 第三鈕＋6 wrapper〔roleId 維 number ⚠️r〕＋Button/Endpoint typings〔declaration-merge 不動 frozen〕＋i18n endpointAuth/endpointProtected〔先 Schema 後 locale〕。**typings 收斂 fold-in（D5/U5）**：`RoleListItemRev3=Omit<Role,'roleDesc'>&{roleDesc:string|null}`／`UserListItemRev3` honest 三欄＋既有 sessionPolicy／新增 `fetchGetRoleListRev3`·`fetchGetUserListRev3` honest wrapper〔getRoleList/getUserList 在 frozen system-manage.ts、故新 rev3 wrapper 非改 frozen〕、消 User/Role wire type-lie。net-new main.rs 6 route R_SUPER require_policy＋`AS_BUILT_ROUTES` 37→43。**5 執行單元 Workflow 驅動（rust serial→base-web 跨棧並行）**（U1 button 後端 `44ffd4f`→U2 endpoint 後端〔含 encoding-correctness 對抗證偽 9 項全擊破〕`4929406`→U3 治理統一 v2-推導+gate+2-instance `066f91b`→U4 base-web UI `d2a1990d`→U5 typings 收斂 `c2c61893`），每執行單元一支 Workflow〔implementer TDD→spec-compliance review→fix loop→code-quality review、皆 pass〕＋主線單元邊界獨立 ground-truth 自驗〔git show --stat 抓範圍＋容器內 force-touch build/lint/live `--test-threads=1`＋psql H1 baseline＋**主線親跑 cv6 2-instance**〕＋逐單元兩段式 commit〔worktree→outer pin、S9〕。**C-V-0~8 全綠**（build／兩 lint〔entity_access＋endpoint_coverage `[&str;43]` registered==as-built、Assertion A 6 端點 m002 seed、M2 雙向 registry〕／live button〔revoke→archive-move+grant+restore+buttons 反映〕／live endpoint〔雙鍵+鎖出〔撤 protected→Rejected 零變更〕+**enforce 自 DB 建 Enforcer 驗 revoke 拒/grant 通**+UI-gateway getRoleList restore〕／gate 精準〔空-diff Applied 仍 reload、Rejected skip〕／curl Super 6 端點 200·Admin 403／**2-instance cv6 跨副本**〔button getUserInfo 收斂+endpoint enforce 收斂+restore+CLIENT KILL pubsub 重訂閱韌性〕／**CDP 三維 UI 6/6**〔三鈕在地化〔接口权限 resolve 非 raw key〕+button/endpoint modal 真打真資料+撤 protected→在地化「受保护的核心接口存取不可移除」原子拒+無 console error+manage 頁無回歸〕／零回歸〔25 ignored+155 non-ignored〕+zero-migration〔`git diff 2ad2029..066f91b migration/` 空〕+prod target image build〔binary 落地、零新 crate〕+typecheck），final holistic review（fresh-agent 冷讀+2 fresh agent 交叉）**READY_TO_FINISH**〔endpoint 編碼鐵則成立=真實列無平行編碼、§4.2 五 invariants 延伸、SC-001~008+US1~US3 全覆蓋、constitution 9/9、0 critical/high/medium〕。H1 baseline 守住（live 測 teardown 清自身 casbin op-log trace）、dev DB 寫端原子無自身殘留。**零 migration、零新 crate**（6 編輯端點 policy m002:148-153／button policy 16／endpoint policy〔15 protected〕／button JSON `sys_menu.buttons` 全波0已備、Constitution **9/9 PASS**）。**4 拍板**：scope C（button+endpoint 完整）／un-protect 不做（延續 015 A）／回收桶 v2-推導／endpoint 鎖出靠既有 15 protected seed。pins：rust-api `2ad2029`→`066f91b`、base-web `0adfd12d`→`c2c61893`。merge --no-ff、feature branch `016-button-endpoint-policy` 保留。 |
| `63a46f5` | 2026-06-23 | **pre-波4 follow-up sweep（§3.A/§3.B/§3.E/§3.G/§3.H、直接 surgical commits 非 spec-kit；outer `5b74cdb`+`eea0447`+`63a46f5`）** — 對照實碼驗證 §3 backlog（5 支 Workflow 唯讀 agent）後收 ripe 項。**rust**〔worktree `d1cf733`〕：清 pre-existing dead（`RequestContext.operator_id` never-read〔007〕／`sys_casbin_rule` 測試 `ConnectionTrait` unused import〔011〕）＋vendored `sea-orm-adapter` 移除 async-trait/tokio 被忽略的 `default-features=false`（消 cargo future hard-error 警告）＋settings/policy watcher reconnect 後補一次 re-read/reload（補 backoff window 漏失 invalidate）＋Redis boot connect 包 `tokio::timeout(5s)`（host 不可達/SYN 黑洞不阻塞 boot）＋`normalize_xff_tokens` `MAX_XFF_TOKENS=32` cap（防 flooding、test-first `normalize_caps_token_count`）。**base-web**〔worktree `7a011c00`〕：request 攔截器 `onError` 補 else-if——native axios error（HTTP 403/404、`code≠BACKEND_ERROR_CODE`）也抽 `error.response.data.msg` 經 `translateBackendMsg` 在地化（§3.G、⚠️aa BASE-WEB-I18N-WIRING 授權；真 HTTP 404 envelope `{code:4040,msg:system.notFound}` curl 證；★ CDP 實證：注入 Admin token→fetchUpdateUser 觸發真 HTTP 403→toast「权限不足」〔非 raw key/英文、curl≠modal browser 軌驗〕）＋excel demo honest `fetchGetUserListRev3` 遷移（getTableValue 簽名 User→UserListItemRev3）＋016 dead type 註記。**deploy**〔outer〕：nginx `server_tokens off`＋prod 443 HSTS/X-Frame-Options/X-Content-Type-Options（`nginx -t` 掛 rev3_net 過）＋base-web runtime `nginx:alpine→nginx:1.31.0-alpine`（對齊 front-nginx、唯一無版本 image；★ node/postgres 維持大版本 pin＝拍板#4、不 patch-pin；debian:bookworm-slim pin 延後）＋`generate-secrets` dual-write drift 自動防護（依賴 leaf 本次 GENERATED→URL 連動重生）。驗證：rust 容器內 build/test 綠（normalize 11 passed）＋base-web typecheck 綠＋nginx -t 過＋bash -n 過＋新 tag Docker Hub 存在性確認。stale 自述校正（早已做）：/health 雙 CT・script chmod600/離線 fallback/renew 警告・prod migrate healthcheck disable。未收（own-cut/defer）：§3.B drawer null-flow・§3.C 全・§3.F policy-seed（動 m002 migration 需拍板；016 runtime 途徑或令其非必要）・§3.A trust-model/acme/cleanup-job/多副本/external cert・§3.E lint 守門・§3.H Redis pointer 快取/examples fixture・debian pin。 |
| `7e4ae19` | 2026-06-23 | **pre-波4 polish/cleanup sweep（§3.B/§3.H、直接 surgical commits；outer `8e0963f`+`7e4ae19`）** — 第二輪 backlog 收（3 支 Workflow agent 驗證後）。**rust**〔worktree `b63928b`〕：移除 `Binding.dual_role` net-dead 欄位（只寫不讀、audit_ctx 只消費 `MyPublicEntry.dual_role`、bindings 迭代從不讀 `b.dual_role`；6 處移除〔struct/RawBinding/mapping/test toml+assert/audit_ctx fixture〕、`MyPublicEntry.dual_role` 維持、156 unit test 綠）。**base-web**〔worktree `662430a4`〕：§3.B drawer null-flow（role/user-operate-drawer 編輯路徑 `Object.assign` 後 coalesce honest 讀型 nullable 欄 null→''〔roleDesc／nickName/userPhone/userEmail〕、消 type-lie、submit 不送 null、同 sessionPolicy 風格）＋§3.H ArchivedPolicy createdTime 欄（policy-archive 表加「建立时间」欄〔型/wire 已備、null→empty、scroll-x 1200〕+i18n zh/en+app.d.ts Schema）＋§3.H endpoint-auth-modal flat→依 path 第一段分組 NTree（`check-strategy=child`→checkedKeys 只含 leaf、submit 邏輯不變、default-expand-all；實測單一 `/systemManage` 群）。驗：rust 156 test／base-web typecheck exit 0／**CDP 實證**（createdTime 欄頭「建立时间」+ endpoint 群組 modal 渲染〔/systemManage 群+leaves expanded〕+ 兩頁零 console error/exception）。**§3.F 翻案＝維持現狀**（深查確認 updateUser POST 在 `ALL_ENDPOINT_POLICIES` registry、manage_role 為真 sys_menu→R_SUPER 經 016 endpoint/menu-auth-modal runtime 即可授 R_ADMIN、零 migration、非缺陷；user 親決不動 seed）。stale/moot 確認（已從 CHECKLIST 清）：filter_routes 兩端早已遞迴、iframe props rev3 不走該路徑、soft_delete 原子性已由 mutate_in_txn 保證、.gitignore /target 錨正確。 |
| `c7f5936` | 2026-06-23 | **017-audit-center-enhancement 全綠收刀（波 4 前 §3.C 審計中心 enhancement 刀；merge `c7f5936`）** — 三子功能、零 migration/端點/crate：**C-4 op-log 角色 delta**〔rust `8b58c7c`：純函式 `with_roles`〔roles 排序入 jsonb、空→[] 非 null、非 Object passthrough〕+create/update/soft_delete 三寫端 payload enrich〔update/soft_delete replace/刪前讀 `roles_of_user(&txn)`、create before=None〕+擴充 4 支 #[ignore] live smoke〔trace_id+entity_table 隔離〕〕；**C-3 CSV 匯出**〔rust `57de37c`：三審計讀端點加 `export` query 變體、CSV-in-envelope〔`Res::ok(String)`、繞過 normalize_size、page=0/size=CAP=1萬〕、手寫 `csv_escape_field`/`records_to_csv`〔UTF-8 BOM 僅檔首、F2 逗號/引號/換行轉義〕、op-log CSV 專屬 rolesBefore/After 欄〔F5 空值語意：[] vs ""〕+base-web `368964bf`：`downloadCsv` util〔Blob+a[download]〕+3 export wrapper〔flatRequest {data,error}〕+三表匯出鈕+截斷 toast〔itemCount>CAP〕+i18n〕；**C-1 http_status class filter**〔rust `e988a29`：`parse_http_status_class`〔2xx/4xx/5xx 半開區間、畸形/空→Ok(None) 不 2222、FR-012〕+AccessLogFilter 範圍 apply_if〔gte(lo).and(lt(hi))、與單值 eq 並存 AND〕+base-web `3713f94a`+`a555326d`：class NSelect〔全部=null→pruneNullParams 剔除→wire 缺席〕+i18n〔col.statusClass 名詞 label——CDP 驗收抓出原誤用 filter.statusClass placeholder 措辭當 label、修〕〕。**5 執行單元 Workflow 驅動**（EU1 C-4→EU2 C-3 後端→EU3 C-3 前端→EU4 C-1 全棧→EU5 polish），每單元 implementer(TDD)→spec-compliance review→fix loop→code-quality review＋主線單元邊界獨立 ground-truth 自驗〔git show --stat＋容器內 cargo build/test/live `--test-threads=1`＋**容器網路 node curl**〔host port-forward 退化期繞道〕＋bump pin S9〕。C-V-0~5 全綠（純測 with_roles/csv ×5/parse_http_status_class／三表 export wire〔BOM+roles 欄+Admin 403+0 列僅表頭〕／class filter wire〔4xx 收窄/空+畸形守門/AND〕／psql op-log roles delta／**CDP UI**〔三 tab 匯出下載 `<table>_<ts>.csv`+class NSelect 收窄+label 在地化〕／零 migration diff＋prod target image build＋全量 cargo test 166 passed＋lint 2+2），final holistic review（fresh-agent 冷讀）**ALL PASS**〔SC-001~008 全覆蓋、跨子功能接縫〔C-4→C-3 roles F5 兩端一致／C-1 filter 與 C-3 export 共用同一 AccessLogFilter〕、wire 3 端對齊無 type-lie、Constitution 9/9〔item 2/7：audit 頁 rev3 自建增強+page.manage UI i18n、非 frozen-upstream〕〕。pins：rust-api `57de37c`→`e988a29`、base-web `368964bf`→`a555326d`。merge --no-ff、feature branch `017-audit-center-enhancement` 保留。 |
| `6e90865` | 2026-06-23 | **§3.A cleanup-job 排程上線（pre-波4 backlog、014 U5 follow-up）** — 加 profile-gated compose sidecar（master `docker-compose.yml` `cleanup-job` service、`profiles:[prod]` opt-in〔與 acme 同 prod 軌、一般 dev/prod up 不啟〕、entrypoint sleep-loop `while true; do /usr/local/bin/cleanup-job --execute; sleep $${CLEANUP_INTERVAL_SECS:-86400}; done`〔`$$` escape 留容器 sh、預設每日〕、復用 prod image rev3-admin-rust-api:latest〔014 U5 Dockerfile 已 COPY binary〕+database_url secret〔APP_DATABASE_URL_FILE、cleanup-job `resolve_database_url` 自讀檔〕、`healthcheck: disable: true`〔image 繼承 server /health 對 loop 無意義、否則永遠 unhealthy〕、depends_on postgres healthy、restart unless-stopped）。cleanup-job 只刪 `expires_at < now()-60s`〔SKEW_MARGIN、冪等、與 status 無關、不碰 valid token〕。**live 驗（dev stack + --profile prod up）**：dry-run 連 dev DB count 0／executed 刪 0 列／State running 非 crash-loop／sh escaping 無誤／healthcheck disable 後無 unhealthy／收掉乾淨；`compose config` OK。outer 單檔（cleanup-job binary 014 已落地、無 submodule 變動）。仍 open（low）：最小權限 secret `cleanup_database_url`（現走全權 DATABASE_URL）。 |
| `ea30721` | 2026-06-24 | **審計頁 render bug 修（CDP 接地、非 §3 backlog；base-web `2ad39a2b`）** — 審計中心三子表用 flex-height NDataTable，但被 NTabs 夾在 naive-ui NCard 的 `display:block` content 內、flex 高度鏈斷→table body 塌成 0px→有資料時列在 DOM 但被 overflow 裁掉、視覺空白（012 起 latent、空表顯 empty-state 看不出、有資料才暴露）。systematic-debugging 全程：CDP 多層證據〔wire records 在 envelope／DOM tr 有 cell textContent／量 `.n-data-table-base-table-body` 高=0〕→祖先鏈定位斷點→注入 flex-chain 驗 body_h 0→177→碼修。修法全收 index.vue（NCard `content-style` flex-col＋NTabs flex:1 flex-col＋`:deep` pane-wrapper/pane/子表 root/table card fill），三子表元件不動。驗：CDP 三 tab body_h **0→177/119/177**、列現形（截圖）＋typecheck 綠。memory 存 reusable 教訓。 |
| `7ac45c7` | 2026-06-24 | **§3.C F4 審計 CSV 截斷信號嚴格化（最後一個 codeable §3 項；rust `483eeba`/base-web `134ddcc2`）** — C-3 export 截斷 toast 原依前端 stale `pagination.itemCount`（最近同篩選 list total、改篩未重查即漂移）→改後端權威旗標。**rust**：export data 由裸 CSV 字串改 `{csv,truncated}`（`ExportCsv` camelCase）、`truncated=is_export_truncated(total)=total>CSV_EXPORT_CAP`〔facade list 的 total 為完整計數、不受 size=CAP 限〕、3 export 分支同步；2 單元測〔boundary ==CAP→false 剛好全取／>CAP→true；序列化鎖 wire 形〕。**base-web**：新 `Api.SystemManage.AuditCsvExport`＋3 wrapper `request<string>`→`request<AuditCsvExport>`＋3 onExport 讀 `data.truncated`＋清 orphan〔3 vue 的 CSV_EXPORT_CAP import/pagination destructure、download.ts 的 cap 常數〕。驗：cargo test 168 passed／typecheck 綠／CDP 三 tab export XHR 回 `{csv,truncated:false}`〔BOM+表頭、無 console 例外〕。truncated=true 路徑由單元測覆蓋（dev 無 >1萬列）。 |
| `f1e762a` | 2026-06-24 | graphify 增量同步到 008-017（補回缺席 rust 後端、4176→5127 node／5917 edge／661 community；全 rust 64 AST＋base-web semantic、外科式 dedup=False 併入） |
| `d54e693` | 2026-06-24 | GRAPHIFY-NOTES.md 落地（§1 圖統計／§2 八抽取盲點／§3 守則／§4 維護紀律）＋拔 CLAUDE.md GRAPHIFY-NOTES ⏳ |

---

## §2 Roadmap & Phase 狀態 — 完成＋歸檔

>（收 [CHECKLIST §2](INTEGRATION-CHECKLIST.md) 完成波的收縮節〔✅ 標題＋blockquote 摘要〕；波完成批次搬入後 CHECKLIST 該波只留標題＋指標行、永遠聚焦當前波。as-built 詳帳在 DECISIONS §2〔權威〕，本節為收縮節鏡像歸檔。）

### 波 -1 — repo 建構 ✅ 全完成+已歸檔 (2026-06-12)

> 機械建構＋constitution 重鑄兩段全交（pre-spec-kit、全落 default branch、無 feature branch）:outer repo＋worktree/submodule 註冊 `2ec9cda`（⚠️j/⚠️q）/ 設計書入檔＋拍板回填＋歸位改名 `7fd1ac6`→`4aa7c89` / C 方案文件體系 DECISIONS+CHECKLIST+MILESTONES `4724549`・`4300b54` / graphify 首建 `8f66fe0` / 000 base-web bootstrap＋13 端點對映 `46591c4`~`e898421` / SessionStart hook 原樣承接 `ed2a789` / **constitution-rev3 v1.0.0 凍結 `167db96`**（13 項拍板融入）。出口四項全綠（session 健檢/獨立 commit/grep rev2 歸零/speckit 可用）。as-built 詳帳見 [DECISIONS §2](INTEGRATION-DECISIONS.md);commit 史見 [MILESTONES §1](INTEGRATION-MILESTONES.md)。

### 波 0 — 地基 ✅ 全完成+已歸檔 (2026-06-18)

> 七刀全收（001 infra-deploy `c9ffad5`／002 schema-baseline `9233ae0`／003 envelope `13a01b1`／004 soft-delete-infra `a1105f0`／005 audit-op-log `98f1f7e`／006 auth-island-min `e279f23`／007 audit-overlay `96280d8`;sub-crate 刀 ⚠️v 消解併入 002/007）。前置拍板 4 項（①flat-in-main／④僅 join FK／⚠️d redis pin／⚠️k mNNN）全拍（2026-06-13）。出口四項達標：dev stack healthy✅・三守恆〔entity_access_lint✅ 004・migration up→down→up✅ 002・endpoint_coverage_lint ⚠️x 豁免移波1〕・envelope 13 碼✅ 003・login→getUserInfo→enforce✅ 006 → 換波 1。as-built 詳帳見 [DECISIONS §2](INTEGRATION-DECISIONS.md);commit 史見 [MILESTONES §1](INTEGRATION-MILESTONES.md)。

### 波 1 — 第一刀＝`system_settings` 打樋 ✅ 全完成+已歸檔 (2026-06-18)

> 1 刀打樋（merge `b52dafe`）＝最輕 KV entity 跑完 §8.1 全管線、達 3 全專案首次（首個 policy-governed 端點＋require_policy 5003 live／首個 op-log threading live consumer INET round-trip／立 endpoint_coverage_lint ⚠️x）;零 migration、無新 crate;C-V-0~11 全綠、holistic ready-to-merge 0 blocking。as-built 詳帳見 [DECISIONS §2](INTEGRATION-DECISIONS.md);commit 史見 [MILESTONES §1](INTEGRATION-MILESTONES.md);D1 波2 選單可見性 follow-up（010 已兌現）。

---

## §3 Follow-up Backlog — 完成＋歸檔

>（收 [CHECKLIST §3](INTEGRATION-CHECKLIST.md) 已完成 `### 3.X` follow-up 節，累積數節後批次搬入、原處留收合指標。目前空。）

### §3 Follow-up Backlog thematic 重組歸檔（2026-06-22）

> CHECKLIST §3 於 2026-06-22 由 per-feature（§3.1~§3.20）**thematic 重組**消膨脹（281 行→約 55 行；20 散節→8 主題群 §3.A~§3.H）。各刀歷史 `[x]` follow-up（42 項）與本輪結案項**不再逐項保留於 CHECKLIST**、完整明細見 **git history**（pre-debloat commit `16b53a3` 的 CHECKLIST §3）。
> **本輪驗證結案 8 項**：123 pin-時點紀律〔CLAUDE.md §4.1 S9 已收〕／216 policy-archive view〔015 已建、console error 消解〕／233 CDP id=40 op-log〔dev-DB 全重建 oplog_casbin=0〕／287 rust_api2_target 卷〔dev 重建已刪〕／344 C-V-8 migration down→up〔fresh dev DB 實跑 m006 rollback→re-apply 綠、chain 可逆、seed 完整 142/6/15〕／104 pg healthcheck〔已含 `-d soybean_admin_rust`〕／113 outer .gitignore〔無 stale 前代 feature# 註解〕／271 dev-fallback〔行為註記、非 todo〕。
> 開放 follow-up 續見 [CHECKLIST §3.A~§3.H](INTEGRATION-CHECKLIST.md)（公網部署硬化／typings-sweep／審計 enhancement／alt-login／test-lint 健壯化／policy-seed 校正／interceptor i18n／keep-deferred 雜項）。
> **舊→新 §錨對照**（重組前 per-feature §3.X 引用沿此解析）：§3.1/3.3/3.5→§3.H｜§3.2(graphify)→§2 持續性維護｜§3.4→§3.A｜§3.6→§3.G/§3.H｜§3.7~3.9·3.11(004/005/006/007 全 done)→本歸檔｜§3.10→§3.E/§3.H｜§3.12/3.13(User/Menu)→§3.B｜§3.14→§3.B/§3.F｜§3.15→§3.C/§3.A｜§3.16→§3.A/§3.E｜§3.17→§3.A/§3.H｜§3.18→§3.D｜§3.19→§3.C/§3.H｜§3.20→§3.B/§3.H。

### §3.B／§3.F／§3.G 批次歸檔（2026-06-23、pre-波4）

> CHECKLIST §3.B（typings 收斂 sweep）／§3.F（policy-seed 校正翻案）／§3.G（request interceptor i18n）三節全完成，依 [CLAUDE.md §7.5](../CLAUDE.md) 批次搬入、CHECKLIST 原處留收合指標。

- **§3.B typings 收斂 sweep（跨刀 009/010/011/016）✅ 全完成 (2026-06-23)**：MenuList type-lie〔010、`fetchGetMenuListV2` 取代 frozen `fetchGetMenuList`、rev3 wrapper latent dead 註記、不可動 frozen 確認消謊〕／drawer Model null-flow〔016 U5 法 B、role/user-operate-drawer 編輯路徑 `Object.assign` 後 coalesce honest nullable 欄 null→''〔roleDesc／nickName/userPhone/userEmail〕、消 type-lie、submit 不送 null、CDP 開 drawer 無 error〕／excel demo〔016、`views/plugin/excel` 改 `fetchGetUserListRev3`、getTableValue 簽名 User→UserListItemRev3、typecheck 過〕／016 未引用型〔`RoleButtonUpdate`/`RoleEndpointsUpdate` 註記「未引用、保留供具名化」、BASE-WEB-ADAPT 禁刪〕。殘餘 User/Role wire type-lie 016 U5 已部分消解、若再現留未來 typings-sweep 刀。
- **§3.F policy-seed 對齊校正 ✅ 翻案結案 (2026-06-23、維持現狀)**：深查翻案（pre-波4 triage）——updateUser POST 在 `ALL_ENDPOINT_POLICIES` registry、`manage_role` 為真 sys_menu→R_SUPER 經 016 endpoint/menu-auth-modal **runtime 即可授 R_ADMIN、零 migration、非缺陷**（原「必動 m002 需拍板」前提不成立）。**user 親決維持現狀**（不動 m002 seed；R_ADMIN 不對齊由 operator runtime 自授）。日後要 seed 開箱預設＝trivial 加 m002 兩列〔updateUser POST + manage_role menu，動 baseline 須再拍板〕。
- **§3.G request interceptor i18n（跨刀 003/009/010）✅ 全完成 (2026-06-23、pre-波4 sweep)**：`4040`/`5003`（HTTP 404/403）native axios error 在地化——`onError` 補 else-if（`code≠BACKEND_ERROR_CODE` 但 `error.response.data.msg` 存在→`translateBackendMsg`）；⚠️aa BASE-WEB-I18N-WIRING §III.2(i) 授權涵蓋；003 locale 已備（自述「需新增 key」不成立）；CDP 實證真 HTTP 403→toast「权限不足」（非 raw key/英文、browser 軌驗）；commit `7a011c00`（base-web）。

