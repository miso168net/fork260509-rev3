# REVIEW-20260702 — rev3 歷次 review 統整（單一留存版）

> **本檔整併四份歷史 review 報告**（REVIEW-001-019／REVIEW-018-observability／REVIEW-correctness-security-rev3／REVIEW-001-025，四檔已刪除、**全文存 git 史**：詳版證據需要時 `git show 9776edff:docs/REVIEW-001-025.md` 等）。
> **用途**：①歷次 review 結論的永久紀錄 ②**仍 open findings 的統一修復工作清單（§3）** ③supersession 總映射（§5）④已閉合防重修（§4）。
> **cross-ref 紀律**：本檔只引用留存文件（DECISIONS／MILESTONES／CHECKLIST／spec／code）與 git SHA，不引用已刪報告檔名路徑。

---

## 1. 歷次 review 總覽（4 輪）

| 輪 | 日期 | 範圍 | 方法 | 結果 | 詳版 git 錨 |
|---|---|---|---|---|---|
| REVIEW-018-observability | 2026-06-25 | 018 收刀彙整 | 4 支 Workflow（per-unit impl→雙審→fix）＋holistic 3 lens＋主線自驗 | PASS 可 merge；3 攔截真缺陷（FR-006 explicit event／grafana `$__file{}`／5xx noDataState）皆修於刀內 | `git show c1a3224^..c1a3224` 附近；MILESTONES §1 `c1a3224` |
| REVIEW-001-019 | 2026-06-25 | 001~019 spec-compliance（cumulative） | Workflow fan-out 19 reviewer＋對抗式 verifier；**純靜態**（不跑 cargo/pnpm/CDP） | **19/19 PASS、0 真缺陷**；~20 low+1 med 全為 needs-runtime 或 likelySupersession；supersession map ~45 條 | MILESTONES §1 `0bddc499` |
| REVIEW-correctness-security-rev3 | 2026-06-25 | spec 之外的 correctness bug＋security 弱點 | 兩輪 fan-out（correctness 13 區／security 9 區）＋1-vote 對抗式查證；靜態 | correctness 13 CONFIRMED／security 4 CONFIRMED；**H-1~H-4＋M-1~M-9 已全數修復閉合（§4）**；🟡 low 登記（§6） | MILESTONES §1 `e09aa4dc`+`55dec914`／`0c14abdb`+`d5dd55c7`+`8e636891` |
| REVIEW-001-025 | 2026-07-02 | 001~025 spec-compliance（cumulative **＋ live/CDP 實測**） | 3 批 serial Workflow ×25 reviewer（001→025 依序）；live 讀端（curl/psql/redis）＋CDP 真瀏覽器；非破壞紀律 | **25/25 PASS 級**（18 PASS＋7 PASS_WITH_NOTES）、**零 Critical/Important**、3 Minor＋~40 Observation；無無依據漂移 | `git show 9776edff:docs/REVIEW-001-025.md`（810 行全文） |

---

## 2. 當前有效結論 — 25 feature verdict 表（2026-07-02、pins rust-api `ef1d21a`／base-web `f3ad8e23`）

| # | Feature | Verdict | 備註（摘） |
|---|---|---|---|
| 001 | infra-deploy | ✅ PASS | live 4 入口全通、migrate gate、rev2 字樣零命中 |
| 002 | rev2-schema-baseline | ✅ PASS | 13 表/m001~m009/seed 逐項 psql 證；零漂移重放 needs-runtime（腳本資產在） |
| 003 | envelope | ✅ PASS | 信封形/13 碼矩陣凍結、025 淨新 biz key 走 2222 未擴矩陣 |
| 004 | soft-delete-infra | ✅ PASS | find_active_*/成對欄/idempotent no-op（M-2 強化後） |
| 005 | audit-op-log | ✅ PASS | mutate_in_txn 24 caller 管道健在、redact 實證 |
| 006 | auth-island-min | ✅ PASS | login/getUserInfo/enforce；single-session→014 policy-aware（supersession） |
| 007 | audit-overlay | ✅ PASS | access_log/login_attempt；exactly-one→021 as-built 反轉（spec 有勘誤註記） |
| 008 | system-settings | ✅ PASS | KV+super-only；版面→024 重構、hot-apply→014（皆有據） |
| 009 | user-management | ✅📝 PASS_WITH_NOTES | 2 Minor（重置鈕/用户名欄、皆 upstream 原樣）＋1 Obs（初始密碼 123456） |
| 010 | menu-management | ✅ PASS | 樹/回收桶/guard 零漂移；M010-01 修復比基線更貼 spec |
| 011 | role-management | ✅ PASS | CRUD+DB-first casbin+守門；020/016 擴充有據 |
| 012 | audit-log-query | ✅📝 PASS_WITH_NOTES | 1 Minor（見 §3）；三表查詢+空字串守門 CDP 實證 |
| 013 | xff-real-ip-forensics | ✅ PASS | 三 Tier 信任鏈+七態 confidence；022 tunnel fallback 擴充有據 |
| 014 | auth-token-session | ✅ PASS | rotation TOCTOU 閉合/policy-aware/denylist；psql 多 session 合法態證 |
| 015 | policy-governance | ✅ PASS | archive-move 原子+watcher（H-3 修復後 preserving reload） |
| 016 | button-endpoint-policy | ✅ PASS | 三維 DB-first、protected-reject、M-6 no-escalation 強化 |
| 017 | audit-center-enhancement | ✅ PASS | class filter/CSV（H-1 防注入後）/roles delta；CSV 表頭 camelCase＝Obs |
| 018 | observability | ✅ PASS | profile-gated 全套、trace_id join、alert idle inactive live 證 |
| 019 | login-lockout | ✅ PASS | 滑動窗 gate；021 L1 反轉/022 白名單 skip 皆有勘誤註記 |
| 020 | role-delete-policy-archive | ✅ PASS | archive-move 同 txn/restorable 衍生/鎖序；CDP reason 標籤在地化 |
| 021 | login-lockout-redis-cache | ✅📝 PASS_WITH_NOTES | 3 Obs（TTL 借用〔已登 CHECKLIST §4.2〕/②c 非原子/token 過期取證限制） |
| 022 | ip-access-control | ✅ PASS | ipgate 白>黑>default/自鎖防護/CDP 頁渲染；e2e defer 項已登 §3.E |
| 023 | list-column-sort | ✅ PASS | 7 頁排序 3-state CDP 實操、非法 2222、持久化還原 |
| 024 | password-policy | ✅📝 PASS_WITH_NOTES | 2 Obs（find_all 無 ORDER BY 位置漂移/"+8" 正規化、見 §3） |
| 025 | user-center | ✅📝 PASS_WITH_NOTES | 1 Minor（doc 註解殘留）＋3 Obs（見 §3） |

（006/012/021 等 PASS_WITH_NOTES 與 PASS 的差異僅在有無可列 findings，無任何 FR 違反。）

---

## 3. Open findings 統一修復清單 ★（2026-07-02 統一修復輪已執行、F-1~F-11 全數處置）

> 分流四類。修復時逐項在本節標 `✅ 已修（commit）`／`➡️ 已登記（CHECKLIST §x）`／`🚫 不修（理由）`。
> ✅ **2026-07-02 修復輪 as-built**：直接修 5（F-1/F-2/F-3/F-4/F-6）＋不修 1（F-5、user 拍板）＋登記 4（F-7~F-10）＋spec 勘誤 1（F-11）；rust `77b8d18`（255 測零回歸＋live 序驗）／base-web `0626ead6`（typecheck＋CDP 兩頁 PASS）；F-4/F-5/F-6 拍板紀錄見各列處置欄。

### 3.1 建議直接修（小、明確、無需拍板）

| # | 來源 | 檔 | 問題 | 建議修法 | 處置 |
|---|---|---|---|---|---|
| F-1 | 025 Minor | `base-web/src/service/api/rev3-user-center.ts`＋`src/typings/api/rev3-user-center.d.ts` | 檔頭 doc 殘留 as-built 前語意：.ts 檔頭「3 auth-only 端點」（實 4）＋`fetchUpdateProfile` 註解「各卡保存共用、送全 model」（實為部分更新）；.d.ts 檔頭 L2「3 端點 wire」（L4 已修、L2 漏） | 2-3 行註解對齊 as-built（4 端點/部分更新） | ✅ 已修（base-web `0626ead6`） |
| F-2 | 024 Obs | `rust-api/server/src/model/facade/system_settings.rs` `find_all` | 無 ORDER BY（heap order）→ 任一設定列被更新後 UI 位置漂移（實測「密碼最小長度」漂到密碼策略區塊最末） | 加 `.order_by_asc(Column::SettingKey)`（消費端 024 頁/025 getPasswordPolicy/changePassword 僅序敏感、無行為風險）＋跑 `cargo test -p server --bin server` 零回歸 | ✅ 已修（rust `77b8d18`；255 測＋2 lint 零回歸、live curl getPasswordPolicy 序升冪實證） |
| F-3 | 002+025 Obs（兩輪點名） | `CLAUDE.md` §8.1 | sys_user 欄位描述僅列 5 欄、實為 16 欄（gender/phone/email/status/session_policy/created_*/updated_* 等 m001 即有）——曾誤導規劃、靠 memory 補救 | §8.1 表格下註記改為 16 欄實況（或列全 16 欄） | ✅ 已修（CLAUDE.md §8.1 列全 16 欄、psql `\d` 對過） |

### 3.2 需 user 拍板（行為變更；問法附具體渲染範例——見 memory `ui-behavior-options-need-concrete-examples` 教訓）

| # | 來源 | 檔 | 問題 | 拍板點 | 處置 |
|---|---|---|---|---|---|
| F-4 | 009 Minor | `base-web/src/views/manage/user/modules/user-search.vue:40-43`（role 頁 `role-search.vue:53` 同模式） | 搜索「重置」鈕只清表單不重新查詢——清完列表仍停留上次過濾結果、須再按搜索 | ★upstream soybean 原樣（git 屬 upstream commit）、非 rev3 引入。修＝reset() 補 emit('search')；動 upstream 檔走 fork-delta ⚠️s rev3-inline 標記；user/role 兩頁需一致。修 or 維持 upstream 原樣？ | ✅ 已修（user 拍板 2026-07-02 修＝對齊全站慣例〔全站 7 頁帶重置鈕、其餘 5 rev3 頁皆「清空＋重查」、僅此 2 upstream 頁例外〕；base-web `0626ead6`、CDP 兩頁 PASS：過濾 3→1、按重置不按搜索即回 3＋輸入清空） |
| F-5 | 009 Minor | `base-web/src/views/manage/user/modules/user-operate-drawer.vue:149-151` | edit 模式「用户名」欄可編輯、但後端 `build_update_active_model` 刻意不寫 user_name → 改了按確定顯示「更新成功」而該欄未變（欄位級靜默 no-op；安全無虞、UX 縫隙） | 修＝edit 模式 `:disabled`；同屬 upstream 檔、同 F-4 紀律。修 or 接受？ | 🚫 不修（user 拍板 2026-07-02 維持 upstream 原樣＝零 fork-delta；「改了顯示成功但未變」縫隙接受、安全無虞） |
| F-6 | 025 Obs | `rust-api/server/src/handler/user_center.rs` | self 列 concurrent 消失時行為不一致：get_profile 回 Internal(5000)、update_profile/change_password 經 facade no-op（Ok(None)）後仍回 0000「成功」——實際沒寫卻報成功 | 極端邊角（active self 被並發硬刪才可達）。修＝handler 對 facade 回 None 改回 biz.user.notFound；與 CHECKLIST §3.H「空 body 時戳 bump」同族可同刀。修 or 登記？ | ✅ 已修（user 拍板 2026-07-02 修；rust `77b8d18`、update_profile/change_password 2 處 `.ok_or(biz.user.notFound)`、i18n 鍵既有；get_profile 維持 Internal 比照 auth::get_user_info） |

### 3.3 建議分流登記（登 CHECKLIST、不修碼）

| # | 來源 | 內容 | 登記去向 | 處置 |
|---|---|---|---|---|
| F-7 | 009 Obs | addUser 初始密碼寫死 `"123456"`、不經 024 password_policy 驗證（`system_manage.rs:1180`）——admin 設嚴政策後新帳號初始密碼即不合政策。符合 009 假設＋024/025 scope、非缺陷 | §4.2 future（「初始密碼政策化／強迫首登改密」候選 feature） | ➡️ 已登記（CHECKLIST §4.2） |
| F-8 | 024 Obs | `validate_value_type` number 分支接受非正規形（`"+8"`/`"0008"`、u32::parse 寬鬆）且原字串照存——僅 curl 直送可觸發、UI 恆正規形 | §3.H misc（low、觸發時 normalize 後存） | ➡️ 已登記（CHECKLIST §3.H misc） |
| F-9 | 021 Obs | ②c 節流麵包屑非原子（get→GETDEL→set_ex 三步、`auth.rs:328-344`）：併發 L1 命中可能同窗 >1 筆 warn／計數拆兩筆——觀測層 best-effort、無安全影響 | §3.H misc（low） | ➡️ 已登記（CHECKLIST §3.H misc） |
| F-10 | 018 Obs | `casbin_enforce_total{decision="deny"}` 未 pre-register 0（重啟後至首 deny 前序列不存在、dashboard 空態非 0）；pushgateway 無持久卷（重啟失憶至下次 cleanup） | §3.I（obs 慣例級、與既有 018 backlog 併記） | ➡️ 已登記（CHECKLIST §3.I） |
| F-11 | 017 Obs | op-log CSV 專屬欄表頭 `rolesBefore/rolesAfter`（camelCase）與 spec 字面 `roles_before/roles_after` 不同名——與其餘 14 欄 camelCase 一致、data-model 已載、FR 本質滿足 | 不登記亦可；如要根治＝spec 勘誤一行 | ✅ 已修（017 spec FR-005a 補 as-built 勘誤一行） |

### 3.4 明確不修（by-design／spec 拍板／已有機制）

- **025** updateProfile 部分更新語意下無法把已設欄清回 NULL（前端空字串存 `''`、null 整欄跳過）——spec「容忍空值」、與 admin 路徑（009）行為一致；如未來要「清空」語意需 wire 約定擴充（feature 級、非 bug）。
- **025** `password_forbid_username` 無前端即時提示（7 政策僅 6 有 hint、後端權威攔）——已登 CHECKLIST §4.2（025 future enhancements）。
- **021** ip 維 lockout/suppressed TTL 借 `PER_USER_WINDOW_SECS`——已登 CHECKLIST §4.2（021 遞延、待 runtime 可調門檻一併改）。
- **017** 匯出鈕位於預設收合的搜索面板內（可發現性 UX）——三表一致模式、SC 展開後成立；動它＝版面拍板級。
- **001** `front_nginx_certs` 卷 dev 不 materialize／prod 起停未重演——compose 正常行為＋非破壞紀律，收刀 C-V 背書。
- REVIEW-001-019 輪全部 findings（needs-runtime ×多＋likelySupersession）——非缺陷分類、其 runtime 面已被 2026-07-02 輪的 live/CDP 大面積補證。

---

## 4. 已閉合 findings（防重修；全部完成於 2026-06-25、詳 MILESTONES §1 對應行）

**H-tier（correctness+security 高優先、4/4 閉合）**：
- **H-1** 審計 CSV formula injection：`csv_escape_field` 首字 ∈ `{= + - @ \t \r}` 前綴 `'`＋RFC4180（單 helper 覆三表；未認證可注入面〔attempted_user_name/XFF/trace_id〕已斷）。rust `6017732`。
- **H-2** self-lock 守門 `"02"` 繞過：改持久化同款 `normalize_enum_filter==Ok(Some(2))`。rust `6017732`。
- **H-3** policy watcher「fail-OPEN」實為 deny-all：`reload_enforcer_preserving` 先 build_fresh 成功才 swap、失敗保留舊策略。rust `cda07e9`。
- **H-4** 審計日期 end-of-day off-by-one：rust `parse_audit_date` End→23:59:59.999…＋base-web 三表 `setHours(23,59,59,999)`。base-web `f334feec`。

**M-tier（9/9 閉合）**：M-1 endpoint method 大寫 normalize+白名單／M-2 soft_delete 已刪 idempotent no-op／M-3 status Some-only 不寫 NULL／M-4 redis pubsub 5s timeout／M-5 prometheus EndpointLabel fallback `<other>`／**M-6 三維 RBAC grant no-escalation 守門**（user 拍板；非 R_SUPER desired⊄effective→2222 grantExceedsOwn）／M-7 CF 偽造＝prod internal-only 文件化／M-8 nginx prod 安全 header（Referrer/Permissions/結構性 CSP）／M-9 nginx `limit_req`（auth 5r/s burst40）。rust `3bfab71`+`46cce39`+`4eb710c`、base-web `28029fa5`（2 biz key i18n）、deploy `8e636891`。

**018 刀內攔截（3/3 修於收刀前）**：FR-006 explicit completion event（span-only 對 /health 無 log→補 event、join key=`fields_trace_id`）／grafana `$__file{}` provider（`__FILE` 後綴無效）／5xx alert `noDataState=OK`+`or vector(0)`（修 idle false-firing）。

**其他歷次已修**（各刀收刀時）：019 vite stale-locale raw-key toast（restart 紀律化）／012 flex-height table 塌 0／014 lock-then-redecide TOCTOU／020 P-011-1 角色重建繼承（HIGH）／025 confirm-rule toRef race＋credential 明文殘顯。

---

## 5. Supersession 總映射（spec 字面 vs as-built 差異的合法依據；兩輪 review 合併去重）

> 價值＝證明「所有偏離皆有拍板依據的前向演進、無無依據漂移」。依據欄皆為留存文件。

| 早期 spec 行為 | 被誰演進 | 依據 |
|---|---|---|
| 001 FR-008 schema 空跑 | 002~024 落 m001~m009 真 migration | DECISIONS ⚠️t＋MILESTONES 各刀 |
| 001 5-service 基底 | +cleanup-job/rust-api-2/obs 8 service（全 profile-gated） | MILESTONES 014/018 |
| 001/013 nginx 入口 | +limit_req（M-9）＋CF header 對＋prod 安全 header（M-8） | CHECKLIST §3.J＋013 spec FR-010＋⚠️ae |
| 002 12 表口徑 | 13 表（+sys_ip_rule m007） | ⚠️ae／MILESTONES `d9c809e6` |
| 002 system_settings 1 列 | 8 列（+7 密碼政策 m009） | ⚠️ag／MILESTONES `aca02c81` |
| 003「From\<DbErr\> 不在本刀」 | 006 帶入（首個產 DbErr 切片） | 003 spec R7 明示 |
| 004/005 log entity 單 IP 欄 | 013 四欄 forensic（m006 可逆 ALTER） | ⚠️ac amendment |
| 005 payload 無 roles | 017 with_roles 排序快照 | 017 C-4 拍板 |
| 006 single-session「永遠踢」 | 014 policy-aware `is_current`（effective Off 放行多 session） | DECISIONS §2 014「修 006 永遠踢 bug」 |
| 006 無 refreshToken/lockout/停用 gate | 014 rotation／019 lockout／009 status==2→1000 | 各刀 spec+MILESTONES |
| 007 FR-004/SC-002 exactly-one per terminal result | 021 L1 短路命中不寫 login_attempt（007/019 spec 有 as-built 勘誤註記） | ⚠️ad／MILESTONES `5e4f3228` |
| 008 hot-apply NOT in scope | 014 redis `settings:invalidate` watcher | MILESTONES 014 |
| 008 原版面/明文 passthrough | 024 分區塊+NInputNumber+number 型驗證 | ⚠️ag |
| 009 無即時 session 逐出 | 014 `revoke_user_sessions`（停用/刪除硬撤） | 009 FR-009 明示非本刀＋014 U4 |
| 009 讀端 frozen 型謊 | 016 D5 `fetchGetUserListRev3` honest wrapper | DECISIONS §2 016 |
| 010 route store 不改（R-cr） | 010 自身 clarify option1 授權合併 builtin 常數路由 | 010 spec Clarifications |
| 012 單值 filter | 017 http_status class filter＋CSV 匯出 | 017 C-1/C-3 |
| 015 二維治理（menu/button） | 016 endpoint 維度（(path,method) 雙鍵） | 016 scope C |
| 015 archive 無 reason 治理 | 020 role_soft_delete reason＋restorable 讀時衍生＋notRestorable 守門 | P-011-1／MILESTONES `ebbd6c2c` |
| 016 AS_BUILT_ROUTES=43 | →44（018 /metrics）→50（022 +6）→**54**（025 +4） | 各刀 MILESTONES |
| 017 C-3 裸 CSV 字串 | F4 後端 truncated 旗標（CSV-in-envelope） | MILESTONES `c7f5936` |
| 019 brainstorm「鎖定 N 分鐘」 | 拍板 D3 靜態一般化訊息（FR-012） | ⚠️w |
| 019 全請求皆計 | 022 白名單 IP 跳 lockout（L0 seam 兌現） | ⚠️ae |
| 023 7 頁列表原生無排序 | server-side 多欄排序＋各 list 端點 +sort query | ⚠️af／MILESTONES `009901fa` |
| 024 `password_policy` dormant | 025 喚醒消費（改密 enforce） | ⚠️ah／MILESTONES `ded99ee9` |
| 025 設計期 3 端點/送全 model/adminUpdatedAt | as-built 4 端點（+getPasswordPolicy 避 super-only 403）/部分更新/updatedAt+updatedBy 三態（user 收尾拍板） | ⚠️ah＋025 spec/contract as-built 勘誤節 |

---

## 6. 🟡 low／accepted 登記（正式追蹤在 CHECKLIST §3.J；此處為內化摘要）

- **[accepted、spec 拍板]**：lockout fail-OPEN（D-07）／per-user lockout DoS（≤15 分自解）／IPv6 /64 規避（§4.2）／寬 internal_default 信任（013 明示排除）。
- **[open low、CHECKLIST §3.J]**：login timing oracle（not-found 不跑 argon2→枚舉 side-channel；緩解＝dummy argon2 拉平）／op-log payload PII（phone/email 未 redact、視合規）／trace_id 未濾控制字元（log injection 上游、CSV sink 已由 H-1 斷）／完整資源 CSP（script/style/connect-src、需 prod CDP 逐源驗）／CONFIRMED-low ×4（Tier-1 CDN 錨不驗 untrusted／XFF take(32) 空 token／lockout count 非原子 race／m004 down 非對稱 casbin DELETE）。
- **PLAUSIBLE 未確認**（hardening/edge 級、原兩輪對抗式查證未達 CONFIRMED）：reparent_check TOCTOU／roles_of_user 未濾軟刪角色（authz 用）／wire_id 2^53 邊界／denylist TTL(1h)<refresh(7d)／refreshToken 無節流（M-9 部分緩解）等——完整 reasoning 見 git 史 `55dec914` 前後的 workflow 輸出紀錄。

---

## 7. 方法論備忘（給後續 review/修復輪）

- **證據等級**：2026-06-25 三輪＝純靜態（並發護欄、needs-runtime 標註誠實）；2026-07-02 輪＝live 讀端＋CDP 真瀏覽器補證了大部分 runtime 面；**寫端 FR**（add/delete/changePassword/restore…）兩輪皆以「實碼＋各刀收刀 C-V 全綠」背書、review 輪不重演（非破壞紀律：禁寫端/禁 lockout 觸發/禁建 ip 規則）。
- **仍屬 needs-runtime 的項**：002 零漂移重放（依賴前代 image、腳本資產 git-tracked 於 tests/002）；016 SC-005 跨副本收斂（需第 2 副本）；prod 起停/acme（需真 prod 環境、§4.2）。
- **CDP 實測的獨特價值**（靜態抓不到、2026-07-02 輪實證）：axios 空字串 query 真實序列化形、重置鈕行為、匯出鈕收合可發現性、設定列位置漂移、i18n raw-key 零洩漏、3-state 排序箭頭與持久化還原。
- **review 編排紀律**：cumulative 審查必須 serial（後刀合法推翻前刀）；reviewer 共用 token 防 login 噪音（`single_session_default=off` 前提）；token 過期時從瀏覽器 localStorage 唯讀恢復；CDP tab 登出態＝token 注入 SOP（`SOY_token`/`SOY_refreshToken`、JSON.stringify 值）。
