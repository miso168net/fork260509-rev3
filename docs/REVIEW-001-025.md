# REVIEW-001-025 — 全 feature spec-compliance review（cumulative as-built + CDP 實測）

> **執行**：2026-07-02 ｜ **方法**：`superpowers:requesting-code-review` 框架 + Workflow serial 編排（001→025 依序、後刀可合法推翻前刀故不平行）
> **oracle**：各 `specs/<NNN>-*/spec.md`（FR/SC/US/Edge）｜ **對象**：cumulative as-built @ pins rust-api `ef1d21a`／base-web `f3ad8e23`（outer `c07c5e78`）
> **與 [REVIEW-001-019](REVIEW-001-019.md)（2026-06-25、19/19 PASS、純靜態）的關係**：本輪為其後繼——新增 020-025 六刀、並對涉 base-web 的 feature 加 **CDP 實測**（127.0.0.1:9229、31080 tab）；Δ 重點＝後六刀是否踩壞舊 feature + 舊 supersession map 沿用/擴充。
> **紀律**：reviewer 只讀 repo（不寫檔/不 git 寫操作）；live 驗證**非破壞**（讀端 API/UI 渲染/list 查詢；禁建/刪/改持久資料、禁改密、login 失敗嘗試 ≤2 次防 lockout、禁建 ip 規則；寫端行為以 code 靜態 + 收刀 C-V 證據）；共用 Super token（不重複 login）；**過程零提問**——所有疑點記入本檔。
> **supersession 紀律**：早期 spec 行為被後續 feature 推翻＝查 [DECISIONS §1](INTEGRATION-DECISIONS.md)/[MILESTONES §1](INTEGRATION-MILESTONES.md) 有無拍板/as-built 依據→有依據標 SUPERSEDED（非缺陷）；無依據漂移才是 finding。
> **verdict 詞彙**：PASS／PASS_WITH_NOTES（有 Minor/observation）／ISSUES（有 Critical/Important）／INCONCLUSIVE（agent 失敗或證據不足）。

---

## 執行狀態

- [x] Batch 1：001–008 ✅ 8/8 PASS（僅 Observation）
- [x] Batch 2：009–016 ✅ 8/8 PASS（009/012 PASS_WITH_NOTES：3 Minor 皆 upstream 原樣或 UX 縫隙、零 Critical/Important）
- [x] Batch 3：017–025 ✅ 9/9 PASS 級（021/024/025 PASS_WITH_NOTES）
- [x] 總結表＋findings 彙總 ✅

---

<!-- BATCH 1 RESULTS -->

## 001-infra-deploy — ✅ PASS

**FR coverage**：10/10 FR 覆蓋（FR-001~004/007 活體實證、FR-005/006/009/010 靜態實碼＋grep、FR-008 活體 gate＋合法 supersession）；無缺項。SC 7/7：5 項活體、SC-001/005 引 001 收刀 C-V 全綠（非破壞紀律不重演 prod 起停）。

**Strengths**：
- migrate gate 鏈條設計乾淨且活體可證：docker-compose.yml:51-57 rust-api depends_on migrate service_completed_successfully＋postgres/redis healthy，live ps 顯示 migrate Exited(0)、rust-api healthy（FR-002/SC-003 閘門順序實證）
- nginx 入口遮蔽用 exact-match 擋塊（deploy/nginx/conf.d/_locations.inc:12-14 location = /api/metrics return 404 優先於 prefix /api/），註解明載內網 prometheus 直連不受影響——live 實測 /api/metrics=404、/api/health=ok 同時成立
- secret 紀律超出 spec 最低要求：deploy/preflight-secrets.sh 接住 docker compose 對缺 bind source 自動建空目錄的 footgun（錯誤訊息誤導問題）、deploy/generate-secrets.sh 有 dual-write drift 自動防護；deploy/secrets/*.txt 全 gitignored、僅 .example 入版控（FR-005）
- FR-009 rev2 指涉歸零在 025 收刀後仍保持：grep -riE rev2 對全部 docker-compose*.yml＋deploy/ 零命中（排除 dev-certs 二進位），20~25 六刀未引入漂移

**Findings**：
- **[Observation]** front_nginx_certs 具名卷在當前 dev host 不存在（docker volume ls 僅 10 卷），prod 起停演練（SC-005/US3）本輪未活體重演——屬非破壞紀律的刻意跳過，依收刀 C-V 全綠聲明背書（`docker-compose.prod.yml`）
  - 證據：docker volume ls --format '{{.Name}}' | grep rev3-admin_ 無 front_nginx_certs（compose 頂層已宣告 docker-compose.yml:281、僅 prod/acme 掛載時才 materialize＝正常 compose 行為）；SC-005 引 specs/001-infra-deploy/contracts/ C-V 與 MILESTONES 001 收刀全綠紀錄
- **[Observation]** FR-010 standalone rust-api compose（docker-compose.rust-api.yml）存在且形狀正確（dev+runtime 雙 target、name: rev3-admin 共用 project），但 CLAUDE.md §2 已標其 DEPRECATED debug 後備——spec 義務仍滿足、僅用途降級（`docker-compose.rust-api.yml`）
  - 證據：grep 顯示 name: rev3-admin / target: dev / target: runtime / rust_api_target 卷對齊正典命名；CLAUDE.md §2 註「docker-compose.rust-api.yml〔DEPRECATED debug 後備〕」

**Supersessions（合法推翻、非缺陷）**：
- FR-008「schema 初始化本階段空跑（零 schema 變更）」→ 002~024 落 m001~m009 真 migration（rust-api/migration/src/ 九支 mNNN_<name> 命名慣例正是 001 ⚠️k 確立形）→ 依據：⚠️t（rev2 終態 squash 基線屬 002 刀、spec Assumptions 明示）＋MILESTONES §1 各刀紀錄；live migrate log「No pending migrations」exit 0 證機制仍健康
- FR-003 入口 /api/ location 後加 limit_req 速率限制（M-9）＋CF 驗證 header 對（X-CF-Verified/CF-Connecting-IP、nginx.conf geo/map）→ 依據：013 spec FR-010＋DECISIONS §1 ⚠️ae（CF Tunnel 拓樸）＋CHECKLIST §3.J 硬化；不改 001 的四類路由行為（live 4/4 仍過）
- 001 的 5-service＋migrate＋acme 殼基底形 → 後加 cleanup-job（014 U5、profiles:[prod]）、rust-api-2（014 U7、profiles:[multi]）、obs/metrics 8 service（018、profiles:[obs]/[metrics]）＋grafana_admin_password 第 7 secret → 依據：MILESTONES §1 014/018 收刀紀錄；全部 profile-gated、一般 up 不啟、FR-006 base 層無 ports/專屬掛載紀律未破
- FR-006 prod 443 server 後加安全 header（HSTS/XFO/nosniff/Referrer-Policy/Permissions-Policy/結構性 CSP）→ 依據：CHECKLIST §3.J M-8 硬化 follow-up（deploy/nginx/conf.d/prod.conf 註解內載）；80→443 redirect＋/health 例外原形不變

**Live 證據**：curl http://127.0.0.1:31080/health→ok；curl -k https://127.0.0.1:31443/health→ok（自簽 TLS）；curl http://127.0.0.1:31081/health→ok（API 直連、純文字非信封＝FR-004）；curl 31080/api/health→ok（前綴剝離）；31080/api/metrics→404（遮蔽）；31080/ →200 622B（前端）。compose ps：front-nginx/base-web/rust-api/postgres/redis-stack 5 service 全 healthy、migrate Exited(0)（log「Applying all pending migrations→No pending migrations」）。容器內 pg_isready→accepting、redis-cli ping→PONG（帶 secret 檔密碼）。psql：sys_user=9、seaql_migrations=9（stack 歷經多次重啟資料保留＝SC-004）。docker volume ls：10 個 rev3-admin_ 前綴卷全符 §8.2.2 正典命名（SC-002 6/6、SC-003/004/006/007 活體過；SC-001/005 引收刀 C-V）。

**CDP 證據**：CDP（pageId AAB4D0C2...）Runtime.evaluate on 31080：url=http://127.0.0.1:31080/home、title=首页、#app 存在、body innerText 520 chars、raw i18n key（page.*/common.*/route.*/backend.*）零命中——證統一入口經 nginx 服務 SPA 完整渲染且 i18n 非殘鍵（US2-AS3 補強）。

---

## 002-rev2-schema-baseline — ✅ PASS

**FR coverage**：10/10 FR 覆蓋（FR-001/002/005 live psql 逐項證；FR-003 腳本資產+checkpoint 設計驗、需 runtime 重放未重跑；FR-004 m003/m004 獨立步驟+FK live 證；FR-006 m004 R_SUPER-only 引基線輪+casbin live；FR-007/008 down() 9/9+ON CONFLICT 全覆蓋+收刀 C-V；FR-009 adapter 委派碼證；FR-010 收刀 prod build+024 migration/src 整目錄 COPY 再證）；SC-001~008 中 SC-001/003 needs-runtime（腳本在、基準 dump git-tracked），餘 live/碼證全中

**Strengths**：
- m001 忠實 squash 品質高：逐欄重現歷史疊加欄序（rust-api/migration/src/m001_rev2_schema.rs:1-15 檔頭明文欄序敏感紀律）、partial WHERE index 刻意 raw SQL 求逐字對齊 dump；live 驗 sys_user 16 欄序與 Iden enum 完全一致、partial uniq ×3（sys_user_user_name/sys_role_code/sys_menu_route_name _active_uniq）齊在
- m002 seed 三紀律全落實：argon2id runtime 生成三列共用（m002:44-46；live DISTINCT password=1、PHC $argon2id$v=19$m=19456）、id 不寫死（INSERT 無 id 欄靠 sequence、sys_user_role 雙向 subquery 解析 m002:76-83）、每條 INSERT 帶 partial-index-aware ON CONFLICT DO NOTHING（FR-008 冪等）
- FR-009 政策表單一來源乾淨：m001:640 `sea_orm_adapter::up()` 委派建 8 欄基底＋同表 ALTER 補 3 治理欄、down 對稱委派 m001:880，無手寫雙來源 DDL
- 零漂移驗證資產 git-tracked 且 checkpoint 正確：tests/002-rev2-schema-baseline/scripts/ 四支（pristine-replay/normalize/diff-baseline/delta-assert），diff-baseline.sh 明確 `up -n 2` 停在 m002 檢查點（delta 前）＝後續 m003~m009 演進不會污染零漂移基準；normalize 含 COPY 段排序第六規則（layer2 物理列序假紅對策）

**Findings**：
- **[Observation]** CLAUDE.md §8.1 對 sys_user 欄位的描述（僅列 id/user_name/password/nick_name/deleted_at 5 欄）與 m001 實際 16 欄長期 stale，易誤導未來規劃（gender/phone/email/status/session_policy 等本就 m001 有）（`CLAUDE.md`）
  - 證據：live psql information_schema：sys_user 16 欄（id,user_name,password,deleted_at,nick_name,user_gender,user_phone,user_email,status,created_at,created_by,updated_by,deleted_by,updated_at,current_session_id,session_policy）；CLAUDE.md §8.1 只列 5 欄。025 規劃期已靠 memory 補救（零 migration 正確判斷），但 repo 文件本身未修
- **[Observation]** FR-003 零漂移驗證屬 needs-runtime：依賴前代 image rev2-admin-rust-api:latest 本機可用（spec Assumptions 已明列），本輪未重跑、僅驗腳本資產完整且 checkpoint 設計對後續 m003~m009 演進免疫（`tests/002-rev2-schema-baseline/scripts/diff-baseline.sh`）
  - 證據：pristine-replay.sh:6 依賴前代 image 跑 35 支 migration；diff-baseline.sh:57-58 `up -n 2` 停 m002 檢查點。git log 顯示三支 commit 後資產零改動（32a1efee 基準 dump git-tracked）。與 REVIEW-001-019.md L59 基線輪同判（needs-runtime）

**Supersessions（合法推翻、非缺陷）**：
- FR-001「12 表口徑」→ 現況 13 表（+sys_ip_rule）：m007（022-ip-access-control、破 0-migration streak）→ MILESTONES §1 d9c809e6／DECISIONS §1 ⚠️ae
- FR-001「基線審計欄四變體」→ 三 append-only 日誌表 rename+加 IP forensic 欄（operator_ip→operator_real_ip、+peer_ip/ip_confidence/operator_x_forwarded_for）：m006（013-xff-real-ip-forensics）→ MILESTONES §1 8980d00＋constitution amendment ⚠️ac（82f49d4、§I.6 釐清刻意/可逆演進不在 retrofit 禁令）
- FR-001「7 個一般索引」→ +4 查詢索引（m005、012-audit-log-query）＋idx_login_attempt_created_at（m008、023 clarify Q1 拍板）→ MILESTONES §1 009901fa／DECISIONS §1 ⚠️af
- FR-002「system_settings 1 列」→ 現況 8 列（+7 密碼政策 KV、m009、024-password-policy）→ MILESTONES §1 aca02c81／DECISIONS §1 ⚠️ag；基線列 single_session_default=off 原值不變（live 證）
- SC-002「92 列／6 表」計數不變式＝基線檢查點口徑：live dev 庫累積態（users 9/roles 8/menus 78/casbin 149）為 m004 delta＋016/022 政策刀＋runtime CRUD 合法演進；基線列本身逐項驗證原值不變（見 liveEvidence）

**Live 證據**：psql：①13 表齊（12 口徑+m007 sys_ip_rule）、seaql_migrations m001~m009 九支序全套 ②sys_user 16 欄歷史欄序逐字合 m001 Iden、partial uniq ×3 在 ③seed 帳號 id 1/2/3=Super/Admin/User、nick 超级管理员/Admin/User01（淨值回填）、三列共用單一 $argon2id$v=19 hash（DISTINCT=1）、status=1 ④sys_role 1/2/3=R_SUPER/R_ADMIN/R_USER_COMMON home='home'、user_role 映射 1→R_SUPER/2→R_ADMIN/3→R_USER_COMMON ⑤delta：sys_user_role FK ×2（fk_sys_user_role_user/role、ON DELETE RESTRICT＝spec 工程預設）+複合 PK ⑥基線不變式現值：casbin g=0、protected=19（僅 m002 產 protected 列→基線政策原值不變）、button policy=16、sys_menu protected=8、基線選單 id 1~10 全在未刪（home/manage/.../manage_policy-archive）、single_session_default=off。curl：GET /auth/getUserInfo（共用 Super token＝login 成功＝123456 驗過 argon2id）→ userId:1、roles:[R_SUPER]、buttons 12 枚（R_SUPER 按鈕政策合 casbin seed）。寫端 FR-007/008（up→down→up、冪等）引收刀 C-V 全綠＋MILESTONES L130 追驗（fresh dev DB m006 rollback→re-apply 綠、chain 可逆、seed 完整）。

**CDP 證據**：N/A（002 為 schema/seed/驗證基建刀、無 UI 面；spec Assumptions 明示業務 API 與 entity 層不在本刀）

---

## 003-envelope — ✅ PASS

**FR coverage**：12/12 FR 覆蓋（FR-001~012 全數有實碼＋live/測試證據；無缺）。SC-001~008 對應：SC-001/002/003/004 live curl＋error.rs 矩陣測、SC-005 zh-cn/en-us 13 碼譯文齊、SC-006 translateBackendMsg=vue-i18n 原生 fallback、SC-007 /health ok＋stack 健康、SC-008 in-crate 8 契約測（025 收刀 254 測全綠聲明、本輪依紀律不重跑）。

**Strengths**：
- reserved 4 碼採型別層凍結（error.rs:32-54 enum 無對應變體＝編譯期保證永不構造，優於 runtime 斷言）；且 020-025 六刀後全 src grep 證實零 reserved 構造點、025 淨新 biz.password.* 三鍵全走 Biz(2222) 不擴矩陣——凍結契約 9 刀下來零漂移
- 前端翻譯點紀律嚴守 FR-008「烤在讀 backend msg 的邊界」：service/request/index.ts:45 單一 translatedMsg 供 dedup filter(:56)/includes 守門(:69)/push(:71)/modal content(:79) 四處共用（不變式四處同值、dedup 不破），未污染通用顯示函式
- 契約測試含反例守護（error.rs:273-276 拒非 4 根/無點/CJK/大寫首字母），FR-011「文法-conformance 斷言能拒絕不合規約識別碼」有實證非僅正例
- locale seed 雙語完整且後續刀（008/009/010/011/012/019/023/024/025）全循 backend.biz.* 規約擴充（zh-cn.ts:917-986 逐刀註記可稽），FR-006 前瞻契約價值實際兌現

**Findings**：
- **[Observation]** PageRes 於 live wire 的 data 內層鍵序被 Res::ok 的 serde_json::to_value 字母序化（current→records→size→total），與 envelope.rs 宣告序/in-crate 測試斷言的 current→size→total→records 不同（`rust-api/server/src/envelope.rs`）
  - 證據：envelope.rs:34-40 Res::ok 走 serde_json::to_value（Value::Object 預設 BTreeMap 排序鍵）；live curl getUserList 實測 data keys=['current','records','size','total']。無害：spec 僅凍結信封欄序 data→code→msg（Key Entities 表「固定欄序」只標信封列；contract §4 不變式 4 對 PageRes 只斷言無 pages/success＋空頁 []），JSON object 鍵序非語意、前端按鍵取值。但 envelope.rs:104-133 的 page_res 欄序測試斷言的是「直接序列化 PageRes」路徑、與實際 wire（經 to_value）不同路徑——若日後有人依測試推斷 wire 鍵序會誤判
- **[Observation]** Res::ok 序列化失敗時靜默降級 data:null 仍回 code 0000（成功碼＋空 payload）（`rust-api/server/src/envelope.rs`）
  - 證據：envelope.rs:36 `serde_json::to_value(data).unwrap_or(serde_json::Value::Null)`——T: Serialize 的 to_value 實務上僅在 map 非 string key 等罕況失敗，現有 DTO 全 struct 不可達；記錄備查、非 bug

**Supersessions（合法推翻、非缺陷）**：
- spec Edge Case／FR-008「4040/5003 native axios 路徑 msg 未被讀取顯示、本刀不修復（登 follow-up）」→ pre-波4 sweep §3.G（2026-06-23、base-web commit 7a011c00）補 onError else-if（index.ts:120-124）令 HTTP 403/404 信封 msg 也經 translateBackendMsg 在地化 → 依據：MILESTONES §3.G 歸檔節（⚠️aa BASE-WEB-I18N-WIRING §III.2(i) 授權、CDP 實證 toast「权限不足」）——此為 spec 自身登記之 follow-up 路徑兌現、非漂移
- spec Assumptions「From<DbErr> 不在本刀（R7）」→ 006-auth-island T009 帶入 error.rs:112-116（DbErr→Internal 5000）→ 依據：MILESTONES §1 006 行＋error.rs:110 註明出處；REVIEW-001-019 基線已認定
- spec「本刀無業務端點、唯一可達錯誤＝NotFound」→ 006~025 各刀陸續消費 LoginFailed/TokenExpired/Biz/PermissionDenied 等變體 → spec 本文預期之階梯（FR-012），MILESTONES §1 各刀行為據；i18n 首檢點（auth/login）與 per-entity 端到端（008 system-settings）均已如 spec 排程兌現

**Live 證據**：① curl GET /api/auth/getUserInfo（Super token）→ HTTP 200 `{"data":{...},"code":"0000","msg":"common.success"}` 欄序 data→code→msg、code 為 string；② 無 token 同端點 → HTTP 200＋`{"data":null,"code":"3333","msg":"auth.token.expired"}`（⚠️e 可顯示錯誤走 200、data:null 不省略）；③ 亂 route /api/no/such/route → HTTP 404＋`{"data":null,"code":"4040","msg":"system.notFound"}`（fallback 信封形）；④ getUserList 分頁 → data keys={current,size,total,records}、無 pages/無 success（python json 驗）；⑤ /health → plain-text "ok" HTTP 200（探針豁免信封、健康）。碼矩陣靜態掃描：全 src AppError 構造僅 9 可發變體（Biz×104/Internal×23/…）、reserved 7778/8889/9998/9999 零構造點（grep 命中全為無關數字字面）、全部 Biz key 皆靜態字面且合 4 根文法（反向 grep 零不合規）。

**CDP 證據**：tab 意外處於登出態（/login redirect）→ 依 SOP 注入 SOY_token 後 navigate http://localhost:31080/manage/system-settings：頁面可達（href 正確、innerText len=239）、核心元素渲染（密码策略/会话设置 區塊＋7 政策欄位標籤全在）、i18n 斷言 raw:[]（body innerText 以 /\b(backend|page|common|route)\.[a-zA-Z]+\./ 掃描零命中＝無 raw key）。簡繁混用（简体標題+繁體描述）＝024 刻意保留待 zh-TW locale feature、非 003 缺陷。biz toast 在地化另有 §3.G sweep 既存 CDP 實證（真 HTTP 403→toast「权限不足」、commit 7a011c00 記錄），本輪未觸發寫端/錯密碼（遵守非破壞紀律）。

---

## 004-soft-delete-infra — ✅ PASS

**FR coverage**：9/9 FR 覆蓋（FR-001 trait+find_active 實碼／FR-002 3 實體 impl+system_settings 例外 facade find() 直讀+022 合法第 4 實體／FR-003 lint+獨立 grep 零違規／FR-004 whiten+前界規則+框架子路徑不誤殺／FR-005 12 實體模組（11+022）／FR-006 scan_self_test 雙證／FR-007 psql+curl 活體實證／FR-008 收刀 SC-005 歷史邊界+寫路徑由後續刀各自帶入（spec assumption 明示）／FR-009 收刀 158MB image+025 prod build ×2 持續綠）；SC-001~007 全對應成立

**Strengths**：
- SoftDeletable trait（rust-api/server/src/model/soft_delete.rs:14-22）仍是純 sea_orm 泛型、零 entity:: 依賴，025 收刀後被 4 個 facade impl（sys_user/sys_role/sys_menu/sys_ip_rule）與大量 find_active_* 讀端消費，US1「宣告 deleted_at_column 即繼承」承諾在 022 sys_ip_rule 上實證兌現（facade/sys_ip_rule.rs:20 impl 後 list/find_active_by_id/restore 全走 find_active、機制本體零改動）
- entity_access_lint（rust-api/server/tests/entity_access_lint.rs）雙證結構完整存活：whiten（行/區塊註解+字串抹白、逐 char 處理 CJK）+ path-root 前界規則（非 ident 非 ':'，brace-group {entity:: 視為違規）+ scan_self_test 9 斷言非 vacuous；獨立 grep 復核 server/src 下 facade 外 path-root entity:: 為零命中，FR-003 在 025 cumulative 態仍守恆
- deleted_at/deleted_by 成對範式（constitution §I.6）活體零漂移：psql 驗 sys_user/sys_role/sys_menu 三表 (deleted_at IS NULL)<>(deleted_by IS NULL) 不成對列數=0（含 11 筆歷史軟刪列全成對）；4 個 partial-unique index（user_name/code/route_name/cidr+rule_type WHERE deleted_at IS NULL）全在
- soft-delete no-op 合約跨 facade 一致：sys_user.soft_delete 已刪→Ok(None) idempotent（M-2 加固、sys_user.rs:67）、sys_role 經 find_active_by_id_for_update None→no-op（020 C1 lock-then-redecide 疊加不破合約）、sys_menu/sys_ip_rule 同範式；restore 端正確用含已刪讀（sys_menu.rs:1025 find_by_id 非 find_active）+ active 衝突預檢（sys_ip_rule.rs:331）

**Findings**：
- **[Observation]** SoftDeletable trait 上的 #[allow(dead_code)]（004 當時「無 caller」暫抑）已 stale——現有 4 個 impl 與大量 find_active 消費者，抑制屬多餘但無害（`rust-api/server/src/model/facade/../soft_delete.rs`）
  - 證據：soft_delete.rs:12-13 註解「本刀（004）無 caller，待業務刀消費 → 暫抑 dead_code」+ #[allow(dead_code)] 仍在；但 grep 顯示 sys_user/sys_role/sys_menu/sys_ip_rule 四 facade 均 impl 且 find_active 被 20+ 處消費。屬過期註解/抑制殘留、不影響 lint 或行為，下次touch該檔可順手拔
- **[Observation]** sys_role.soft_delete 的已刪 no-op 走 find_active_by_id_for_update→None 路徑（隱式），未如 sys_user M-2 般顯式匹配 before.deleted_at.is_some()——行為等價（已刪列不在 find_active 集合、回 Ok(None) 不寫審計），僅範式表達不一（`rust-api/server/src/model/facade/sys_role.rs`）
  - 證據：sys_role.rs:401 let before = find_active_by_id_for_update(&txn, id).await?（None→no-op）；sys_user.rs:61-67 用 find_by_id（含已刪）+ 顯式 Some(before) if before.deleted_at.is_some() 分支。兩者對「重刪已刪列」皆回 Ok(None) 不重寫不重發 SOFT_DELETE，合約一致、無缺陷

**Supersessions（合法推翻、非缺陷）**：
- FR-002/Key Entities「恰好 3 個 soft-delete 實體（user/role/menu）」→ 022-ip-access-control 增第 4 個 sys_ip_rule（m007 建表+partial-unique cidr+rule_type、impl SoftDeletable）——依據 MILESTONES §1 `d9c809e6`（2026-06-29 022 全綠收刀、破 0-migration streak 明載）；屬機制被新切片正當繼承（正是 US1 AC-3 設計目標）非漂移
- FR-005「11 表→11 實體模組」→ 現 12 模組（+sys_ip_rule）——同上 022 依據；「後續切片永不改動既有實體定義」不變式仍守住（新增非改動）
- soft_delete 已刪列語意：004/009 原碼會對已刪列重覆寫 deleted_at/deleted_by 並重發 SOFT_DELETE op-log → M-2 加固為 idempotent no-op（Ok(None)、與 batch「已刪 skip」對齊）——依據 MILESTONES §1 `0c14abdb`（2026-06-25 M-tier 9 項閉合、M-2 明載）；合法強化
- log entity（sys_operation_log/sys_access_log/sys_login_attempt）欄位由 005/013 演進（operator 單 IP→四欄 forensic）——基線 REVIEW-001-019.md §B 已確認之既知 supersession，本輪復核實體檔仍與 as-built schema 對齊
- 020 D10/C1：sys_role 刪除路徑升級 find_active_by_id→find_active_by_id_for_update（FOR UPDATE 序列化 restore-during-delete）——依據 MILESTONES 020 收刀行；疊加於 004 find_active 範式之上、未破壞 active 過濾語意

**Live 證據**：psql①欄位: information_schema 查 deleted_at/deleted_by 成對存在於 sys_user/sys_role/sys_menu/system_settings/sys_ip_rule 各 2 欄。②partial-unique: pg_indexes WHERE deleted_at IS NULL → 4 index（user_name/code/route_name/cidr+rule_type）。③已刪樣本: sys_user 9 列中 6 軟刪、sys_role 8 列中 5 軟刪，全部 deleted_by=1 成對；三表不成對計數=0。④FR-007/SC-002 活體: curl GET /systemManage/getUserList → code 0000 total=3（僅 Super/Admin/User，6 軟刪列 100% 排除）；getRoleList → total=3（R_SUPER/R_ADMIN/R_USER_COMMON，5 軟刪列排除）；未過濾面由 psql 直查證列仍在（資料未實體移除）。⑤FR-003 靜態復核: grep server/src facade 外 path-root entity:: 零命中。寫端（soft_delete/restore）＝碼靜態＋收刀證據（MILESTONES `a1105f0` C-V-0~3 全綠 lint 2 passed/live smoke txn-rollback/prod image 158MB；M-2 `0c14abdb` 184 passed；025 `ded99ee9` 255 unit+lint 2+2+prod build ×2 綠＝entity crate 打包至今無回歸、FR-009 持續成立）。

**CDP 證據**：N/A（本 feature 為資料層基建、無專屬 UI 面；active 過濾對前端的間接效果由 curl getUserList/getRoleList 讀端證據覆蓋）

---

## 005-audit-op-log — ✅ PASS

**FR coverage**：10/10 FR 覆蓋（FR-001 mutate_in_txn+三路徑 live 測、FR-002 24 caller 零改本體、FR-003 redact 純測+psql 0 違、FR-004 insert-only facade+entity_access_lint、FR-005 全欄落 write_in_txn〔IP 四欄=013 合法演進〕、FR-006 no-op Ok(None) 含已刪 idempotent、FR-007 audit.rs 零 entity::、FR-008 顯式→007/013 合法自動化、FR-009/010 歷史邊界收刀時已證〔MILESTONES `98f1f7e` SC-005~007 全綠〕）；無缺者

**Strengths**：
- mutate_in_txn 原子合約三分支（Some(event)→facade write_in_txn+commit／None→no-op commit／Err→txn drop rollback）碼面完整（rust-api/server/src/model/audit.rs:84-97），且 op_log_atomic_tests 三路徑 live smoke（commit/no-op/rollback write-then-Err、savepoint 隔離）常駐 facade/sys_user.rs:693+ 非 vacuous
- redact 雙證扎實：audit_json 手構 json password 固定 "<redacted>"（facade/sys_user.rs:22-43、Model 刻意不 derive Serialize）＋純測 redact_tests（sys_user.rs:656）＋live psql 全表 0 筆未遮蔽
- append-only + facade 唯一管道守恆延續：sys_operation_log facade 僅 write_in_txn/write_standalone 兩個 insert fn、零 update/delete（facade/sys_operation_log.rs:17-44），entity_access_lint 仍在崗、機制本體 audit.rs 零 entity::（僅 sea_orm::entity::prelude::IpNetwork 全路徑、lint-safe）
- 實體中立性經 20 刀實戰驗證：mutate_in_txn 現有 24 個 caller 跨 8 個 facade 模組，005 後新增寫端（009/010/011/015/016/022/024/025）皆零改機制本體，025 password-only 更新亦自動繼承 redact（sys_user.rs:509）

**Findings**：
- **[Observation]** facade 層 ip_host_like 反向依賴 handler 層的 escape_like（層次方向小逆流、非 spec 違反）（`rust-api/server/src/model/facade/sys_operation_log.rs`）
  - 證據：line 61: `crate::handler::system_manage::escape_like(raw)`——012 引入、基線輪已 PASS；功能正確（LIKE escape+綁定值防注入），僅記 model→handler 依賴方向為日後重構候選
- **[Observation]** op-log id 序列有大量空洞（min=11、max=527、count=139），為 rollback 路徑消耗 sequence 的正常痕跡、非資料遺失（`rust-api/server/src/model/audit.rs`）
  - 證據：psql: SELECT min(id),max(id),count(*) FROM sys_operation_log → 11|527|139；碼面零 delete 路徑（cleanup-job 只 DELETE FROM sys_token、cleanup-job/src/main.rs:44），空洞來自 live smoke／失敗 txn rollback＝SC-002 原子性實際運作的側證

**Supersessions（合法推翻、非缺陷）**：
- FR-005/FR-008 operator 單 IP＋顯式參數 → 013 四欄鑑識（operator_real_ip/peer_ip/x_forwarded_for/ip_confidence）＋007/013 audit_ctx RequestContext::to_audit_meta 自動抽取——005 spec Assumptions 自身預告「後續刀回填」；依據 REVIEW-001-019 §B＋MILESTONES §1 013 行
- payload 快照無 roles → 017 with_roles 注入排序角色集（audit.rs:103、017 C-4 拍板）；依據 MILESTONES §1 017 行＋REVIEW-001-019 supersession map
- op-log 無讀端 → 012 getOperationLog list（純 SELECT、R_SUPER）＋023 resolve_sort 白名單排序——皆唯讀、不動 append-only sink；依據 MILESTONES §1 `55d34ff`（012）＋023 收刀行
- sink 新增 write_standalone（獨立 op-log 寫、無 entity 變更事件）→ 022 FR-011 手動解鎖稽核軌——仍 insert-only、留在同 facade（archetype B 不破）；依據 MILESTONES §1 022 行＋facade/sys_operation_log.rs:36-38 檔內註

**Live 證據**：psql ①最近 8 列 op-log：operation=UPDATE、entity_table=sys_user/system_settings、operator_id=1、trace_id 全非空、created_at 連續至 2026-07-02（025 寫端仍經管道落審計）②DISTINCT operation = {INSERT,RESTORE,SOFT_DELETE,UPDATE}＝封閉詞彙集全數且無越集值（FR-005）③sys_user 列 payload_before/after->>'password' 抽樣 5 筆全 '<redacted>'、全表未遮蔽計數=0（FR-003 100%）④min/max/count=11/527/139：序列空洞＝rollback 消耗、碼面零 delete 路徑（cleanup-job 僅 sys_token）→ append-only 成立（FR-004/SC-004）

**CDP 證據**：N/A——005 spec FR-009 明示零業務端點/UI/中介層（機制地基刀）；op-log 資料的 UI 呈現屬 012 審計中心之審查範圍

---

## 006-auth-island-min — ✅ PASS

**FR coverage**：17/17 FR 覆蓋（FR-001~017 全數有實碼＋live/測試證據；FR-009/012/015/016 部分行為由 009/014/019/021/022 合法 supersede、皆有 DECISIONS/MILESTONES 依據，無無依據漂移）

**Strengths**：
- login_inner 的識別前/識別後 operator doctrine 清晰：帳號不存在與密碼錯誤同回 AppError::LoginFailed（1000、同碼同 msg key，rust-api/server/src/handler/auth.rs:177/181）、停用 gate 置 verify 後 carry Some(uid)——防枚舉語意在 020-025 六刀疊加後仍完整保留
- FR-011 原子性實作乾淨：set_pointer + insert_token 綁同一 plain txn（auth.rs:239-269），014 加入的 revoke_other_chains 也收進同 txn，任一失敗整組 rollback
- FR-008 DB-fresh 授權貫徹到位：get_user_info 與 require_policy 都走 roles_of_user(db, claims.uid)、claims.roles 僅 hint（auth.rs:678、enforce.rs:416-417），jwt.rs:18 註解明文標示
- fail-OPEN vs fail-CLOSED 方向正確且有測：bearer 缺/壞/過期一律 TokenExpired 3333（enforce.rs:85-95、jwt leeway=0）；is_current DB 抖動/查無 user 回 Ok(())（enforce.rs:116-118）——Edge case「兩者方向相反不得搞反」守住
- FR-014 密鑰 _FILE 優先在 config.rs:225-229 落實（load_secret_file_first + guard_jwt_secret），且 014 Redis URL、xdb 路徑沿用同一範式

**Findings**：
- **[Observation]** getUserInfo 每請求對 enforce_mw is_current 做一趟 PK 讀（session_gate_of），Redis pointer 快取仍 defer（014 data-model X-01 v1 明文遞延）——非缺陷、屬已登記的效能遞延（`rust-api/server/src/auth/enforce.rs`）
  - 證據：enforce.rs:104-106 註解「Redis sess:{uid} pointer 快取本刀 DEFER（data-model X-01 v1）」；enforce_mw（:324-334）bearer→is_current→denylist_gate 每受保護請求跑一次 sys_user PK 讀
- **[Observation]** US2 顯示名稱 live 回「超级管理员」（簡體 seed nick_name）——與已登記的未來 zh-TW locale feature 一致、勿當 bug；alias 規約本身（User→User01）由 m002 seed 淨值兌現而非 runtime alias 邏輯（`rust-api/migration/src/m002_rev2_seeds.rs`）
  - 證據：m002 :51-54 INSERT nick_name id3='User01'；psql 實測 3|User|User01；live getUserInfo userName="超级管理员"（uid=1 nick_name）——handler auth.rs:691 nick_name fallback user_name，符 FR-005

**Supersessions（合法推翻、非缺陷）**：
- FR-009/US4 single-session「永遠踢」→ 014 policy-aware is_current（effective Off 放行多 session；enforce.rs:97-132 resolve_policy gate、login_inner :247-257 effective on 才 revoke_other_chains）——依據 DECISIONS §2 014「修 006 永遠踢 bug」＋REVIEW-001-019 supersession map；live psql 見 uid=1 有 3 列 active token（default off 下合法多 session）、7777 路徑在 effective On 時保留
- FR-016「無換發端點消費邏輯」→ 014 加 POST /auth/refreshToken（rotation chain 四態＋lock-then-redecide、handler/auth.rs:528-586）——依據 MILESTONES 014-auth-token-session 收刀＋014 spec；且遵 006 FR-015 精神：refresh handler 絕不回 3333（jwt 壞→8888、auth.rs:542）
- FR-016「無 lockout」→ 019 滑動窗 gate＋021 Redis L1 負快取＋022 白名單 bypass/手動解鎖，全部前置於 login_inner 之前（auth.rs:292-420）、不改 login 核心語意——依據 MILESTONES 019/021/022 收刀紀錄
- FR-003 停用帳號收斂 1000：006 基線未實作停用 gate、009 R10 補上 status==2→LoginFailed（auth.rs:184-189、同碼同 msg 防枚舉）——依據 REVIEW-001-019 §B「status==2 停用（009）後續補」
- FR-012 rotation_chain 僅記錄不消費 → 014 開始消費（decide_rotation/revoke_chain）——spec 本文即預告「供後續換發刀」、方向一致

**Live 證據**：curl GET /api/auth/getUserInfo（共用 Super token）→ code 0000、4 欄齊：userId=\"1\"（字串、FR-005）/userName=超级管理员/roles=[R_SUPER]/buttons=12 個（含 user:add…menu:delete、非空殼、FR-006）；無 token 與壞 token 皆 → {\"code\":\"3333\",\"msg\":\"auth.token.expired\"} HTTP 200（FR-007/015 信封＋可在地化 key）。psql（唯讀）：sys_user id1-3 = Super/Admin/User、nick User01（alias seed）、三帳號 current_session_id 皆非空（FR-010 pointer 持久真相）；sys_token uid=1 最近 3 列 status=active、rotation_chain 皆非空、token_hash 64 hex、expires_at>now（FR-012）；casbin_rule v2='button' 16 列（FR-006 seed 非空）。/health 經 front-nginx 31080 與 rust-api 31081 直連皆 ok HTTP 200（FR-017/SC-007 零回歸）。寫端（login/5003 低權帳號測）依禁令未打，5003 由 error.rs:91 PermissionDenied→403 映射＋enforce seam 單元測＋006 收刀 C-V 全綠紀錄背書。

**CDP 證據**：CDP（tab AAB4D0C2CDE633D7A403FE31ACB47168、builtin WebSocket 腳本）：navigate http://localhost:31080/home → 停留 /home（未彈回 /login＝Super token 登入態有效、enforce 鏈放行）；sider 選單渲染（hasSider=true、系統管理/系統設置等項可見）；頁面顯示「早安，超级管理员」（getUserInfo userName 上屏）；body innerText 525 字元掃描 page.*/backend.*/common.*/route.* 樣式殘鍵 → rawKeys=[]（i18n 全在地化、SC-006 顯示路徑無 raw key）。

---

## 007-audit-overlay — ✅ PASS

**FR coverage**：16/16 FR 覆蓋（FR-001/002/003/008/009/010/011/013 live 實證；FR-004/005/012 code＋spec 內 021 勘誤；FR-006/007 code＋audit_ctx 純測（peer-gate/normalize 系列）；FR-014 facade 面檢；FR-015/016 live＋MILESTONES）；SC-001~007/009 直接或引收刀 C-V 佐證、SC-008（prod build xdb baked-in）引 007 收刀紀錄未重跑。

**Strengths**：
- 單一寫點 exactly-one 設計歷經 019/021/022 三刀改造仍成立：rust-api/server/src/handler/auth.rs:427 的 best-effort login_attempt 寫點統一收斂 Ok 與所有 Err 終端路徑（含 019 L2 gated 列 operator=None），僅 021 L1 早 return（auth.rs:349 附 ②b 註解）為有依據反轉且已回註 007 spec FR-004/SC-002 as-built 勘誤
- resolve_client_ip 純函式（audit_ctx.rs:146-207）peer-gate→Tier-1 CDN 錨→Tier-2 skip→Fallback 結構清晰，013 七態 Confidence／022 apply_tunnel_fallback（B2 反偽造、conf 不升）以 overlay 疊加而非改動核心簽名，防偽語意（FR-006/007）零漂移
- op-log 歸屬 seam（RequestContext::to_audit_meta, audit_ctx.rs:90）已由 009+ 各刀 mutating handler 實際消費（system_manage.rs/system_settings.rs/user_center.rs 共 10+ 呼叫點），live psql 見 sys_operation_log operator_id/operator_real_ip/operator_ip_confidence/trace_id 皆真值——FR-013 從 seam-ready 進化為 live-filled
- best-effort 紀律徹底：access-log 寫失敗僅 tracing::warn（audit_ctx.rs:323 best_effort_audit）、login_attempt 同（auth.rs:442）、xdb 缺檔 xdb_ready gate 降級 region=None（audit_ctx.rs:360），三 sink facade 僅暴露 write/count/list、無 update/delete fn（FR-012/014）

**Findings**：
- **[Observation]** front-nginx 無條件以 proxy_set_header X-Request-Id $request_id 覆蓋 client 自帶的 x-request-id，經 ingress 的請求 FR-010「沿用入站標頭」只沿用到 nginx 生成值、client 端關聯 id 到不了 app（`deploy/nginx/conf.d/_locations.inc`）
  - 證據：_locations.inc:25 `proxy_set_header X-Request-Id $request_id;`；live：經 :31080 帶 `x-request-id: review007-trace-abc` → sys_access_log.trace_id=8e74f8c8…（nginx $request_id 32-hex）；直連 :31081 帶 `review007-direct-xyz` → trace_id 逐字沿用。app 層 extract_trace_id 正確；nginx↔app 同 request_id 可互 join（nginx.conf:20 access log 亦記 $request_id），屬設計取捨非 bug，但與 spec Assumptions「反代已轉發 request-id 標頭」的『轉發』措辭有出入（實為『替換』）
- **[Observation]** dev 拓樸下經 nginx 的請求 real_ip 解析結果＝nginx 容器 IP（Fallback）而非真實 host client——因 docker gateway 172.23.0.1 ∈ internal_default、整鏈受信全 skip（`rust-api/server/src/audit_ctx.rs`）
  - 證據：live psql：access_log 列 real_ip=172.23.0.13=peer_ip、ip_confidence=fallback、x_forwarded_for=172.23.0.1（逐字保存）。符合 spec US3 AC3/FR-007 fail-safe（全可信 hop→直連位址），dev-only 現象；prod 外部 client 不在 internal_default 不受影響。鑑識上原始 client 仍可自 x_forwarded_for 欄還原
- **[Observation]** CDP 審計中心「API 存取」tab 列表未見完整 trace_id 文字（hasTrace:false）——列有渲染（getUserInfo/内网 皆在 DOM）但識別碼欄疑似截斷顯示，鑑識人員要完整 trace 需 hover/詳情（`base-web/src/views/manage/audit/index.vue`）
  - 證據：CDP eval：{hasTrace:false, hasNeiwang:true, hasGetUserInfo:true, rawKey:false}——psql 確認 trace_id=review007-direct-xyz 的列存在且在最新頁；屬 012 讀端 UI 呈現細節、非 007 寫入面問題

**Supersessions（合法推翻、非缺陷）**：
- FR-004/SC-002「每終端結果恰一筆」→ 021-login-lockout-redis-cache ②b：L1 Redis 負快取命中（已鎖）之被壓制嘗試早 return 不寫 sys_login_attempt（auth.rs:349）、量級訊號改 ②c 節流摘要 target=security.lockout→loki；依據＝007 spec.md FR-004/SC-002 內嵌 as-built 勘誤（2026-06-28、021 T016）＋DECISIONS §1（021）——上鎖前歷程與觸發那發仍恰一筆，live 未觸鎖驗證（非破壞紀律）
- FR-003/FR-005 紀錄欄形 → 013-xff-real-ip-forensics 擴充：peer_ip/ip_confidence（七態）兩鑑識欄＋兩層 TrustModel（cdn/my_public/binding/tunnel）取代單一可信集合；依據＝MILESTONES §1（013）、Confidence enum 註明 013 data-model §4；022 再疊 apply_tunnel_fallback（DECISIONS §1 ⚠️ae）——欄位只增不減、007 原欄全保留
- FR-015「本刀不引入稽核讀端/UI」→ 012-audit-read（波2 規劃內）新增 facade list/resolve_sort 讀 fn＋/manage/audit 三 tab UI；依據＝MILESTONES §1（012）＝spec 自身「各歸後續波次」遞延條款兌現，非漂移

**Live 證據**：①exactly-one/operator-gate：sys_access_log count 2132 → 打 3 請求（auth GET getUserInfo=200、無 token GET=3333、/health=ok）→ 2133（恰 +1，unauth/health 0 列＝FR-001/002/016）。②欄位齊全（FR-003/008/009、SC-004/007）：最新列 operator_id=1|GET|/auth/getUserInfo|200|real_ip inet|peer_ip|fallback|xff="172.23.0.1" 逐字|region="0|0|0|内网IP|内网IP" 非空內網標|trace_id|created_at。③FR-010：直連 :31081 帶 x-request-id → trace_id="review007-direct-xyz" 逐字沿用；經 nginx 則為 $request_id（見 Observation）。④login_attempt 近 3 列（FR-005/011）：attempted_user_name=Super|t|operator_id=1|real_ip/peer/confidence/xff/region/trace 全填。⑤FR-013 seam live：sys_operation_log 列 operator_id=1+operator_real_ip+operator_ip_confidence=fallback+trace_id 非空。失敗路徑/偽造 XFF/鎖定寫入依非破壞紀律未打，引 code（單一寫點 auth.rs:390-442）＋007/013/021 C-V 收刀全綠紀錄。

**CDP 證據**：007 本體無 UI 面；以 012 讀端 /manage/audit（消費 007 寫入列）做下游渲染取證：頁面可達（url=…/manage/audit）、三 tab（操作异动/API 存取/登录尝试）＋13 欄表頭渲染（含 013 可信度/直连 peer/真实 IP/转发链）、op-log 列顯 Super/UPDATE/sys_user/172.23.0.9；點「API 存取」tab 後 {hasGetUserInfo:true, hasNeiwang:true（内网 region 在地化呈現）, rawKey:false}——body innerText 無 page.*/backend.*/common.* 殘鍵。截圖 scratchpad/audit-page.png。

---

## 008-system-settings — ✅ PASS

**FR coverage**：11/11 FR 覆蓋（FR-001/004/005/008/011 live+CDP+實碼直證；FR-002/003/006/007 實碼＋收刀 C-V 證據；FR-009 零 schema 由 024 m009 seed 合法演進；FR-010 NOT-in-scope 由 014 合法接手）；SC-001~007 全對應、無缺者

**Strengths**：
- require_policy（rust-api/server/src/auth/enforce.rs:403）DB-fresh roles_of_user(db, claims.uid) 不信 claims.roles、per-route layer 掛 GET/POST 兩端點（main.rs:226-239）＋casbin seed 僅 R_SUPER 兩列（psql 實證）——FR-004/005 樣式立得乾淨且被後續全部治理端點沿用
- update_by_key（model/facade/system_settings.rs:59-95）mutate_in_txn 同 txn op-log、查無→Ok(None) no-op 不寫審計、build_update_active_model 純測 seam §I.6 成對＋live INET round-trip #[ignore] 測（by trace_id、外層 txn savepoint rollback 不污染 seed）——FR-003 原子審計實作與測試層次完整
- endpoint_coverage_lint（rust-api/server/tests/endpoint_coverage_lint.rs:116-117）registry 含兩端點、隨 025 增至 AS_BUILT=54 仍維護中——FR-008 守恆檢查存活至 cumulative 態
- validate_value_type（handler/system_settings.rs:62-86）024 補 number 型時刻意整串比對 early-return 避 split_once 死碼、附 4 組單元測含空字串/越界——008 明文 passthrough 缺口被 024 前向補強而非漂移

**Findings**：
- **[Observation]** number 型驗證（:66）接受 u32::parse 的寬鬆字面（如 "0008"、"+8"）且原字面 verbatim 入庫，下游 password_policy::from_settings 再 parse 可正常吸收，僅字面不正規化的美觀疑點、非 FR 違反（`rust-api/server/src/handler/system_settings.rs`）
  - 證據：validate_value_type 對 value.parse::<u32>() Ok 即放行、update_by_key Set(new_value) 原樣寫入；u32 parse 接受前導零與 '+'。功能無害（from_settings 同 parse 語意），記錄供日後 canonicalization 參考
- **[Observation]** find_all（:19）回全列不濾 deleted_at（schema 有 soft-delete 欄），若日後引入設定鍵刪除路徑須回補 filter——目前無刪除路徑故 latent、非缺陷（`rust-api/server/src/model/facade/system_settings.rs`）
  - 證據：facade doc comment 明示「§5.0 system_settings 不套用 §5.8、回全列」＝拍板刻意（本刀無增刪鍵路徑）；psql 現況 8 列 deleted_at 全 NULL

**Supersessions（合法推翻、非缺陷）**：
- 008 FR-010 hot-apply NOT in scope → 014 加 redis `settings:invalidate` best-effort publish（handler/system_settings.rs:118-124、fail-open）——依據 REVIEW-001-019 Supersession Map「008 設定熱套用→014 U1 settings 島」＋MILESTONES 014 行
- 008 FR-006/US4 值型別僅涵蓋 enum → 024 補 bare `number` 整串比對驗證 1..=1024（補 008 明文 passthrough 缺口）——依據 MILESTONES `aca02c81`／DECISIONS §2 024 行（spec Assumptions 預告「其他型別隨需要擴充」）
- 008 前端單卡 KV 列表版面 → 024 重構為分區塊 2 NCard（密码策略/会话设置）＋2 欄 NGrid＋NInputNumber render 分支＋滾動修復——依據 MILESTONES `aca02c81` 收尾 UI 三輪打磨（user 逐項回饋）
- 008 FR-004 設定值僅 R_SUPER 可讀 → 025 新增 auth-only `GET /userCenter/getPasswordPolicy` 讓任何登入者讀 7 個 password_* KV（7-key allowlist、不洩其他設定鍵）——依據 DECISIONS §1 ⚠️ah（review loop 接住原計畫前端讀 super-only 端點會 403、merge `ded99ee9`）
- 008 FR-009 零 schema 變更基線 → 024 m009 seed 新增 7 政策列（seed data、非 schema DDL；up→down→up 可逆）——依據 MILESTONES `aca02c81`／DECISIONS §2

**Live 證據**：curl GET /api/systemManage/getSystemSettings（Super token）→ code:0000、8 筆（single_session_default=off enum:on,off＋7 個 password_* 政策鍵）camelCase wire 形正確；無 token → code:3333 auth.token.expired data:null（認證守門、不洩值）；psql system_settings 8 列與 wire 一致；psql casbin_rule 僅 2 列 p|R_SUPER|getSystemSettings GET／updateSystemSetting POST（無其他角色 policy＝403 by absence）；讀端 5 次計時 7.9~9.4ms（SC-007 p95<300ms 大幅達標）。寫端（FR-002/003/006/007）依非破壞紀律未觸發：由實碼（update_by_key mutate_in_txn＋validate_value_type＋notFound 映射）＋008 收刀 C-V-0~11 全綠（MILESTONES `b52dafe`：live op-log INET round-trip、policy-gate 5003、2222×2、CDP toast、prod build）＋024 收刀 live（非法 number→2222 值不變）背書。

**CDP 證據**：CDP navigate http://localhost:31080/manage/system-settings（Super 登入態 tab）：頁面可達（url 斷言）、2 NCard 區塊標題「密码策略/会话设置」渲染、6 NSwitch（5 password enum＋1 single_session）＋2 NInputNumber（min/max length）齊、body innerText 無 page.*/backend.*/common.*/route.* raw i18n 殘鍵（rawKey:false）、含「单一」session 描述——024 重構後版面如 MILESTONES `aca02c81` 所述、SC-005 讀面達成（改值/toast 依非破壞紀律不按保存、引 008/024 收刀 CDP 全綠紀錄）。

---


<!-- BATCH 2 RESULTS -->

## 009-user-management — ✅📝 PASS_WITH_NOTES

**FR coverage**：FR-001 ✅（facade list_active apply_if＋ilike LOWER LIKE ESCAPE＋eq 精確＋normalize 守門；live curl 3 態＋CDP 空參數真形）；FR-002 ✅（UserListItem 無 password、live 證）；FR-003 ✅（add_user 預設密碼 argon2＋create 同 txn＋map_write_err 23505→2222 duplicateUserName；靜態＋收刀 C-V）；FR-004 ✅（build_update_active_model user_name/password 不 Set＋self_lock_violation 呼 facade 前整筆拒＋None→2222 notFound；replace_roles find_active 略過無效角色）；FR-005 ✅（soft_delete 單/批、id==uid 與 batch_contains_self pre-check 先於任何寫、缺漏 Ok(None) 靜默略過；psql 軟刪列排除證）；FR-006 ✅（mutate_in_txn 三路＋with_roles payload＋password redact；op-log 27 列）；FR-007 ✅（6 端點 require_policy＋DB-fresh roles_of_user＋m002 seed read=SUPER+ADMIN/write=SUPER；未認證 3333 live 證、403(5003) 靠收刀 C-V-8）；FR-008 ✅（getAllRoles find_active、live 3 角色）；FR-009 ✅（auth.rs:187 status==2→LoginFailed 統一 1000；未觸發登入嘗試、靜態＋收刀 C-V）；FR-010 ✅（endpoint_coverage_lint 含 6 端點、tests/ 存在）；FR-011 ✅（CDP Network 實抓真請求、i18n biz.user.* 5 鍵 zh-cn/en-us、頁面零 raw key）；FR-012 ✅（009 零 migration；cumulative m005-m009 皆他刀且不改 sys_user 結構、login/健康探針現行 live 正常）。12/12 covered。

**Strengths**：
- FR-006 原子性落實嚴謹：create/update/soft_delete 全走 mutate_in_txn，user 寫＋replace_roles_in_txn＋op-log 同一 txn（sys_user.rs:318-404），audit_json 逐欄手構且 password 固定 <redacted>（含純測 audit_json_redacts_password）
- FR-001 空字串守門經真瀏覽器實證：CDP 實抓 axios 送出 ?status=&userName=dm&userGender=&nickName=&userPhone=&userEmail=，後端 normalize_str_filter/normalize_enum_filter 正確 skip 空欄、模糊命中 Admin（大小寫不敏感 LOWER LIKE ESCAPE、escape_like 防 %/_ wildcard 注入）
- FR-007 授權判定 DB-fresh：require_policy 內 roles_of_user(&state.db, claims.uid)（enforce.rs:415-417、註明不信 claims.roles），未認證回 3333 data:null 不洩資料（live 實測）
- 守門邏輯全為可測純函式：self_lock_violation（H-2 representation 一致——status 用同款 normalize 後比 ==2、防 "02" 繞過）、batch_contains_self、map_write_err（23505→2222 dup、禁裸 ?）
- roles_for_users 三步固定查無 N+1（sys_user_role.rs:60-113），且只解 find_active 角色＝已刪角色自然略過（FR-004 略過無效角色語意同源）
- wire 誠實：id 帶 2^53 MAX_SAFE 守門（wire_id）、016 D5 fetchGetUserListRev3 honest 讀型消 frozen 型謊、UserListItem 無 password 欄（live 證）
- 020-025 六刀後零回歸：023 sorter（6 欄白名單 resolve_sort、CDP 見 6 sorter 頭）、025 新增 update_own_profile/change_own_password 與 009 端點完全不交叉（009 update 仍 user_name/password Unchanged）

**Findings**：
- **[Minor]** 搜尋「重置」鈕只清空表單、不重新查詢——清完後列表仍停在上次過濾結果（實測：搜 dm 得 1 列→按重置→表單清空但列表仍 1 列/共1条，須再按搜索才回 3 列）（`base-web/src/views/manage/user/modules/user-search.vue`）
  - 證據：user-search.vue:40-43 reset() 只 restoreValidation+resetModel、無 emit('search')；index.vue:214 亦只綁 @search。CDP 實測 AFTER_RESET rows=1、firstInput=''。屬 upstream soybean example 分支原樣（role-search.vue:53 同 pattern、git log 顯示此檔為 upstream commit 0919dd07 非 rev3-inline）、非 009 引入；spec 無「重置」FR、不構成 FR 違反，但與 Edge「空白條件回全部」的 UX 期待有落差
- **[Minor]** 編輯 drawer 的「用户名」欄在 edit 模式仍可編輯，但後端 update 刻意不寫 user_name——改了帳號名按確定會顯示「更新成功」而該欄實際未變（欄位級靜默 no-op）（`base-web/src/views/manage/user/modules/user-operate-drawer.vue`）
  - 證據：drawer 模板 :149-151 NInput v-model model.userName 無 :disabled="operateType==='edit'"；後端 build_update_active_model（sys_user.rs:286-311）明註「user_name/password 刻意不重設」＝FR-004 由後端強制、正確。惟 FR-011 精神（不得僅顯示成功提示而未實際異動）在此單欄上有 UX 縫隙；基線輪（REVIEW-001-019 PASS）已含此 as-built、非 020-025 回歸
- **[Observation]** addUser 預設初始密碼寫死 "123456"、不經 024 password_policy 驗證——admin 若設較嚴密碼政策，新建帳號初始密碼即不合政策（`rust-api/server/src/handler/system_manage.rs`）
  - 證據：system_manage.rs:1180 hash_password("123456")；grep validate_password_complexity 在 system_manage.rs 零命中。符合 009 假設「預設初始密碼沿既有約定」與 024/025 拍板 scope（024=dormant 原語、025 只 enforce 自助改密碼），非缺陷；登記為未來 gap（初始密碼政策化／強迫首登改密）候選

**Supersessions（合法推翻、非缺陷）**：
- 014-auth-token-session：getUserList/UpsertReq 增 sessionPolicy 欄（inherit/on/off、handler 守門非法→2222 biz.user.invalidSessionPolicy）＋updateUser 停用（status==2）/deleteUser/batchDeleteUser 後 revoke_user_sessions 即時撤 session（system_manage.rs:1229-1233/1265-1269/1297-1301、warn-not-fatal）——009 FR-009 明示「即時逐出 NOT in scope」、014 U3/U4 兌現（DECISIONS §2 014）
- 023-list-column-sort：getUserList 增 sort query（parse_sort_spec＋resolve_sort 6 欄白名單、非法→2222 biz.common.invalidSort）＋前端 useTableSort/SortClearButton＋6 sorter 欄頭（CDP 實見 sorterHeads=6）——依據 MILESTONES §1 023、空 sort 走 Id desc byte-identical 現況
- 010-menu-management MW(b) retroactive：user 頁寫入鈕 hasAuth('user:*') 顯隱 gating——009 FR-011 明示延 Menu 刀、010 兌現（DECISIONS §2 010 D1）
- 016-button-endpoint-policy D5：index.vue 讀端改用 fetchGetUserListRev3 honest 讀型 wrapper（frozen system-manage.ts fetchGetUserList 型謊不動）——依據 DECISIONS §2 016
- 025-user-center：sys_user facade 新增 update_own_profile/change_own_password 自助窄寫（不動 009 admin CRUD 路徑）——009 假設「改密碼為獨立未來功能」由 025 兌現；M-3 update status 改 Some-only 條件 Set（省略不覆寫 NULL、鏡像 sessionPolicy）屬 correctness 修正非行為漂移

**Live 證據**：curl getUserList（共用 Super token、帶全空 filter 模擬 axios）：code 0000、3 筆（User/Admin/Super）、total=3、records 各含 userName/userGender/nickName/userPhone/userEmail/status/userRoles、零 password 欄（FR-001/002/SC-002）；userName=us 模糊→僅 User、total=1（大小寫不敏感部分比對）；current=99 超範圍→records=0 但 total=3（FR-001 超範圍頁）。curl getAllRoles→3 啟用角色 {id,roleCode,roleName}（FR-008）。無 token/壞 token→code 3333 data:null（未認證守門、不洩資料）。psql sys_user：id 1-3 active＋6 筆 zztest 軟刪列（deleted=t）、list 正確排除軟刪（FR-005 軟刪語意活體證）；sys_operation_log entity_table='sys_user' 27 列（FR-006 審計軌存在）。寫端 add/update/delete 未觸發（非破壞紀律）——由實碼＋009 收刀 C-V（MILESTONES：62 bin＋7 live --test-threads=1、C-V-9~11 CDP 真發 add/edit/delete/batch、C-V-13 零回歸、C-V-14 prod build 全綠）背書。

**CDP 證據**：CDP（tab AAB4D0C2、Super 登入態）navigate /manage/user：表格 3 列渲染、欄頭全中文（序号/用户名/性别/昵称/手机号/邮箱/用户状态/操作）、hasRawKey=false（i18n 非 raw key）、新增鈕在、sorter 欄頭 ×6（023 supersession 實見）。開「新增」drawer：標題「新增用户」、8 欄位（用户名*/性别/昵称/手机号/邮箱/用户状态*/用户角色/会话策略）、角色下拉存在（getAllRoles 餵）、無 raw key；未按確定、關閉成功（drawerVisible=false）。搜尋端到端：展開 NCollapse 搜索、輸入 userName=dm、按搜索→Network 實抓 GET /proxy-default/systemManage/getUserList?current=1&size=10&status=&userName=dm&userGender=&nickName=&userPhone=&userEmail=（空字串守門真實觸發形）→表格縮為 1 列 Admin（SC-001 瀏覽器證）；清空後再搜→回 3 列（全空回全部、非零筆）。附帶觀察：重置鈕清表單不重查（見 findings）。

---

## 010-menu-management — ✅ PASS

**FR coverage**：FR-001✔（get_user_routes DB-fresh roles+casbin menu 過濾+祖先包含組樹 route.rs:38-58；live curl+內建 C-V-4 live smoke 斷言三角色子集鏈）；FR-002✔（route store initAuthRoute 失敗→authStore.resetStore 導登入 index.ts:232；builtin 常數路由保留 a59c2738；瀏覽器破壞性驗證不重跑、引 010 C-V）；FR-003✔（list_all 含已刪+deleted 旗標 vs list_active 側欄排除；live 兩端點對照）；FR-004✔（route_name partial-unique+map_menu_write_err 23505→biz.menu.duplicateRouteName、含並發競態不外洩 5000；寫端引 C-V）；FR-005✔（reparent_check 4 守門→4 biz 碼 facade:653-681+handler:1924-1942；Ok(None)→notFound 不靜默）；FR-006✔（delete_check protected/hasActiveChildren+batch_soft_delete 同 txn 先全檢後刪整批拒 facade:878-943）；FR-007✔（restore 孤兒→頂層 D5+cd5221a 撞名 2222 補強）；FR-008✔（全寫端經 mutate_in_txn 同 txn op-log）；FR-009✔（8 端點 require_policy main.rs:315-370+auth-only getUserRoutes+未認證 3333 live 實證+lint 守恆）；FR-010✔（rev3-system-manage.ts honest wrapper、CDP 頁面實載 DB 78 選單、i18n 零 raw key）；FR-011✔（menu+user 頁 hasAuth 三碼；settings skip＝plan Q2 surface 授權遞延）；FR-012✔（010 零 migration；020-025 未動 menu facade/route.rs；user 頁 CDP tab 原駐留正常）。SC-001~011 全數由碼+live/CDP+010 C-V 組合覆蓋、無缺口。

**Strengths**：
- 核心選單域碼自基線後零漂移：rust-api facade sys_menu.rs 最後觸碰＝016（44ffd4f、僅加 button registry 讀端）、route.rs 最後觸碰＝010 U1（1369ca1）——020~025 六刀未動 reparent_check 3+1 守門、delete_check（protected/hasActiveChildren）、batch_soft_delete（同 txn 先全檢後刪、整批拒）、restore 孤兒→頂層邏輯
- post-baseline 修復 cd5221a（M010-01）把 restoreMenu 的 route_name 撞名 23505 從裸 ? 外洩 5000 改攔成 2222 biz.menu.duplicateRouteName（handler/system_manage.rs:2036-2041 有明確設計註）——比基線態更貼 spec『業務衝突不以系統內部錯誤呈現』精神
- FR-009 授權接線完整：8 個 /systemManage/*Menu* 端點逐一 require_policy（main.rs:315-370）、getUserRoutes DB-fresh roles 不信 claims.roles（route.rs:42-43、§I.3）、endpoint_coverage_lint 涵蓋全部 menu 端點（tests/endpoint_coverage_lint.rs 20 處命中）
- route.rs 內建 C-V-4 live smoke（live_get_user_routes_per_role_filtering）斷言三角色嚴格子集鏈 super⊋admin⊋user 且 R_USER_COMMON 零 manage_*——與本次 psql 實測 casbin v2='menu' 分佈（R_SUPER 77／R_ADMIN 5／R_USER_COMMON 3）一致
- FR-011 hasAuth gating 三頁落實可 grep 驗證：menu 頁 menu:add/edit/delete（index.vue:185-216,311-312）＋user 頁 retroactive user:add/edit/delete（user/index.vue:151-223）、皆帶 rev3-inline 標記
- ⚠️r id 兩域紀律活體證實：getUserRoutes 路由域 id 為 string（"1"、"52"）、getMenuList/v2 管理域 id 為 number（1、18），與 spec Assumptions 識別碼拍板一致

**Findings**：無

**Supersessions（合法推翻、非缺陷）**：
- route store 合併 builtin 常數路由（login/403/404/500、base-web commit a59c2738、store/modules/route/index.ts:163 rev3-inline 標記）——010 自身 clarify option1 授權，基線 REVIEW-001-019 supersession map 已載、非漂移
- system-settings 頁按鈕 hasAuth gating skip（該頁至今零 hasAuth）——plan.md Q2 surface 明載：system-settings:* button code 未 seed＋頁面 R_SUPER-only 選單已隱→gating moot、不為 moot 破零-migration；spec Assumptions／OUT scope 明文『按鈕碼未就緒者延後』授權此路徑，非缺口
- batchDeleteMenu 對 parse 失敗/查無 id 採缺漏 skip（M-2 idempotent no-op sentinel own-cut）——§3.H 已知遞延、依審查指示非 finding
- getConstantRoutes 現回 []（seed 無 constant 選單）——spec Assumptions『常數頁前端寫死』明文授權，login/404/403 為前端 builtin、非缺件

**Live 證據**：psql：sys_menu 共 78 列、protected 8、deleted_at 非空 0（與 focus 預期 78/8 吻合）。casbin_rule v2='menu' 角色分佈 R_SUPER 77／R_ADMIN 5／R_USER_COMMON 3（R_SUPER 未授的唯一 active route_name='manage' 目錄——由 build_user_route_tree 祖先包含自動帶入、live getUserRoutes 實回含 manage 子樹、by design）。curl（Super token）：GET /systemManage/getMenuList/v2 回統一清單樹（含 deleted:false 旗標、id number、CommonRecord 欄）；GET /route/getUserRoutes 回 {routes,home:"home"}（id string、meta 完整）；GET /route/getConstantRoutes（public 無 token）回 []；GET /route/isRouteExist?routeName=home 回 true。未認證/壞 token 打 getMenuList/v2 均回 3333 auth.token.expired、data:null 零洩漏（FR-009 未認證路徑實證）。非超管 403（FR-009/SC-008）與全部寫端 FR-004~008（新增/搬移/刪除/復原/審計原子）依非破壞紀律不實打——以實碼（reparent_check 4 變體→4 biz 碼、delete_check、mutate_in_txn 同 txn 審計、map_menu_write_err 23505→2222）＋010 收刀 C-V 全綠聲明（MILESTONES、含 C-V-4 三角色 live smoke）佐證。

**CDP 證據**：CDP（pageId AAB4D0C2…、Super 登入態）navigate http://127.0.0.1:31080/manage/menu：頁面可達（url 確認）、.n-data-table tbody 渲染 11 個頂層樹列（78 選單樹狀折疊、首列 home/document/function/alova/plugin 與 DB 一致）、每列含 名稱/類型（菜单/目录）/路由/狀態（启用）/回收桶狀態欄（现役）/操作鈕（新增子菜单/编辑/删除——Super hasAuth 全顯）、header 見 新增/批量删除 鈕；全頁 body innerText 掃 raw i18n key（route.*/page.*/common.*）零命中＝i18n 非 raw key。註：spec FR-003 設計＝統一清單（已刪/現役同列、狀態欄區分）而非分離「回收桶 tab」，頁面實作與 spec 一致；現庫 0 筆已刪選單、已刪列渲染態無法活體目視（碼路徑 build_management_list_tree 含已刪＋restore 鈕分支 index.vue:182-205 靜態證實、010 收刀 C-V 已覆蓋）。截圖 scratchpad/menu-page.png。

---

## 011-role-management — ✅ PASS

**FR coverage**：FR-001 ✓（live：分頁/三 filter/空字串略過/軟刪排除；023 sort 為合法擴充）；FR-002 ✓（碼：map_role_write_err 23505→2222 duplicateRoleCode 禁裸 ?；baseline C-V-2 live 綠）；FR-003 ✓（碼：update id parse 失敗/查無→2222 notFound 不靜默、code/home 不動）；FR-004 ✓（碼：role_delete_guard 純函式三守門＋5 單測、批次 Phase1 全檢整批拒；020 後同 txn archive-move 強化；baseline live role_guard 綠；窄 TOCTOU 記 Observation）；FR-005 ✓（碼：set_role_dimension DB-first＋protected-reject＋reload_and_publish〔015 supersede〕；live getRoleMenu 讀端＋CDP modal 勾選現值；baseline role_menu_loop 閉環綠）；FR-006 ✓（live getRoleHome＝psql home 欄；set_home 原子；baseline F2 CDP redirect 綠）；FR-007 ✓（main.rs :398-473 九端點 per-route require_policy〔getRoleList R_SUPER+R_ADMIN、其餘 R_SUPER〕、DB-fresh roles 非憑證宣告；live 未認證 3333 零洩漏；baseline C-V-7 Admin/User 5003）；FR-008 ✓（碼：全寫端 mutate_in_txn 同 txn op-log before/after＋IP 四欄）；FR-009 ✓（index.vue hasAuth role:add/edit/delete 三處＋CDP Super 正向實見）；FR-010 ✓（零 migration 收刀紀錄＋020/023 演進亦零 schema；CDP /manage/role 與讀端全綠＝無回歸跡象）。SC-001/004/006/009 本輪補上基線缺的 browser 實測（CDP 渲染/modal/在地化）；SC-002/003/005/007 寫端引 baseline C-V 全綠紀錄；SC-008 lint 檔現況實讀確認；SC-010 live 讀端＋psql 佐證。10/10 FR 覆蓋、0 缺口。

**Strengths**：
- 刪除守門為純函式 seam（rust-api/server/src/handler/system_manage.rs:869 role_delete_guard，seeded→in-use→self 固定序）＋5 支單元測試（:3332-3390）；批次採 Phase1 全 ids 先檢、任一中即整批拒、Phase2 才進單一 txn batch_soft_delete（FR-004 no-partial 忠實）
- casbin 選單指派全程 DB-first：update_role_menu（:1518）走 facade::sys_casbin_rule::set_role_dimension（mutate_in_txn 直寫 entity::casbin_rule＋同 txn op-log＋②protected-reject→biz.role.menuProtected），零 MgmtApi；020 後補 RoleDeleted→notFound 鏡像、015 後 Applied（含空-diff）必 reload_and_publish 跨副本
- 23505 撞碼防 type-lie：add/update 皆經 map_role_write_err（:841 sql_err→UniqueConstraintViolation→2222 biz.role.duplicateRoleCode）、禁裸 ?，含並發競態不以 5000 呈現（FR-002）
- M-6 no-escalation 強化（:1534）：非 R_SUPER operator 指派選單須 ⊆ 自身 effective menu set，否則 biz.role.grantExceedsOwn（zh-cn/en-us locale 鍵齊）——spec 之上的合法安全強化
- 前端接線完整：index.vue hasAuth('role:add/edit/delete') 鈕層顯隱（FR-009）、menu-auth-modal 真發 fetchGetRoleMenu/UpdateRoleMenu/GetRoleHome/UpdateRoleHome（原 stub 註記保留）、biz.role.* 9 鍵 i18n 雙語齊備
- 審計原子性一致：create/update/set_home/soft_delete/batch_soft_delete 全走 mutate_in_txn（facade/sys_role.rs），before/after payload＋operator IP confidence 四欄同 txn（FR-008）；020 起 soft_delete 內 FOR UPDATE 鎖列＋同 txn archive-move 該 code 全維 casbin

**Findings**：
- **[Observation]** 角色刪除守門（in-use/self）讀取在 txn 外、facade soft_delete txn 內未重判，存在「守門通過後、軟刪 txn 前」被並發指派使用者的窄 TOCTOU 窗，可能留下指向已刪角色的孤兒 user-role 指派（`rust-api/server/src/handler/system_manage.rs`）
  - 證據：delete_role :1425-1431：count_users_by_role_id/roles_of_user 在 facade::sys_role::soft_delete（mutate_in_txn＋FOR UPDATE）之前執行且鎖內不重跑 role_delete_guard；020 的 lock-then-redecide（DECISIONS §1 P-011-1 拍板 A）只涵蓋四類 casbin 寫端（delete/batch/restore/grant）與 sys_role 列序列化，未覆蓋 sys_user_role 指派寫端 vs 刪角色的競態。窗極窄、後果為孤兒指派（非提權），且 spec FR-004 未明文要求指派-刪除序列化——記疑點非缺陷
- **[Observation]** updateRoleHome 對 home 值不做任何後端驗證（任意字串皆寫入），僅靠前端 NSelect 下拉限制頁面集合（`rust-api/server/src/handler/system_manage.rs`）
  - 證據：:1599-1617 直接 facade::sys_role::set_home(req.home) 落庫；R_SUPER-only 且登入導向對未知 route 會走前端 fallback，風險低。spec FR-006 未要求驗證、baseline C-V F2 首頁 redirect 已實測綠——體驗層疑點非 FR 違反

**Supersessions（合法推翻、非缺陷）**：
- 015-policy-governance：updateRoleMenu 由 011 原「reload-on-changed」改為「Ok(Applied、含空-diff)→無條件 reload_and_publish（load_policy＋PUBLISH casbin:policy:invalidate 跨副本）」——依據 MILESTONES §1 `65f1838`（明載「調整 011、user 2026-06-22 確認」）、handler :1516 註記 §4.2 ③④
- 016-button-endpoint-policy：spec Assumptions「按鈕權限鈕留示意 mock、端點權限介面屬後續」→ 016 un-mock button-auth-modal＋新建 endpoint-auth-modal（role-operate-drawer 三鈕 CDP 實見：菜单权限/按钮权限/接口权限）；另 getRoleList 前端改用 fetchGetRoleListRev3 honest wrapper（typings fold-in D5/U5）——依據 MILESTONES §1 `fa17def`
- 020-role-delete-policy-archive：推翻 011 data-model「軟刪殘留選單授權策略無害」前提（spec Edge Case 同述）——deleteRole/batchDeleteRole 現於同 txn FOR UPDATE 鎖列＋archive-move 該 code 全維 casbin 授權（reason=role_soft_delete、不可復原 denylist）＋archived>0→reload_and_publish；updateRoleMenu txn 內鎖 sys_role、角色已刪→SetDimensionError::RoleDeleted→notFound——依據 DECISIONS §1 P-011-1（✅ 已決、user 親決 2026-06-27、明載「推翻 specs/011 data-model 前提、已加 as-built 註記」）＋MILESTONES `ebbd6c2c`
- M-6 no-escalation 守門（update_role_menu :1534 grantExceedsOwn）：011 spec 未要求、後續安全強化（非 R_SUPER 只能授自身 effective menu 子集）——合法強化非漂移（review 任務書明示）
- 023-list-column-sort：getRoleList 加 sort 參數（parse_sort_spec＋facade resolve_sort 白名單、biz.common.invalidSort）——023 列表欄位排序刀之合法擴充（CLAUDE.md §6 已收刀記錄）

**Live 證據**：curl（Super token、31080/api）：getRoleList 空 filter（?roleName=&roleCode=&status= 模擬前端空字串）→0000、恰 3 active 角色（R_SUPER/R_ADMIN/R_USER_COMMON），psql sys_role 實有 8 列（5 列 deleted_at 非 NULL 之測試殘留角色全被 find_active 過濾＝FR-001 空條件略過＋軟刪不現身雙證）；roleCode=R_ADMIN filter→僅 ['R_ADMIN']；getRoleMenu?roleId=2→0000 data=[1,6,8,3,7]（menu id 集、讀端閉環）；getRoleHome?roleId=2→0000 "home"（NULL→預設 home 對齊 psql home 欄）；getRoleMenu?roleId=999→2222 biz.role.notFound（不靜默）；未帶 token→3333 auth.token.expired、零資料洩漏（FR-007 認證守門）。寫端（add/update/delete/updateRoleMenu/updateRoleHome）依非破壞紀律不打，驗證＝實碼＋收刀 C-V 紀錄：MILESTONES §1 `b80c9e3` 載 C-V-0~11 全綠（live role_crud/role_guard/role_menu_loop〔DB-first 讀寫閉環＋protected-reject＋原子審計〕/role_home、C-V-7 Admin/User→5003、prod build）；endpoint_coverage_lint（server/tests/endpoint_coverage_lint.rs:132-138）現仍列 9 個 role 端點於治理清單（SC-008 守恆）。

**CDP 證據**：CDP（tab AAB4D0C2…、Super 登入態）：navigate /manage/role 等 4.5s→url 正確、表格 3 列（首列「1 超级管理员 R_SUPER 启用 编辑/删除」）、表頭「序号/角色名称/角色编码/角色描述/角色状态/操作」全在地化、hasRawKey=false（無 page.*/biz.*/backend.* raw key）、新增鈕可見（Super hasAuth role:add＝FR-009 正向）。點首列「编辑」→drawer 開、鈕組＝[菜单权限, 按钮权限, 接口权限, 取消, 确认]（016 三維演進實見）→點「菜单权限」等 3.5s→menu-auth modal：樹 11 節點渲染（home/document/function/alova/插件示例/…）、10 個勾選盒反映當前指派（getRoleMenu 現值回填）、首頁 NSelect 顯 "home"（getRoleHome 接通）、modalRawKey=false。以「取消」關 modal＋drawer、全程零保存零寫端（非破壞紀律守住）。

---

## 012-audit-log-query — ✅📝 PASS_WITH_NOTES

**FR coverage**：FR-001 ✓（sys_menu 77＋CDP 單頁三分頁）；FR-002 ✓（facade order_by_desc(created_at).desc(id)＋live 首筆最新＋CDP 翻頁 total 正確）；FR-003 ✓（live+CDP access tab）；FR-004 ✓（live+CDP login tab）；FR-005 ✓（enrich 走不濾 deleted_at 的 names_for_ids；null→null live 實證；「已刪者仍顯名」當前資料無已刪 operator、以碼＋012 C-V 佐證）；FR-006 ✓（CDP expand payload before/after）；FR-007 ✓（fuzzy IP/名/文字＋精確 operation/success/http_status＋日期範圍全 live 實證＋UI 表單 CDP 實證）；FR-008 ✓（空 param 全略過、無匹配回空頁）；FR-009 ✓碼+政策態（casbin 僅 R_SUPER、require_policy 依 DB 當下角色；403 活測受只讀紀律限制未跑、引 C-V）；FR-010 ✓（查詢前後 op-log count 恆 139；access-log 由基建照記＝spec 明文允許）；FR-011 ✓（wire Option 無 skip_serializing_if、live null 忠實）；FR-012 ✓（索引/選單/政策在位；可逆性引 012 收刀 migration down→up 證據；020-025 六刀後本 feature 零踩壞——023 sort、017 增強均為有據演進）。SC-001~004/006/007/009 本輪活體+CDP 直證；SC-005 政策態+碼證；SC-008 由後續各刀收刀 C-V 累積背書。

**Strengths**：
- 三端點空字串守門完整且分層清晰：normalize_str_filter（Some("")→None）＋parse_entity_id/parse_http_status/parse_success/parse_audit_date 各自守門，live 帶 9 個空 param 全回 200 且 total 與 psql 全表計數逐一相等（op-log 139=139、login 357=357、success=false 101=101）——FR-008「空條件略過」實測零漂移
- operator 名解析審計正確性到位：facade sys_user::names_for_ids／ids_for_name_ilike 明確標註「★不濾 deleted_at」（rust-api/server/src/model/facade/sys_user.rs:570-609），live 驗 NULL operator 忠實回 null（(None,None,'Super')）、按名無匹配回空頁 total 0 而非錯誤
- IP 模糊比對用 host(col)::text LIKE 去 /32 mask＋escape_like 防 wildcard 注入＋綁定值防 SQL 注入（sys_operation_log.rs ip_host_like），live 驗片段 172.23 命中 2169、9.9.9 命中 0
- 唯讀契約實證：全輪查詢前後 sys_operation_log count 恆 139（SC-006 零新增）；payload 展開含 password:"<redacted>"（005 redact 貫穿到讀端）
- 授權面 DB 態乾淨：casbin_rule 三條 p 全為 R_SUPER×GET、無多餘授權；main.rs 三 route 均掛 require_policy（依系統當下角色 enforce）
- CDP 全鏈綠：/manage/audit 三分頁渲染（操作异动/API 存取/登录尝试）、每 tab 10 列、分頁「共 357 条」與 psql 一致（SC-009 無假資料）、table body 高 301px（2026-06-24 flex-height 修復有效、非塌 0）、展開見异动前/后 JSON、搜尋摺疊展開後 8 input+3 select+daterange+重置/搜索/导出/清除排序全在、全程無 raw i18n key
- SC-007 效能遠超標：三端點 curl 實測 10~12ms

**Findings**：
- **[Minor]** 非日期欄位的畸形輸入錯誤訊息誤導：httpStatus/success/entityId 解析失敗時復用 biz.audit.invalidDateRange，前端 toast 會顯示「日期范围格式不正确」而非對應欄位的錯誤描述（`rust-api/server/src/handler/system_manage.rs`）
  - 證據：parse_http_status（:643-652）、parse_success（:668-676）、parse_entity_id（:681-690）皆 Err(AppError::Biz(Cow::Borrowed("biz.audit.invalidDateRange")))，碼註自陳「復用 2222 形」＝刻意；live 實測 entityId=abc 回 {"code":"2222","msg":"biz.audit.invalidDateRange"}，zh-cn locale 該鍵＝「日期范围格式不正确」（base-web/src/locales/langs/zh-cn.ts:963）。實務影響極低（entityId 走 NInputNumber、operation/success 走下拉，正常 UI 打不出非法值、僅 curl/手改 query 觸發），但訊息語意錯置、值得未來刀順手補專屬 key

**Supersessions（合法推翻、非缺陷）**：
- 017-audit-center-enhancement：012 單值 filter → 加 http_status_class 類別 quick-filter（2xx/4xx/5xx 半開區間、無法識別→略過不報錯）＋三表 CSV 匯出（export 旗標、CAP 10000、後端 truncated 旗標）＋op-log CSV rolesBefore/After 欄——REVIEW-001-019 supersession map 明載（017 C-1/C-3）、MILESTONES 有據
- 023-list-column-sort：三 query DTO 加 sort 參數＋facade resolve_sort 白名單（防注入、未命中→biz.common.invalidSort）＋前端 SortClearButton；export 共用 list 呼叫故 CSV 自動反映排序（023 FR-016）——有 MW(f) 拍板依據
- 013-xff-real-ip-forensics：012 spec 假設「讀現有 IP 欄」（D11 延後），013 落地四欄 forensic（real_ip/peer_ip/ip_confidence/x_forwarded_for）後 012 讀端與 UI 演進為顯示/篩選四欄＋七態 confidence tag（ip-confidence-tag.tsx）——013 spec＋m006 可逆 ALTER（⚠️ac）有據
- 2026-06-24 flex-height 修復：012 起 latent 的 NTabs 內 table body 塌 0px bug 已於 index.vue 補 flex 鏈修復（rev3-inline fix 註記完整）；本輪 CDP 實測 body 高 301px、有資料列正常顯示＝修復有效
- §3.E audit_query live 測已 marker 化（CHECKLIST 歸檔紀律、非行為漂移）

**Live 證據**：共用 token 已過期（3333），改自 CDP tab localStorage 讀取現行 SOY_token（未執行任何 login）。① 三端點帶 9 個空字串 param（operatorName=&realIp=&entityId=&httpStatus=&success=&createdFrom=…）全回 0000、非 400、total 與 psql 全表計數逐一相等：op-log 139=139、login-attempt 357=357。② filter 語意：realIp=172.23 命中 2169（樣本 realIp=172.23.0.13）、realIp=9.9.9 → total 0 空頁非錯誤；operatorName=upe → 138 筆全 Super、zzzzqq → 0；success=false → 101（=psql 101）且列 flag 全 false；createdFrom/To=2026-07-02 → 13 筆僅當日。③ 畸形輸入不靜默：entityId=abc、createdFrom=notadate 均 2222 biz.audit.invalidDateRange。④ NULL operator 忠實 null：(operatorId=None, operatorName=None)。⑤ 唯讀：整輪查詢後 sys_operation_log count 仍 139。⑥ 授權 DB 態：casbin_rule 三條 p 全 R_SUPER；sys_menu id=77 /manage/audit status=1。⑦ 索引在位：idx_{operation,access,login}_*_created_at／operator_time／ip_time／user_time。⑧ SC-007：三端點 10~12ms。非超管 403（FR-009/SC-005）未活測（無非超管 token、禁自行 login）——以 casbin 政策列＋require_policy 碼＋012 收刀 C-V 全綠紀錄佐證。

**CDP 證據**：tab AAB4D0C2CDE633D7A403FE31ACB47168 navigate http://127.0.0.1:31080/manage/audit（Super 登入態）。① 頁可達、card 標題「审计中心」、三分頁 tab 渲染＝['操作异动','API 存取','登录尝试']。② 操作异动 tab 10 列、表頭［时间/操作者/操作类型/资料表/识别码/可信度/直连 peer/真实 IP/转发链］、首列＝2026-07-02T01:08…Super UPDATE sys_user 1（與 API 首筆一致、新到舊）。③ 切 API 存取 tab → 10 列（首列 GET /systemManage/getOperationLog 200）；切登录尝试 tab → 10 列、分頁「共 357 条」＝psql 357（SC-009 真後端）。④ table body clientHeight=301px（非塌 0＝flex-height 修復有效）。⑤ FR-006：點 expand trigger（10 個）→ 展開見「异动前」＋完整 JSON（password 顯 <redacted>）。⑥ FR-007 UI：點「搜索」摺疊展開 → 8 NInput＋3 select＋daterange picker、標籤全在地化［资料表/操作类型/操作者/可信度/直连 peer/真实 IP/转发链/识别码/追踪码/时间］＋重置/搜索/导出/清除排序鈕。⑦ 全程 document.body 無 'page.manage.audit' raw key（i18n 非 raw）。未按任何保存/寫端鈕（expand/collapse/切 tab 均純讀 UI）。

---

## 013-xff-real-ip-forensics — ✅ PASS

**FR coverage**：FR-001（rightmost-untrusted+peer-gate）✅ 碼 audit_ctx.rs:144-207+12 案例純測+live Direct 16 列；FR-002（IIS 雜訊）✅ normalize_xff_tokens 7 測（+/port/zone/bracket/garbage/cap32）；FR-003（TrustModel operator 設定+安全預設）✅ config.rs parse_trust_model per-set fail-safe 清空+load_trust_model 缺檔 fallback flat env→all-direct、dev/prod compose 皆 wire；FR-004（七態 confidence）✅ enum+as_str 契約、live 三表分布含 4 態；FR-005（CF 交叉驗證不覆蓋 real_ip）✅ apply_cf_overlay 矩陣測+碼 real_ip 恆不變、正向 cdn_verified 需真 CF 源＝引用收刀 C-V（案例 2/9B 測+C-V CF 正向全綠聲明）；FR-006（四欄×三表）✅ live psql 14 欄+樣本、op-log 4/4 AuditMeta threading（to_audit_meta audit_ctx.rs:90-101）；FR-007（Tunnel）✅ 案例 11 測+022 apply_tunnel_fallback 窄化擴充（⚠️ae）+trust-model.toml tunnel 段；FR-008（審計中心四欄顯示+篩選）✅ CDP 表頭序/NTag 在地化/三模糊一下拉+curl 四篩+空字串守門 live；FR-009（geo 保留+可逆）✅ region live 2213 列+m006 down 嚴格反序（up→down→up 引用收刀 C-V、本輪非破壞紀律不重跑）；FR-010（CF 判定不可偽造）✅ 本輪 live negative 重現（proxy_clean≠cdn_mismatch）。SC-001/005/006＝純測碼在位+引用 C-V 全綠（rust 紀律不跑 cargo）；SC-002 live 探針佐證（邊界內例外屬 spec 明文前提）；SC-003/004/007 本輪 live 直證。

**Strengths**：
- 解析核心完整無回歸：rust-api/server/src/audit_ctx.rs 的 resolve_client_ip 三分支（peer-gate Direct／Tier-1 CDN 位置錨 CdnAnchored／Tier-2 逐點 skip ProxyClean/ProxySoft/Fallback）+ normalize_xff_tokens IIS-aware + apply_cf_overlay（real_ip 不變、只動 confidence）純函式與 research §4 全 12 案例純測、7 normalize 測、overlay 矩陣測在 025 收刀後 HEAD 全數在位、且新增 apply_tunnel_fallback 4 測（022 合法擴充）
- FR-010 偽造中和本輪【live 實證重現】：帶 X-CF-Verified:1 + CF-Connecting-IP:9.9.9.9 + XFF:6.6.6.6 經 nginx 打 getUserList，psql 讀回 ip_confidence='proxy_clean'（非 cdn_mismatch/cdn_verified）＝nginx geo($remote_addr) 覆寫 X-CF-Verified→0、map strip CF-Connecting-IP 全鏈有效（deploy/nginx/nginx.conf:46,77 + conf.d/_locations.inc:29-30 仍在位）
- 四欄鑑識×三表 live 全綠：sys_access_log/sys_login_attempt（real_ip/peer_ip/ip_confidence/x_forwarded_for/region）+ sys_operation_log（operator_ 前綴四欄）14 欄 information_schema 全在；三表皆有非空樣本值（access fallback 2193/direct 16、login proxy_clean/fallback/direct、oplog fallback 123＝op-log 4/4 AuditMeta threading 仍活）；歷史列 confidence NULL＝spec「不回填」如實
- 篩選端 live 全綠：ipConfidence=direct 精確篩回 16 列全 direct、realIp=6.6.6 模糊篩精準命中偽造探針列（四欄 wire honest 讀回）、operatorXForwardedFor 模糊篩 122 列、空字串四參數守門 total 2215 不誤篩 0 列（US3-3）
- CDP 實測（基線輪未做）：/manage/audit 表頭依 C2 拍板序「可信度→直连 peer→真实 IP→转发链」、confidence NTag 渲染在地化文字「退回」非 raw key（rawKey=false）、三模糊 NInput（直连 peer/真实 IP/X-Forwarded-For）+ 下拉 select 皆在、10 列渲染
- m006 嚴格可逆結構仍在（rename 先、add 後；down 反序 DROP IF EXISTS→RENAME 回）、m007~m009 後續 migration 未觸三表鑑識欄；region 推導零回歸（xdb 續餵 real_ip、access_log 2213/2215 列有 region、樣本「内网IP」）

**Findings**：
- **[Observation]** 信任邊界內客戶端可自注 XFF 令任意 IP 以【高可信 proxy_clean】被歸因（本輪探針實測 real_ip=6.6.6.6/proxy_clean）——屬 spec 明文部署前提盲區、非缺陷，但 confidence 落 proxy_clean（高）而非低可信態值得留意（`rust-api/server/src/audit_ctx.rs`）
  - 證據：live 探針：host（docker gw 172.23.0.1 ∈ internal_default）經 nginx 帶 X-Forwarded-For:6.6.6.6 → psql sys_access_log id=2213: real_ip=6.6.6.6, peer_ip=172.23.0.13, ip_confidence=proxy_clean, xff='6.6.6.6, 172.23.0.1'。Tier-2 由右而左 skip 內網跳後 6.6.6.6 為第一個非我方 IP＝ProxyClean（audit_ctx.rs:196-202）。spec Edge Cases「主防線是網路層阻擋直連＝部署前提」+ SC-002 明文條件「在網路信任邊界未被破壞的前提下」；dev 拓樸 internal_default 含 172.16.0.0/12 使 dev host 天然在邊界內，設計如實。無需改碼；若未來要區分「XFF 由受信 proxy 轉述 vs 邊界內自報」需新訊號源，屬新 feature 範疇
- **[Observation]** 共用 review token 已過期（3333 auth.token.expired），本輪改由 CDP tab localStorage SOY_token 取活 token 完成取證——記錄供後續 reviewer 參考，非 feature 缺陷（`rust-api/server/src/handler/auth.rs`）
  - 證據：curl /api/auth/getUserInfo with scratchpad review-token.txt → {"code":"3333","msg":"auth.token.expired"}；改用 CDP Runtime.evaluate 讀 tab AAB4D0C2... 的 localStorage SOY_token 後全部 curl 取證通過（0000）。token 過期屬 014 rotation TTL 正常行為

**Supersessions（合法推翻、非缺陷）**：
- 022-ip-access-control（⚠️ae、merge d9c809e6）合法擴充：TrustModel 加窄欄 tunnel:Vec<IpNetwork>（config.rs:41、deploy/trust-model.toml tunnel=[127.0.0.1/32,::1/128] ⊂ internal_default 依鐵則註明）＋新純函式 apply_tunnel_fallback（audit_ctx.rs:295-308、B2 反偽造：peer∈internal 但∉tunnel 偽 CF-CIP 不採信、conf 維持 Fallback 不升）插 audit_mw resolve 後/overlay 前（audit_ctx.rs:366-367）、不改 resolve_client_ip 簽名——DECISIONS §1 ⚠️ae (D) 明文背書、4 純測在位
- pre-波4 polish sweep（MILESTONES 7e4ae19、rust worktree b63928b）：移除 Binding.dual_role net-dead 欄（只寫不讀；MyPublicEntry.dual_role 維持且 Tier-2 軟降邏輯完整 audit_ctx.rs:179-193）——MILESTONES 明載、合法清理非漂移
- 019/021 lockout 在 login sink 前加 gate 但 sys_login_attempt 四欄鑑識寫入不變（live 樣本 proxy_clean/fallback/direct 值齊、m008 只加 created_at index）；017 審計中心加 class filter/CSV 未動 013 四欄顯示序與 confidence tag（CDP 實證表頭序仍 C2 拍板序）

**Live 證據**：psql：三表 information_schema 14 鑑識欄全在（access/login: real_ip,peer_ip,ip_confidence,x_forwarded_for,region；oplog: operator_real_ip,operator_peer_ip,operator_ip_confidence,operator_x_forwarded_for）；值分布 access=fallback 2193/direct 16/NULL 2（歷史不回填）、login=proxy_clean 3/fallback 337/direct 16/NULL 1、oplog=fallback 123/NULL 16；region 2213/2215 列非空（「内网IP」xdb 續餵）。curl（活 token）：偽造探針 XFF:6.6.6.6+X-CF-Verified:1+CF-Connecting-IP:9.9.9.9 → sys_access_log id=2213 real_ip=6.6.6.6/peer=172.23.0.13/proxy_clean（≠cdn_mismatch ＝ FR-010 nginx 中和 live 重現）；ipConfidence=direct 精確篩 16 列全 direct、realIp=6.6.6 模糊篩命中探針列四欄 honest 讀回、operatorXForwardedFor=172.23 模糊篩 122 列、realIp=&peerIp=&ipConfidence=&xForwardedFor= 空字串守門 total 2215 不誤篩。conf：docker-compose.dev.yml:66,75 + prod.yml:42,45 皆 bind-mount deploy/trust-model.toml + TRUST_MODEL_FILE；toml 內 CF 官方 v4×15+v6×7 段 + tunnel ⊂ internal_default 鐵則註在位。

**CDP 證據**：tab AAB4D0C2CDE633D7A403FE31ACB47168 @ http://127.0.0.1:31080/manage/audit（Super 登入態）：表頭序=[时间,操作者,操作类型,资料表,识别码,可信度,直连 peer,真实 IP,转发链]＝C2 拍板四欄固定序（confidence→peer→real→xff、operation 分頁 operator_ 前綴）；confidence 以 NTag 渲染在地化「退回」（fallback 態、非 raw key，rawKey 掃描 false）；篩選區含三模糊 NInput（请输入直连 peer/请输入真实 IP/请输入 X-Forwarded-For）+ 2 個 NSelect（含 ipConfidence 下拉）；tbody 10 列渲染。三表共用 ip-confidence-tag.tsx（access/operation/login-attempt-table.vue 各 import renderConfidenceTag 實證）。另以 CDP Runtime.evaluate 取 localStorage SOY_token 供 curl 取證（共用 token 已過期）。

---

## 014-auth-token-session — ✅ PASS

**FR coverage**：FR-001 rotation 單次使用：PASS（rotate_locked_or_revoke mark_used WHERE status='active'＋live DB chain 72d09b66 used→active 實證）。FR-002 重放偵測撤整鏈：PASS（decide_rotation Reuse＋revoke_chain 不論原 status、pre-read/鎖後雙路共用 revoke_chain_and_logout 必回 8888）。FR-003 grace 雙擊容忍：PASS（GRACE_SECS=30 嚴格 <、邊界=30 判 Reuse、單元測全覆蓋）。FR-004 三態×全站解析：PASS（resolve_policy 純函式＋未知值保守 inherit＋5 測；live 全 user inherit）。FR-005 僅 On 施行：PASS（is_current effective Off 直接放行；live off 態多 active 鏈並存）。FR-006 全站設定＋逐 user 覆寫：PASS（getSystemSettings/getUserList wire curl 實證＋CDP drawer 会话策略 NSelect 渲染；updateUser sessionPolicy 寫端依收刀 C-V）。FR-007 停用/刪除即時撤銷：PASS（revoke_user_sessions 三掛點＋denylist iat<=revoked_at fail-secure＋revoke_all_user_chains 含 used）。FR-008 重新啟用恢復：PASS（iat<=revoked_at 邊界界定＝撤銷後新簽 token iat>revoked_at 放行、TTL=access_ttl 自動清）。FR-009 熱套用跨進程：PASS（updateSystemSetting PUBLISH settings:invalidate（system_settings.rs:122）→watcher 重載 AtomicBool）。FR-010 多副本＋重訂閱：PASS（rust-api-2 profiles:[multi] 仍在；watcher backoff 重訂閱＋後續 M-4 timeout/reconnect re-read 加固）。FR-011 cleanup dry-run/execute/冪等：PASS（main.rs 唯一 --execute 旗標、Postgres-side cutoff；live 讀 expired=37 可清）。FR-012 前端無縫/乾淨登出：PASS（.env 8888 LOGOUT/7777 MODAL_LOGOUT codes＋refresh handler 絕不回 3333；瀏覽器本輪自動輪替成功側證；兩通道 CDP 依收刀 C-V）。FR-013 fail-OPEN 韌性：PASS（is_current Err/None→Ok、denylist 無 handle/不可達放行、redis facade 全 best-effort）。FR-014/SC-010 零回歸零 migration：PASS with note（014 交付時 git diff migration/ 空；其後 m008 為 023 拍板 index-only、不動 014 表＝supersession 記錄）。SC-001~009 對應上述＋收刀 C-V 全綠記錄，無一退化。14/14 FR 覆蓋、0 缺口。

**Strengths**：
- rotation 決策核心（decide_rotation 四態＋GRACE_SECS=30 嚴格 < 邊界＋used_at NULL fail-closed）仍為純函式、零 entity 依賴，8 支單元測覆蓋全部邊界（rust-api/server/src/auth/session.rs:73-95）
- TOCTOU lock-then-redecide 完整保留：pre-read 僅作 fast router，Rotate/Benign 一律進 txn FOR UPDATE 鎖列後重判，鎖後翻 Reuse→revoke_chain+8888 絕不重鑄（handler/auth.rs:614-663），並有 live #[ignore] 測 toctou_revoked_chain_locks_to_reuse_no_rebake 斷言 active 列數=0（facade/sys_token.rs:141-218）
- denylist 同秒 fail-secure（iat<=revoked_at、enforce.rs:351-360）＋refresh public route 對稱雙查（handler/auth.rs:548）＋revoke_all_user_chains 含 used 列防 grace 重鑄繞撤（sys_token.rs:113-130），三個 014 設計時攔截的漏洞修補全數健在
- 020-025 六刀後零踩壞、反而有治理下的加固：M-4 subscribe_pubsub 5s timeout（redis.rs:176-197、防 watcher SYN 黑洞永久卡死）＋settings watcher reconnect 後補 re-read（main.rs:790-792、閉 backoff 窗口漏 invalidate）皆強化 FR-010 韌性
- live 實證 rotation 真實運作：sys_token 列 275（chain 72d09b66）status=used used_at=03:27:26 與列 277 同 chain active 同秒簽發＝真實 refresh 輪替事件；single_session_default=off＋uid 1 五條 active 鏈並存＝FR-005 Off 多 session 語意正確（006「永遠踢」bug 修復仍生效）
- revoke_user_sessions 三掛點完整（system_manage.rs:1230 updateUser status==2／:1266 deleteUser／:1298 batchDelete 逐 id）、warn-not-fatal 語意與 D-02 註解未被後續 user handler 改動（009→025 多次重構）破壞

**Findings**：
- **[Observation]** 共用 review token 已過期（curl 直打回 3333），需自 CDP 瀏覽器 localStorage 重取有效 token 才能完成讀端驗證——純環境現象、非產品缺陷，但後續 reviewer 會再撞（`specs/014-auth-token-session/spec.md`）
  - 證據：curl getUserList with scratchpad/review-token.txt → {"code":"3333","msg":"auth.token.expired"}；改用 CDP tab localStorage SOY_token 後同端點回 0000。瀏覽器 tab 的 auto-refresh 攔截器（FR-012）在 03:27:26 真實輪替出新對（sys_token 列 275 used → 277 active 同 chain），反而成為 FR-001 的活體證據
- **[Observation]** dev stack 過期 token 累積（37 列 expires_at 已過 cutoff）——cleanup-job sidecar 為 profiles:[prod] opt-in、dev 一般 up 不啟，累積屬 sanctioned 設計（§3.A 6e90865）而非缺陷；記錄供維運自覺（`rust-api/cleanup-job/src/main.rs`）
  - 證據：psql: select count(*) from sys_token where expires_at < now() - make_interval(secs => 60) → 37；docker-compose.yml:145 cleanup-job service profiles:[prod]。dry-run 預設/--execute 唯一旗標/冪等紀律碼面全數健在（main.rs:29,43-67）

**Supersessions（合法推翻、非缺陷）**：
- FR-014/SC-010「零資料庫結構變更」為 014 收刀時態聲明：其後 023 加 m008（idx_login_attempt_created_at index-only migration）有拍板依據（023 clarify Q1、MILESTONES §1 009901fa 明載「推翻 0-migration、不觸 §I.6」），且不動 014 相關表（sys_token/sys_user/system_settings 結構不變）——非漂移
- settings watcher／subscribe_pubsub 較 014 原碼有兩處加固：M-4 pub-sub 連線 5s timeout（4eb710c、pre-波4 hygiene sweep 有 MILESTONES 記錄）＋reconnect 後補 re-read single_session_default（§3.H 韌性、main.rs:790 註明）——行為只增強 FR-010、無語意反轉
- cleanup-job 增 push_metrics best-effort pushgateway 推送（018-observability U2、MILESTONES 有據），不改 dry-run/--execute/冪等三紀律；US5 排程面由 §3.A compose sidecar（6e90865）補完
- login handler 在 token 簽發前疊 019 失敗節流 gate＋021 Redis lockout L1＋022 IP gate（各自 feature 收刀記錄），均在 014 rotation/session 機制之外前置、不改 §4.1/§4.3 狀態機語意
- 006 single-session「永遠踢」→ 014 policy-aware is_current 本身即本 feature 的核心 supersession（DECISIONS §2 明載），基線輪已登錄

**Live 證據**：psql：sys_token 表形狀完整（token_hash varchar(64) NOT NULL／rotation_chain varchar(36)／status／issued_at/expires_at NOT NULL／used_at nullable）＝data-model 全欄就緒；status 分佈 active=80/revoked=170/used=21＝三態狀態機真實運轉；列 275（chain 72d09b66）used_at=2026-07-02 03:27:26 與列 277 同 chain active 同秒＝實測 rotation 事件（FR-001）。single_session_default=off＋全 user session_policy=inherit＋uid 1 多條 active 鏈並存＝FR-004/FR-005 Off 態正確。redis：SCAN revoked:* 與 sess:* 皆空（無近期停用/刪除、denylist TTL 已清＝預期）。curl（Super token）：getUserList 回 0000 且逐列 emit sessionPolicy:'inherit'（FR-006 wire）；getSystemSettings 回 0000 含 ('single_session_default','off')（FR-006/FR-009 設定面）；getUserInfo 0000 roles=[R_SUPER]（零回歸）。compose：rust-api-2 profiles:[multi]（docker-compose.dev.yml:108）＋cleanup-job sidecar profiles:[prod]（docker-compose.yml:145）皆在。寫端 FR（rotation reuse 撤鏈/踢除/停用撤銷/cleanup --execute）依禁寫紀律未真跑，引用 MILESTONES §1 3066bc2 C-V-0~10 全綠記錄（live rotation·single-session·denylist·cleanup·2-instance＋CDP 7777/8888 兩通道）＋in-repo live 測 toctou_revoked_chain_locks_to_reuse_no_rebake。

**CDP 證據**：CDP（pageId AAB4D0C2CDE633D7A403FE31ACB47168、Super 登入態）：navigate http://127.0.0.1:31080/manage/user → 表格渲染 3 列；點首列「编辑」開 user-operate-drawer → 斷言 hasSessionLabel:true（「会话策略」欄位渲染、現值顯示「继承」＝inherit 三態 NSelect 活著、FR-006 per-user 覆寫 UI）、hasRawKey:false（i18n 零 raw key、drawer 全文「编辑用户/用户名/性别/…/会话策略/继承/取消/确认」皆在地化）；以「取消」關閉 drawer、未按確認（零寫入）。附帶：瀏覽器既有 session 在本輪 review 期間自動完成一次 refresh 輪替（token 過期→攔截器換新對→psql 觀測到 used→active 同 chain 列）＝FR-012 輪替對使用者透明的活體側證。驗畢導回 /manage/audit 還原 tab 原狀。

---

## 015-policy-governance — ✅ PASS

**FR coverage**：FR-001 PASS（archive-move 同 txn：sys_casbin_rule.rs:126-186 protected-check→insert_archived→DELETE→grant→op-log 單一 mutate_in_txn；11-col snapshot+archived_at/by/reason）；FR-002 PASS（list 分頁+role_code/dimension filter+空字串守門 normalize_str_filter；live curl 0000、CDP 8 欄、total=psql count 一致）；FR-003 PASS（restore Applied：INSERT casbin_rule+DELETE archive+Restore op-log {role,target,dimension} 同 txn、handler Applied→reload_and_publish；015 D2 live 紀錄）；FR-004 PASS（NoOp 消費 archive 不重插不審計 :185-191、NotFound→2222 biz.policy.notFound 零變更不審計；D3/D4 live 紀錄；020 加 NotRestorable 第四態＝有依據收窄）；FR-005 PASS（protected-reject 在任何寫之前、整批拒零變更含 archive 零新列；D6 live 紀錄；psql protected=19 seed 在役）；FR-006 PASS（mutate_in_txn 綁 AuditEvent、payload 記 role/target/dimension 非 archive id）；FR-007 PASS（gate：Applied（含空-diff、015 拍板）→reload+publish、Rejected/NoOp/NotFound/NotRestorable skip；gate live 測 reload_publish_roundtrip 綁對 channel）；FR-008 PASS（spawn_policy_watcher backoff 重訂閱+reconnect 補 reload+H-3 preserve-on-failure；redis PUBSUB CHANNELS 活體訂閱證；015 C-V-5 2-instance+CLIENT KILL 紀錄）；FR-009 PASS（main.rs:586-599 require_policy 雙端點+m002 seed R_SUPER-only；無 token 3333 live、Admin 5003 引 C-V-4 紀錄）；FR-010 PASS（撤銷終態一致：desired-set diff 語意不變；023 空 sort byte-identical 預設；live 空 param 查詢正常）；FR-011 PASS（archive 表 m001 既有、015 收刀 git diff migration/ 空證；後續 m006-m009 皆他 feature）；FR-012 PASS（零 MgmtApi 寫：撤銷/復原全走 entity 直寫+load_policy 全量重讀 DB；grep 證 reload 唯一入口 reload_and_publish/reload_enforcer_preserving）。SC-001~004 對應上述碼證+live 紀錄；SC-005 跨副本引 C-V-5；SC-006 CDP 本輪可達性+在地化實證、復原端到端引 C-V-6；SC-007/008 引收刀 prod build+零-diff 紀錄。

**Strengths**：
- archive-move 原子性實作嚴謹：set_role_dimension 於單一 mutate_in_txn 內 (a)read→(c)protected-reject 在任何寫之前→(d)insert_archived+DELETE→(e)grant→op-log（sys_casbin_rule.rs:93-210），FR-001/005/006 同 txn 保證成立
- restore 三態（Applied/NoOp/NotFound）+020 第四態 NotRestorable 全在 facade 單一 txn 內判定（sys_casbin_policy_archive.rs:142-236），NoOp 消費 archive 不重插不審計、NotFound 零變更，並含 created_at NULL→NN coerce（D5）與 020 C1 lock-then-redecide FOR-UPDATE 守門
- reload gate 紀律清楚：restorePolicy 僅 Applied→reload_and_publish、NoOp/NotFound/NotRestorable skip（system_manage.rs:2140-2153）；updateRoleMenu Applied（含空-diff、015 拍板調 011）→無條件 reload+publish、Rejected 走 Err 短路不 reload（FR-007）
- watcher 韌性完備且已強化：spawn_policy_watcher backoff 指數重連+重訂閱後補一次 reload（補 backoff window 錯過的 invalidate），H-3 preserve-on-failure（reload_enforcer_preserving 對全新 enforcer load、成功才 swap）根除舊 load_policy clear-then-load 的 deny-all 風險（main.rs:816-890）
- live 實測：casbin:policy:invalidate 頻道當下真有訂閱者（redis PUBSUB CHANNELS）、getArchivedPolicies 空字串 param 守門正確（?roleCode=&dimension=&sort= 回 0000 非誤 filter）、無 token 拒 3333、CDP /manage/policy-archive 渲染 8 欄含「建立时间」且零 raw i18n key
- facade live smoke 覆蓋密：D1 archive-move 原子/D2 Applied+審計 payload {role,target,dimension}/D3 NoOp 不重複不審計/D4 NotFound delta-isolation/D5 NULL coerce/D6 protected-reject archive 零新列，皆無條件 teardown 防 seed 污染

**Findings**：
- **[Observation]** archive 快照不含 protected 欄、restore 一律 protected=false——protected 語意在封存/復原往返中不保留（實務不可達：protected 列經 UI 撤銷會被 ②protected-reject 整批擋、role_soft_delete 歸檔列復原亦被 020 NotRestorable 擋，故現行無路徑讓 protected 列進入 archive 再復原）（`rust-api/server/src/model/facade/sys_casbin_policy_archive.rs`）
  - 證據：insert_archived（:35-62）snapshot 欄僅 ptype/v0-v5/created_at/created_by+archive 三欄、無 protected；restore（:196-208）固定 `protected: Set(false)`。防護鏈：sys_casbin_rule.rs:126-133 protected-reject 在任何寫之前；restore 守門 :157-171 NotRestorable。spec FR-001 封存清單（角色/目標/維度/原建立資訊）亦未含 protected 標記，故非 FR 違反、僅記為將來若開 un-protect（A 拍板明確不做）時的語意注意點
- **[Observation]** live 環境 sys_casbin_policy_archive 當前 0 列（治理狀態乾淨），回收桶列級渲染（reason 標籤文字、建立时间值、復原鈕/notRestorable 分支）本輪無法以純讀端在真列上取證，依賴 015 C-V-6 與 020 收刀 CDP 紀錄（`base-web/src/views/manage/policy-archive/modules/policy-archive-table.vue`）
  - 證據：psql count(sys_casbin_policy_archive)=0；CDP dump rowCount=0（與 API total=0 一致＝FR-002 呈現與底層一致的空集案）；列級碼審：:126-146 row.restorable===false→notRestorable 標籤、else NPopconfirm→fetchRestorePolicy→reload，i18n $t 非硬編碼。造列需寫端（撤銷授權）＝本輪禁止，非缺陷

**Supersessions（合法推翻、非缺陷）**：
- 016-button-endpoint-policy（MILESTONES `fa17def`）：015 的 menu 維治理擴至 button/endpoint 三維——archive list dimension filter 加 v2-推導（dimension="endpoint"→V2 IN HTTP_METHODS、menu/button 維持 V2.eq 零回歸，sys_casbin_policy_archive.rs:110-118）＋handler dimension_display 群組顯示；spec 015 僅 menu 維、有拍板依據（016 scope C）
- 020-role-delete-policy-archive（MILESTONES 020 行）：RestoreOutcome 由三態擴為四態（+NotRestorable）＋restorable 衍生判定（denylist role_soft_delete + active 角色 created_at 比對）＋C1 lock-then-redecide FOR-UPDATE 守門＋list 回應加 restorable 欄＋UI 條件渲染 notRestorable 標籤——spec 015 FR-003/FR-004 的「回收桶項皆可復原」被 020 FR-005/FR-012 安全邊界收窄、有拍板依據
- 023-list-column-sort（MILESTONES `009901fa`）：archive list 簽章加 sort 參數＋resolve_sort 6 欄白名單（非白名單→2222 biz.common.invalidSort、live 實證）；空 sort byte-identical 預設 ArchivedAt desc+Id desc＝FR-002 零回歸
- H-3 correctness/security 修復輪（MILESTONES `e09aa4dc`+`55dec914` 2026-06-25）：watcher reload 由直接 load_policy（clear-then-load、DB 暫錯→空策略 deny-all）改 reload_enforcer_preserving preserve-on-failure——強化 FR-008 韌性語意、非行為漂移
- 022-ip-access-control：reload_and_publish 範式被鏡像為 reload_and_publish_ipgate（system_manage.rs:1077）——015 機制被複用、對 015 本身零影響

**Live 證據**：共用 review token 已過期（3333 auth.token.expired）→ 依 SOP 自 CDP tab localStorage 取 Super 活 token（未自行 login、0 失敗嘗試）。讀端取證：① GET /systemManage/getArchivedPolicies?current=1&size=10&roleCode=&dimension=&sort=（刻意帶空字串模擬 base-web axios）→ 0000 {records:[],total:0}＝空字串守門正確；② dimension=endpoint filter → 0000 total:0（v2-推導路徑 200 不炸）；③ 無 Authorization → 3333 拒（FR-009 後端守門）；④ sort=v2:asc（非白名單欄）→ 2222 biz.common.invalidSort（023 supersession 白名單生效）。psql：casbin_rule=149 列、protected=19 列（受保護核心 seed 在役）、sys_casbin_policy_archive=0 列（回收桶當前空、與 API total=0 逐項一致＝FR-002 空集案）。redis：PUBSUB CHANNELS 實列 casbin:policy:invalidate（running instance watcher 訂閱中＝FR-008 訂閱活體證）。寫端 FR（撤銷/復原/跨副本收斂/斷線重訂閱）依非破壞紀律不實跑：實作碼逐行核對＋引 015 收刀 C-V 紀錄（MILESTONES `65f1838`：C-V-4 curl Super 200·Admin 5003·NotFound 2222／C-V-5 2-instance A revoke→B 反映+CLIENT KILL pubsub ~1s 重訂閱仍收斂／facade live D1-D6 全綠）。

**CDP 證據**：CDP（pageId AAB4D0C2CDE633D7A403FE31ACB47168、Super 登入態）navigate http://127.0.0.1:31080/manage/policy-archive → 頁面可達（未被導回 /login＝route 級可達性 OK）；表頭 8 欄完整渲染：角色/目标/维度/撤销时间/建立时间/撤销执行者/撤销原因/操作（含 spec 要求的「建立时间」原建立資訊欄）；麵包屑「系统管理 / 授权回收桶」＋sider 選單「授权回收桶」入口可見（R_SUPER menu policy seed 生效）；rawKey 正則掃描 body innerText＝false（零 raw i18n key、backend./page./route. 皆已在地化）；tbody 0 列與 psql archive count=0、API total=0 三方一致（FR-002 呈現=底層）。列級互動（復原 NPopconfirm、notRestorable 標籤）因回收桶空且造列屬寫端禁區、未實點——碼審 policy-archive-table.vue:126-160 wiring 完整（fetchRestorePolicy→reload list、restorable===false→標籤分支），引 015 C-V-6 CDP 紀錄（list 真打真資料、點復原→200→列消失、console error 消解）與 020 收刀 CDP 補證。

---

## 016-button-endpoint-policy — ✅ PASS

**FR coverage**：FR-001 ✅（update_role_button→set_role_dimension DB-first＋無條件 reload_and_publish；live buttons 12 DB-fresh）；FR-002 ✅（set_role_endpoints 動真實 (v0=role,v1=path,v2=method) enforce 列、雙鍵 diff；M-1 正規化）；FR-003 ✅（mutate_in_txn 內 insert_archived＋DELETE by id 同 txn 原子、archive 11-col snapshot＋撤銷時間/人/原因）；FR-004 ✅（protected-reject 在任何 delete/insert 之前、no-op txn 零變更、Rejected→2222 endpointProtected；六編輯端點自身 protected=true＝恢復路徑恆保留；C-V-3 鎖出守門收刀實證）；FR-005 ✅（getArchivedPolicies dimension 三維 v2-推導 filter live 通、restorePolicy 共用三維）；FR-006 ✅（op-log 同 mutate_in_txn；restore 審計記 raw-v2）；FR-007 ✅（Rejected/notFound Err 提前 return 不觸 reload/publish、Applied 含空-diff 必 reload+publish——控制流保證）；FR-008 ✅碼＋收刀證（spawn_policy_watcher SUBSCRIBE casbin:policy:invalidate 在 main.rs 常駐；2 副本收斂＝016 C-V-6 收刀全綠、本輪未重跑〔需起第 2 副本、超出讀端範圍〕）；FR-009 ✅（psql 六端點 R_SUPER-only＋require_policy layer 掛載 grep 證）；FR-010 ✅（menu 維 set_role_dimension/archive V2.eq 路徑零改動、REVIEW-001-019 基線 015 PASS、CDP 菜单权限鈕仍在）；FR-011/SC-008 ✅（016 零 migration〔收刀 diff 證〕、6 端點 seed 於 m002）；FR-012 ✅（psql button 列 protected=true 計 0、facade grant 固定 protected=false）。SC-001/002/003/004 由收刀 C-V-2~4 live smoke＋本輪實碼覆核；SC-005 收刀 C-V-6；SC-006 本輪 CDP＋psql super-only 證；SC-007 025 收刀零回歸＋prod build ×2 全綠聲明。

**Strengths**：
- DB-first casbin 寫全程零 MgmtApi（constitution §I.7/B1）：update_role_button 復用 set_role_dimension(v2='button')、update_role_endpoints 走淨新 set_role_endpoints——mutate_in_txn 內 read-current→雙鍵 diff→protected-reject（任何寫之前、no-op txn＝零變更）→archive-move（insert_archived＋DELETE 按 id、防變動 v2 誤刪）→grant→op-log 同 txn 原子（FR-003/004/006 一體落實，rust-api/server/src/model/facade/sys_casbin_rule.rs）
- ALL_ENDPOINT_POLICIES registry（42 對）與 main.rs require_policy 掛載集（grep 實數 42）由 endpoint_coverage_lint 雙向 assertion【精確相等】守恆（⊇防漏授＋⊆防幻影孤兒列）；020~025 六刀後 AS_BUILT_ROUTES 逐刀更新到 54、025 收刀 lint(2+2) 全綠聲明（MILESTONES ded99ee9）
- M-1 method 正規化（normalize_endpoint_method：大寫化＋HTTP_METHODS 白名單、非法整批拒 2222）與 M-6 no-escalation 守門（非 R_SUPER operator desired ⊆ 自身 effective set、button/endpoint 兩維皆有）超出 spec 字面、閉掉隱形 orphan 列與越權自授兩類洞
- FR-007 精準重載結構性保證：Rejected/notFound 走 Err 提前 return、reload_and_publish 只在 Ok（Applied 含空-diff）路徑執行——gate 由控制流而非旗標保證
- UI 三 modal 完整落地且 un-mock（button-auth-modal 三處 [rev3-inline 016 MW(c)] 標記原 mock→真發；endpoint-auth-modal synthKey/parseKey 對稱、check-strategy=child、依 path 首段分組）；CDP 實測 drawer 三鈕（菜单/按钮/接口权限）、兩 modal NTree 渲染與勾選現值正確、零 raw i18n key
- 回收桶三維 v2-推導（dimension='endpoint'→V2 IN HTTP_METHODS、menu/button 維持 V2.eq 零回歸）＋restore 復用 015 治理島——無 v2='endpoint' 平行編碼

**Findings**：
- **[Observation]** R_SUPER 有 2 條 seeded 但未掛 require_policy 的 endpoint 政策列（getDeletedMenus GET／updateUserSessionPolicy POST、皆 protected=true）不在 ALL_ENDPOINT_POLICIES registry（42）內：getRoleEndpoints 對 R_SUPER 回 44 對、endpoint-auth-modal 樹只有 42 個 leaf，2 個 checked key 無對應樹節點。若 naive-ui/treemate 在使用者互動後把 unknown checked key 掉出 model，保存 R_SUPER 端點集會嘗試撤這 2 條 protected 列→整批 protected-reject 2222（fail-safe：零變更、無鎖出，但 R_SUPER 的端點 modal 可能永遠存不了）。registry 排除為 enforce.rs 文件化的刻意設計（『m002 另 seed…刻意不入 registry』）、且兩列 protected=true 使守門兜底——非缺陷，但屬 UI 可用性潛在死角，登記備查。（`rust-api/server/src/auth/enforce.rs`）
  - 證據：psql：R_SUPER endpoint 列=44 vs registry/getAllEndpoints=42，差集恰為 /systemManage/getDeletedMenus GET 與 /systemManage/updateUserSessionPolicy POST（protected=t）。enforce.rs ALL_ENDPOINT_POLICIES doc comment 明文『registry＝【已註冊 route 集】、非 seeded 全集（m002 另 seed getDeletedMenus/updateUserSessionPolicy 等未掛 require_policy route 者、刻意不入 registry）』。本次 review 依非破壞紀律未按保存驗證 NTree unknown-key 行為。

**Supersessions（合法推翻、非缺陷）**：
- AS_BUILT_ROUTES 43→44（018 /metrics）→50（019 getLoginAttempt/unlockLogin、021、022 +6 ip 路由）→54（025 4 條 /userCenter auth-only 路由）——逐刀合法演進，endpoint_coverage_lint 檔頭逐段記帳、MILESTONES §1 各收刀行背書；ALL_ENDPOINT_POLICIES 同步 36→42 對、lint 雙向 assertion 持續守恆
- 020 role 刪除治理為 set_role_dimension/set_role_endpoints 增 RoleDeleted 錯誤變體（併發軟刪→鎖讀無 active 角色→2222 notFound），update_role_button/endpoints match 臂同步擴充——016 原三態錯誤通道的合法擴充（handler 註記『★ 020 U1b』、MILESTONES 020 行）
- 025 的 4 條 /userCenter self 端點刻意 auth-only（enforce_mw 注 Claims、無 require_policy）、不入 ALL_ENDPOINT_POLICIES——符合 registry『精確等於 policy-governed route 集』語意，非 016 漏授（main.rs:198 註解＋025 收刀行 ded99ee9 明載）

**Live 證據**：共用 review token 已過期（3333 auth.token.expired）→依「不自行 login」紀律改經 CDP 自瀏覽器 localStorage 抽現役 SOY_token。curl（經 :31080/api）：getUserInfo→code 0000、超级管理员 [R_SUPER]、buttons 恰 12 枚（B_CODE1-3＋user/role/menu × add/edit/delete，DB-fresh）；getAllButtons→12 條 {code,label}（sys_menu.buttons 萃取、label 為譯文非 code）；getAllEndpoints→42 對（＝registry）；getRoleButton?roleId=1→12 codes；getRoleEndpoints?roleId=2→3 對（R_ADMIN：getUserList/getRoleList/getAllRoles GET，與 psql 逐列一致）；getArchivedPolicies?dimension=button|endpoint→0000 空頁（archive 現況 0 列、filter 參數通）。psql：casbin_rule v2='button' 16 列（R_SUPER 12/R_ADMIN 3/R_USER_COMMON 1、protected=true 者 0＝FR-012 按鈕無受保護項成立）；endpoint 列（v2∈HTTP methods）48 列、protected=true 15 列；016 六端點政策列全部 R_SUPER-only 且自身 protected=true（FR-009＋FR-004 恢復路徑自護）。寫端（updateRoleButton/Endpoints/restorePolicy）依非破壞紀律未實發——以實碼＋facade in-crate #[ignore] live smoke（trace_id 016-button-smoke/016-endpoint-smoke 實存於 sys_casbin_rule.rs）＋016 收刀 C-V-2/3/4/6 全綠紀錄（MILESTONES fa17def）＋025 收刀 rust 255 unit+lint(2+2) 全綠（ded99ee9）佐證。

**CDP 證據**：CDP（tab AAB4D0C2CDE633D7A403FE31ACB47168、Super 登入態）：navigate /manage/role→URL 確認、表格 3 列。點首列「编辑」開 drawer→三鈕齊列「菜单权限|按钮权限|接口权限」（i18n 譯文、零 raw key）。開按钮权限 modal→標題「编辑按钮权限」、NTree 12 節點、12 勾選（＝getRoleButton roleId=1 的 12 codes、R_SUPER 全勾）。取消後開接口权限 modal→標題「编辑接口权限」、NTree 分組渲染（父節點 /systemManage＋leaf 如 POST /systemManage/addIpRule；virtual-scroll 視窗渲 12 節點、勾選狀態正確呈現）。全頁 innerText 掃 page.manage.*/common.*/backend.* raw key→none。未按任何保存/確定（非破壞紀律）、modal 與 drawer 均以取消收尾。

---


<!-- BATCH 3 RESULTS -->

## 017-audit-center-enhancement — ✅ PASS

**FR coverage**：FR-001 ✅（with_roles 三寫端同 txn 快照＋psql delta 實證）；FR-002 ✅（create 初始角色/update replace 前讀/soft_delete 刪前讀/batch_delete_user 迴圈走同一 soft_delete）；FR-003 ✅（單筆 op-log 含 operator_id/name、entity_id、payload_before.roles、payload_after.roles、created_at；SC-002 單筆可判讀）；FR-004 ✅（空→[] 非 null、純測 with_roles_empty_yields_empty_array_not_null＋live \"[]\" vs \"\" 區分）；FR-005 ✅（三表 export=true 變體、live 三表皆取得 CSV）；FR-005a ✅（rolesBefore/rolesAfter 專屬欄＋payloadBefore/After 完整保留；表頭命名 camelCase 偏離字面見 Observation）；FR-006 ✅（export 與 list 共用同一 filter struct＋live 4xx 匯出 228 列=list total）；FR-007 ✅（CSV_EXPORT_CAP=10000、page=0 繞過 normalize_size、is_export_truncated boundary 純測＋前端 toast；>1 萬列場景引收刀 C-V）；FR-008 ✅（BOM 檔首一次＋CRLF＋live BOM_count=1；SC-004 依 C-V CDP Excel-compat 聲明）；FR-009 ✅（同 3 GET 端點同 require_policy gate、casbin 僅 R_SUPER、psql 實證）；FR-010 ✅（2xx/4xx/5xx 半開區間、live 三類別全驗）；FR-011 ✅（apply_if 並存 AND、live 交集/矛盾組合實證）；FR-012 ✅（畸形/空→Ok(None) 不 2222、live xyz＋空字串實證）；FR-013 ✅（不帶新參數之 list live 正常、PageRes 零變、CDP 三 tab 瀏覽正常）；FR-014 ✅（零 migration——017 收刀紀錄「零 migration diff」、filter/CSV 皆查詢層）。SC-001~008 全對應覆蓋；US1 AS1-4/US2 AS1-6/US3 AS1-3/Edge 全數有碼證或 live 證。

**Strengths**：
- C-4 with_roles（rust-api/server/src/model/audit.rs:103）設計嚴謹：roles 排序入 jsonb（集合語意）、空→[] 非 null（FR-004 可區分「無角色 vs 未記錄」）、非 Object passthrough 防呆；create/update/soft_delete 三寫端皆同 txn 讀 roles_of_user 一致快照（sys_user.rs:73/380 刪前讀、update replace 前讀），batch_delete_user 迴圈逐 id 走同一 soft_delete → FR-002 全生命週期含批次刪除自然覆蓋
- C-3 CSV 管線 test-first 且轉義正確：csv_escape_field 一律 quote 包覆＋"→""（不依內容判斷、含逗號/換行的 payload JSON 不破欄）＋H-1 formula-injection 前綴（後續 security 審查補強、OWASP）；records_to_csv BOM 僅檔首一次；live 匯出 228 列 4xx CSV 經 python csv.reader 全列 parse ok（all_parse_ok=True）
- F4 截斷信號後端權威化：is_export_truncated(total>CAP、==CAP 不算截斷有 boundary 純測)＋{csv,truncated} envelope，前端三表 toast 皆據後端旗標非 stale itemCount——比 spec 原字面更 robust 的同 feature 演進
- C-1 parse_http_status_class 刻意異於 parse_http_status（畸形→Ok(None) 不 2222、註解明引 FR-012 dropdown 來源受限理由）；filter 以半開區間 gte(lo)+lt(hi) apply_if 與單值 eq 並存 AND，live 實證交集（4xx+403→僅 403；2xx+403→0）
- matrix 式 live 證據全綠：三表匯出（含 0 列僅表頭）、roles delta psql 樣本三情境（SOFT_DELETE [roles]→[]、INSERT 無before→[初始]、UPDATE 未動角色前後相同不誤報）、CDP UI 4xx 收窄實渲染全 403/404、i18n 零 raw key

**Findings**：
- **[Observation]** CSV 專屬欄表頭實作為 camelCase `rolesBefore`/`rolesAfter`，與 FR-005a/US2-AS6 字面 `roles_before`/`roles_after` 不同名（`rust-api/server/src/handler/system_manage.rs`）
  - 證據：OPERATION_LOG_CSV_HEADERS（system_manage.rs:2270 起）含 "rolesBefore","rolesAfter"，live 匯出表頭實證同；與其餘 14 欄 camelCase 表頭一致、data-model §3.3 已記載。FR 本質（專屬可直接分析的角色 delta 欄＋payload 完整欄保留）滿足，僅命名慣例偏離 spec 字面，非違反。
- **[Observation]** 匯出鈕置於預設收合的「搜索」NCollapse 面板內，收合態 DOM 無匯出鈕，稽核者須先展開搜尋面板才能匯出（`base-web/src/views/manage/audit/modules/operation-log-table.vue`）
  - 證據：CDP 實測：收合態 querySelectorAll('button') 僅 icon＋avatar、無「导出」；點擊 .n-collapse-item__header-main 展開後出現 重置/搜索/导出/清除排序。SC-003「單一動作取得 CSV」在展開後成立；純 UX 可發現性註記，三表一致此模式。
- **[Observation]** parse_http_status_class 回傳型為 Result 但實作永不 Err（死 Err 臂），呼叫端 `?` 為形式一致而存在（`rust-api/server/src/handler/system_manage.rs`）
  - 證據：system_manage.rs:657 match 四臂全回 Ok；註解明述 FR-012「無法識別→視為未篩選、不回 Err、異於 parse_http_status」為刻意設計（dropdown 來源受限）。保留 Result 使簽名與同族 parse_* 一致，無行為問題。

**Supersessions（合法推翻、非缺陷）**：
- 023-list-column-sort FR-016/MW(f)：匯出帶入當前排序——handler 內 export 與一般 list 共用同一 resolve_sort＋facade list 呼叫（system_manage.rs:2226「023 FR-016」註）、前端 onExport 合併 sort: sortString（operation-log-table.vue:230）→ CSV 列序與畫面排序一致。依據：MILESTONES 023 收刀行、spec 023 FR-016。
- 025-user-center：自助 profile-only/password-only 更新之 op-log 刻意不套 with_roles（sys_user.rs:457/509 註「無角色改」）→ 該類列 CSV rolesBefore/After 呈 ""（未記錄）而非 "[]"。psql 實證 8 列 2026-07-01/02 UPDATE 無 roles key。依據：specs/025-user-center/data-model.md:40「user_name/password/status/roles Unchanged…無 with_roles」；與 017 F5 空值語意（"" vs "[]" 可區分）相容、非漂移。
- 017 同 feature 演進 F4：截斷告知由前端 list itemCount 啟發式改為後端權威 truncated 旗標（CSV-in-envelope {csv,truncated} 取代 C-3 初版裸 CSV 字串）。依據：REVIEW-001-019 017 行「F4 後端 truncated 旗標(同 feature 演進)」＋ MILESTONES c7f5936。
- post-017 security 審查 H-1（outer 55dec914、rust cda07e9 系列）：csv_escape_field 增 formula-injection 緩解（cell 首字 =+-@\t\r 前綴 '）——對 FR-008/C-3 的補強非漂移。依據：MILESTONES e09a44dc+55dec914 行 H-1 條目。

**Live 證據**：共用 token 已過期（3333 auth.token.expired）→ 依「不自行 login」紀律改由 CDP 讀取瀏覽器 Super session 的 localStorage SOY_token（唯讀）取得有效 token。C-1 filter matrix（GET /systemManage/getAccessLog）：baseline total=2252；httpStatusClass=4xx→total=228 且回列 statuses ⊂{400,403,404,405,415}（匯出全量驗證）；2xx→2026 全 200；5xx→0；畸形 xyz→code 0000 total≈baseline（FR-012 視為未篩選不報錯）；全空參數模擬 base-web 序列化（httpStatusClass=&httpStatus=&path=&method=）→0000 不縮限（空字串守門）；4xx+httpStatus=403 AND 交集→121 列全 403；矛盾組合 2xx+403→0（FR-011）。C-3 匯出（GET export=true、讀端）：op-log CSV BOM 僅檔首 1 次、16 欄表頭含 rolesBefore/rolesAfter/payloadBefore/payloadAfter（FR-005a）、truncated=false；access log 帶 4xx filter 匯出 228 列與 list total 一致（FR-006 所見即所匯）且 python csv.reader 全列欄數對齊（F2 轉義）；login attempt 帶不存在 attemptedUserName→僅表頭 1 行不報錯（US2-AS5）。C-4 psql（SELECT only）：SOFT_DELETE before=[\"R_ADMIN\",\"R_USER_COMMON\"]→after=[]、INSERT before 無→after=[初始角色]、UPDATE 未動角色前後相同（不誤報）；roles 排序可見。SC-008：casbin_rule 僅 R_SUPER 有 3 audit GET policy（匯出同端點同 gate）；Admin 403 wire 見 017 C-V 收刀紀錄。>1 萬列截斷 toast 無法非破壞性 live 產生→引 is_export_truncated boundary 純測（==CAP false/CAP+1 true）＋前端三表 data.truncated toast 碼證＋017 C-V 全綠聲明。

**CDP 證據**：tab AAB4D0C2CDE633D7A403FE31ACB47168 navigate /manage/audit：頁面可達、三 tab 渲染（操作异动/API 存取/登录尝试）、op-log 表 10 列、i18n 零 raw key（hasRawKey=false、無 page.manage.audit.*/backend.* 字樣）。切換至 API 存取 tab＋展開「搜索」collapse：11 個 filter label 含「状态类别」（col.statusClass 名詞 label、017 EU5 修正後形）、按鈕 重置/搜索/导出/清除排序 齊備（三表同構、导出鈕存在=C-3 UI）。互動驗證（唯讀）：開状态类别 NSelect→選項 [全部,2xx,4xx,5xx]→選 4xx→按搜索→表格收窄、狀態碼欄全列 ∈{403,404}（SC-006 UI 端到端）；驗畢按重置還原共用 tab 狀態（10 列 baseline）。未按任何寫端鈕（保存/删除/匯出下載未觸發寫 API——匯出本身為讀端但由 curl 已覆蓋、CDP 僅驗鈕存在）。

---

## 018-observability — ✅ PASS

**FR coverage**：FR-001✅（default services 零 obs+live 卷/secret opt-in）FR-002✅（obs/metrics 獨立 profile；grafana profiles:[obs,metrics] 兩軌任一可帶起）FR-003✅（dev loopback、prod override 零 ports internal-only）FR-004✅（MILESTONES：零 migration/零 base-web gitlink 變動）FR-005/SC-002✅（alloy docker-SD 全容器採集、loki 查得 redis-stack/base-web/nginx 等 log）FR-006/SC-003✅（span+completion event、live DB↔loki join 實證）FR-007✅（KEEP relabel rev3-admin 錨定 regex+loki label 實證）FR-008✅（loki 72h retention+compactor）FR-009✅（axum-prometheus per-endpoint counter+duration histogram live）FR-010✅（casbin_enforce_total 2-site observe-only；deny 序列 lazy=Observation）FR-011✅（postgres/redis exporter targets up）FR-012✅（cleanup push 碼層 best-effort 隔離+C-V-3 紀錄；pushgateway in-memory=Observation）FR-013✅（/metrics 公開格式無信封、nginx exact-match 404 live）FR-014✅（prometheus 15d flag）FR-015✅（3 rules via grafana API）FR-016✅（for 2m/2m/5m）FR-017✅（僅 rules.yml provisioning、無 contact point/notification policy 檔＝v1 不投遞、spec 明列）FR-018/SC-007✅（datasource+6 dashboard provisioning、API 實證載入）FR-019/SC-006✅（provisioning 檔案式冪等、21h 運行無 crash-loop；收刀 C-V-5 全綠）FR-020✅（grafana 匿名 401+新 secret $__file{}）FR-021✅（exporter 復用 postgres/redis secret、無新依賴憑證）FR-022/SC-009✅（rust-api healthy、業務端點 metrics 顯示大量 200、收刀 C-V-8 lint 2+2+168 unit 零回歸）。SC-001（一般啟動零 obs）以 compose config 結構性驗證（當前 dev 環境刻意帶 profile 運行、收刀 C-V-0 起停實測全綠）；SC-005 告警轉態依規則 for 門檻+收刀紀錄（本審不做破壞性 down 模擬）。

**Strengths**：
- Profile gating 乾淨：預設 compose services 僅 6 個核心（base-web/redis-stack/postgres/migrate/rust-api/front-nginx），obs 元件全部 profiles:[obs]/[metrics] opt-in（FR-001/002 結構性成立），且 obs-min/obs-full 卷與 secret 皆 opt-in 才建
- FR-006/SC-003 關聯鍵設計落地紮實：audit_ctx.rs:399 span .instrument + 每請求保證一筆 completion event（trace_id 落 fields、loki |json 攤平 fields_trace_id），live 實證 DB trace_id=vGS3aYcNWxJ5hqS95vvS0 在 loki 精準 join 到同請求 log 行
- FR-007 alloy KEEP relabel 以 compose project=rev3-admin 全錨定 regex 限本工作區，loki compose_project label values 僅 rev3-admin 一值（live 實證無跨工作區混入）
- FR-013/SC-008 雙層落實：nginx _locations.inc exact-match `location = /api/metrics` 404（live 實測 404），內網直連 31081/metrics 正常回 Prometheus 格式（不套信封）
- 告警規則品質：3 條 rule 帶 for 門檻（2m/2m/5m）+severity，且 high-5xx 特別處理 noDataState=OK + `or vector(0)` + clamp_min 防 idle false-firing（down 類才 noDataState=Alerting）——語義區分正確
- cleanup-job pushgateway push 為真 best-effort：recorder install 失敗與 ureq PUT 失敗皆 eprintln warn 後 return，不 panic、不改清理結果與 exit code（FR-012 隔離乾淨）
- prod override 中 obs 服務零 ports 映射（internal-only），dev 才綁 127.0.0.1 loopback；grafana 匿名 API 401、admin 密碼走新增 grafana_admin_password secret（$__file{} provider），exporters 復用既有 postgres/redis secret（FR-020/021）

**Findings**：
- **[Observation]** casbin_enforce_total{decision="deny"} series 未預註冊為 0，rust-api 重啟後至首次 deny 前該序列不存在（live /metrics 目前僅見 allow=100）；PromQL 對不存在序列回 no data，儀表板 deny panel 在無 deny 期間顯空態而非 0。（`rust-api/server/src/auth/enforce.rs`）
  - 證據：curl 127.0.0.1:31081/metrics 僅回 casbin_enforce_total{decision="allow"} 100、無 deny 序列；enforce.rs:148/154 兩站點 counter! 為 lazy 首增才建序列。收刀 C-V-3 曾實證 deny 序列可出現，功能面無違反 FR-010，純 metrics 慣例（pre-register at 0）層級的觀察。
- **[Observation]** pushgateway 無持久卷（in-memory），obs 重啟後 cleanup 指標（上次成功時間/刪除列數）消失、須待下次 cleanup-job 執行才恢復；FR-012「上次成功時間」在 obs 層重啟後短暫不可查。（`docker-compose.yml`）
  - 證據：live curl 127.0.0.1:39091/metrics 現無任何 cleanup 序列（pushgateway Up 21h、cleanup-job 屬 profiles:[prod] 未在 dev 排程執行屬預期）；compose 註明「pushgateway in-memory、restart 失憶」為收刀時已記檔的設計取捨，FR-012 主張（指標提供+best-effort 推送）由 cleanup-job/src/main.rs push_metrics 碼與 018 C-V-3 全綠紀錄支撐。

**Live 證據**：obs 兩 profile 現正運行（docker compose ps：loki/alloy/grafana + prometheus/postgres_exporter/redis_exporter/pushgateway 共 7 obs 容器 Up）。loki /ready=ready（33100）、grafana /api/health ok v13.0.2（33000）、prometheus /-/ready Ready（33090）。rust-api 直連 31081/metrics 回 axum_http_requests_total（per-endpoint/method/status）+348 條 duration_seconds_bucket（延遲分佈）+casbin_enforce_total{allow}=100（FR-009/010）。nginx 公網路徑 curl localhost:31080/api/metrics=404（SC-008）。prometheus targets 4/4 up（rust-api/postgres/redis/pushgateway，FR-011 依賴健康）。rust-api docker logs 尾行為 JSON 結構化：{timestamp,level,fields:{message:\"request completed\",trace_id,method,path,http_status},target}。SC-003 端到端 join：psql 取 sys_access_log 最新 trace_id=vGS3aYcNWxJ5hqS95vvS0 → loki query fields_trace_id 精準命中 1 stream 同請求行。SC-002 涵蓋：loki 查得 redis-stack 21h 前 boot log（\"Ready to accept connections\"）+ service label 含 base-web/front-nginx/postgres/rust-api 等（label-values API 預設窗口短、非採集缺漏）。FR-007：compose_project label values=[rev3-admin] 唯一。grafana API（admin secret 認證）：6 dashboards 全在（全域總覽/後端應用/PostgreSQL/Redis/審計日誌/維運 Job）、3 provisioned alert rules（rustapi-down 2m critical/infra-exporter-down 2m critical/high-5xx 5m warning）；匿名 curl 401（FR-020）。retention：loki-config retention_period=72h+retention_enabled（FR-008）、prometheus --storage.tsdb.retention.time=15d（FR-014）。預設 compose config --services 不含任何 obs 服務（FR-001 opt-in）。

**CDP 證據**：N/A — grafana 非 base-web 面、零 base-web 改動（gitlink 零變動）；儀表板/告警以 grafana HTTP API 取證替代（6 dashboards + 3 alert rules + 401 匿名擋），收刀時 C-V-6 已有 6 dashboard CDP UI 實渲染截圖紀錄（MILESTONES c1a3224）。

---

## 019-login-lockout — ✅ PASS

**FR coverage**：FR-001＝PER_USER_THRESHOLD=5/窗 900＋is_locked_out `user_fails>=5`＋邊界純測 MET。FR-002＝PER_IP_THRESHOLD=20/窗 900＋`ip_fails>=20`＋邊界純測 MET。FR-003（OR）＝`||`＋or_semantics 測 MET。FR-004＝ctx.client_ip 來自 007 audit_ctx trusted-proxy 防偽鏈（＋022 tunnel 窄 fallback），count 端 IpNetwork::from 與寫端同款（D-04）MET。FR-005＝facade 兩 count 皆 `.filter(Success.eq(false))` MET。FR-006＝since=now-900s 每請求重算、無持久鎖狀態（DB 真相）；021 L1 TTL 900 固定不 refresh 對齊 MET（live smoke sliding_window_recovery）。FR-007＝無 reset-on-success 碼路徑（022 reset-marker 為 admin 動作非成功登入）MET。FR-008＝L2 gated 列流過單一寫點 success=false/operator=None MET；「審計痕跡」半在 021 L1 命中路徑依已記檔勘誤反轉（supersession、spec.md:84）。FR-009＝L1 早 return／L2 locked→不進 login_inner、不評估帳密 MET。FR-010＝count DbErr `.unwrap_or(0)`＋redis 全鏈 fail-open（021 C-V-5 stop-redis 實證）MET。FR-011＝gate 先於帳號查找、真假帳號同路徑同 2222；未鎖態 1000 亦同（live 探針實證）MET。FR-012＝單一 key auth.login.locked、無維度/時間/存在性（②c dimension 只進 server log 非 wire）MET。FR-013＝zh-cn/en-us locale＋Schema＋translateBackendMsg 鏈齊（收刀 C-V-7 CDP 雙語 toast）MET。FR-014＝gate 未達即走原 login_inner 6 路徑零改（live 探針 1000 照舊）MET。FR-015＝attempted_user_name 原樣寫入、count 不分真假帳號（live 探針：不存在帳號寫 success=f 列）MET。SC-001~007＝收刀 C-V-3/4/6/7＋021 活體 22 PASS 覆蓋；SC-007 sticky 延長性依 021 勘誤弱化（documented）。15/15 FR、7/7 SC 皆 MET 或已記檔 supersession，零無依據漂移。

**Strengths**：
- 鎖定判決為純函式 decision seam（rust-api/server/src/handler/auth.rs:69 `is_locked_out`），門檻邊界（4/5/6、19/20/21）與 OR 語意（FR-003）皆有 test-first 純測覆蓋（is_locked_out_per_user_boundary / per_ip_boundary / or_semantics）
- fail-OPEN 一致貫穿：L2 count DbErr→`.unwrap_or(0)`（D-07/FR-010）、redis is_locked 抖動→false 退 L2（redis.rs:138）、mark_locked/incr/take_suppressed 全 fail-OPEN —— 鎖定機制不可能使登入入口不可用
- D-04 寫讀同款轉換防靜默失效：count 端 `IpNetwork::from(ctx.client_ip)` 與寫端 `login_attempt_active_model` 同款（sys_login_attempt.rs:63-66 明標 load-bearing）；021 G1 key 同源（lockout_keys↔tripped_keys 含 IPv6）有專測 tripped_keys_match_lockout_keys_g1
- gated 列匯流既有單一寫點（success=false/operator=None/ctx 四欄照填）＝sticky＋審計留痕、與真失敗列無從區分（FR-008/011）；L2 鎖定路徑 `Err((None, Biz("auth.login.locked")))` 自然流過 exactly-one 寫點
- 防枚舉 by construction：gate 跑在 find_by_user_name 之前，真假帳號走完全同一 count→判決→2222 路徑（SC-004）；帳號查找與 count 皆精確 eq、大小寫語意一致無規避縫
- 兩複合索引（idx_login_attempt_ip_time / idx_login_attempt_user_time）實存於 DB（pg_indexes 實查）且與 count 查詢形狀對齊，收刀 C-V-5 有 EXPLAIN enable_seqscan=off 佐證
- 0 migration／0 新表／0 新 route，唯讀消費 007 既備審計軌，spec『不新增任何資料儲存』承諾守住（021 才加 redis 快取層且有拍板）

**Findings**：
- **[Observation]** L1 ②c suppressed key TTL 與 L2 mark_locked 對 ip 維度也用 PER_USER_WINDOW_SECS，若未來 per-ip 窗值與 per-user 分岔會靜默沿用 user 窗（目前兩 const 同值 900、無行為差異）（`rust-api/server/src/handler/auth.rs`）
  - 證據：auth.rs:328 `h.incr(&sup_k, PER_USER_WINDOW_SECS as u64)`；L2 tripped loop `h.mark_locked(&k, PER_USER_WINDOW_SECS as u64)`（含 lockout:ip:* key）；PER_IP_WINDOW_SECS 僅餵 since_ip 計算、未餵任何 TTL。純前瞻性備忘、非現行 bug

**Supersessions（合法推翻、非缺陷）**：
- 019 FR-008『鎖定擋下的嘗試亦留審計痕跡（逐筆）』一半被 021 L1 Redis 負快取命中②b 有意識反轉（命中早 return、不寫 sys_login_attempt、改 ②c 節流麵包屑 target=security.lockout suppressed=N→loki）——依據：spec.md:84 as-built 勘誤（2026-06-28）＋MILESTONES §1 `5e4f3228` T016 勘誤段＋DECISIONS §1 ⚠️ad；同段亦更正 021 設計鏈誤標『019 FR-004』（真反轉擁有者＝007 FR-004/SC-002＋019 FR-008 審計半）
- 019 §Assumptions 明列 defer 的『per-IP 信任白名單』由 022 兌現：login gate 最頂端 L0 trusted-ip bypass（auth.rs `state.ip_rules.load().allow` 同源 ipgate 白名單、白名單來源跳過 L1/L2 lockout）——依據：021 FR-012 預留 L0 seam＋022-FR-004＋MILESTONES §1 `d9c809e6`
- 019 §Assumptions 明列 defer 的『管理員手動解鎖』由 022 兌現：per-dim reset-marker（lockout_reset_key 抬升滑動窗下界 since_floor_ts＝max(now-窗, reset_at)、redis miss 退 now-窗零回歸）——依據：022 D7/B3＋MILESTONES §1 `d9c809e6`；FR-006 自動解除語意原封不動（since_floor_ts_lifts_on_reset 純測驗證無 reset 時退既有窗）
- 019 sticky 延長語意（FR-008/SC-007『唯有停手滿一時窗才解』）在 021 後弱化為『觸發後固定 TTL 900s 自癒』（鎖中嘗試被 L1 壓制不寫 DB→不延長鎖）——021 D2/D3 拍板（防永久鎖退化）、spec.md:84 勘誤段明文涵蓋；SC-007『無法縮短』方向仍成立
- （既載基線）019 brainstorm『鎖定 N 分鐘』→拍板 D3 靜態一般化訊息（FR-012），REVIEW-001-019 supersession map 已記（⚠️w）

**Live 證據**：psql：sys_login_attempt 11 欄實存（attempted_user_name/success/operator_id/real_ip inet/peer_ip/ip_confidence/x_forwarded_for/region/trace_id/created_at），idx_login_attempt_ip_time＋idx_login_attempt_user_time 兩複合索引實存（pg_indexes）。單發安全探針（全程僅 1 次失敗登入、拋棄式不存在帳號 zz_rev019_probe_noexist、不觸發任何鎖）：POST /api/auth/login 回 {"code":"1000","msg":"auth.login.failed"} 與密碼錯路徑同碼同 msg（FR-011/015 輸入端實證），psql 立見 id=849 一列 success=f、operator_id=NULL、real_ip=172.23.0.13、trace_id 有值（exactly-one 寫點＋不存在帳號同等入計數池）。redis KEYS lockout:* 空（當前無活鎖、乾淨態）。近 8 列 attempts 全為 Super success=t（facade count 過濾 success=false＝FR-005）。負路徑（實際觸發 5/20 門檻、2222、滑動窗恢復）依非破壞紀律不重放，引收刀證據：MILESTONES `dbc5902f` C-V-0~9 全綠（per-user curl 2222＋EXPLAIN 索引命中＋in-crate #[ignore] live smoke ×4〔lockout_live_per_user_count_and_decide／per_ip_count_isolation_and_d04／sliding_window_recovery，sys_login_attempt.rs:263-385 實存〕＋零回歸 171 passed）；021 收刀活體 22 PASS（C-V-5 stop redis→第 6 次仍 2222＝fail-open 退 L2 實證 FR-010）。

**CDP 證據**：依任務指示不真觸發鎖定、以檔證代替：i18n 鏈完整——後端 msg=auth.login.locked（auth.rs:347 L1／L2 Err 同 key）→ translateBackendMsg 補 backend. 前綴（base-web/src/locales/index.ts:25-27 唯一翻譯邊界）→ Schema app.d.ts:324 `auth: { login: { failed: string; locked: string } }` → zh-cn.ts:920 locked:'登录失败次数过多，请稍后再试'／en-us.ts:925 'Too many failed login attempts. Please try again later.'（FR-012 靜態一般化、無維度/剩餘時間；FR-013 雙語齊）。onBackendFail 四處共用 translatedMsg（request/index.ts:45）確保 2222 toast 走翻譯後字串。真渲染證據引收刀 C-V-7：MILESTONES `dbc5902f`『CDP zh-cn/en-us 雙語在地化 toast（抓到並修正 vite stale-locale：restart base-web 後在地化）』全綠。

---

## 020-role-delete-policy-archive — ✅ PASS

**FR coverage**：FR-001 ✅ archive_all_role_policies 全維（無 v2 filter）+ delete_many 同 txn（sys_casbin_rule.rs:467-500）＋psql 已刪 4 code 0 live 列實證；FR-002 ✅ insert_archived 先於 delete（snapshot 全 11 欄、archived_by/created_at 攜原值）；FR-003 ✅ 單一 mutate_in_txn（軟刪+archive+op-log 同 txn，sys_role.rs:390-436/446-486）＋巢狀 savepoint rollback live 測；FR-004 ✅ getArchivedPolicies 不過濾 role_soft_delete 列＋archive_reason 欄位透出（system_manage.rs:2100-2117）＋FE reason 標籤映射；FR-005 ✅ restore 持鎖套 restorable、denylist 拒→NotRestorable→2222 biz.policy.notRestorable（sys_casbin_policy_archive.rs:159-172＋system_manage.rs:2151）；FR-006 ✅ wire `restorable: bool` 逐列衍生（system_manage.rs:2103-2115）＋FE 據以停用鈕；FR-007 ✅ restorable 條件一為 denylist 形（非列舉撤銷 reason）、live 撤銷列 created<archived→true（live 測①）＋C-V-7 收刀綠；FR-008 ✅ archive 無 protected-reject（by-design vacuous、註記與 spec 勘誤一致）；FR-009 ✅ handler 守門先行、batch Phase-1 全檢後才寫（system_manage.rs:1427/1462-1470）；FR-010 ✅ archived>0→reload_and_publish gate（:1434-1436/:1477-1479）＋C-V-8；FR-011 ✅ zh-cn/en-us reasonLabels 雙語＋CDP 無 raw key；FR-012 ✅ restorable 條件二/三（無 active→None→false、重用 code created>archived→false）＝讀時衍生非 in-place（與 spec:97 勘誤註一致）＋live 測②③④。SC-001~007 由碼面+226 測+收刀 C-V 全覆蓋；SC-005 失敗注入僅 live 測層（contract 已明示 handler 層 curl 無法安全注入、如實標註）。12/12 FR 無違反。

**Strengths**：
- restorability 為單一純函式 `restorable()`（rust-api/server/src/handler/system_manage.rs:518-525、denylist `reason != ROLE_SOFT_DELETE_REASON` ∧ active created_at < archived_at、strict `<` 同-µs errs-closed 安全向），getArchivedPolicies 列表衍生（system_manage.rs:2103）與 restorePolicy 守門（sys_casbin_policy_archive.rs:159-172）共用同一函式＝零漂移；reason 字面唯一真相 `ROLE_SOFT_DELETE_REASON` 常數（system_manage.rs:505），facade 寫端（sys_casbin_rule.rs:487）與 denylist 同源
- 原子性落實乾淨：`soft_delete`/`batch_soft_delete`（sys_role.rs:390-486）在單一 mutate_in_txn 內 FOR-UPDATE 鎖 role 列（find_active_by_id_for_update）→ 設 deleted_at/by → 同 txn `archive_all_role_policies`（snapshot→insert_archived(reason=role_soft_delete)→delete_many、全維無 v2 filter、含 protected、0 授權 no-op 回 0）→ 同 txn op-log；handler 依 archived>0 gate `reload_and_publish`（FR-010、PolicyMutated gate）
- 四類 casbin 寫端（delete/batch/restore/grant）全部先 FOR-UPDATE 鎖 sys_role 列再寫、同序無死鎖：restore 走 `active_created_at_by_code_for_update`（sys_role.rs:183-193 `lock_exclusive()`）持鎖套 restorable 後才 pre-check/Applied；grant（set_role_dimension sys_casbin_rule.rs:98 / set_role_endpoints :333）起手同一鎖讀、無 active→`GrantReject::RoleDeleted`→handler 映 2222 `biz.role.notFound`（system_manage.rs:1566/1705/1839）＝lock-then-redecide 完整封 grant-during-delete 並發窗（DECISIONS §1 P-011-1 拍板 A 如實落地）
- 後端拒絕為安全邊界＋前端條件渲染雙層：restorePolicy NotRestorable→2222 `biz.policy.notRestorable`（system_manage.rs:2151-2154、零變更）；policy-archive-table.vue:131-137 依 `row.restorable === false` 渲染停用鈕（undefined 容錯維持既有鈕、不誤鎖 015 舊行為）；reason 友善標籤 3 值映射＋未知值 fallback 原字串（:26-28、:117-120）、zh-cn/en-us 雙語（zh-cn.ts:769-774『角色删除』/en-us.ts:773-778 'Role deleted'）滿足 FR-004/FR-011
- 守門先行（FR-009）：delete_role handler 於任何寫之前跑 role_delete_guard（seeded/in-use/self、system_manage.rs:1427）；batch 版 Phase 1 對所有 ids 全檢查任一中整批拒、Phase 2 才進單 txn（:1462-1473）；live `#[ignore]` 測覆蓋三實例判別場景（sys_casbin_policy_archive.rs:662-822 ①live restorable ②刪後雙 false ③重用 code 仍 false ④restore→NotRestorable）＋巢狀 savepoint rollback 原子測（sys_role.rs:1157+）

**Findings**：
- **[Observation]** batch_soft_delete 以 caller 提供的 ids 順序逐一 FOR-UPDATE 鎖 sys_role 列（未排序），兩個並發批次刪除若含相同角色但順序相反，理論上可互鎖——PG 會偵測 deadlock 中止其中一 txn、整批回滾（原子性不破、FR-003 仍守），僅該請求得 5000 需重試（`rust-api/server/src/model/facade/sys_role.rs`）
  - 證據：sys_role.rs:459-464 `for id in ids { let before = match find_active_by_id_for_update(&txn, id)...`——ids 未 sort；單筆 delete/restore/grant 各僅鎖一列不受影響，僅 batch×batch 重疊反序這一窄場景。收刀聲明『同序無死鎖』對四寫端間成立（各自單列鎖），batch 內多列鎖序未正規化屬遺留窄窗、非 FR 違反
- **[Observation]** delete_role 的守門讀（find_active_by_id、count_users_by_role_id、roles_of_user）在 FOR-UPDATE 鎖之前的無鎖快照上執行，guard 通過與 soft_delete 落鎖之間存在窄 TOCTOU（如此刻有人把 user 掛上該角色，仍會刪成）——屬 011 既有 handler 範式，FR-009 字面（守門於任何授權移除之前先檢查）已滿足（`rust-api/server/src/handler/system_manage.rs`）
  - 證據：system_manage.rs:1420-1432：(1) find_active_by_id →(2) count_users_by_role_id + role_delete_guard →(3) facade::sys_role::soft_delete（此時才 FOR UPDATE 鎖）。020 的 C1 鎖序拍板明確聚焦 restore/grant-during-delete 兩路徑（皆已閉），assign-user-during-delete 不在 spec Edge Cases 清單內、未被任何 FR 要求
- **[Observation]** 本輪 live 環境 sys_casbin_policy_archive 為 0 列（live 測自清理後狀態），FR-004/FR-006/FR-011 的列級渲染（reason 標籤在地化、restorable 欄、復原鈕條件態）與 wire `restorable` 欄位無法在只讀約束下重新實測，僅能以碼面＋020 收刀 C-V-9/226 測全綠紀錄佐證（製造歸檔列需 revoke/delete 寫端、本 review 禁用）（`specs/020-role-delete-policy-archive/contracts/verification-commands.md`）
  - 證據：psql `SELECT count(*) FROM sys_casbin_policy_archive`＝0、reason 分佈查詢空集；getArchivedPolicies 實測回 `{current:1,records:[],total:0}` code 0000（空字串 filter 守門正常）；MILESTONES `ebbd6c2c` 行記 C-V-9 CDP（reason 在地化、復原鈕條件態、無 raw key）全綠

**Supersessions（合法推翻、非缺陷）**：
- 020 spec 本身推翻 011-role-management data-model.md 的『軟刪殘留 menu policy 無害』前提——已依 supersession 紀律記檔於 DECISIONS §1 P-011-1（『已加 as-built 註記』）＝合法演進、非漂移
- 023-list-column-sort 對 policy-archive 列表頁追加受控排序（policy-archive-table.vue `useTableSort`、getArchivedPolicies `resolve_sort`）——純加法、有 023 收刀紀錄依據，不改 020 任何 FR 行為；020 之後（021/022/024/025）無任何 feature 觸碰 archive/restore 語意，spec 行為零被推翻

**Live 證據**：psql（讀端）：① `sys_casbin_policy_archive` 現況 0 列、reason 分佈空（live 測後自清理態）；② 關鍵殘留斷言——4 個軟刪角色（zztest_pol_2542117078/zztest_pol_79c1/zztest_role_093c/zztest_role_4806、deleted_at IS NOT NULL）對 `casbin_rule ptype='p' v0=code` 全部 **0 live 列**＝FR-001「刪除後零 active 授權殘留」在當前 DB 實證成立；③ live p-policy 分佈僅 R_SUPER=133/R_ADMIN=11/R_USER_COMMON=5（全為 active 種子角色、無已刪 code 孤兒）。curl（經 tab 自動刷新 token）：`getArchivedPolicies?current=1&size=10&roleCode=&dimension=`（刻意帶空字串 param 模擬前端）→ 0000 `{current:1,records:[],size:10,total:0}`＝空字串 filter 守門＋端點可用。寫端 FR（刪角色/restore 2222/原子回滾）依禁令未實測，佐證＝碼面（見 findings/strengths 逐行）＋020 收刀 C-V-1/2/5/6/7/8 與 rust 226 測（190 unit+36 live）全綠紀錄（MILESTONES `ebbd6c2c`）。註：共用 review-token 及 scratchpad 各備援 token 均已 3333 過期，未自行 login，改由 CDP tab 內建 refresh 流程恢復會話。

**CDP 證據**：CDP tab AAB4D0C2CDE633D7A403FE31ACB47168 navigate → `http://127.0.0.1:31080/manage/policy-archive`（Super 會話經 app axios 自動 refresh 恢復、未落 /login）：頁面可達、表格渲染 8 欄在地化標頭「角色/目标/维度/撤销时间/建立时间/撤销执行者/撤销原因/操作」、`bodyHasRawKey`（/page\.manage\.policyArchive|backend\.biz/ 對 body.innerText）＝false（無 raw i18n key）。tbody 0 列＝與 psql archive 0 列一致（誠實空態）。列級 reason 標籤（『角色删除』）與復原鈕條件態因無資料列本輪無法再現，依 020 C-V-9 收刀紀錄（reason 在地化+復原鈕條件態+backend.biz.policy toast 雙語+restart base-web 防 vite stale-locale）佐證；zh-cn.ts:772『角色删除』/en-us.ts:776 'Role deleted'/notRestorable 標籤碼面在庫。

---

## 021-login-lockout-redis-cache — ✅📝 PASS_WITH_NOTES

**FR coverage**：FR-001/FR-003（鎖後 O(1) 固定成本短路）：L1 命中＝至多 2 次 redis GET＋1 INCR＋節流分支、早 return 於 DB count 與單一寫點之前（auth.rs:314-349）✓。FR-002（雙維度、user 維短路任意來源）：lockout:user:{name} 與來源 IP 無關、user 維優先判（auth.rs:317）；tripped_keys 依達標維度各自 set（:122-131）✓。FR-004（鎖中不逐筆寫稽核）：②b 早 return :347 在寫點 :427 之前 ✓。FR-005/FR-011（節流鑑識、壓制次數必含、結構化可告警）：incr suppressed＋warn target=security.lockout 帶 locked_key/dimension/suppressed→loki（:325-345）✓（distinct-source 可選未做＝clarify firm）。FR-006（上鎖前歷程＋觸發那發完整留存）：L2 鎖判 Err 路徑匯流單一寫點照寫（:407-416→:427）✓。FR-007（固定時窗自動解除）：set_ex 900 固定、is_locked 純 get 不 refresh（redis.rs:132-140）＋live TTL 單調不增測 ✓。FR-008（非權威 fail-OPEN）：redis=None 跳 L1 退 L2、全 op 吞 error 降級 ✓（殘留風險 O2 spec 已誠實記）。FR-009（未鎖零回歸）：L1 miss→既有 019 gate→login_inner 原封（022 trusted_ip/per-dim since 為有據 supersession）✓。FR-010（政策值不變）：5/20/900 常數沿 019、is_locked_out 未動 ✓。FR-012（豁免 seam）：L0 位置保留且已由 022 兌現 ✓。SC-001~SC-007：SC-002/003 由收刀 C-V-2/6（flood 12→count 仍 6、恰 1 筆摘要）證、SC-004 ②c 訊號、SC-005 TTL 自癒（C-V-4 898 不回彈）、SC-006 C-V-5 stop-redis、SC-007 C-V-7 零回歸——12/12 FR＋7/7 SC 覆蓋、無違反。

**Strengths**：
- G1 key 同源紀律落實徹底：L1-read（lockout_keys）與 L2-write（tripped_keys）皆經單源 ip_key/user_key→lockout_dim_key 導出（handler/auth.rs:87-131），純測含 IPv6 縮寫渲染一致斷言（tripped_keys_match_lockout_keys_g1）＋live smoke 以 L2-write key set→L1-read key 讀交叉驗（lockout_cache_per_user_and_per_ip_g1），完整防 D-04-class 靜默永不命中
- fail-OPEN 全鏈一致（FR-008/SC-006）：RedisHandle 每個 op（get/set_ex/incr/take_suppressed）內部吞 error 降級（redis.rs:60-129）、state.redis=None 時 L1 整段跳過退 L2 DB gate（auth.rs:314 if let Some）、is_locked 抖動→false→退 L2；C-V-5 活體（stop redis→第6次仍 2222）已收刀證實
- ②b+②c 實作乾淨：L1 命中早 return 於單一寫點之前（auth.rs:347、0 DB 寫），節流麵包屑 incr(TTL 900 自清)→should_flush→GETDEL→tracing::warn!(target=security.lockout, locked_key/dimension/suppressed)→set_ex flushed 60s（auth.rs:325-345），結構化可告警訊號（FR-005/FR-011）零 migration 落觀測層
- T016 as-built 勘誤三處全落地且內容準確：007 spec FR-004(:108)/SC-002(:137)＋019 spec FR-008(:84) 均有 forward-pointing 021 反轉註記、並含「原誤標 019 FR-004」的歸屬勘誤說明，與 DECISIONS §1 ⚠️ad 一致
- D2/D3 固定 TTL 不 refresh 正確：mark_locked=set_ex 薄包、is_locked=get 只讀不寫（redis.rs:132-140），live 測斷言多次命中後 TTL 單調不增（lockout_ttl_no_refresh_and_failopen）＝攻擊停 ~900s 自癒、否決 refresh-on-hit 永久鎖

**Findings**：
- **[Observation]** L2 觸發時 ip 維度 lockout key 的 TTL 用 PER_USER_WINDOW_SECS（auth.rs:413 對 tripped_keys 全部 key 統一 900），概念上 ip 維應取 PER_IP_WINDOW_SECS；②c suppressed incr TTL（auth.rs:328）同樣不分維度用 PER_USER_WINDOW_SECS（`rust-api/server/src/handler/auth.rs`）
  - 證據：auth.rs:411-414 `for k in tripped_keys(...) { h.mark_locked(&k, PER_USER_WINDOW_SECS as u64) }`——兩窗常數現皆 900（auth.rs:59/64）故零行為差異、spec FR-007「設定時窗」仍滿足；惟未來若兩窗值分岔（019 §4.2 runtime 可調 future），ip 維 TTL 會靜默跟錯常數。latent、非 bug
- **[Observation]** ②c 節流閘非原子（get flushed→GETDEL→set_ex 三步、auth.rs:330-344）：兩個併發 L1 命中可同時見 flushed 不存在→各自 GETDEL＋各發一筆 warn（同 60s 窗 >1 筆），且計數可能拆成兩筆（一筆取 N、一筆取 0）（`rust-api/server/src/handler/auth.rs`）
  - 證據：auth.rs:330 `if should_flush(h.get(&flush_k).await.is_some())` 與 :344 `h.set_ex(&flush_k,"1",60)` 之間無原子性。成長仍有上界（每次競態至多常數筆、不隨攻擊量成正比）→ SC-003「有固定上限」實質滿足、僅「至多一筆」字面在競態下可短暫 2 筆；suppressed 總量跨 flush 守恆（INCR/GETDEL 原子）。無需修
- **[Observation]** live API 取證受限：共用 review token 已過期（getUserInfo 回 3333 auth.token.expired），依「不自己 login」紀律無法補 API 面活體；本 feature 主要活體證據（redis SCAN、psql）不受影響、鎖定路徑（2222）本就依非破壞紀律禁止觸發（`specs/021-login-lockout-redis-cache/contracts/verification-commands.md`）
  - 證據：curl getUserInfo → {"code":"3333"}。鎖後行為以收刀 C-V 22 PASS/0 FAIL（MILESTONES `5e4f3228`：C-V-2 flood 12→psql count 仍=6、C-V-3b per-ip 跨帳號、C-V-5 fail-open、C-V-6 恰 1 筆 suppressed=1）＋實碼比對替代

**Supersessions（合法推翻、非缺陷）**：
- 022-ip-access-control（merge d9c809e6、DECISIONS §1 ⚠️ae）填實 021 FR-012 預留的 L0 seam：trusted_ip 白名單來源於 gate 最前端跳過 L1/L2 lockout（auth.rs:296-313 l1_redis=None＋:353 直走 login_inner）——021 spec 明言「保留位置、不實作」、022 兌現＝合法演進非漂移
- 022 D7/B3 對 021 觸碰的 L2 gate 加 per-dim reset-marker（lockout:reset:{dim}:{value}→since_floor_ts 抬升滑動窗下界、auth.rs:366-403）＋admin 手動解鎖清 lockout/companion keys（suppressed_key/flushed_key 因此升 pub(crate)、auth.rs:141-149）——有 ⚠️ae 拍板依據；redis None/miss→reset_at=None 退 now-窗、不破 021 fail-OPEN
- 021 自身對 007-audit-overlay FR-004/SC-002（exactly-one per terminal result）與 019 FR-008（gated 列 sticky 審計痕跡半條）的有意識反轉（②b 鎖後不寫列）：三處 spec 均已有 as-built 勘誤註記（007 spec:108/:137、019 spec:84）、DECISIONS §1 ⚠️ad 記錄含「原誤標 019 FR-004」歸屬勘誤——已記檔、非 finding

**Live 證據**：redis SCAN lockout:*（含 suppressed/flushed/reset companion）→ 0 keys＝當前無生效鎖定、無 stale 永久 key 殘留（與 FR-007 TTL 自動老化一致、SCAN 為讀端非破壞）。psql sys_login_attempt：358 列（102 failed）、最新 5 列為 Super 成功登入＋一筆 zz_rev019_probe_noexist 失敗（別的 review 軌跡）——表結構/寫入鏈健在、零 migration 佐證（021 無新表）。API 面：共用 token 過期（3333）→ getUserInfo/list 活體受限（見 Observation 3）；鎖定觸發路徑依非破壞紀律不打（登入失敗 0 次）、以收刀 C-V 22 PASS/0 FAIL 記錄＋實碼替代。live smoke 測試碼（redis_lockout_live_tests ×4、#[ignore]+REDIS_URL-gated）覆蓋 mark_locked/is_locked/incr/take_suppressed/TTL 不 refresh/connect fail-open。

**CDP 證據**：N/A——021 為純 rust-api 層（0 新 route/0 新 wire/0 base-web 變動），鎖定訊息 backend.auth.login.locked 的 i18n toast 渲染屬 019 範圍（019 C-V-7 已驗）；依派工指示 CDP 不適用本刀。

---

## 022-ip-access-control — ✅ PASS

**FR coverage**：FR-001/016 ✔ ipgate.rs decide+mw 內聯白>黑>default-allow、RuleSet 僅兩袋 CIDR（order 不進判定）、4+2 純測含白∩黑白優先；FR-002 ✔ INET 欄+IpNetwork、v6 測（decide_ipv6/would_self_lock_ipv6）；FR-003/SC-001 ✔ lock-free ArcSwap.load μs 判定、被擋路徑僅 O(1) redis 節流 ops（有界常數）；FR-004 ✔ auth.rs:296-313/351-354 trusted_ip 跳 L0/L1/L2、寫點保留；FR-005/SC-004 ✔ load_ruleset DB錯→空集、ctx 缺→放行、redis-down→本機 store 仍生效；FR-006 ✔ STRUCTURAL_EXEMPT 6 段+/health|/metrics path 短路（live /health 200）、豁免僅阻擋非 lockout（D6 註明）；FR-007/SC-003 ✔ 6 route CRUD+reload_and_publish（本機即時+publish ≤5s、C-V-6 收刀實測）；FR-008 ✔ facade list 含已刪+deleted flag+搜尋分頁（live curl 0000）+restore 衝突守門（live_tests）＋CDP 頁面/回收桶欄；FR-009/SC-005 ✔ would_self_lock 於 add/update 寫前擋（純測 3 支；delete-allow 邊角見 Observation）；FR-010/SC-006 ✔ unlock_login reset-marker per-dim+since_floor_ts、CDP modal 兩維提示；FR-011 ✔ CRUD mutate_in_txn 同 txn op-log+unlock standalone op-log（lockout_reset/UPDATE）；FR-012 ✔ 阻擋 reuse AppError::PermissionDenied（5003/403、與授權拒同形）；FR-013/SC-009 ✔ blocked_obs incr+TTL 900 自清+≤1/60s flush、access-log operator-gate（未認證 0 列/已認證留列）；FR-014/SC-007 ✔ apply_tunnel_fallback 窄 tunnel+B2 四象限純測+trust-model.toml footgun 註（tunnel e2e＝§3.E 已知遞延）；FR-015/SC-008 ✔ 未命中→next.run 透傳、login 寫點/L2 gate 原封（C-V-14 收刀綠）；SC-002 判定 100% 由純測+C-V-2/3 收刀覆蓋（live 0 規則不可無寫驗證）。16/16 FR 覆蓋、0 違反。

**Strengths**：
- ipgate_mw 短路順序嚴謹（探針 path → ctx 缺失 fail-OPEN → 結構豁免 → 白>黑>default-allow），且刻意不跨 await 持有 ArcSwap guard、絕不用 mandatory Extension extractor（缺 ctx 500=fail-closed 的坑有註解明示並規避）— rust-api/server/src/ipgate.rs:155-206
- FR-013 雙向皆對：audit_mw 用寬鬆 bearer 自行 parse operator（audit_ctx.rs:380）→ 帶有效 token 的被擋請求仍留一列 403 access-log（鑑識痕跡）、未認證被擋請求 0 持久列；②c 節流摘要 blocked/flushed key 同源 helper + 純測鎖定格式（防 D-04-class 渲染分歧）
- 寫端紀律完整：would_self_lock 純函式（含 v6 測）在 add/update 皆於寫前擋；normalize_cidr host-bit 歸零防同段不同字面繞 partial-unique；validate_rule_type 白名單防髒值佔鍵；restore 衝突守門唯讀在寫前、殘餘 race 由 partial unique 23505 兜底（設計註明說為何 restore 不需 TOCTOU txn 內讀）
- reload_and_publish_ipgate 同時做本機 ArcSwap store + redis publish——redis 降級單副本下 CRUD 仍即時生效（修掉 publish-only 在 redis-down 永不更新的缺口）；watcher 斷線 backoff 重連
- apply_tunnel_fallback 窄信任（peer∈tm.tunnel 而非整個內網）+ B2 反偽造四象限純測；trust-model.toml 內把『tunnel ⊂ internal_default 否則 SC-007 靜默破』footgun 寫成部署註解（final-review #8 產物）
- unlock_login 的 value 正規化（IpAddr parse→to_string 與 lockout key 渲染同源）、reset-marker TTL per-dim 取對應窗、稽核先於 redis 變更（稽核失敗不動狀態、語意乾淨）；since_floor_ts 純函式 max(now-窗, reset_at) 使解鎖不竄改歷史列（archetype-B append-only）
- 白名單 trusted_ip 只跳過 L0/L1/L2 鎖定判定、login_inner 結果仍匯流單一 login_attempt 寫點（auth.rs:428-444）——019 的 exactly-one 紀錄零回歸、僅鎖定豁免（FR-004 語意精確）

**Findings**：
- **[Observation]** FR-009 自鎖防護只涵蓋新增/修改 deny 規則命中操作者；【軟刪一條正在保護操作者的 allow 規則】（操作者同時被某 active deny 涵蓋、靠白優先放行時）不經 would_self_lock 檢查、可能自鎖（`rust-api/server/src/handler/system_manage.rs`）
  - 證據：delete_ip_rule（system_manage.rs:2654-2674）直接 soft_delete + reload，無自鎖檢查；would_self_lock（ipgate.rs:88-90）只判『新規則為 deny 且命中操作者』。spec FR-009 文字僅要求『新增/修改一條會命中操作者當前來源 IP 的阻擋規則被拒』→ as-built 與 spec 逐字相符、非違反；且 spec Edge『管理者自鎖』明列白優先與 loopback/私網結構豁免為恢復途徑（實務 dev/內網操作者必落 STRUCTURAL_EXEMPT、擋不住自己）。記為值得留意的 v2 邊角，非缺陷。

**Supersessions（合法推翻、非缺陷）**：
- 023-list-column-sort 對 getIpRuleList 增加 sort 多欄排序參數（resolve_sort 白名單 6 欄、facade list 簽名擴充 sort 參數、空 sort byte-identical 保留 022 原排序）——依據 MILESTONES §1 023 收刀行與 facade 內 023 註記，非漂移。實測非白名單欄 sort=id:asc → 2222 biz.common.invalidSort＝023 設計。
- spec §3.E 兩項 test-coverage defer（tunnel e2e 實鏈驗證／decide() 純函式與 ipgate_mw 內聯判定的 parity 測）＝收刀時已登記之已知遞延，per 指示不列 finding。

**Live 證據**：token：scratchpad review-token.txt 已過期（3333 auth.token.expired），改自 CDP 已登入 Super tab 的 localStorage 取得 fresh SOY_token（未自行 login）。curl（讀端）：① GET /api/systemManage/getIpRuleList?current=1&size=10&cidr=&ruleType=（刻意帶空字串 filter 模擬 base-web axios）→ 0000、total=0、records=[]（空字串守門 normalize_str_filter 生效、無 400/0 列 bug）；② sort=id:asc → 2222 biz.common.invalidSort（023 白名單拒非法欄、非 500）；③ GET :31081/health 無 token → 200（探針豁免可達）。psql：sys_ip_rule count=0（0 active/0 deleted＝dev 未建規則、正常）；pg_indexes 確認 sys_ip_rule_cidr_type_active_uniq partial unique 存在。redis 讀：keys ipgate:* 與 lockout:reset:* 皆空（無被擋洪水/無解鎖 marker、與 0 規則一致）。寫端 FR（add/update/delete/restore/unlockLogin、封鎖行為、fail-OPEN、熱刷新 ≤5s、自鎖攔截、tunnel e2e）依只讀紀律未實打，驗證＝實碼逐 FR 對讀 + 收刀 C-V 契約（specs/022-ip-access-control/contracts/verification-commands.md C-V-0~14 對映 FR-001~016/SC-001~009）+ MILESTONES d9c809e6 全綠聲明（final holistic 3/3 SHIP、prod build 綠、migration up→down→up 綠）。

**CDP 證據**：tab AAB4D0C2CDE633D7A403FE31ACB47168 navigate http://127.0.0.1:31080/manage/ip-rule（等 4.5s）：頁面可達、選單「IP 存取控制」入口存在；表格 9 欄表頭正確渲染（序号/IP 网段（CIDR）/规则类型/排序/描述/是否已删除/建立时间/更新时间/操作）；工具列含【手动解锁/新增/刷新/列设置/清除排序】；0 列＝「无数据 共 0 条」（與 psql 0 列一致）；全頁 rawKey 檢測 false（無 backend.*/route.manage_ip-rule 裸鍵）。開「手动解锁」modal（不保存）：標題「手动解锁登入锁定」、兩維皆解提示文案（「…需两个维度都解锁才能恢复登入」＝FR-010/SC-006 UX 對齊）、解锁维度（帐号）+解锁对象欄位、rawKey false → 按取消關閉。開「新增」modal（不保存）：CIDR 欄含 v4/v6 placeholder 範例、规则类型預設「黑名单（阻挡）」、排序/描述欄、rawKey false → 取消關閉。全程零寫端操作。

---

## 023-list-column-sort — ✅ PASS

**FR coverage**：FR-001 ✓ 7 頁 getColumnSortProps 掛可排序欄＋CDP 點擊實測。FR-002 ✓ facade order_by 於 paginate 前（整資料集）、curl opLog asc 首列 id=11/total 139。FR-003 ✓ CDP 3-state desc→asc→取消。FR-004 ✓ reconcileSortState append 保位＋CDP 多欄 LS 點擊序＋curl 多欄主次序。FR-005 ✓ SortEntry per-欄獨立 order。FR-006 ✓ watch(sortString)→getDataByPage(1)＋CDP pagination=1。FR-007 ✓ clearAll＋SortClearButton CDP 實測。FR-008/009 ✓ localStg per storageKey（SOY_ prefix、純本機、無伺服端儲存）。FR-010 ✓ 7 facade 空 sort 分支 byte-identical＋curl 空/無參數等同。FR-011 ✓ 7 facade match 白名單＋curl 非法欄/方向/重複→2222 不 5000。FR-012 ✓ 白名單僅單一底層欄（archive V0/V1 亦單欄映射）、operatorName 衍生欄不掛。FR-013 ✓ user/role/op-log/access-log/login-attempt/policy-archive/ip-rule 7 頁全接、menu 無 useTableSort。FR-014 ✓ CDP 箭頭指示＋badge:false 無優先序號碼。FR-015 ✓ restoreSortState validKeys 過濾＋CDP bogusColumn 注入實測丟棄。FR-016 ✓ export 共用 list facade＋curl CSV asc/desc 首列相異＋前端 onExport 帶 sort。SC-001~007 全數有 live/CDP 證據（見 liveEvidence/cdpEvidence）。Edge cases（空 sort/非法/重複欄/排序×篩選並存〔sortString 獨立於 searchParams、reset 不清排序〕/樹狀排除/失效欄）全覆蓋。

**Strengths**：
- 後端排序管線層次乾淨：純函式 parse_sort_spec（handler 層、無 entity 型別、9 支單元測涵蓋空/多欄/trim/非法方向/重複欄/畸形/wire key 逐字）＋ per-facade resolve_sort match 白名單（字串只經 match 映 Column 才入 order_by、注入面為零；7 facade 各有 sort_tests 純測拒 id/password/payloadBefore 等非白名單欄）
- FR-010 零回歸實作嚴謹：7 facade 皆 sort.is_empty() 分支保留原預設序 byte-identical（user Id desc、role Id asc、3 日誌 createdAt+Id desc、archive archivedAt+Id desc、ip_rule 領頭 deleted_at IS NULL desc 群組+order+id asc 兩分支皆保留＝回收桶沉底 UX 不變），live curl 空 sort/無 sort 參數逐列等同 [3,2,1]
- 前端 use-table-sort-core.ts 純函式核心（reconcile 保位/append/移除、wire 序列化、restore FR-015 過濾）與 composable 分檔，tsx 可測不觸 storage 模組鏈；reconcileSortState 不覆寫 naive-ui 原生 3-state 循環、只依 payload 維護自身有序清單（點擊序＝優先序）
- 三端對齊零漂移：7 頁 validKeys 逐欄與後端 resolve_sort 白名單完全一致（含 op-log 8 欄、archive roleCode→V0/target→V1 映射）；operatorName 等衍生 join 欄明確不掛 sorter（FR-012）
- FR-016 以共用 facade list 呼叫達成（export 與 list 同路徑、無 export 專屬接線可漂移），live curl export=1 asc/desc 首列分別為 id 11/527 證實 CSV 反映排序；前端 onExport 亦帶 sortString
- audit 3 子表複合 storageKey route.name:access/:login/:operation 防互覆寫（F3）；m008 index idx_login_attempt_created_at 已在 live DB（psql 證實）

**Findings**：
- **[Observation]** FR-015 還原時失效欄僅從 active state 丟棄，localStorage 內的 stale token 會保留到下一次排序變更才被改寫（防禦目標已達成、請求與 UI 皆乾淨，屬無害殘留）（`base-web/src/hooks/common/use-table-sort.ts`）
  - 證據：setup 還原走 restoreSortState 過濾（L38-41），但持久化 watch 只在 sortString 變更時觸發（L48-56、非 immediate）；CDP 實測注入 {manage_user:'bogusColumn:asc,userName:desc'} 重載後 UI 只還原 userName:desc（箭頭正確、list 抓取成功無 2222），惟 LS 仍含 bogusColumn 直到點清除後變 {}。spec FR-015 只要求「防禦性忽略」＝已滿足，不構成違反。
- **[Observation]** 共用 review token（scratchpad review-token.txt）已過期（3333 auth.token.expired）；本次改以 CDP 讀瀏覽器 tab localStorage SOY_token 取得有效 Super token 完成 live 取證（未自行 login、未觸發任何登入嘗試）（`specs/023-list-column-sort/spec.md`）
  - 證據：curl 首輪全回 {"code":"3333","msg":"auth.token.expired"}；經 CDP Runtime.evaluate 讀 tab AAB4D0C2… 的 localStorage.getItem('SOY_token') 取得 exp=1782970539 的有效 token 後全部讀端取證完成。屬 review 基建疑點、非 feature 缺陷。

**Live 證據**：API base http://localhost:31080/api、Super token（自 CDP tab localStorage 讀出、非自行 login）。(1) getUserList?sort=userName:asc → 0000 ['Admin','Super','User']（整資料集升冪極值首列、SC-001）；(2) 多欄 sort=status:asc,userName:desc → 0000 [('1','User'),('1','Super'),('1','Admin')]（主/次優先序正確、SC-002）；(3) 非白名單欄 sort=password:asc、非法方向 userName:sideways、重複欄 userName:asc,userName:desc → 三者皆 2222 biz.common.invalidSort（SC-005、FR-011、Edge 重複欄）；(4) sort=空字串 與 無 sort 參數 → 皆 0000 [3,2,1]（空字串守門＋預設序 byte-identical、FR-010/SC-006）；(5) getOperationLog?sort=createTime:asc 首列 id=11（最舊、total 139 跨頁）vs 預設 desc 首列 id=527；(6) export=1&sort=createTime:asc CSV 首資料列 \"11\" vs desc 首列 \"527\"（FR-016 匯出反映排序）；(7) getLoginAttempt/getAccessLog/getRoleList/getIpRuleList/getArchivedPolicies 各帶白名單欄 sort 皆 0000、getArchivedPolicies?sort=id:asc → 2222（7 端點全覆蓋）；(8) psql pg_indexes：idx_login_attempt_created_at 存在（m008 落地）。

**CDP 證據**：tab AAB4D0C2CDE633D7A403FE31ACB47168 → /manage/user（等 4.5s、3 列渲染、「清除排序」鈕存在＝i18n 非 raw key）。(1) 3-state：點「用户名」th 第 1 下 sorter--desc＋首列 User＋LS {manage_user:'userName:desc'}；第 2 下 sorter--asc＋首列 Admin＋LS userName:asc；第 3 下箭頭全消＋首列回 User（預設 id desc）＋LS {}（SC-007/FR-003、per-route 持久化 FR-008）。(2) FR-015：注入 LS bogusColumn:asc,userName:desc 後重載 → 只還原 userName desc 箭頭、list 正常抓取（bogus 未上 wire）。(3) 清除鈕：點「清除排序」→ 箭頭全消、回預設序、LS 鍵刪除（FR-007/SC-003）。(4) 多欄：先點昵称再點用户名 → LS 'nickName:desc,userName:desc'（點擊序＝優先序 FR-004）、sorter 內無優先序號碼 badge（FR-014）、pagination active=1（FR-006 回第一頁）。測後已清理排序狀態（LS 回 {}）、全程僅讀端互動。

---

## 024-password-policy — ✅📝 PASS_WITH_NOTES

**FR coverage**：FR-001 ✓（CDP 8 項各附描述渲染）；FR-002 ✓（m009 seed 7 鍵、psql 實證 2 number＋5 enum:on,off）；FR-003 ✓（update_setting→update_by_key 持久、CDP 顯 server 真值、寫端引 024 C-V persist round-trip 全綠）；FR-004 ✓（system_settings.rs:65-72 整串比對 parse::<u32> 1..=1024、先驗後寫保留原值、單元測 abc/0/-1/1025/9999/空字串→2222、C-V live 同組全綠）；FR-005 ✓（enum 分支 members 比對、非成員→2222、單元測）；FR-006 ✓（validate_password_complexity 逐條收集 Vec 非短路、validate_returns_all_violations_non_short_circuit 測證）；FR-007 ✓（is_ascii_graphic&&!is_ascii_alphanumeric、password_policy.rs:88）；FR-008 ✓（eq_ignore_ascii_case:92、測 Admin/admin）；FR-009 ✓（chars().count():66、邊界==min/==max Ok 測、多位元組 5字元9bytes 測）；FR-010 ✓（from_settings 缺鍵/不可解析→預設 8/64/false、4 測）；FR-011 ✓（main.rs:224-242 兩 route require_policy super-only 不變；025 getPasswordPolicy 窄自助讀=有記錄演進、見 supersessions）；FR-012 superseded（025 喚醒、⚠️ah）；FR-013 ✓（psql 現值仍 8/64/全 off＝保守預設、登入零回歸引收刀紀錄）；FR-014 ✓（m009 純 INSERT 零 DDL、single_session_default 完好且 CDP 渲染於會話設定區）。SC-001~006 對應覆蓋（SC-006 由 m009 down 對稱 DELETE＋收刀 up→down→up 綠證）。Edge cases：缺項→預設✓、min>max reject-all✓（測證＋R3 定案前端 NInputNumber :min/:max 為輕量防呆、跨欄硬驗刻意延後）、多位元組✓、邊界含✓、空密碼依長度判✓、政策全關僅長度生效✓（validate_default_policy_ok）。

**Strengths**：
- validate_value_type 的 number 分支用整串比對 early-return（rust-api/server/src/handler/system_settings.rs:65），並留註解說明為何不可走 split_once（Some(("number",_)) 對無冒號 bare 型永不命中＝靜默放行）——把 research R2 的設計陷阱直接烤進碼內防回歸
- update_setting 嚴守「先驗後寫」順序（find_by_key→validate_value_type→update_by_key），FR-004「不寫入、保留原值」由控制流結構性保證，且查無→notFound、race 二查都在（system_settings.rs:97-116）
- password_policy.rs 純模組品質高：零 DB/零 entity::、from_settings 吃非-entity KV 對、缺鍵/不可解析全退保守預設（FR-010）、17 個單元測試逐 FR 對映（非短路多違規、min>max reject-all、多位元組 chars().count、邊界含等於、eq_ignore_ascii_case）
- m009 冪等（ON CONFLICT DO NOTHING）＋down 對稱只刪自己 7 鍵（SC-006 可逆、不波及 single_session_default），零 DDL 守 FR-014
- 前端 handleNumberUpdate 鏡像 handleToggle 的「成功失敗都 refetch 回 server 真值」語意，null（清空）也 refetch 讓受控 NInputNumber 回顯真值，不留前端幻影值
- 025 喚醒 dormant 原語時新增的 getPasswordPolicy 用 7-key allowlist 過濾（user_center.rs:118-126），只回 setting_key/setting_value、不洩 session/IP 等其他設定——政策先行 2 刀組接線乾淨

**Findings**：
- **[Observation]** facade find_all 無 ORDER BY（heap order），任一設定列被更新後在列表/UI 的位置會漂移——實測「密碼最小長度」現在渲染於密碼策略區塊最末（在 5 個開關之後），與 m009 seed 順序不符（`rust-api/server/src/model/facade/system_settings.rs`）
  - 證據：find_all=Entity::find().all(conn)（:22、無 order_by）；CDP dump labels=[密碼最大長度,需含大寫字母,…,禁止密碼與帳號相同,密碼最小長度,全站單一-session 預設]＝min_length 因曾被 update（新 tuple 落 heap 尾）而排最後。spec 未定順序、非 FR 違反，且屬 008 承襲行為非 024 引入；純 UX 小疵（每次編輯後項目會換位），留記供日後 polish
- **[Observation]** validate_value_type number 分支接受非正規數字字串表示（如 "+8"、"0008"——u32::parse 接受前導 + 與前導零）且原字串照存 DB；僅 curl 直送可觸發（UI 恆送 String(number) 正規形）（`rust-api/server/src/handler/system_settings.rs`）
  - 證據：system_settings.rs:66 value.parse::<u32>() 對 "+8"/"0008" 皆 Ok 且落 1..=1024 → 放行、setting_value 存原字串。無功能破口：消費端 from_settings（password_policy.rs:37 parse::<usize>）對同形字串解析一致、前端 Number(item.settingValue) 亦正常；僅 DB 內可能存非正規形。FR-004「合法正整數」語意上 "+8" 可辯為合法表示，記為疑點非缺陷

**Supersessions（合法推翻、非缺陷）**：
- FR-012（判定能力 MUST NOT 在本刀接入實際流程／dormant）— 已被 025-user-center 合法喚醒：changePassword handler 接線 from_settings＋validate_password_complexity（user_center.rs:204-205、刪 #![allow(dead_code)]）。依據：DECISIONS §1 ⚠️ah ✅ 已決＋MILESTONES `ded99ee9`「喚醒 024 dormant password_policy（政策先行 2 刀組收尾）」——此即 024 spec Assumptions 明文預告的刀2 接線，屬設計內演進非漂移
- FR-011（政策檢視僅限系統管理權限）— 025 新增 auth-only `GET /userCenter/getPasswordPolicy` 讓任一登入者可讀 7 個密碼政策鍵值（僅 key+value、7-key allowlist、不含 description/其他設定）。原 system-settings 頁讀寫通道不變（getSystemSettings/updateSystemSetting 仍 require_policy super-only、main.rs:224-242 實證）。依據：MILESTONES `ded99ee9` 明文記錄（原計畫前端讀 super-only 端點會 403 違 025 FR-009/FR-014、U2 review loop 接住新增第 4 端點）＝有記錄的合法演進，024 FR-011 的「設定」限制完整保留、「檢視」多出一條窄自助通道

**Live 證據**：curl（Super token）GET /api/systemManage/getSystemSettings → code 0000、8 筆（7 password_*＋single_session_default），每筆含 settingKey/settingValue/valueType/description（FR-001/002）。psql SELECT system_settings → 8 列：password_min_length=8/password_max_length=64（value_type=number）＋5 個 password_require_*/forbid_username=off（enum:on,off）＋single_session_default=off 完好（FR-013 預設保守、FR-014 既有項不受影響、SC-005）。scratchpad 首選 review-token.txt 已過期（3333）、改用同目錄 review-token-fresh.txt 成功。寫端 FR（FR-003/004/005 拒絕與持久）依只讀紀律未實打，引 024 收刀 C-V 紀錄：MILESTONES `aca02c81`＝rust 254 unit＋lint(2+2)＋migration up→down→up＋live curl abc/-1/0/9999/1025/空字串→2222 值不變・合法值持久＋CDP persist round-trip 全綠。

**CDP 證據**：CDP tab AAB4D0C2CDE633D7A403FE31ACB47168（Super 登入態）navigate http://127.0.0.1:31080/manage/system-settings、等 4s dump：url 正確、cardTitles=["密码策略","会话设置"]（2 NCard 分區塊）、inputNumbers=2（值 ["64","8"]＝NInputNumber 顯 server 真值）、switches=6（密碼策略區 5 枚＋會話設定區 single_session 1 枚）、labels 8 項全繁體描述如實渲染（密碼最大長度/需含大寫字母/…/全站單一-session 預設）、rawKey=false（頁面無 page.manage.systemSettings.*／backend.* raw i18n key）。區塊標題 i18n 三檔同步實證：zh-cn.ts:663-664（密码策略/会话设置）＋en-us.ts:667-668＋app.d.ts:900-901 Schema。簡標題＋繁描述混用＝刻意（m010 revert、未 flag）。未點任何 switch/輸入（只讀紀律）。

---

## 025-user-center（个人中心：自助檢視/編輯 profile＋改密碼消費 024 政策；4 auth-only self 端點＋4 塊狀 2 欄自助頁） — ✅📝 PASS_WITH_NOTES

**FR coverage**：FR-001✓（getProfile 10 欄+live+CDP 唯讀渲染）FR-002✓（update_own_profile 實碼+C-V-2 紀錄）FR-003✓（UpdateProfileReq 無 userName/roles/password 欄、serde 忽略未知欄、facade 明註 Unchanged、前端唯讀純文字）FR-004/SC-007✓（4 端點全 claims.uid、無任何 body/query id 參數＝結構性無法指定他人）FR-005~008✓（change_password 固定順序四攔截、3 淨新 biz.password.* 碼+zh-cn/en-us 雙 locale 齊、C-V-1 紀錄）FR-009△✓（動態 rule 讀 auth-only getPasswordPolicy DB-fresh；forbid_username 無前端即時提示＝已登記遞延、後端權威半邊完整）FR-010✓（email/phone 卡三控件佔位、CDP toast 實測）FR-011✓（createdAt+createdBy 三類、live system 案例+CDP 渲染）FR-012✓（依 as-built 勘誐版：一律顯示三態、實碼 updatedText 逐分支對齊 contract §2 表）FR-013✓（classify_operator 純三值、live 回應無 operator 身分欄）FR-014✓（route_auth 範式無 require_policy、免 casbin seed、路由 meta 無角色限制、無 token→3333）FR-015✓（migration 目錄 m001~m009 零新增）。SC-001/002/003/004→實碼+收刀 C-V 紀錄；SC-005/006→本輪 CDP+live 直證；SC-007→結構性直證；SC-008→AS_BUILT_ROUTES 54 lint+收刀 C-V-6 紀錄。

**Strengths**：
- getPasswordPolicy 7-key allowlist 實作乾淨且 live 實證不洩漏：PASSWORD_POLICY_KEYS 常數過濾（handler/user_center.rs:118-126），live curl 恰回 7 個 password_* 鍵、DB 中同表的 single_session_default 被正確濾掉（psql 8 列 vs API 7 鍵交叉驗證）
- changePassword 固定順序（notFound→mismatch→oldMismatch→tooWeak→hash→窄寫）與 contract §3 逐行一致，且喚醒 024 dormant password_policy 時 DB-fresh 現查（find_all→from_settings）保證 SC-004 政策即時性
- 2 窄寫 facade fn 紀律嚴：update_own_profile 部分更新只 Set 有帶 Some 的欄（sys_user.rs:437-448）、change_own_password 只動 password＋不撤 session（§I.7）、兩者皆 §I.6 updated_at/updated_by 成對＋mutate_in_txn；op-log password redact 有 live 證據（sys_operation_log payload_after->>'password' = '<redacted>' ×3 列）
- classify_operator 純函式＋單元測試（None→system/==uid→self/其他→admin），不 join、不回 operator uid，FR-013 隱私以型別層面保證（&'static str 三值）
- 前端兩個 latent-bug 修補品質好：confirm rule 傳 toRef 消值快照 race（password-card.vue:68-71 附因果註解）、切換验证方式 watch 清空 credential 防舊密明文殘顯（:type 隨 radio 翻 password↔text）
- 三端型對齊（rust serde camelCase ↔ Api.UserCenter typings ↔ ProfileModel component state）無 type-lie，updatedAt string|null 三態貫通到 UI 渲染（未修改/bare/（管理员修改））
- 各區塊保存只送自己欄位（index.vue saveBasic/saveEmail/savePhone）＋後端部分更新，天然隔離跨區塊誤送——設計與實作互相印證

**Findings**：
- **[Minor]** wrapper 與 typings 檔頭 doc 註解殘留 as-built 前的舊語意：rev3-user-center.ts 檔頭寫「3 auth-only 端點」（實為 4）、fetchUpdateProfile 註解寫「各卡保存共用送全 model」（as-built 已改為各卡只送自己欄位的部分更新、與 contract §1 矛盾）；typings 檔頭同樣寫「3 端點 wire」（`base-web/src/service/api/rev3-user-center.ts`）
  - 證據：rev3-user-center.ts:1「self-service wrapper（3 auth-only 端點）」但檔內 4 個 fetch fn；:13「各卡保存共用送全 model」vs index.vue:45 saveBasic 只送 {userGender,nickName}；typings rev3-user-center.d.ts:2「3 端點 wire：getProfile/updateProfile/changePassword」漏 getPasswordPolicy。另 handler/user_center.rs:9 doc「created/updated 語意 9 欄」而 GetProfileRes 實為 10 欄（live curl 回 10 key）。純 doc 漂移、不影響行為，但「送全 model」註解會誤導未來維護者對部分更新契約的理解
- **[Observation]** FR-009 前端即時規則只覆蓋 7 政策鍵中的 6 鍵：password_forbid_username 開啟時無前端即時提示（僅後端 validate_password_complexity 權威攔截、submit 時才知 tooWeak）（`base-web/src/views/user-center/modules/password-card.vue`）
  - 證據：buildPolicyRules（password-card.vue:43-63）處理 min/max/uppercase/lowercase/digit/special 6 鍵、無 forbid_username 分支（前端明知 userName、技術上可做）；當前 live 政策該鍵=off 故無現行落差。已登記於 CHECKLIST §4.2 025 future（commit 03d83098「forbid_username 提示」）＝已知遞延、非漂移；後端權威把關滿足 FR-007/雙重把關的權威半邊
- **[Observation]** self 列 concurrent 消失時兩端行為不一致：get_profile 回 Internal(5000)，而 update_profile/change_password 經 facade no-op（回 Ok(None)）後 handler 仍回 0000 成功——實際什麼都沒寫卻報成功（`rust-api/server/src/handler/user_center.rs`）
  - 證據：get_profile:96 `.ok_or(AppError::Internal)`；update_own_profile facade（sys_user.rs:431）查無→Ok((txn,None,None)) no-op，handler update_profile:159-169 不檢查返回 Option、一律 Res::ok(Null)。僅在「登入中被管理員 soft-delete 自己」的窄 race window 可達（token 仍有效期間），實務影響極低；changePassword 因 step(1) 先 find 已擋 notFound、僅 facade 內二次查無的 TOCTOU 縫隙
- **[Observation]** updateProfile 部分更新語意下無法將已設欄位清回 NULL：前端空字串會存成 ''（非 NULL）、null 值則整欄跳過不更新（`rust-api/server/src/model/facade/sys_user.rs`）
  - 證據：sys_user.rs:437-448 `if let Some(v)`→Set(Some(v))：清空邮箱送 {userEmail:""}→DB 存 ''；送 null→None→Unchanged 完全不動。Edge case「手机/邮箱可為空；顯示與儲存容忍空值」以空字串語意滿足（getProfile null/'' 前端皆 coalesce ''、顯示一致），DB 層 NULL vs '' 混存屬美感問題非行為 bug

**Supersessions（合法推翻、非缺陷）**：
- FR-012 後半（本人修改/從未修改→不顯示更新資訊）被 U-polish user 收尾拍板推翻：修改时间列一律顯示（null→「未修改」、self→bare 時間、admin→（管理员修改）、system→（系统修改））——spec 內嵌 as-built 勘誤註記＋contract §2 表＋MILESTONES ded99ee9「created/updated 收尾拍板（user 直接給渲染範例）」有據＝合法演進，實碼（basic-info-card.vue updatedText 三態）與勘誤版一致
- 原計畫 3 端點（前端直讀 getSystemSettings 取政策）被 as-built U2 校正為 4 端點：getSystemSettings 為 R_SUPER-only、非-super 撞 403 違 FR-009/FR-014，故新增 auth-only /userCenter/getPasswordPolicy（contract §0 as-built 校正註記＋MILESTONES「U2 review loop 接住」有據）、AS_BUILT_ROUTES 50→54 同步
- UpdateProfileReq 原設計送全 model，as-built U-polish 校正為部分更新（各區塊只送自己欄位、後端只 Set 有帶欄）——contract §1 as-built 註記＋MILESTONES 有據；唯 wrapper 舊註解未同步（見 Minor finding）

**Live 證據**：token 檔 review-token.txt 已過期（3333），改用同 scratchpad 的 review-token-fresh.txt（有效、未自行 login）。①getProfile：200/0000 回恰 10 欄 {userName:Super, roles:[R_SUPER], userGender/nickName/userPhone/userEmail, createdAt rfc3339, createdBy:\"system\", updatedAt:\"2026-07-02T01:08:28…\", updatedBy:\"self\"}，無任何 operator uid/name 欄（FR-013）；psql 交叉：sys_user id=1 created_by=NULL→system、updated_by=1(=自己)→self，語意分類與 DB ground-truth 一致。②getPasswordPolicy：恰 7 個 password_* 鍵（min 8/max 64/5 off），psql 證 system_settings 實有 8 列（含 single_session_default）→ allowlist 過濾實證、零 session/IP 洩漏。③無 token 打 getProfile→3333（auth gate 生效）。④op-log redact live 證：sys_operation_log 最近 3 筆 sys_user UPDATE payload_after->>'password' 皆 '<redacted>'、operator_id=1。⑤FR-015：migration/src 僅 m001~m009、零新 migration。寫端 FR（002/005~008）依只讀紀律未實打，以實碼逐行＋收刀 C-V-1/C-V-2 全綠紀錄（MILESTONES ded99ee9：happy＋3 negatives＋新密 login＋部分更新單欄不動他欄）佐證。

**CDP 證據**：tab AAB4D0C2CDE633D7A403FE31ACB47168 navigate /user-center（Super 登入態）＋4s 等待後 dump：①4 塊 NCard 順序＝【修改密码→邮箱→手机号→基本资料】與 spec/contract C-V-4 完全一致；②.uc-readonly 4 枚純文字 span（非 input）＝[Super, R_SUPER, \"2026-06-22 11:15:30（系统创建）\", \"2026-07-02 01:08:28\"]——created=system 帶標註、updated=self bare 時間無標註，正合 contract §2 表兩情境；③radio 5 枚＝验证方式 3 枚（旧密码/邮箱验证码/手机验证码）＋性别 2 枚（男/女）；④點「发送验证码」佔位鈕（安全、不接後端）→ toast「功能建置中」譯文非 raw key（US4/SC-005）；⑤全頁 innerText 掃 page.userCenter.*/backend.biz.* raw key＝null（i18n 零 raw key）；⑥截圖 review-025-uc.png 存 scratchpad。動態 rule dynamism（admin 改政策→rule 反映）未重演（涉改 system settings＝寫端禁區），以 password-card.vue loadPolicyRules 讀 live 端點的實碼＋收刀 CDP「min 8→12」紀錄佐證。

---


<!-- SUMMARY -->

## 總結

**25/25 feature 全數 PASS 級**（20 PASS ＋ 5 PASS_WITH_NOTES）；**零 Critical、零 Important**。cumulative as-built（rust `ef1d21a`／base-web `f3ad8e23`）對 25 份 spec 的合規性經 live（curl/psql/redis 讀端）＋ CDP 真瀏覽器實測確認；所有 spec 字面偏離均有 DECISIONS/MILESTONES 拍板依據（supersession）、無無依據漂移。

| # | Feature | Verdict | 備註 |
|---|---|---|---|
| 001 | 001-infra-deploy | ✅ PASS | 2 Obs |
| 002 | 002-rev2-schema-baseline | ✅ PASS | 2 Obs |
| 003 | 003-envelope | ✅ PASS | 2 Obs |
| 004 | 004-soft-delete-infra | ✅ PASS | 2 Obs |
| 005 | 005-audit-op-log | ✅ PASS | 2 Obs |
| 006 | 006-auth-island-min | ✅ PASS | 2 Obs |
| 007 | 007-audit-overlay | ✅ PASS | 3 Obs |
| 008 | 008-system-settings | ✅ PASS | 2 Obs |
| 009 | 009-user-management | ✅📝 PASS_WITH_NOTES | 2 Minor、1 Obs |
| 010 | 010-menu-management | ✅ PASS | clean |
| 011 | 011-role-management | ✅ PASS | 2 Obs |
| 012 | 012-audit-log-query | ✅📝 PASS_WITH_NOTES | 1 Minor |
| 013 | 013-xff-real-ip-forensics | ✅ PASS | 2 Obs |
| 014 | 014-auth-token-session | ✅ PASS | 2 Obs |
| 015 | 015-policy-governance | ✅ PASS | 2 Obs |
| 016 | 016-button-endpoint-policy | ✅ PASS | 1 Obs |
| 017 | 017-audit-center-enhancement | ✅ PASS | 3 Obs |
| 018 | 018-observability | ✅ PASS | 2 Obs |
| 019 | 019-login-lockout | ✅ PASS | 1 Obs |
| 020 | 020-role-delete-policy-archive | ✅ PASS | 3 Obs |
| 021 | 021-login-lockout-redis-cache | ✅📝 PASS_WITH_NOTES | 3 Obs |
| 022 | 022-ip-access-control | ✅ PASS | 1 Obs |
| 023 | 023-list-column-sort | ✅ PASS | 2 Obs |
| 024 | 024-password-policy | ✅📝 PASS_WITH_NOTES | 2 Obs |
| 025 | 025-user-center | ✅📝 PASS_WITH_NOTES | 1 Minor、3 Obs |

### Findings 彙總（全 25 feature、按嚴重度）

**Critical／Important：無。**

**Minor（3、皆非本輪引入之回歸）**：
1. 〔009〕搜索「重置」鈕只清表單不重新查詢（upstream soybean 原樣、非 rev3 引入；user/role 頁同模式）
2. 〔009〕edit drawer「用户名」欄可編輯但後端刻意不寫（欄位級靜默 no-op、FR-004 由後端強制故無安全問題；UX 縫隙）
3. 〔025〕`rev3-user-center.ts`／`rev3-user-center.d.ts` 檔頭 doc 殘留「3 端點／送全 model」舊語意（as-built 已是 4 端點＋部分更新；純註解債、U-polish 漏刷）

**行動建議（不阻擋、後續處置候選）**：
- 修 025 wrapper/typings 檔頭註解（2 行、下次動 base-web 順手）
- 009 初始密碼 `123456` 不經 024 政策 → 已屬「初始密碼政策化／強迫首登改密」future gap（建議登 CHECKLIST §4.2）
- 025 self 列 concurrent 消失時 update/changePassword no-op 仍回 0000（與 getProfile 回 5000 不一致）→ 極端邊角、可與空 body 邊角（已登 §3.H）同刀處理
- 024 `find_all` 無 ORDER BY → 設定列更新後 UI 位置漂移（「密碼最小長度」現渲染在區塊末）→ 下次動 system-settings 時 `ORDER BY setting_key` 或 seed 序
- 002 reviewer 再度點名 CLAUDE.md §8.1 sys_user 5 欄描述 stale（已有 memory 補救、repo 文件未修）

### 方法論備忘
- 3 批 serial Workflow（001-008／009-016／017-025）×25 reviewer agent、共 ~2.7M subagent tokens、~2.6 小時。
- CDP 實測抓到基線輪（純靜態）抓不到的真相：009 重置鈕行為、017 匯出鈕收合可發現性、024 設定列位置漂移、022/023/025 各頁 i18n 零 raw key 實證。
- 共用 token 於 batch 3 中途過期（exp ~7 天但 login 時刻不同）；017/021 reviewer 依「不自行 login」紀律改由 CDP 讀瀏覽器 localStorage 有效 token（唯讀）恢復——SOP 有效。
- 非破壞紀律全程維持：零寫端 API、零 psql 寫、零 lockout 觸發；寫端 FR 以實碼＋各刀收刀 C-V 全綠聲明背書。
