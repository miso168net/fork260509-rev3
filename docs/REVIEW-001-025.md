# REVIEW-001-025 — 全 feature spec-compliance review（cumulative as-built + CDP 實測）

> **執行**：2026-07-02 ｜ **方法**：`superpowers:requesting-code-review` 框架 + Workflow serial 編排（001→025 依序、後刀可合法推翻前刀故不平行）
> **oracle**：各 `specs/<NNN>-*/spec.md`（FR/SC/US/Edge）｜ **對象**：cumulative as-built @ pins rust-api `ef1d21a`／base-web `f3ad8e23`（outer `c07c5e78`）
> **與 [REVIEW-001-019](REVIEW-001-019.md)（2026-06-25、19/19 PASS、純靜態）的關係**：本輪為其後繼——新增 020-025 六刀、並對涉 base-web 的 feature 加 **CDP 實測**（127.0.0.1:9229、31080 tab）；Δ 重點＝後六刀是否踩壞舊 feature + 舊 supersession map 沿用/擴充。
> **紀律**：reviewer 只讀 repo（不寫檔/不 git 寫操作）；live 驗證**非破壞**（讀端 API/UI 渲染/list 查詢；禁建/刪/改持久資料、禁改密、login 失敗嘗試 ≤2 次防 lockout、禁建 ip 規則；寫端行為以 code 靜態 + 收刀 C-V 證據）；共用 Super token（不重複 login）；**過程零提問**——所有疑點記入本檔。
> **supersession 紀律**：早期 spec 行為被後續 feature 推翻＝查 [DECISIONS §1](INTEGRATION-DECISIONS.md)/[MILESTONES §1](INTEGRATION-MILESTONES.md) 有無拍板/as-built 依據→有依據標 SUPERSEDED（非缺陷）；無依據漂移才是 finding。
> **verdict 詞彙**：PASS／PASS_WITH_NOTES（有 Minor/observation）／ISSUES（有 Critical/Important）／INCONCLUSIVE（agent 失敗或證據不足）。

---

## 執行狀態

- [x] Batch 1：001–008 ✅ 8/8 PASS（僅 Observation）
- [ ] Batch 2：009–016
- [ ] Batch 3：017–025
- [ ] 總結表＋findings 彙總

---

<!-- BATCH 1 RESULTS -->

## 001-infra-deploy — ✅ PASS

**FR coverage**：10/10 FR 覆蓋（FR-001~004/007 活體實證、FR-005/006/009/010 靜態實碼＋grep、FR-008 活體 gate＋合法 supersession）；無缺項。SC 7/7：5 項活體、SC-001/005 引 001 收刀 C-V 全綠（非破壞紀律不重演 prod 起停）。

**Strengths**：
- migrate gate 鏈條設計乾淨且活體可證：docker-compose.yml:51-57 rust-api depends_on migrate service_completed_successfully＋postgres/redis healthy，live ps 顯示 migrate Exited(0)、rust-api healthy（FR-002/SC-003 閘門順序實證）
- nginx 入口遮蔽用 exact-match 擋塊（deploy/nginx/conf.d/_locations.inc:12-14 location = /api/metrics return 404 優先於 prefix /api/），註解明載內網 prometheus 直連不受影響——live 實測 /api/metrics=404、/api/health=ok 同時成立
- secret 紀律超出 spec 最低要求：deploy/preflight-secrets.sh 接住 docker compose 對缺 bind source 自動建空目錄的 footgun（錯誤訊息誤導問題）、deploy/generate-secrets.sh 有 dual-write drift 自動防護；deploy/secrets/*.txt 全 gitignored、僅 .example 入版控（FR-005）
- FR-009 rev2 指涉歸零在 025 收刀後仍保持：grep -riE rev2 對全部 docker-compose*.yml＋deploy/ 零命中（排除 dev-certs 二進位），20~25 六刀未引入漂移

**Findings**：
- **[Observation]** front_nginx_certs 具名卷在當前 dev host 不存在（docker volume ls 僅 10 卷），prod 起停演練（SC-005/US3）本輪未活體重演——屬非破壞紀律的刻意跳過，依收刀 C-V 全綠聲明背書（`docker-compose.prod.yml`）
  - 證據：docker volume ls --format '{{.Name}}' | grep rev3-admin_ 無 front_nginx_certs（compose 頂層已宣告 docker-compose.yml:281、僅 prod/acme 掛載時才 materialize＝正常 compose 行為）；SC-005 引 specs/001-infra-deploy/contracts/ C-V 與 MILESTONES 001 收刀全綠紀錄
- **[Observation]** FR-010 standalone rust-api compose（docker-compose.rust-api.yml）存在且形狀正確（dev+runtime 雙 target、name: rev3-admin 共用 project），但 CLAUDE.md §2 已標其 DEPRECATED debug 後備——spec 義務仍滿足、僅用途降級（`docker-compose.rust-api.yml`）
  - 證據：grep 顯示 name: rev3-admin / target: dev / target: runtime / rust_api_target 卷對齊正典命名；CLAUDE.md §2 註「docker-compose.rust-api.yml〔DEPRECATED debug 後備〕」

**Supersessions（合法推翻、非缺陷）**：
- FR-008「schema 初始化本階段空跑（零 schema 變更）」→ 002~024 落 m001~m009 真 migration（rust-api/migration/src/ 九支 mNNN_<name> 命名慣例正是 001 ⚠️k 確立形）→ 依據：⚠️t（rev2 終態 squash 基線屬 002 刀、spec Assumptions 明示）＋MILESTONES §1 各刀紀錄；live migrate log「No pending migrations」exit 0 證機制仍健康
- FR-003 入口 /api/ location 後加 limit_req 速率限制（M-9）＋CF 驗證 header 對（X-CF-Verified/CF-Connecting-IP、nginx.conf geo/map）→ 依據：013 spec FR-010＋DECISIONS §1 ⚠️ae（CF Tunnel 拓樸）＋CHECKLIST §3.J 硬化；不改 001 的四類路由行為（live 4/4 仍過）
- 001 的 5-service＋migrate＋acme 殼基底形 → 後加 cleanup-job（014 U5、profiles:[prod]）、rust-api-2（014 U7、profiles:[multi]）、obs/metrics 8 service（018、profiles:[obs]/[metrics]）＋grafana_admin_password 第 7 secret → 依據：MILESTONES §1 014/018 收刀紀錄；全部 profile-gated、一般 up 不啟、FR-006 base 層無 ports/專屬掛載紀律未破
- FR-006 prod 443 server 後加安全 header（HSTS/XFO/nosniff/Referrer-Policy/Permissions-Policy/結構性 CSP）→ 依據：CHECKLIST §3.J M-8 硬化 follow-up（deploy/nginx/conf.d/prod.conf 註解內載）；80→443 redirect＋/health 例外原形不變

**Live 證據**：curl http://127.0.0.1:31080/health→ok；curl -k https://127.0.0.1:31443/health→ok（自簽 TLS）；curl http://127.0.0.1:31081/health→ok（API 直連、純文字非信封＝FR-004）；curl 31080/api/health→ok（前綴剝離）；31080/api/metrics→404（遮蔽）；31080/ →200 622B（前端）。compose ps：front-nginx/base-web/rust-api/postgres/redis-stack 5 service 全 healthy、migrate Exited(0)（log「Applying all pending migrations→No pending migrations」）。容器內 pg_isready→accepting、redis-cli ping→PONG（帶 secret 檔密碼）。psql：sys_user=9、seaql_migrations=9（stack 歷經多次重啟資料保留＝SC-004）。docker volume ls：10 個 rev3-admin_ 前綴卷全符 §8.2.2 正典命名（SC-002 6/6、SC-003/004/006/007 活體過；SC-001/005 引收刀 C-V）。

**CDP 證據**：CDP（pageId AAB4D0C2...）Runtime.evaluate on 31080：url=http://127.0.0.1:31080/home、title=首页、#app 存在、body innerText 520 chars、raw i18n key（page.*/common.*/route.*/backend.*）零命中——證統一入口經 nginx 服務 SPA 完整渲染且 i18n 非殘鍵（US2-AS3 補強）。

---

## 002-rev2-schema-baseline — ✅ PASS

**FR coverage**：10/10 FR 覆蓋（FR-001/002/005 live psql 逐項證；FR-003 腳本資產+checkpoint 設計驗、需 runtime 重放未重跑；FR-004 m003/m004 獨立步驟+FK live 證；FR-006 m004 R_SUPER-only 引基線輪+casbin live；FR-007/008 down() 9/9+ON CONFLICT 全覆蓋+收刀 C-V；FR-009 adapter 委派碼證；FR-010 收刀 prod build+024 migration/src 整目錄 COPY 再證）；SC-001~008 中 SC-001/003 needs-runtime（腳本在、基準 dump git-tracked），餘 live/碼證全中

**Strengths**：
- m001 忠實 squash 品質高：逐欄重現歷史疊加欄序（rust-api/migration/src/m001_rev2_schema.rs:1-15 檔頭明文欄序敏感紀律）、partial WHERE index 刻意 raw SQL 求逐字對齊 dump；live 驗 sys_user 16 欄序與 Iden enum 完全一致、partial uniq ×3（sys_user_user_name/sys_role_code/sys_menu_route_name _active_uniq）齊在
- m002 seed 三紀律全落實：argon2id runtime 生成三列共用（m002:44-46；live DISTINCT password=1、PHC $argon2id$v=19$m=19456）、id 不寫死（INSERT 無 id 欄靠 sequence、sys_user_role 雙向 subquery 解析 m002:76-83）、每條 INSERT 帶 partial-index-aware ON CONFLICT DO NOTHING（FR-008 冪等）
- FR-009 政策表單一來源乾淨：m001:640 `sea_orm_adapter::up()` 委派建 8 欄基底＋同表 ALTER 補 3 治理欄、down 對稱委派 m001:880，無手寫雙來源 DDL
- 零漂移驗證資產 git-tracked 且 checkpoint 正確：tests/002-rev2-schema-baseline/scripts/ 四支（pristine-replay/normalize/diff-baseline/delta-assert），diff-baseline.sh 明確 `up -n 2` 停在 m002 檢查點（delta 前）＝後續 m003~m009 演進不會污染零漂移基準；normalize 含 COPY 段排序第六規則（layer2 物理列序假紅對策）

**Findings**：
- **[Observation]** CLAUDE.md §8.1 對 sys_user 欄位的描述（僅列 id/user_name/password/nick_name/deleted_at 5 欄）與 m001 實際 16 欄長期 stale，易誤導未來規劃（gender/phone/email/status/session_policy 等本就 m001 有）（`CLAUDE.md`）
  - 證據：live psql information_schema：sys_user 16 欄（id,user_name,password,deleted_at,nick_name,user_gender,user_phone,user_email,status,created_at,created_by,updated_by,deleted_by,updated_at,current_session_id,session_policy）；CLAUDE.md §8.1 只列 5 欄。025 規劃期已靠 memory 補救（零 migration 正確判斷），但 repo 文件本身未修
- **[Observation]** FR-003 零漂移驗證屬 needs-runtime：依賴前代 image rev2-admin-rust-api:latest 本機可用（spec Assumptions 已明列），本輪未重跑、僅驗腳本資產完整且 checkpoint 設計對後續 m003~m009 演進免疫（`tests/002-rev2-schema-baseline/scripts/diff-baseline.sh`）
  - 證據：pristine-replay.sh:6 依賴前代 image 跑 35 支 migration；diff-baseline.sh:57-58 `up -n 2` 停 m002 檢查點。git log 顯示三支 commit 後資產零改動（32a1efee 基準 dump git-tracked）。與 REVIEW-001-019.md L59 基線輪同判（needs-runtime）

**Supersessions（合法推翻、非缺陷）**：
- FR-001「12 表口徑」→ 現況 13 表（+sys_ip_rule）：m007（022-ip-access-control、破 0-migration streak）→ MILESTONES §1 d9c809e6／DECISIONS §1 ⚠️ae
- FR-001「基線審計欄四變體」→ 三 append-only 日誌表 rename+加 IP forensic 欄（operator_ip→operator_real_ip、+peer_ip/ip_confidence/operator_x_forwarded_for）：m006（013-xff-real-ip-forensics）→ MILESTONES §1 8980d00＋constitution amendment ⚠️ac（82f49d4、§I.6 釐清刻意/可逆演進不在 retrofit 禁令）
- FR-001「7 個一般索引」→ +4 查詢索引（m005、012-audit-log-query）＋idx_login_attempt_created_at（m008、023 clarify Q1 拍板）→ MILESTONES §1 009901fa／DECISIONS §1 ⚠️af
- FR-002「system_settings 1 列」→ 現況 8 列（+7 密碼政策 KV、m009、024-password-policy）→ MILESTONES §1 aca02c81／DECISIONS §1 ⚠️ag；基線列 single_session_default=off 原值不變（live 證）
- SC-002「92 列／6 表」計數不變式＝基線檢查點口徑：live dev 庫累積態（users 9/roles 8/menus 78/casbin 149）為 m004 delta＋016/022 政策刀＋runtime CRUD 合法演進；基線列本身逐項驗證原值不變（見 liveEvidence）

**Live 證據**：psql：①13 表齊（12 口徑+m007 sys_ip_rule）、seaql_migrations m001~m009 九支序全套 ②sys_user 16 欄歷史欄序逐字合 m001 Iden、partial uniq ×3 在 ③seed 帳號 id 1/2/3=Super/Admin/User、nick 超级管理员/Admin/User01（淨值回填）、三列共用單一 $argon2id$v=19 hash（DISTINCT=1）、status=1 ④sys_role 1/2/3=R_SUPER/R_ADMIN/R_USER_COMMON home='home'、user_role 映射 1→R_SUPER/2→R_ADMIN/3→R_USER_COMMON ⑤delta：sys_user_role FK ×2（fk_sys_user_role_user/role、ON DELETE RESTRICT＝spec 工程預設）+複合 PK ⑥基線不變式現值：casbin g=0、protected=19（僅 m002 產 protected 列→基線政策原值不變）、button policy=16、sys_menu protected=8、基線選單 id 1~10 全在未刪（home/manage/.../manage_policy-archive）、single_session_default=off。curl：GET /auth/getUserInfo（共用 Super token＝login 成功＝123456 驗過 argon2id）→ userId:1、roles:[R_SUPER]、buttons 12 枚（R_SUPER 按鈕政策合 casbin seed）。寫端 FR-007/008（up→down→up、冪等）引收刀 C-V 全綠＋MILESTONES L130 追驗（fresh dev DB m006 rollback→re-apply 綠、chain 可逆、seed 完整）。

**CDP 證據**：N/A（002 為 schema/seed/驗證基建刀、無 UI 面；spec Assumptions 明示業務 API 與 entity 層不在本刀）

---

## 003-envelope — ✅ PASS

**FR coverage**：12/12 FR 覆蓋（FR-001~012 全數有實碼＋live/測試證據；無缺）。SC-001~008 對應：SC-001/002/003/004 live curl＋error.rs 矩陣測、SC-005 zh-cn/en-us 13 碼譯文齊、SC-006 translateBackendMsg=vue-i18n 原生 fallback、SC-007 /health ok＋stack 健康、SC-008 in-crate 8 契約測（025 收刀 254 測全綠聲明、本輪依紀律不重跑）。

**Strengths**：
- reserved 4 碼採型別層凍結（error.rs:32-54 enum 無對應變體＝編譯期保證永不構造，優於 runtime 斷言）；且 020-025 六刀後全 src grep 證實零 reserved 構造點、025 淨新 biz.password.* 三鍵全走 Biz(2222) 不擴矩陣——凍結契約 9 刀下來零漂移
- 前端翻譯點紀律嚴守 FR-008「烤在讀 backend msg 的邊界」：service/request/index.ts:45 單一 translatedMsg 供 dedup filter(:56)/includes 守門(:69)/push(:71)/modal content(:79) 四處共用（不變式四處同值、dedup 不破），未污染通用顯示函式
- 契約測試含反例守護（error.rs:273-276 拒非 4 根/無點/CJK/大寫首字母），FR-011「文法-conformance 斷言能拒絕不合規約識別碼」有實證非僅正例
- locale seed 雙語完整且後續刀（008/009/010/011/012/019/023/024/025）全循 backend.biz.* 規約擴充（zh-cn.ts:917-986 逐刀註記可稽），FR-006 前瞻契約價值實際兌現

**Findings**：
- **[Observation]** PageRes 於 live wire 的 data 內層鍵序被 Res::ok 的 serde_json::to_value 字母序化（current→records→size→total），與 envelope.rs 宣告序/in-crate 測試斷言的 current→size→total→records 不同（`rust-api/server/src/envelope.rs`）
  - 證據：envelope.rs:34-40 Res::ok 走 serde_json::to_value（Value::Object 預設 BTreeMap 排序鍵）；live curl getUserList 實測 data keys=['current','records','size','total']。無害：spec 僅凍結信封欄序 data→code→msg（Key Entities 表「固定欄序」只標信封列；contract §4 不變式 4 對 PageRes 只斷言無 pages/success＋空頁 []），JSON object 鍵序非語意、前端按鍵取值。但 envelope.rs:104-133 的 page_res 欄序測試斷言的是「直接序列化 PageRes」路徑、與實際 wire（經 to_value）不同路徑——若日後有人依測試推斷 wire 鍵序會誤判
- **[Observation]** Res::ok 序列化失敗時靜默降級 data:null 仍回 code 0000（成功碼＋空 payload）（`rust-api/server/src/envelope.rs`）
  - 證據：envelope.rs:36 `serde_json::to_value(data).unwrap_or(serde_json::Value::Null)`——T: Serialize 的 to_value 實務上僅在 map 非 string key 等罕況失敗，現有 DTO 全 struct 不可達；記錄備查、非 bug

**Supersessions（合法推翻、非缺陷）**：
- spec Edge Case／FR-008「4040/5003 native axios 路徑 msg 未被讀取顯示、本刀不修復（登 follow-up）」→ pre-波4 sweep §3.G（2026-06-23、base-web commit 7a011c00）補 onError else-if（index.ts:120-124）令 HTTP 403/404 信封 msg 也經 translateBackendMsg 在地化 → 依據：MILESTONES §3.G 歸檔節（⚠️aa BASE-WEB-I18N-WIRING §III.2(i) 授權、CDP 實證 toast「权限不足」）——此為 spec 自身登記之 follow-up 路徑兌現、非漂移
- spec Assumptions「From<DbErr> 不在本刀（R7）」→ 006-auth-island T009 帶入 error.rs:112-116（DbErr→Internal 5000）→ 依據：MILESTONES §1 006 行＋error.rs:110 註明出處；REVIEW-001-019 基線已認定
- spec「本刀無業務端點、唯一可達錯誤＝NotFound」→ 006~025 各刀陸續消費 LoginFailed/TokenExpired/Biz/PermissionDenied 等變體 → spec 本文預期之階梯（FR-012），MILESTONES §1 各刀行為據；i18n 首檢點（auth/login）與 per-entity 端到端（008 system-settings）均已如 spec 排程兌現

**Live 證據**：① curl GET /api/auth/getUserInfo（Super token）→ HTTP 200 `{"data":{...},"code":"0000","msg":"common.success"}` 欄序 data→code→msg、code 為 string；② 無 token 同端點 → HTTP 200＋`{"data":null,"code":"3333","msg":"auth.token.expired"}`（⚠️e 可顯示錯誤走 200、data:null 不省略）；③ 亂 route /api/no/such/route → HTTP 404＋`{"data":null,"code":"4040","msg":"system.notFound"}`（fallback 信封形）；④ getUserList 分頁 → data keys={current,size,total,records}、無 pages/無 success（python json 驗）；⑤ /health → plain-text "ok" HTTP 200（探針豁免信封、健康）。碼矩陣靜態掃描：全 src AppError 構造僅 9 可發變體（Biz×104/Internal×23/…）、reserved 7778/8889/9998/9999 零構造點（grep 命中全為無關數字字面）、全部 Biz key 皆靜態字面且合 4 根文法（反向 grep 零不合規）。

**CDP 證據**：tab 意外處於登出態（/login redirect）→ 依 SOP 注入 SOY_token 後 navigate http://localhost:31080/manage/system-settings：頁面可達（href 正確、innerText len=239）、核心元素渲染（密码策略/会话设置 區塊＋7 政策欄位標籤全在）、i18n 斷言 raw:[]（body innerText 以 /\b(backend|page|common|route)\.[a-zA-Z]+\./ 掃描零命中＝無 raw key）。簡繁混用（简体標題+繁體描述）＝024 刻意保留待 zh-TW locale feature、非 003 缺陷。biz toast 在地化另有 §3.G sweep 既存 CDP 實證（真 HTTP 403→toast「权限不足」、commit 7a011c00 記錄），本輪未觸發寫端/錯密碼（遵守非破壞紀律）。

---

## 004-soft-delete-infra — ✅ PASS

**FR coverage**：9/9 FR 覆蓋（FR-001 trait+find_active 實碼／FR-002 3 實體 impl+system_settings 例外 facade find() 直讀+022 合法第 4 實體／FR-003 lint+獨立 grep 零違規／FR-004 whiten+前界規則+框架子路徑不誤殺／FR-005 12 實體模組（11+022）／FR-006 scan_self_test 雙證／FR-007 psql+curl 活體實證／FR-008 收刀 SC-005 歷史邊界+寫路徑由後續刀各自帶入（spec assumption 明示）／FR-009 收刀 158MB image+025 prod build ×2 持續綠）；SC-001~007 全對應成立

**Strengths**：
- SoftDeletable trait（rust-api/server/src/model/soft_delete.rs:14-22）仍是純 sea_orm 泛型、零 entity:: 依賴，025 收刀後被 4 個 facade impl（sys_user/sys_role/sys_menu/sys_ip_rule）與大量 find_active_* 讀端消費，US1「宣告 deleted_at_column 即繼承」承諾在 022 sys_ip_rule 上實證兌現（facade/sys_ip_rule.rs:20 impl 後 list/find_active_by_id/restore 全走 find_active、機制本體零改動）
- entity_access_lint（rust-api/server/tests/entity_access_lint.rs）雙證結構完整存活：whiten（行/區塊註解+字串抹白、逐 char 處理 CJK）+ path-root 前界規則（非 ident 非 ':'，brace-group {entity:: 視為違規）+ scan_self_test 9 斷言非 vacuous；獨立 grep 復核 server/src 下 facade 外 path-root entity:: 為零命中，FR-003 在 025 cumulative 態仍守恆
- deleted_at/deleted_by 成對範式（constitution §I.6）活體零漂移：psql 驗 sys_user/sys_role/sys_menu 三表 (deleted_at IS NULL)<>(deleted_by IS NULL) 不成對列數=0（含 11 筆歷史軟刪列全成對）；4 個 partial-unique index（user_name/code/route_name/cidr+rule_type WHERE deleted_at IS NULL）全在
- soft-delete no-op 合約跨 facade 一致：sys_user.soft_delete 已刪→Ok(None) idempotent（M-2 加固、sys_user.rs:67）、sys_role 經 find_active_by_id_for_update None→no-op（020 C1 lock-then-redecide 疊加不破合約）、sys_menu/sys_ip_rule 同範式；restore 端正確用含已刪讀（sys_menu.rs:1025 find_by_id 非 find_active）+ active 衝突預檢（sys_ip_rule.rs:331）

**Findings**：
- **[Observation]** SoftDeletable trait 上的 #[allow(dead_code)]（004 當時「無 caller」暫抑）已 stale——現有 4 個 impl 與大量 find_active 消費者，抑制屬多餘但無害（`rust-api/server/src/model/facade/../soft_delete.rs`）
  - 證據：soft_delete.rs:12-13 註解「本刀（004）無 caller，待業務刀消費 → 暫抑 dead_code」+ #[allow(dead_code)] 仍在；但 grep 顯示 sys_user/sys_role/sys_menu/sys_ip_rule 四 facade 均 impl 且 find_active 被 20+ 處消費。屬過期註解/抑制殘留、不影響 lint 或行為，下次touch該檔可順手拔
- **[Observation]** sys_role.soft_delete 的已刪 no-op 走 find_active_by_id_for_update→None 路徑（隱式），未如 sys_user M-2 般顯式匹配 before.deleted_at.is_some()——行為等價（已刪列不在 find_active 集合、回 Ok(None) 不寫審計），僅範式表達不一（`rust-api/server/src/model/facade/sys_role.rs`）
  - 證據：sys_role.rs:401 let before = find_active_by_id_for_update(&txn, id).await?（None→no-op）；sys_user.rs:61-67 用 find_by_id（含已刪）+ 顯式 Some(before) if before.deleted_at.is_some() 分支。兩者對「重刪已刪列」皆回 Ok(None) 不重寫不重發 SOFT_DELETE，合約一致、無缺陷

**Supersessions（合法推翻、非缺陷）**：
- FR-002/Key Entities「恰好 3 個 soft-delete 實體（user/role/menu）」→ 022-ip-access-control 增第 4 個 sys_ip_rule（m007 建表+partial-unique cidr+rule_type、impl SoftDeletable）——依據 MILESTONES §1 `d9c809e6`（2026-06-29 022 全綠收刀、破 0-migration streak 明載）；屬機制被新切片正當繼承（正是 US1 AC-3 設計目標）非漂移
- FR-005「11 表→11 實體模組」→ 現 12 模組（+sys_ip_rule）——同上 022 依據；「後續切片永不改動既有實體定義」不變式仍守住（新增非改動）
- soft_delete 已刪列語意：004/009 原碼會對已刪列重覆寫 deleted_at/deleted_by 並重發 SOFT_DELETE op-log → M-2 加固為 idempotent no-op（Ok(None)、與 batch「已刪 skip」對齊）——依據 MILESTONES §1 `0c14abdb`（2026-06-25 M-tier 9 項閉合、M-2 明載）；合法強化
- log entity（sys_operation_log/sys_access_log/sys_login_attempt）欄位由 005/013 演進（operator 單 IP→四欄 forensic）——基線 REVIEW-001-019.md §B 已確認之既知 supersession，本輪復核實體檔仍與 as-built schema 對齊
- 020 D10/C1：sys_role 刪除路徑升級 find_active_by_id→find_active_by_id_for_update（FOR UPDATE 序列化 restore-during-delete）——依據 MILESTONES 020 收刀行；疊加於 004 find_active 範式之上、未破壞 active 過濾語意

**Live 證據**：psql①欄位: information_schema 查 deleted_at/deleted_by 成對存在於 sys_user/sys_role/sys_menu/system_settings/sys_ip_rule 各 2 欄。②partial-unique: pg_indexes WHERE deleted_at IS NULL → 4 index（user_name/code/route_name/cidr+rule_type）。③已刪樣本: sys_user 9 列中 6 軟刪、sys_role 8 列中 5 軟刪，全部 deleted_by=1 成對；三表不成對計數=0。④FR-007/SC-002 活體: curl GET /systemManage/getUserList → code 0000 total=3（僅 Super/Admin/User，6 軟刪列 100% 排除）；getRoleList → total=3（R_SUPER/R_ADMIN/R_USER_COMMON，5 軟刪列排除）；未過濾面由 psql 直查證列仍在（資料未實體移除）。⑤FR-003 靜態復核: grep server/src facade 外 path-root entity:: 零命中。寫端（soft_delete/restore）＝碼靜態＋收刀證據（MILESTONES `a1105f0` C-V-0~3 全綠 lint 2 passed/live smoke txn-rollback/prod image 158MB；M-2 `0c14abdb` 184 passed；025 `ded99ee9` 255 unit+lint 2+2+prod build ×2 綠＝entity crate 打包至今無回歸、FR-009 持續成立）。

**CDP 證據**：N/A（本 feature 為資料層基建、無專屬 UI 面；active 過濾對前端的間接效果由 curl getUserList/getRoleList 讀端證據覆蓋）

---

## 005-audit-op-log — ✅ PASS

**FR coverage**：10/10 FR 覆蓋（FR-001 mutate_in_txn+三路徑 live 測、FR-002 24 caller 零改本體、FR-003 redact 純測+psql 0 違、FR-004 insert-only facade+entity_access_lint、FR-005 全欄落 write_in_txn〔IP 四欄=013 合法演進〕、FR-006 no-op Ok(None) 含已刪 idempotent、FR-007 audit.rs 零 entity::、FR-008 顯式→007/013 合法自動化、FR-009/010 歷史邊界收刀時已證〔MILESTONES `98f1f7e` SC-005~007 全綠〕）；無缺者

**Strengths**：
- mutate_in_txn 原子合約三分支（Some(event)→facade write_in_txn+commit／None→no-op commit／Err→txn drop rollback）碼面完整（rust-api/server/src/model/audit.rs:84-97），且 op_log_atomic_tests 三路徑 live smoke（commit/no-op/rollback write-then-Err、savepoint 隔離）常駐 facade/sys_user.rs:693+ 非 vacuous
- redact 雙證扎實：audit_json 手構 json password 固定 "<redacted>"（facade/sys_user.rs:22-43、Model 刻意不 derive Serialize）＋純測 redact_tests（sys_user.rs:656）＋live psql 全表 0 筆未遮蔽
- append-only + facade 唯一管道守恆延續：sys_operation_log facade 僅 write_in_txn/write_standalone 兩個 insert fn、零 update/delete（facade/sys_operation_log.rs:17-44），entity_access_lint 仍在崗、機制本體 audit.rs 零 entity::（僅 sea_orm::entity::prelude::IpNetwork 全路徑、lint-safe）
- 實體中立性經 20 刀實戰驗證：mutate_in_txn 現有 24 個 caller 跨 8 個 facade 模組，005 後新增寫端（009/010/011/015/016/022/024/025）皆零改機制本體，025 password-only 更新亦自動繼承 redact（sys_user.rs:509）

**Findings**：
- **[Observation]** facade 層 ip_host_like 反向依賴 handler 層的 escape_like（層次方向小逆流、非 spec 違反）（`rust-api/server/src/model/facade/sys_operation_log.rs`）
  - 證據：line 61: `crate::handler::system_manage::escape_like(raw)`——012 引入、基線輪已 PASS；功能正確（LIKE escape+綁定值防注入），僅記 model→handler 依賴方向為日後重構候選
- **[Observation]** op-log id 序列有大量空洞（min=11、max=527、count=139），為 rollback 路徑消耗 sequence 的正常痕跡、非資料遺失（`rust-api/server/src/model/audit.rs`）
  - 證據：psql: SELECT min(id),max(id),count(*) FROM sys_operation_log → 11|527|139；碼面零 delete 路徑（cleanup-job 只 DELETE FROM sys_token、cleanup-job/src/main.rs:44），空洞來自 live smoke／失敗 txn rollback＝SC-002 原子性實際運作的側證

**Supersessions（合法推翻、非缺陷）**：
- FR-005/FR-008 operator 單 IP＋顯式參數 → 013 四欄鑑識（operator_real_ip/peer_ip/x_forwarded_for/ip_confidence）＋007/013 audit_ctx RequestContext::to_audit_meta 自動抽取——005 spec Assumptions 自身預告「後續刀回填」；依據 REVIEW-001-019 §B＋MILESTONES §1 013 行
- payload 快照無 roles → 017 with_roles 注入排序角色集（audit.rs:103、017 C-4 拍板）；依據 MILESTONES §1 017 行＋REVIEW-001-019 supersession map
- op-log 無讀端 → 012 getOperationLog list（純 SELECT、R_SUPER）＋023 resolve_sort 白名單排序——皆唯讀、不動 append-only sink；依據 MILESTONES §1 `55d34ff`（012）＋023 收刀行
- sink 新增 write_standalone（獨立 op-log 寫、無 entity 變更事件）→ 022 FR-011 手動解鎖稽核軌——仍 insert-only、留在同 facade（archetype B 不破）；依據 MILESTONES §1 022 行＋facade/sys_operation_log.rs:36-38 檔內註

**Live 證據**：psql ①最近 8 列 op-log：operation=UPDATE、entity_table=sys_user/system_settings、operator_id=1、trace_id 全非空、created_at 連續至 2026-07-02（025 寫端仍經管道落審計）②DISTINCT operation = {INSERT,RESTORE,SOFT_DELETE,UPDATE}＝封閉詞彙集全數且無越集值（FR-005）③sys_user 列 payload_before/after->>'password' 抽樣 5 筆全 '<redacted>'、全表未遮蔽計數=0（FR-003 100%）④min/max/count=11/527/139：序列空洞＝rollback 消耗、碼面零 delete 路徑（cleanup-job 僅 sys_token）→ append-only 成立（FR-004/SC-004）

**CDP 證據**：N/A——005 spec FR-009 明示零業務端點/UI/中介層（機制地基刀）；op-log 資料的 UI 呈現屬 012 審計中心之審查範圍

---

## 006-auth-island-min — ✅ PASS

**FR coverage**：17/17 FR 覆蓋（FR-001~017 全數有實碼＋live/測試證據；FR-009/012/015/016 部分行為由 009/014/019/021/022 合法 supersede、皆有 DECISIONS/MILESTONES 依據，無無依據漂移）

**Strengths**：
- login_inner 的識別前/識別後 operator doctrine 清晰：帳號不存在與密碼錯誤同回 AppError::LoginFailed（1000、同碼同 msg key，rust-api/server/src/handler/auth.rs:177/181）、停用 gate 置 verify 後 carry Some(uid)——防枚舉語意在 020-025 六刀疊加後仍完整保留
- FR-011 原子性實作乾淨：set_pointer + insert_token 綁同一 plain txn（auth.rs:239-269），014 加入的 revoke_other_chains 也收進同 txn，任一失敗整組 rollback
- FR-008 DB-fresh 授權貫徹到位：get_user_info 與 require_policy 都走 roles_of_user(db, claims.uid)、claims.roles 僅 hint（auth.rs:678、enforce.rs:416-417），jwt.rs:18 註解明文標示
- fail-OPEN vs fail-CLOSED 方向正確且有測：bearer 缺/壞/過期一律 TokenExpired 3333（enforce.rs:85-95、jwt leeway=0）；is_current DB 抖動/查無 user 回 Ok(())（enforce.rs:116-118）——Edge case「兩者方向相反不得搞反」守住
- FR-014 密鑰 _FILE 優先在 config.rs:225-229 落實（load_secret_file_first + guard_jwt_secret），且 014 Redis URL、xdb 路徑沿用同一範式

**Findings**：
- **[Observation]** getUserInfo 每請求對 enforce_mw is_current 做一趟 PK 讀（session_gate_of），Redis pointer 快取仍 defer（014 data-model X-01 v1 明文遞延）——非缺陷、屬已登記的效能遞延（`rust-api/server/src/auth/enforce.rs`）
  - 證據：enforce.rs:104-106 註解「Redis sess:{uid} pointer 快取本刀 DEFER（data-model X-01 v1）」；enforce_mw（:324-334）bearer→is_current→denylist_gate 每受保護請求跑一次 sys_user PK 讀
- **[Observation]** US2 顯示名稱 live 回「超级管理员」（簡體 seed nick_name）——與已登記的未來 zh-TW locale feature 一致、勿當 bug；alias 規約本身（User→User01）由 m002 seed 淨值兌現而非 runtime alias 邏輯（`rust-api/migration/src/m002_rev2_seeds.rs`）
  - 證據：m002 :51-54 INSERT nick_name id3='User01'；psql 實測 3|User|User01；live getUserInfo userName="超级管理员"（uid=1 nick_name）——handler auth.rs:691 nick_name fallback user_name，符 FR-005

**Supersessions（合法推翻、非缺陷）**：
- FR-009/US4 single-session「永遠踢」→ 014 policy-aware is_current（effective Off 放行多 session；enforce.rs:97-132 resolve_policy gate、login_inner :247-257 effective on 才 revoke_other_chains）——依據 DECISIONS §2 014「修 006 永遠踢 bug」＋REVIEW-001-019 supersession map；live psql 見 uid=1 有 3 列 active token（default off 下合法多 session）、7777 路徑在 effective On 時保留
- FR-016「無換發端點消費邏輯」→ 014 加 POST /auth/refreshToken（rotation chain 四態＋lock-then-redecide、handler/auth.rs:528-586）——依據 MILESTONES 014-auth-token-session 收刀＋014 spec；且遵 006 FR-015 精神：refresh handler 絕不回 3333（jwt 壞→8888、auth.rs:542）
- FR-016「無 lockout」→ 019 滑動窗 gate＋021 Redis L1 負快取＋022 白名單 bypass/手動解鎖，全部前置於 login_inner 之前（auth.rs:292-420）、不改 login 核心語意——依據 MILESTONES 019/021/022 收刀紀錄
- FR-003 停用帳號收斂 1000：006 基線未實作停用 gate、009 R10 補上 status==2→LoginFailed（auth.rs:184-189、同碼同 msg 防枚舉）——依據 REVIEW-001-019 §B「status==2 停用（009）後續補」
- FR-012 rotation_chain 僅記錄不消費 → 014 開始消費（decide_rotation/revoke_chain）——spec 本文即預告「供後續換發刀」、方向一致

**Live 證據**：curl GET /api/auth/getUserInfo（共用 Super token）→ code 0000、4 欄齊：userId=\"1\"（字串、FR-005）/userName=超级管理员/roles=[R_SUPER]/buttons=12 個（含 user:add…menu:delete、非空殼、FR-006）；無 token 與壞 token 皆 → {\"code\":\"3333\",\"msg\":\"auth.token.expired\"} HTTP 200（FR-007/015 信封＋可在地化 key）。psql（唯讀）：sys_user id1-3 = Super/Admin/User、nick User01（alias seed）、三帳號 current_session_id 皆非空（FR-010 pointer 持久真相）；sys_token uid=1 最近 3 列 status=active、rotation_chain 皆非空、token_hash 64 hex、expires_at>now（FR-012）；casbin_rule v2='button' 16 列（FR-006 seed 非空）。/health 經 front-nginx 31080 與 rust-api 31081 直連皆 ok HTTP 200（FR-017/SC-007 零回歸）。寫端（login/5003 低權帳號測）依禁令未打，5003 由 error.rs:91 PermissionDenied→403 映射＋enforce seam 單元測＋006 收刀 C-V 全綠紀錄背書。

**CDP 證據**：CDP（tab AAB4D0C2CDE633D7A403FE31ACB47168、builtin WebSocket 腳本）：navigate http://localhost:31080/home → 停留 /home（未彈回 /login＝Super token 登入態有效、enforce 鏈放行）；sider 選單渲染（hasSider=true、系統管理/系統設置等項可見）；頁面顯示「早安，超级管理员」（getUserInfo userName 上屏）；body innerText 525 字元掃描 page.*/backend.*/common.*/route.* 樣式殘鍵 → rawKeys=[]（i18n 全在地化、SC-006 顯示路徑無 raw key）。

---

## 007-audit-overlay — ✅ PASS

**FR coverage**：16/16 FR 覆蓋（FR-001/002/003/008/009/010/011/013 live 實證；FR-004/005/012 code＋spec 內 021 勘誤；FR-006/007 code＋audit_ctx 純測（peer-gate/normalize 系列）；FR-014 facade 面檢；FR-015/016 live＋MILESTONES）；SC-001~007/009 直接或引收刀 C-V 佐證、SC-008（prod build xdb baked-in）引 007 收刀紀錄未重跑。

**Strengths**：
- 單一寫點 exactly-one 設計歷經 019/021/022 三刀改造仍成立：rust-api/server/src/handler/auth.rs:427 的 best-effort login_attempt 寫點統一收斂 Ok 與所有 Err 終端路徑（含 019 L2 gated 列 operator=None），僅 021 L1 早 return（auth.rs:349 附 ②b 註解）為有依據反轉且已回註 007 spec FR-004/SC-002 as-built 勘誤
- resolve_client_ip 純函式（audit_ctx.rs:146-207）peer-gate→Tier-1 CDN 錨→Tier-2 skip→Fallback 結構清晰，013 七態 Confidence／022 apply_tunnel_fallback（B2 反偽造、conf 不升）以 overlay 疊加而非改動核心簽名，防偽語意（FR-006/007）零漂移
- op-log 歸屬 seam（RequestContext::to_audit_meta, audit_ctx.rs:90）已由 009+ 各刀 mutating handler 實際消費（system_manage.rs/system_settings.rs/user_center.rs 共 10+ 呼叫點），live psql 見 sys_operation_log operator_id/operator_real_ip/operator_ip_confidence/trace_id 皆真值——FR-013 從 seam-ready 進化為 live-filled
- best-effort 紀律徹底：access-log 寫失敗僅 tracing::warn（audit_ctx.rs:323 best_effort_audit）、login_attempt 同（auth.rs:442）、xdb 缺檔 xdb_ready gate 降級 region=None（audit_ctx.rs:360），三 sink facade 僅暴露 write/count/list、無 update/delete fn（FR-012/014）

**Findings**：
- **[Observation]** front-nginx 無條件以 proxy_set_header X-Request-Id $request_id 覆蓋 client 自帶的 x-request-id，經 ingress 的請求 FR-010「沿用入站標頭」只沿用到 nginx 生成值、client 端關聯 id 到不了 app（`deploy/nginx/conf.d/_locations.inc`）
  - 證據：_locations.inc:25 `proxy_set_header X-Request-Id $request_id;`；live：經 :31080 帶 `x-request-id: review007-trace-abc` → sys_access_log.trace_id=8e74f8c8…（nginx $request_id 32-hex）；直連 :31081 帶 `review007-direct-xyz` → trace_id 逐字沿用。app 層 extract_trace_id 正確；nginx↔app 同 request_id 可互 join（nginx.conf:20 access log 亦記 $request_id），屬設計取捨非 bug，但與 spec Assumptions「反代已轉發 request-id 標頭」的『轉發』措辭有出入（實為『替換』）
- **[Observation]** dev 拓樸下經 nginx 的請求 real_ip 解析結果＝nginx 容器 IP（Fallback）而非真實 host client——因 docker gateway 172.23.0.1 ∈ internal_default、整鏈受信全 skip（`rust-api/server/src/audit_ctx.rs`）
  - 證據：live psql：access_log 列 real_ip=172.23.0.13=peer_ip、ip_confidence=fallback、x_forwarded_for=172.23.0.1（逐字保存）。符合 spec US3 AC3/FR-007 fail-safe（全可信 hop→直連位址），dev-only 現象；prod 外部 client 不在 internal_default 不受影響。鑑識上原始 client 仍可自 x_forwarded_for 欄還原
- **[Observation]** CDP 審計中心「API 存取」tab 列表未見完整 trace_id 文字（hasTrace:false）——列有渲染（getUserInfo/内网 皆在 DOM）但識別碼欄疑似截斷顯示，鑑識人員要完整 trace 需 hover/詳情（`base-web/src/views/manage/audit/index.vue`）
  - 證據：CDP eval：{hasTrace:false, hasNeiwang:true, hasGetUserInfo:true, rawKey:false}——psql 確認 trace_id=review007-direct-xyz 的列存在且在最新頁；屬 012 讀端 UI 呈現細節、非 007 寫入面問題

**Supersessions（合法推翻、非缺陷）**：
- FR-004/SC-002「每終端結果恰一筆」→ 021-login-lockout-redis-cache ②b：L1 Redis 負快取命中（已鎖）之被壓制嘗試早 return 不寫 sys_login_attempt（auth.rs:349）、量級訊號改 ②c 節流摘要 target=security.lockout→loki；依據＝007 spec.md FR-004/SC-002 內嵌 as-built 勘誤（2026-06-28、021 T016）＋DECISIONS §1（021）——上鎖前歷程與觸發那發仍恰一筆，live 未觸鎖驗證（非破壞紀律）
- FR-003/FR-005 紀錄欄形 → 013-xff-real-ip-forensics 擴充：peer_ip/ip_confidence（七態）兩鑑識欄＋兩層 TrustModel（cdn/my_public/binding/tunnel）取代單一可信集合；依據＝MILESTONES §1（013）、Confidence enum 註明 013 data-model §4；022 再疊 apply_tunnel_fallback（DECISIONS §1 ⚠️ae）——欄位只增不減、007 原欄全保留
- FR-015「本刀不引入稽核讀端/UI」→ 012-audit-read（波2 規劃內）新增 facade list/resolve_sort 讀 fn＋/manage/audit 三 tab UI；依據＝MILESTONES §1（012）＝spec 自身「各歸後續波次」遞延條款兌現，非漂移

**Live 證據**：①exactly-one/operator-gate：sys_access_log count 2132 → 打 3 請求（auth GET getUserInfo=200、無 token GET=3333、/health=ok）→ 2133（恰 +1，unauth/health 0 列＝FR-001/002/016）。②欄位齊全（FR-003/008/009、SC-004/007）：最新列 operator_id=1|GET|/auth/getUserInfo|200|real_ip inet|peer_ip|fallback|xff="172.23.0.1" 逐字|region="0|0|0|内网IP|内网IP" 非空內網標|trace_id|created_at。③FR-010：直連 :31081 帶 x-request-id → trace_id="review007-direct-xyz" 逐字沿用；經 nginx 則為 $request_id（見 Observation）。④login_attempt 近 3 列（FR-005/011）：attempted_user_name=Super|t|operator_id=1|real_ip/peer/confidence/xff/region/trace 全填。⑤FR-013 seam live：sys_operation_log 列 operator_id=1+operator_real_ip+operator_ip_confidence=fallback+trace_id 非空。失敗路徑/偽造 XFF/鎖定寫入依非破壞紀律未打，引 code（單一寫點 auth.rs:390-442）＋007/013/021 C-V 收刀全綠紀錄。

**CDP 證據**：007 本體無 UI 面；以 012 讀端 /manage/audit（消費 007 寫入列）做下游渲染取證：頁面可達（url=…/manage/audit）、三 tab（操作异动/API 存取/登录尝试）＋13 欄表頭渲染（含 013 可信度/直连 peer/真实 IP/转发链）、op-log 列顯 Super/UPDATE/sys_user/172.23.0.9；點「API 存取」tab 後 {hasGetUserInfo:true, hasNeiwang:true（内网 region 在地化呈現）, rawKey:false}——body innerText 無 page.*/backend.*/common.* 殘鍵。截圖 scratchpad/audit-page.png。

---

## 008-system-settings — ✅ PASS

**FR coverage**：11/11 FR 覆蓋（FR-001/004/005/008/011 live+CDP+實碼直證；FR-002/003/006/007 實碼＋收刀 C-V 證據；FR-009 零 schema 由 024 m009 seed 合法演進；FR-010 NOT-in-scope 由 014 合法接手）；SC-001~007 全對應、無缺者

**Strengths**：
- require_policy（rust-api/server/src/auth/enforce.rs:403）DB-fresh roles_of_user(db, claims.uid) 不信 claims.roles、per-route layer 掛 GET/POST 兩端點（main.rs:226-239）＋casbin seed 僅 R_SUPER 兩列（psql 實證）——FR-004/005 樣式立得乾淨且被後續全部治理端點沿用
- update_by_key（model/facade/system_settings.rs:59-95）mutate_in_txn 同 txn op-log、查無→Ok(None) no-op 不寫審計、build_update_active_model 純測 seam §I.6 成對＋live INET round-trip #[ignore] 測（by trace_id、外層 txn savepoint rollback 不污染 seed）——FR-003 原子審計實作與測試層次完整
- endpoint_coverage_lint（rust-api/server/tests/endpoint_coverage_lint.rs:116-117）registry 含兩端點、隨 025 增至 AS_BUILT=54 仍維護中——FR-008 守恆檢查存活至 cumulative 態
- validate_value_type（handler/system_settings.rs:62-86）024 補 number 型時刻意整串比對 early-return 避 split_once 死碼、附 4 組單元測含空字串/越界——008 明文 passthrough 缺口被 024 前向補強而非漂移

**Findings**：
- **[Observation]** number 型驗證（:66）接受 u32::parse 的寬鬆字面（如 "0008"、"+8"）且原字面 verbatim 入庫，下游 password_policy::from_settings 再 parse 可正常吸收，僅字面不正規化的美觀疑點、非 FR 違反（`rust-api/server/src/handler/system_settings.rs`）
  - 證據：validate_value_type 對 value.parse::<u32>() Ok 即放行、update_by_key Set(new_value) 原樣寫入；u32 parse 接受前導零與 '+'。功能無害（from_settings 同 parse 語意），記錄供日後 canonicalization 參考
- **[Observation]** find_all（:19）回全列不濾 deleted_at（schema 有 soft-delete 欄），若日後引入設定鍵刪除路徑須回補 filter——目前無刪除路徑故 latent、非缺陷（`rust-api/server/src/model/facade/system_settings.rs`）
  - 證據：facade doc comment 明示「§5.0 system_settings 不套用 §5.8、回全列」＝拍板刻意（本刀無增刪鍵路徑）；psql 現況 8 列 deleted_at 全 NULL

**Supersessions（合法推翻、非缺陷）**：
- 008 FR-010 hot-apply NOT in scope → 014 加 redis `settings:invalidate` best-effort publish（handler/system_settings.rs:118-124、fail-open）——依據 REVIEW-001-019 Supersession Map「008 設定熱套用→014 U1 settings 島」＋MILESTONES 014 行
- 008 FR-006/US4 值型別僅涵蓋 enum → 024 補 bare `number` 整串比對驗證 1..=1024（補 008 明文 passthrough 缺口）——依據 MILESTONES `aca02c81`／DECISIONS §2 024 行（spec Assumptions 預告「其他型別隨需要擴充」）
- 008 前端單卡 KV 列表版面 → 024 重構為分區塊 2 NCard（密码策略/会话设置）＋2 欄 NGrid＋NInputNumber render 分支＋滾動修復——依據 MILESTONES `aca02c81` 收尾 UI 三輪打磨（user 逐項回饋）
- 008 FR-004 設定值僅 R_SUPER 可讀 → 025 新增 auth-only `GET /userCenter/getPasswordPolicy` 讓任何登入者讀 7 個 password_* KV（7-key allowlist、不洩其他設定鍵）——依據 DECISIONS §1 ⚠️ah（review loop 接住原計畫前端讀 super-only 端點會 403、merge `ded99ee9`）
- 008 FR-009 零 schema 變更基線 → 024 m009 seed 新增 7 政策列（seed data、非 schema DDL；up→down→up 可逆）——依據 MILESTONES `aca02c81`／DECISIONS §2

**Live 證據**：curl GET /api/systemManage/getSystemSettings（Super token）→ code:0000、8 筆（single_session_default=off enum:on,off＋7 個 password_* 政策鍵）camelCase wire 形正確；無 token → code:3333 auth.token.expired data:null（認證守門、不洩值）；psql system_settings 8 列與 wire 一致；psql casbin_rule 僅 2 列 p|R_SUPER|getSystemSettings GET／updateSystemSetting POST（無其他角色 policy＝403 by absence）；讀端 5 次計時 7.9~9.4ms（SC-007 p95<300ms 大幅達標）。寫端（FR-002/003/006/007）依非破壞紀律未觸發：由實碼（update_by_key mutate_in_txn＋validate_value_type＋notFound 映射）＋008 收刀 C-V-0~11 全綠（MILESTONES `b52dafe`：live op-log INET round-trip、policy-gate 5003、2222×2、CDP toast、prod build）＋024 收刀 live（非法 number→2222 值不變）背書。

**CDP 證據**：CDP navigate http://localhost:31080/manage/system-settings（Super 登入態 tab）：頁面可達（url 斷言）、2 NCard 區塊標題「密码策略/会话设置」渲染、6 NSwitch（5 password enum＋1 single_session）＋2 NInputNumber（min/max length）齊、body innerText 無 page.*/backend.*/common.*/route.* raw i18n 殘鍵（rawKey:false）、含「单一」session 描述——024 重構後版面如 MILESTONES `aca02c81` 所述、SC-005 讀面達成（改值/toast 依非破壞紀律不按保存、引 008/024 收刀 CDP 全綠紀錄）。

---


<!-- BATCH 2 RESULTS -->

<!-- BATCH 3 RESULTS -->

<!-- SUMMARY -->
